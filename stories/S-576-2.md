---
document_type: story
level: ops
story_id: "S-576-2"
epic_id: "SOH-ATTACHMENTS-1"
title: "jr issue attachment download — single/batch/newest, streaming, CWE-22 sanitization (issue #576)"
wave: feature-followup
status: ready
intent: attachment-read
feature_type: feature
mode: feature
scope: standard
severity: HIGH
trivial_scope: false
points: 8
priority: P1
tdd_mode: strict
producer: story-writer
timestamp: "2026-07-17T00:00:00"
phase: 3
inputs:
  - ".factory/phase-f2-spec-evolution/prd-delta-576.md"
  - ".factory/specs/prd/bc-2-issue-read.md"
  - ".factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md"
  - ".factory/phase-f1-delta-analysis/impact-boundary-576.md"
input-hash: "51b72ad"
traces_to: ".factory/specs/prd/bc-2-issue-read.md"
cycle: cycle-attachment-576
estimated_effort: large
estimated_days: 3
target_module: src/cli/issue/attachments.rs
subsystems: ["SS-03", "SS-09"]
depends_on: ["S-576-1"]
blocks: []
behavioral_contracts:
  - "BC-2.7.007"
  - "BC-2.7.008"
  - "BC-2.7.009"
  - "BC-2.7.010"
  - "BC-2.7.011"
  - "BC-2.7.012"
bcs:
  - "BC-2.7.007"
  - "BC-2.7.008"
  - "BC-2.7.009"
  - "BC-2.7.010"
  - "BC-2.7.011"
  - "BC-2.7.012"
verification_properties:
  - "VP-576-001"
holdout_anchors:
  - "H-NEW-ATTACHMENT-002"
  - "H-NEW-ATTACHMENT-003"
  - "H-NEW-ATTACHMENT-007"
nfr_anchors: []
adr_refs: ["ADR-0017"]
sd_refs: []
parent_phase: F3-incremental-stories
spec_source: ".factory/phase-f2-spec-evolution/prd-delta-576.md"
implementation_strategy: tdd
module_criticality: HIGH
acceptance_criteria_count: 19
assumption_validations: []
risk_mitigations: []
created: "2026-07-17"
version: "1.39"
last_updated: "2026-07-20"
breaking_change: false
retroactive: false
origin: >
  Issue #576 (attachment download surface). Covers single-file download (--id),
  batch download (--all), and newest-N download (--newest N). Introduces
  sanitize_attachment_filename (CWE-22 path-traversal mitigation; BC-2.7.011
  5-step algorithm + containment check). Requires reqwest 'stream' feature
  (ADR-0017 S-576-2 slot). Ships CLAUDE.md gotchas for CWE-22 filename
  sanitization and GHSA-9857-6MW7-FQ2M redirect behavior. Depends on S-576-1
  (depends_on: ["S-576-1"]) — reuses display_sanitize_filename and
  src/cli/mod.rs dispatch wired by S-576-1. Also adds src/cli/mod.rs
  attachment dispatch registration. F2 spec converged at v1.3.81
  (DEC-184 authorized story decomposition).
changelog:
  - "1.39 (2026-07-20): Mutant-kill round count sync (commit 3aabc92f) — integration suite grew 29→33 (+4 mutation-kill integration tests); mutation coverage 79%→projected 95%; 13 mutants killed, 4 confirmed equivalent (481:28, 481:47, 602:32-always-true, 721:24-always-true). Updated 4 count-bearing surfaces: Token Budget ~509, Task 1 ~525, Task 7 header ~564, File Structure ~626: 29→33, parenthetical now '22 AC tests + 11 adversarial/mutation regression pins added during Step 4.5 + CI mutation gate'. input-hash updated."
  - "1.38 (2026-07-20): P9-001 test-count sync — test_bc_2_7_007_rename_failure_error_display_sanitizes_filename added as 29th test (7th adversarial regression pin). Updated 4 count-bearing surfaces: Token Budget ~508, Task 1 ~524, Task 7 header ~563, File Structure ~625: 28→29, '6 adversarial regression pins'→'7 adversarial regression pins'. input-hash updated."
  - "1.37 (2026-07-20): P8-001 follow-up test-count sync — test_bc_2_7_007_success_hint_display_sanitizes_filename added as 28th test (6th adversarial regression pin). Updated 4 count-bearing surfaces: Token Budget ~504, Task 1 ~520, Task 7 header ~559, File Structure ~620: 27→28, '5 adversarial regression pins'→'6 adversarial regression pins'. input-hash updated."
  - "1.36 (2026-07-20): P8-001 — Two sites updated: (1) AC-018 (~431): added DISPLAY-SANITIZATION bullet — filename portion of echoed path MUST route through display_sanitize_filename per BC-2.7.011 ~936 every-call-site clause (SEC-576-011/CWE-116); test note extended with sanitized-form sub-assertion; (2) Architecture Compliance Rules table (~598): added future-proofing row — 'Every stderr/stdout string interpolating a server-supplied filename routes through display_sanitize_filename'. input-hash updated f0b3ae8→98eaaf4 (body content change)."
  - "1.35 (2026-07-20): P7-001 — File Structure row struct name corrected AttachmentDownloadResult → AttachmentDownloadEntry (stale label; target correct)."
  - "1.34 (2026-07-20): Step 4.5 test-count sync — F-P4-002: suite grew to 27 (was 22 at last count-sync; 22 AC tests + 5 adversarial regression pins: P1-005 single-id degenerate fallback, P1-006 VP-576-001 containment proptest, P1-009 batch collision-skip, P3-003 filter-then-newest composition, P4-001 sparse-object pin). Updated 4 count-bearing surfaces: (1) Token Budget row ~503 '22 tests'→'27 tests'; (2) Task 1 body ~519 'Write all 22 tests'→'Write all 27 tests'; (3) Task 7 header ~558 '22 tests GREEN'→'27 tests GREEN'; (4) File Structure row ~619 '22 integration tests'→'27 integration tests'. No AC or spec content change; input-hash unchanged."
  - "1.33 (2026-07-19): Propagation sweep v1.3.95/96 — P1-002 author-curated-form ruling + P2-001 EC-2.7.001-3; no body citation sites in S2. input-hash updated 71f97e7→f0b3ae8 (bc-2-issue-read.md changed at v1.3.95/96)."
  - "1.32 (2026-07-19): Status draft→ready per DEC-185 F3 gate approval (mechanical flip; no content change)."
  - "1.31 (2026-07-18): PRE-F4-UNICODE propagation @ v1.3.94 — 2 sites updated (pointer style, obligation discharged): (1) SETTLED RULINGS ~124: PRE-F4-UNICODE banner replaced with DISCHARGED scope notice pointing to BC-2.7.011 display-sanitization primary clause; (2) Out-of-Scope ~636: entry updated from 'out of scope for v1.3.81' to discharged pointer. No AC or test count change; input-hash updated 5c14ae6→71f97e7 (v1.3.94: bc-2, bc-3, BC-INDEX, prd-delta changed)."
  - "1.30 (2026-07-18): Pre-gate hash refresh @ v1.3.93 — no content change; input-hash updated 7e3a22e → 5c14ae6 (v1.3.93 cosmetics: error-taxonomy 401 row, prd-delta enumeration lines 84/85, ADR-0017 command-name + ID-type)."
  - "1.29 (2026-07-18): Fix round 70, adversary pass 70 — P70-001 (MEDIUM): batch fail-soft exit code (EC-2.7.008-6/7/8) was unpinned — no test asserted exit 1 on partial/full failure. Fixed at 4 sites: (1) AC-007 body ~263: bold exit-code requirement added — partial fail → exit 1 (EC-2.7.008-7); all-fail → exit 1 + 'Downloaded 0 of M...' (EC-2.7.008-8); manifest emitted at exit 1 (EC-2.7.008-6); (2) AC-007 test note ~270: extended test_bc_2_7_008_all_batch_fail_soft with EC-2.7.008-7 partial-fail sub-assertion (1-of-2 succeeds, exit 1, 'Downloaded 1 of 2...') + EC-2.7.008-8 all-fail sub-assertion (exit 1, 'Downloaded 0 of 2...') — both required, not optional; (3) AC-012 body ~326: bold exit-code requirement added — partial/full JSON-mode fail → exit 1; (4) AC-012 test note ~328: extended test_bc_2_7_008_json_mode_error_vs_hint_taxonomy with explicit exit_code == 1 sub-assertion. No new test fn; no AC or test count change; input-hash unchanged (spec v1.3.92, 7e3a22e)."
  - "1.28 (2026-07-18): Fix round 53 — prd-delta v1.3.92 reconciliation; input-hash refresh 3134afd→7e3a22e (prd-delta v1.3.91→v1.3.92; no body citation sites in S2). No AC or test count change."
  - "1.27 (2026-07-18): Fix round 51, adversary pass 51 — P51-001 (LOW): AC-007 lacked a reaching test for EC-2.7.008-1 (empty issue on --all → exit 0, stderr HINT 'No attachments on <KEY>.'; JSON: {'downloaded':[]} + NO hint) and EC-2.7.009-4 (--newest on empty). Added test_bc_2_7_008_empty_issue_no_attachments_hint (3 sub-assertions: human --all empty, JSON --all empty, --newest empty/EC-2.7.009-4). 4-surface sweep: test count 21→22; Task 1 AC-007 2→3 fns; Task 7 header; Token Budget 21→22/~6,900→~7,100/Total ~44,600→~44,800; File Structure 21→22/AC-007 2→3 fns. acceptance_criteria_count unchanged at 19. INFO-1 FOLD (LOW): AC-009 test note ~302 enumeration made exhaustive — added: invalid AID (non-numeric) → exit 64 + 'invalid attachment id:'; AID 404 → exit 64 + 'Attachment' AND 'not found or not accessible'; KEY 404 → exit 64 + 'Issue' AND 'not found or not accessible'; other write error → exit 1 only (unpinnable). input-hash unchanged (spec v1.3.91, 3134afd)."
  - "1.26 (2026-07-18): Fix round 45, adversary pass 45 — P45-001 (LOW): AC-009 taxonomy table ~290 vague '403 | 1 | Permission denied message' split into two rows mirroring BC-2.7.012: (a) AID 403 (single --id path) → exit 1 + 'Permission denied: cannot access attachment <AID>.' (EC-2.7.007-1b / BC-2.7.012 ~961); (b) KEY 403 (batch --all/--newest path) → exit 1 + 'Permission denied: cannot access issue <KEY>.' (BC-2.7.012 ~959). Test note ~299 updated: '403 → exit 1 + permission denied message' replaced with both cases pinned as loose-substring with explicit single-vs-batch distinction. No AC or test function count change; input-hash unchanged (spec v1.3.91, 3134afd)."
  - "1.25 (2026-07-18): Fix round 41, adversary pass 41 — P41-001 (LOW): EC-2.7.008-4 (--out-dir exists but not a directory) was named in AC-007 body but unpinned. Fixed at 3 sites: (1) AC-007 body ~260: canonical string 'Not a directory: <PATH>' added next to EC-2.7.008-4 reference; (2) AC-007 test note ~266: sub-assertion added to test_bc_2_7_008_all_batch_fail_soft (existing-file path → exit 64 + stderr contains 'Not a directory:'; mirrors P25-003 EC-2.7.008-2 treatment); (3) BC-table scope cell ~142: 'dir-not-exist exit 64' extended to 'dir-not-exist exit 64; not-a-directory exit 64'. No AC or test function count change; input-hash unchanged (spec v1.3.90, ccd570b)."
  - "1.24 (2026-07-18): Fix round 35, adversary pass 35 — P35-001 (LOW): AC-017 Layer 1 gains 10th discriminating clap case: 'download <KEY> --newest 3 --out /tmp/foo → exit 2' (EC-2.7.007-9 ~770; --out invalid with --newest as well as --all; discriminates requires=id from conflicts_with=all mis-wiring). Test annotation bumped '9 clap exit-2 cases'→'10 clap exit-2 cases'. No new test function; overall integration test count unchanged at 21; input-hash unchanged (spec v1.3.90, ccd570b)."
  - "1.23 (2026-07-18): Fix round 30, adversary pass 30 — P30-003 (LOW): AC-015 (~365) reworded: 'applies to both human mode and JSON mode' was self-contradictory (parenthetical said 'suppressed in JSON mode'); corrected to 'HUMAN MODE ONLY — JSON mode suppresses the warning entirely (raw value retained in manifest filename field; no TTY exposure vector)'. INFO FOLD (AC-005 ~228): 'use id itself as the basename' tightened to 'use the id as the basename component, RETAINING the SHA-1 prefix (<sha1-of-id>_<id>)' per BC-2.7.010 ~869 — prevents bare-<id> misreading. No AC or test count change; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.22 (2026-07-18): Fix round 29, adversary pass 29 — P29-001 (LOW): replaced malformed ID 'BC-2.7.008-6' (no such BC ID exists — it's an EC) with 'EC-2.7.008-6' at all 4 occurrences: changelog 1.21, SETTLED RULING §2.7 taxonomy, AC-007 (~256), AC-012 (~317); round-28 regression; no AC or test count change; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.21 (2026-07-18): Fix round 28, adversary pass 28 — P28-002 (LOW): AC-007 (~255) + AC-012 (~316) corrected fail-soft error channel: both previously said 'per-file errors... as JrError elements in JSON mode' / 'per-file download failures ARE emitted as JrError shapes'. Fixed to: per-file failures emit canonical STDERR WARNING STRING 'warning: failed to download attachment <AID>: <reason>' to stderr in BOTH modes (EC-2.7.008-6/EC-2.7.008-7; not suppressed in JSON mode; nothing goes to stdout as a JSON error object); manifest contains only successfully downloaded entries; no new test function; count stays 21; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.20 (2026-07-18): Fix round 27, adversary pass 27 — P27-002 (LOW): pinned ADR-authorized version in both surfaces: Library table version cell updated from description-only to include 'sha1 = { version = \"^0.10\" }' per ADR-0017 § Authorized Dependencies; Task 3 sha1 Cargo.toml bullet updated from placeholder '\"...\"' to '{ version = \"^0.10\" }'; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.19 (2026-07-18): Fix round 26, adversary pass 26 — P26-001 (MEDIUM): Library table sha1 row corrected: version cell was 'as in Cargo.lock (check existing; add if absent)' — FALSE; sha1 is NOT in Cargo.lock; S-576-2 adds it as a NEW direct dependency. Version cell updated to 'NEW direct dependency — NOT currently in Cargo.lock; RustCrypto sha1 crate per ADR-0017 2026-07-18 sha1 amendment'; SHA-1 weakness note + H-NEW-ATTACHMENT-003 40-hex pin added. Task 3 header + body extended: header 'Add reqwest stream feature' → 'Add reqwest stream feature + sha1 crate'; body gains explicit sha1 Cargo.toml step + non-crypto rationale + H-NEW-ATTACHMENT-003 pin + cargo deny obligation for new crate. P26-002 (LOW): Additive-only coordination guard added to File Structure section (after .cargo/mutants.toml row): 'AttachmentSubcommand (src/cli/mod.rs) and its dispatch match (src/cli/issue/mod.rs) are edited ADDITIVELY by S2/S3/S4/S5 — append your variant + arm only; never remove or reorder a sibling story's variant or arm; expect a rebase if a sibling merges first'; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.18 (2026-07-18): Fix round 25, adversary pass 25 — P25-003 (LOW): AC-007 (~253) pinned the canonical EC-2.7.008-2 string for --out-dir not-exist path: exit 64 + stderr 'Output directory does not exist: <DIR>'; added sub-assertion to test_bc_2_7_008_all_batch_fail_soft test note (explicit sub-case, not a combined catch-all); no new test function; count stays 21; input-hash unchanged (spec v1.3.89, edea7b8)."
  - "1.17 (2026-07-18): Fix round 23, adversary pass 23 — P23-001 (MEDIUM): File Structure gains src/cli/issue/mod.rs MODIFY row (Add AttachmentSubcommand::Download dispatch match arm; inline match mirrors comment-CRUD pattern at mod.rs:75); Task 5 header + body updated to 'Wire dispatch + Implement handle_attachment_download' with explicit mod.rs dispatch-arm wiring step; src/cli/mod.rs row re-scoped to clap VARIANT definition only (not dispatch arm; removes P1-011 ambiguity). P23-002 (LOW): AC-009 table gains two missing BC-2.7.012 rows — 'KEY or AID 5xx → exit 1 → API error (<N>) (single mode; per-file fail-soft batch)' and 'Network error → exit 1 → stderr contains Could not reach (single mode; per-file fail-soft batch)'; test_bc_2_7_012_error_taxonomy sub-assertion list extended to pin both (5xx: loose-substring API error (; network: loose-substring Could not reach); no new test function; count stays 21. P23-003 (LOW): AC-001 (~152) gains EC-2.7.007-3 DISTINCT-HOST mandate: two wiremock servers MUST use distinct host strings (e.g., 127.0.0.1 vs [::1]); same-host-different-port is vacuous because reqwest cross-host check compares host_str() which IGNORES ports; input-hash unchanged (spec v1.3.89, edea7b8)."
  - "1.16 (2026-07-18): Fix round 22, adversary pass 22 — P22-001 (LOW): spec moved v1.3.88→v1.3.89; input-hash refreshed (fb4b623→edea7b8). Task 2 mutants.toml obligation updated to confirm-present/ADD IF ABSENT (primary delivery moved to S-576-1 per prd-delta-576.md S1 obligation (f) / P22-001); File Structure row updated to match; no AC or test count change."
  - "1.15 (2026-07-18): Fix round 21, adversary pass 21 — MIRROR of P21-001 (bundle consistency): AC-009 test_bc_2_7_012_error_taxonomy assertion note extended to explicitly cover ALL taxonomy rows as named sub-assertions: 401 → exit 2 + stderr contains 'Not authenticated' AND 'jr auth login' (v1.3.88 loose-substring); 403 → exit 1 + permission denied message; ENOSPC/EACCES already present from P20-003; no new test fns — sub-assertions within existing taxonomy test; test count unchanged at 21; input-hash unchanged (spec v1.3.88, fb4b623)."
  - "1.14 (2026-07-18): Fix round 20, adversary pass 20 — spec moved v1.3.87→v1.3.88; input-hash refreshed (8aa6154→fb4b623). P20-001 (MEDIUM): AC-009 (~277) 401 row updated from exact-quoted 'Not authenticated. Run `jr auth login`.' to loose-substring form: stderr contains \"Not authenticated\" AND \"jr auth login\" (full literal: Not authenticated. Run \"jr auth login\" to connect. — src/error.rs::JrError + client.rs); exit 2 unchanged. P20-003 (LOW): AC-009 (~279) write-error row split into three BC-2.7.012 rows: ENOSPC → exit 1 'Disk full: not enough space to write <path>'; EACCES → exit 1 'Permission denied: cannot write to <dir>'; other → exit 1 raw OS message; test_bc_2_7_012_error_taxonomy assertion note extended to pin the two jr-formatted strings; no test-count change (count stays 21; AC-009 still has 1 test function)."
  - "1.13 (2026-07-18): Fix round 18, adversary pass 18 — P18-001 (MEDIUM): AC-001 (~146) and AC-011 (~296) corrected temp-file scheme to BC-2.7.007 ~749 mandate — 'tmp_<random> in SAME DIRECTORY as final path; NOT <basename>.tmp; NOT OS temp dir (cross-filesystem rename is not atomic)'; AC-011 extended with second test test_bc_2_7_007_temp_file_same_dir_tmp_random_prefix (pins same-dir + non-basename naming; EC-2.7.007-8 concurrent correctness); test count 20→21 (acceptance_criteria_count stays 19; AC-011 now has 2 test functions); 4-surface sweep: Token Budget 6,700→6,900/Total 44,400→44,600, Task 1 '20 tests (... AC-007 has 2, AC-017 has 2'→'21 tests (... AC-007 has 2, AC-011 has 2, AC-017 has 2', Task 7 '20'→'21', File Structure '20 integration tests'→'21 integration tests' with AC-011 has 2 added. P18-002 (LOW): SETTLED RULINGS P31-002 (~101) and AC-006 (~232) false claim 'tokio::io::copy is available in the tokio crate but does not provide per-file bytes-written counting' removed and replaced with true constraint 'bridging bytes_stream() to tokio::io::copy would require a tokio_util::io::StreamReader adapter; tokio-util is not in the S-576-2 slot (ships at S-576-3 per ADR-0017); the explicit futures::StreamExt write loop is used instead, which also yields the per-file byte count'; input-hash unchanged (spec v1.3.87, 8aa6154)."
  - "1.12 (2026-07-18): Fix round 17, adversary pass 17 — P17-003 (LOW): Task 1 and File Structure per-AC breakdown note updated from '(AC-007 has 2 test functions)' to full breakdown '(19 ACs; AC-001 has 3, AC-007 has 2, AC-017 has 2; AC-004/AC-014 are src unit/proptest — 0 in attachment_download.rs; AC-013 uses existing test files — 0 new)'; test count unchanged at 20; acceptance_criteria_count unchanged at 19; input-hash unchanged (spec v1.3.87, 8aa6154)."
  - "1.11 (2026-07-18): Fix round 14, adversary pass 14 — P14-005 (LOW): AC-007 extended with BC-2.7.008 'default dir is cwd' batch behavior (~785/791): '--all' WITHOUT '--out-dir' writes to cwd; test test_bc_2_7_008_all_no_out_dir_defaults_to_cwd added (TempDir-as-cwd technique); test count 19→20 (acceptance_criteria_count stays 19 — no new AC, AC-007 has 2 test functions); 4-surface sweep: Task 1 body '19 AC tests'→'20 tests (19 ACs; AC-007 has 2 test functions)', Task 7 header '19→20', Token Budget tests 6,500→6,700/Total 44,200→44,400, File Structure '19 integration tests (all 19 ACs)'→'20 integration tests (19 ACs; AC-007 has 2 test functions)'; input-hash unchanged (spec v1.3.87, 8aa6154)."
  - "1.10 (2026-07-18): Fix round 12, adversary pass 12 — P12-004 (LOW): Task 2 mutants.toml delivery reworded to 'ADD IF ABSENT (idempotent)' (was 'Add ...'); File Structure mutants.toml row updated same; P12-005 (LOW): SETTLED RULING P31-002 + AC-006 justification repaired — removed factually wrong 'tokio::io::copy requires tokio-util deferred to S-576-3'; corrected to 'tokio::io::copy is available in the tokio crate but does not provide per-file bytes-written counting; explicit write loop required for P31-002 byte-count tracking semantics'; mechanism choice (futures::StreamExt + AsyncWriteExt) unchanged; input-hash refreshed to spec v1.3.87 (8aa6154)."
  - "1.9 (2026-07-18): Fix round 11, adversary pass 11 — P11-002: holdout_anchors populated: ['H-NEW-ATTACHMENT-002','H-NEW-ATTACHMENT-003','H-NEW-ATTACHMENT-007'] (Group 19 BC-2.7.007 write-to-temp/atomic-rename, BC-2.7.008/010/011 batch/SHA-1/path-traversal, BC-2.7.011 CWE-22 security); input-hash refreshed to spec v1.3.86 (1850586)."
  - "1.8 (2026-07-18): Fix round 10, adversary pass 10 — P10-004: AC-017 Layer 1 gains 8th+9th ArgGroup cases: '--out-dir /tmp (no batch selector) → exit 2' and '--out-dir /tmp --id <AID> → exit 2' (EC-2.7.008-9 ~812; --out-dir requires [all,newest] ArgGroup); test annotation '7 clap exit-2 cases'→'9 clap exit-2 cases'; P10-002: AC-018 added (single-id success hint to stderr 'Downloaded: <path> (<size_human>).' per BC-2.7.007 ~747; suppressed in JSON mode; test: test_bc_2_7_007_single_id_success_hint_stderr); P10-003: AC-019 added (filtered-to-zero hint 'No attachments matched the filter on <KEY>.' per EC-2.7.008-10 ~814/EC-2.7.009-3 ~839; distinct from empty-issue EC-6; JSON mode: {'downloaded':[]}; test: test_bc_2_7_008_filtered_to_zero_hint); acceptance_criteria_count 17→19; 4-surface sweep: Task 1 count 17→19; Task 7 count 17→19; Token Budget tests 17→19 (~6,000→~6,500; Total ~43,700→~44,200); File Structure tests row 17→19; input-hash updated to e4fc90e (spec v1.3.85)."
  - "1.7 (2026-07-17): Fix round 7, adversary pass 7 — P7-007: CHANGELOG-append string verified byte-for-byte against amended prd-delta-576.md S2(f): 'feat(issue): attachment download single/batch/newest + streaming + CWE-22 sanitization (#576)' matches exactly; no content change required; version bump is hygiene pass only; input-hash refreshed to spec v1.3.85 (3ef78cf)."
  - "1.6 (2026-07-17): Fix round 6, adversary pass 6 — P6-006: AC-017 Layer 1 gains 7th clap constraint: '--id+--filter → exit 2 (EC-2.7.007-10, conflicts_with=\"id\")'; test annotation updated '6 clap exit-2 cases'→'7 clap exit-2 cases' (AC count stays 17; no new AC); P6-007: SETTLED RULINGS P31-002 + AC-006 corrected: removed 'tokio::io::copy' mechanism prescription (tokio-util is deferred to S-576-3 per ADR-0017; not available in S2); replaced with 'streaming write loop — futures::StreamExt + tokio AsyncWriteExt'; input-hash refreshed to spec v1.3.84 (9fc7174)."
  - "1.5 (2026-07-17): Fix round 5, adversary pass 5 — P5-001+P5-004: AC-017 fully rewritten; EC citation anchors corrected (wrong family groupings in P4-003); --newest 0 corrected from clap exit 2 to handler exit 64 (EC-2.7.009-1 ~836; canonical message '--newest requires a positive integer.'); --newest -3 handler exit-64 case added (allow_negative_numbers=true; N ≤ 0 guard); no-selector re-anchored to BC-2.7.007 required-group clause ~729; --all+--out re-anchored to EC-2.7.007-9 ~769; --id+--all re-anchored to EC-2.7.008-3 ~803; test names updated to test_bc_2_7_download_clap_structural_constraints + test_bc_2_7_009_newest_nonpositive_exits_64 (2 functions, not 3); AC count stays 17; input-hash refreshed to spec v1.3.83 (285193a)."
  - "1.4 (2026-07-17): Fix round 4, adversary pass 4 — P4-003: AC-017 added covering 6 clap structural constraints for download flags (EC citation anchors later corrected in 1.5); acceptance_criteria_count 16→17; 4-surface sweep: Task 1 count 16→17, Task 7 count 16→17, Token Budget tests 16→17 (~5,500→~6,000), total ~42,700→~43,700, budget 21%→22%; File Structure tests row 16→17."
  - "1.3 (2026-07-17): Fix round 3, adversary pass 3 — P3-001: AC-006 path corrected to 'as-constructed by jr NOT canonicalized/absolute' (EC-2.7.007-7 / BC-2.7.010 P18-004; snapshot tests must normalize path); P3-010: AC-007 canonical string corrected to 'Downloaded N of M attachments to <dir>.' (BC-2.7.008 line 795 verbatim); P3-015: AC-015 added (display_sanitize_filename required in degenerate-name warning; BC-2.7.010 ~870, SEC-576-011, CWE-116); P3-016: AC-016 added (Windows device-name escape _CON/_NUL at single-id call site; BC-2.7.011 SEC-576-001 ~915); P3-017: PRE-F4-UNICODE-DISPLAY-SANITIZATION banner added to SETTLED RULINGS; P3-018: CHANGELOG-append task added in Task 6 (scoped to 'attachment download'); P3-009: mutants.toml examine_globs delivery obligation moved from S3 to S2 (prd-delta-576.md S2 obligation (e)); acceptance_criteria_count 14→16; 4-surface sweep complete."
  - "1.2 (2026-07-17): Fix round 2, adversary pass 2 P2-001 — AC-004 algorithm corrected (BC-2.7.011 verbatim): step 1=basename extraction (Path::file_name), step 2=pseudo-name rejection ('.'/'..'/empty→None), step 3=NUL→None (rejection not strip), step 4=REPLACE /\\: with _ (not strip), step 5+5.5=214-byte cap+trailing strip; Windows device names NOT handled (CON/NUL/COM1→Some, not None; caller escapes per SEC-576-001); unit tests corrected accordingly; AC-014 VP-576-001 proptest: widened domain to non-ASCII-printable, removed 'does not match Windows device names' assertion, added CON/NUL→Some assertions."
  - "1.1 (2026-07-17): Fix round 1, adversary pass 1 P1-010/P1-011/P1-012/P1-020/P1-021 — Cargo.toml S-576-2 slot = reqwest stream ONLY (tokio-util stays at S3; ADR-0017 AMENDED P1-010); depends_on=['S-576-1'] (not []); src/cli/mod.rs added to File Structure; --force added to SURFACE entries with EC-2.7.007-12 bypass text; AC-010 re-anchored to BC-2.7.007 step 2+EC-2.7.007-2 (not BC-2.7.011); test name fixed to bc_2_7_007; display_sanitize_filename noted for collision-skip warnings (BC-2.7.011 SEC-576-011); added Purity Classification section."
  - "1.0 (2026-07-17): Initial F3 story authored per F2 CONVERGED spec v1.3.81."
---

> **tdd_mode:** strict — full TDD Iron Law enforced (todo!() + Red Gate ≥0.5 required).

> **SETTLED RULINGS — encode these exactly, do not re-litigate:**
> - **P27-001 filename=RAW / path=on-disk pairing**: `downloaded[].filename` in JSON output is the RAW Jira name (pre-sanitization); on-disk basename is recoverable from `downloaded[].path`. This asymmetry is deliberate and load-bearing.
> - **P31-002 manifest size=bytes-written**: `downloaded[].size` = bytes actually written to disk (tracked from the streaming write loop — `futures::StreamExt` + `tokio AsyncWriteExt`; NOT from `tokio::io::copy` — bridging `response.bytes_stream()` to `tokio::io::copy` would require a `tokio_util::io::StreamReader` adapter; `tokio-util` is not in the S-576-2 slot (ships at S-576-3 per ADR-0017); the explicit `futures::StreamExt` write loop is used instead, which also yields the per-file byte count), NOT the list-reported `fields.attachment[].size`.
> - **P32-001 --out local pre-flights before metadata GET**: For single-`--id` downloads with `--out <PATH>`, ALL local pre-flight checks (EC-2.7.007-6 parent-exists, EC-2.7.007-11 path-is-directory, EC-2.7.007-12 overwrite-refuse) MUST fire BEFORE the step-1 metadata GET. Fail cheap/offline first.
> - **§2.7 stderr hint-vs-error taxonomy**: collision-skip warnings (overwrite-refuse for `--all`/`--newest`) are NON-ERROR hints suppressed in JSON mode; per-file download errors emit the canonical STDERR WARNING STRING `"warning: failed to download attachment <AID>: <reason>"` to stderr in BOTH modes (EC-2.7.008-6/EC-2.7.008-7; not suppressed in JSON mode; nothing goes to stdout as a JSON error object); `"Downloaded N of M"` summary fires in human mode only (suppressed in JSON mode).
> - **GHSA-9857-6MW7-FQ2M**: reqwest 0.13 strips Authorization/Cookie on cross-host redirects. This is CORRECT behavior for CDN download — do NOT fight it. A test MUST assert Authorization is absent on redirect-target requests.
> - **JRACLOUD-97046**: `?redirect=false` breaks some file formats. MUST NOT use `?redirect=false`; use redirect-following (reqwest default).
> - **DISPLAY-SANITIZATION SCOPE (BC-2.7.011 v1.3.94 — PRE-F4-UNICODE DISCHARGED):** `display_sanitize_filename` from S-576-1 covers the full extended sanitization set per BC-2.7.011 display-sanitization primary clause (ASCII controls + bidi controls + line separators + NEL). The PRE-F4-UNICODE obligation is discharged @ v1.3.94 — no further scope expansion pending.

# S-576-2: `jr issue attachment download` — single/batch/newest + streaming + CWE-22 sanitization

**Bundle**: SOH-ATTACHMENTS-1
**GitHub issue**: #576
**BC anchors**: BC-2.7.007 (single-file), BC-2.7.008 (batch --all), BC-2.7.009 (--newest N), BC-2.7.010 (output path), BC-2.7.011 (CWE-22 sanitization), BC-2.7.012 (error taxonomy)
**VPs**: VP-576-001 (sanitize_attachment_filename proptest)

---

## Narrative

- **As a** developer or CI script using `jr` against Jira issues
- **I want** `jr issue attachment download <KEY>` to stream attachments to disk with a safe default path, supporting single-file (`--id`), batch (`--all`), and newest-N (`--newest N`) selectors
- **So that** I can retrieve large attachments without memory exhaustion, with server-supplied filenames sanitized against path-traversal attacks (CWE-22)

---

## Behavioral Contracts

| BC | Title | This Story's Scope |
|----|-------|--------------------|
| BC-2.7.007 | Single-file download (`--id`); redirect-following; overwrite-refuse; streaming; JSDCLOUD-10841 | Full |
| BC-2.7.008 | Batch download (`--all`) to `--out-dir`; fail-soft; dir-not-exist exit 64; not-a-directory exit 64 | Full |
| BC-2.7.009 | `--newest N` top-N by created desc | Full |
| BC-2.7.010 | Default output path: batch `<sha1-of-id>_<sanitized-basename>`; single-`--id` bare sanitized basename; id-as-filename degenerate fallback | Full |
| BC-2.7.011 | `sanitize_attachment_filename` CWE-22 5-step algorithm + containment check | Full (disk-path variant; distinct from S1's display_sanitize_filename) |
| BC-2.7.012 | Unknown KEY/AID exit 64; match-by-id invariant | Full |

---

## Acceptance Criteria

### AC-001 (traces to BC-2.7.007 postcondition: two-step wire path; streaming; no ?redirect=false)

`jr issue attachment download <KEY> --id <AID>` issues:
1. Step 1: `GET /rest/api/3/attachment/{id}` (metadata; tolerates partial struct — missing fields do not abort per P26-003).
2. Step 2: `GET /rest/api/3/attachment/content/{id}` (content; follows 302/303 redirects; MUST NOT append `?redirect=false` — JRACLOUD-97046; `Authorization` header is stripped by reqwest on cross-host redirect per GHSA-9857-6MW7-FQ2M — correct behavior).

Response body is streamed chunk-by-chunk via `bytes_stream()` (ADR-0017 reqwest `stream` feature) to a `tmp_<random>` file in the SAME DIRECTORY as the final path (BC-2.7.007 ~749: process-unique random token; NOT basename-derived, e.g. NOT `<basename>.tmp`; NOT OS temp dir — cross-filesystem rename is not atomic; EC-2.7.007-8 concurrent same-dir downloads depend on this naming), then atomically renamed to the final path. On error mid-stream: temp file deleted, exit 1 (EC-2.7.007-4). On Ctrl+C/SIGINT: temp file deleted, exit 130 (EC-2.7.007-5 best-effort; `tokio::signal::ctrl_c()` implementation note).

A wiremock test MUST assert the `Authorization` header is ABSENT from the redirect-target request (SEC-576-003 / BC-2.7.007 EC-2.7.007-3 / CWE-522). **EC-2.7.007-3 DISTINCT-HOST mandate**: the two wiremock servers MUST use DISTINCT HOST STRINGS (e.g., `127.0.0.1` for the Jira API server and `[::1]` or a distinct loopback hostname for the CDN redirect target). Using the same host at different ports (e.g., two `127.0.0.1` instances on different ports) would make the assertion vacuous: reqwest's cross-host check compares `host_str()` which IGNORES port numbers, so a same-host-different-port redirect would NOT strip `Authorization` headers — the test would pass while the credential-stripping invariant goes untested.

- **Tests:** `tests/attachment_download.rs::test_bc_2_7_007_two_step_streaming_wire_path`; `tests/attachment_download.rs::test_bc_2_7_007_no_redirect_false_param`; `tests/attachment_download.rs::test_bc_2_7_007_auth_absent_on_redirect_target`

---

### AC-002 (traces to BC-2.7.007 postcondition: --out pre-flights before metadata GET; P32-001)

For single-`--id` with `--out <PATH>`:
1. Local pre-flights fire BEFORE step-1 metadata GET (P32-001 fail-cheap-first principle):
   - EC-2.7.007-6: `--out` parent directory does not exist → exit 64: `"Output directory does not exist: <parent>"` before HTTP.
   - EC-2.7.007-11: `--out` path is an existing directory → exit 64: `"output path is a directory: <PATH>"` before HTTP.
   - EC-2.7.007-12: `--out` path is an existing regular file AND `--force` absent → exit 64: `"File already exists: <path>. Use --force to overwrite."` before HTTP (SEC-576-010).
2. On double-fault (local check fails), the local check's error message wins.
3. **`--force` bypass**: when `--force` is supplied and `--out <PATH>` targets an existing file, the overwrite-refuse check (EC-2.7.007-12) is skipped and the file is overwritten silently on download completion.

Without `--out`, the unconditional step-1 metadata GET runs (to obtain filename for path computation).

- **Test:** `tests/attachment_download.rs::test_bc_2_7_007_out_preflight_before_get_p32_001`

---

### AC-003 (traces to BC-2.7.007 postcondition: selector-required; AID validation)

`jr issue attachment download <KEY>` without any selector (`--id`, `--all`, `--newest`) → exit 2 (clap required-group). A non-numeric `--id` value → exit 64, `"invalid attachment id: '<VALUE>' (must be numeric)"`, before HTTP.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_007_selector_required_aid_validation`

---

### AC-004 (traces to BC-2.7.011 postcondition: sanitize_attachment_filename CWE-22 disk-path algorithm)

`sanitize_attachment_filename(name: &str) -> Option<String>` implements the BC-2.7.011 5-step CWE-22 disk-path algorithm (verbatim from BC-2.7.011 lines 902-911):

1. **Basename extraction**: `Path::new(name).file_name()` — strips any directory component (`../../etc/passwd` → `"passwd"`; `/etc/passwd` → `"passwd"`; `C:\Windows\system32\calc.exe` → `"calc.exe"`). If `file_name()` returns `None` (path ends in `..` or reduces to empty) → return `None`.
2. **Pseudo-name rejection**: if the extracted basename as a `Path` component equals `"."` or `".."` → return `None`. Empty string after OsStr conversion → return `None`.
3. **NUL byte rejection**: if the name contains a NUL byte (`\0`) → return `None`. NUL terminates strings in OS path APIs; this is a REJECTION (return None), NOT a strip operation.
4. **Character scrub**: REPLACE any remaining `/`, `\`, or `:` in the string with `_`. These are path separators on various platforms. This is a REPLACEMENT with underscore, NOT a strip.
5. **Length cap** (UTF-8-safe): truncate to at most 214 bytes on a valid UTF-8 character boundary (`floor_char_boundary` semantics — never split a multi-byte codepoint). Step 5.5 (SEC-576-007): strip trailing ASCII whitespace characters and trailing `.` from the basename after the length cap.

Returns `Some(sanitized_name)` if all steps produce a non-empty string; otherwise `None`. Fallback to id-as-filename per BC-2.7.010 degenerate case when `None` is returned.

**Windows device names (SEC-576-001 — caller responsibility, NOT this function)**: `sanitize_attachment_filename` does NOT handle Windows device names. The expected results are: `"CON"` → `Some("CON")`; `"NUL"` → `Some("NUL")`; `"COM1"` → `Some("COM1")`; `"nul.txt"` → `Some("nul.txt")`. Device-name escape is the **caller's** responsibility — batch paths get an automatic SHA-1 prefix that disambiguates device names; single-id bare paths need an explicit `_` prefix escape at the call site.

**Containment check** (BC-2.7.011 two-step, SEC-576-002): `canonicalize(out_dir)` + `path.starts_with(out_dir_canonical)`. This check MUST NOT be applied to `--out <PATH>` (trusted operator input — BC-2.7.011 P25-002). **Mutation exemption**: the containment check is intentionally unreachable via any API-supplied filename after steps 1–5; surviving mutants on this path are accepted-class (annotate branch with `// BC-2.7.011 defense-in-depth: unreachable via API-supplied filenames after sanitization steps 1-5`).

**Unit tests** in `src/cli/issue/attachments.rs #[cfg(test)]` MUST cover at minimum (BC-2.7.011 "Unit test coverage required" list):
- `"../../etc/passwd"` → `Some("passwd")` (path traversal stripped)
- `"/etc/passwd"` → `Some("passwd")` (absolute path stripped)
- `"C:\\Windows\\system32\\calc.exe"` → `Some("calc.exe")` (Windows path stripped)
- `"."` → `None`; `".."` → `None`; empty string → `None`
- NUL-containing string (e.g. `"foo\0bar"`) → `None` (NOT `Some("foobar")`)
- Filename with `:` (e.g. `"C:config.txt"`) → `Some("C_config.txt")` (colon replaced, NOT stripped)
- `"CON"` → `Some("CON")` (device name passes through — caller escapes)
- `"NUL"` → `Some("NUL")`; `"COM1"` → `Some("COM1")`; `"nul.txt"` → `Some("nul.txt")`
- Filename exceeding 255 bytes → length ≤ 214 bytes
- Multi-byte UTF-8 codepoint at truncation boundary (214-byte ASCII prefix + 3-byte `é`) → `Some(214-byte prefix)` (char dropped, not split)
- Trailing dot/space strip (e.g. `"foo. "` → `Some("foo")`)

These are inline unit tests in `#[cfg(test)]`, not wiremock.

**VP-576-001**: see AC-014.

- **Tests:** `src/cli/issue/attachments.rs #[cfg(test)]` unit suite; `prop_sanitize_attachment_filename_no_path_traversal` proptest

---

### AC-005 (traces to BC-2.7.010 postcondition: default output path computation)

Default output path computation (no `--out` flag):
- Batch (`--all` / `--newest`): `<out-dir>/<sha1-of-attachment-id>_<sanitize_attachment_filename(filename)>`. SHA-1 prefix is 40 hex chars + `_` + sanitized basename. Combined length ≤ 255 bytes (41 + 214 = 255 ≤ NAME_MAX per ADV-010). When `sanitize_attachment_filename` returns `None` (degenerate name), use the id as the basename component, RETAINING the SHA-1 prefix (`<sha1-of-id>_<id>`).
- Single `--id`: bare `sanitize_attachment_filename(filename)` in cwd (or `--out` override if supplied).

Server-ID trust assumption: the `id` from the Jira API is not independently validated for use in the SHA-1 computation — it is trusted as numeric per BC-2.7.010 SEC-576-008 note.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_010_default_path_sha1_prefix_batch`

---

### AC-006 (traces to BC-2.7.007 EC-2.7.007-7: filename=RAW / path=on-disk pairing; P27-001)

In the download manifest JSON (`--output json`):
- `downloaded[].filename` is the RAW Jira-supplied filename (pre-sanitization; per P27-001 deliberate pairing).
- `downloaded[].path` is the output path as-constructed by `jr` — NOT canonicalized, NOT made absolute (EC-2.7.007-7 / BC-2.7.010 P18-004 ruling). Snapshot tests MUST redact or normalize `path` (e.g., via a TempDir root substitution); exact-match assertions on `path` are only valid with a controlled current working directory.
- `downloaded[].size` is bytes-WRITTEN (tracked byte count from the streaming write loop — `futures::StreamExt` + `tokio AsyncWriteExt` — per P31-002 bytes-written semantics; NOT from `tokio::io::copy` — bridging `bytes_stream()` to `tokio::io::copy` would require a `tokio_util::io::StreamReader` adapter; `tokio-util` is not in the S-576-2 slot (ships at S-576-3 per ADR-0017); the explicit write loop is used instead, which also yields the per-file byte count), NOT the list-reported `fields.attachment[].size`.
- `downloaded[].id` is the attachment AID string.

JSON shape: `{"downloaded": [{"filename": <raw>, "id": "<AID>", "path": "<as-constructed path>", "size": <bytes-written>}]}`. Keys in BTreeMap-alphabetical order within each element.

Output routed through `output::render_json` (#526 invariant).

- **Test:** `tests/attachment_download.rs::test_bc_2_7_007_json_manifest_raw_filename_written_size_p27_p31`

---

### AC-007 (traces to BC-2.7.008 postcondition: --all batch download; fail-soft; out-dir existence)

`jr issue attachment download <KEY> --all --out-dir <DIR>`:
- Fetches attachment list via `GET /rest/api/3/issue/{key}?fields=attachment` (batch metadata source — BC-2.7.008).
- Downloads each attachment fail-soft: per-file errors emit the canonical STDERR WARNING STRING `"warning: failed to download attachment <AID>: <reason>"` to stderr in BOTH modes (EC-2.7.008-6/EC-2.7.008-7; not suppressed in JSON mode; nothing goes to stdout as a JSON error object); other files continue. **Exit code on failure: partial batch failure (at least one file fails) → exit 1 (EC-2.7.008-7). All files fail → exit 1 + human-mode `"Downloaded 0 of M attachments to <dir>."` summary (EC-2.7.008-8). The manifest JSON is still emitted at exit 1 (EC-2.7.008-6 — manifest contains only the successfully downloaded entries; failures appear only as stderr warnings).**
- `--out-dir` not-exist → exit 64 before download, stderr `"Output directory does not exist: <DIR>"` (EC-2.7.008-2). `--out-dir` exists but not a directory → exit 64, stderr `"Not a directory: <PATH>"` (EC-2.7.008-4).
- Collision-skip (overwrite-refuse): when a file would overwrite an existing file, emits a collision-skip warning as a NON-ERROR hint — suppressed in JSON mode (P27-003 / §2.7 hint-vs-error taxonomy). The filename in the collision-skip warning MUST be display-sanitized via `display_sanitize_filename` (from S-576-1) before writing to stderr per BC-2.7.011 SEC-576-011 (CWE-116) — collision-skip warnings are a named display-sanitization call site.
- Empty issue (`--all` on issue with zero attachments): exit 0; JSON mode returns `{"downloaded":[]}` (EC-2.7.008-1 + EC-2.7.001-1 for JSON).
- `"Downloaded N of M attachments to <dir>."` summary fired in human mode ONLY (suppressed in JSON mode per P25-001 / §2.7 taxonomy). Verbatim from BC-2.7.008 line 795: the string MUST include `"to <dir>"` (the actual output directory path).
- `jr issue attachment download <KEY> --all` (no `--out-dir`): `--out-dir` is optional per BC-2.7.008 ~785/791; when absent the output directory defaults to the current working directory. The summary string still includes `"to <dir>"` — the cwd path at invocation time. Test uses `TempDir` as the working directory (change cwd before spawning process, not `--out-dir`) to confirm files land in cwd.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_008_all_batch_fail_soft` (sub-assertion: `--out-dir` not-exist path → exit 64 + stderr contains `"Output directory does not exist: <DIR>"` (EC-2.7.008-2); sub-assertion is an explicit case, not a combined catch-all; sub-assertion: `--out-dir` points at an existing regular file → exit 64 + stderr contains `"Not a directory:"` (EC-2.7.008-4); explicit case, not a catch-all — mirrors P25-003 EC-2.7.008-2 treatment; sub-assertion EC-2.7.008-7 partial-fail exit-1: 2-attachment wiremock issue, one content-GET returns 500 → exit 1 + stderr contains `"warning: failed to download attachment <AID>:"` + human-mode output contains `"Downloaded 1 of 2 attachments to "` (the successfully downloaded file written; the failed one absent from manifest); sub-assertion EC-2.7.008-8 all-fail exit-1: every content-GET fails → exit 1 + human-mode output contains `"Downloaded 0 of 2 attachments to "` — DO NOT leave this optional, it is a required sub-case)
- **Test:** `tests/attachment_download.rs::test_bc_2_7_008_all_no_out_dir_defaults_to_cwd`
- **Test:** `tests/attachment_download.rs::test_bc_2_7_008_empty_issue_no_attachments_hint` (human mode: `--all` on zero-attachment issue → exit 0 + stderr contains `"No attachments on <KEY>."` + no download output (EC-2.7.008-1); JSON mode: `--all` on zero-attachment issue → exit 0 + stdout `{"downloaded":[]}` + NO stderr hint (EC-2.7.001-1 / EC-2.7.008-1 hint suppressed in JSON); sub-assertion: `--newest N` on zero-attachment issue → exit 0 + stdout `{"downloaded":[]}` empty path (EC-2.7.009-4))

---

### AC-008 (traces to BC-2.7.009 postcondition: --newest N top-N by created desc)

`jr issue attachment download <KEY> --newest N` selects the top-N attachments by `created` descending from the full attachment list, AFTER applying any `--filter` flags (filter-before-select order per BC-2.7.003/004/005). Downloads the selected subset. Empty issue on `--newest` follows EC-2.7.008-1 (same empty-result path as `--all`).

- **Test:** `tests/attachment_download.rs::test_bc_2_7_009_newest_n_by_created_desc`

---

### AC-009 (traces to BC-2.7.012 postcondition: unknown KEY/AID error taxonomy)

Error taxonomy for `attachment download`:

| Condition | Exit | Stderr |
|-----------|------|--------|
| Invalid AID (non-numeric) | 64 | `"invalid attachment id: '<VALUE>' (must be numeric)"` |
| AID not found (404 on step 1) | 64 | `"Attachment <AID> not found or not accessible."` (batch-paths: KEY 404 only per P21-006/P22-003) |
| KEY 404 (batch paths: --all/--newest) | 64 | `"Issue <KEY> not found or not accessible."` |
| 401 | 2 | stderr contains `"Not authenticated"` AND `"jr auth login"` (full literal: `Not authenticated. Run "jr auth login" to connect.` — `src/error.rs::JrError` + `client.rs`) |
| AID 403 (single `--id` path) | 1 | `"Permission denied: cannot access attachment <AID>."` (EC-2.7.007-1b / BC-2.7.012 ~961) |
| KEY 403 (batch `--all`/`--newest` path) | 1 | `"Permission denied: cannot access issue <KEY>."` (BC-2.7.012 ~959) |
| KEY or AID 5xx | 1 | `API error (<N>)` (single mode; batch mode: per-file fail-soft per BC-2.7.008) |
| Network error | 1 | stderr contains `"Could not reach"` (full literal: `Could not reach <host> — check your connection` — `src/error.rs::JrError::NetworkError`) (single mode; batch mode: per-file fail-soft per BC-2.7.008) |
| Write error: ENOSPC | 1 | `"Disk full: not enough space to write <path>"` |
| Write error: EACCES | 1 | `"Permission denied: cannot write to <dir>"` |
| Write error: other | 1 | Raw OS error message surfaced |

Note (BC-2.7.012 P21-006): `--id` does NOT server-verify KEY (uses AID directly); KEY-based 404 appears only on batch paths that fetch the issue's attachment list.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_012_error_taxonomy` (assertions MUST cover ALL taxonomy rows as sub-cases: invalid AID (non-numeric) → exit 64 + stderr contains `"invalid attachment id:"` (loose-substring); AID 404 (`--id` path) → exit 64 + stderr contains `"Attachment"` AND `"not found or not accessible"` (loose-substring); KEY 404 (batch `--all`/`--newest` path) → exit 64 + stderr contains `"Issue"` AND `"not found or not accessible"` (loose-substring); 401 → exit 2 + stderr contains `"Not authenticated"` AND `"jr auth login"` per v1.3.88 loose-substring form; AID 403 (single `--id` path) → exit 1 + stderr contains `"Permission denied: cannot access attachment "` (loose-substring; EC-2.7.007-1b / BC-2.7.012 ~961); KEY 403 (batch path) → exit 1 + stderr contains `"Permission denied: cannot access issue "` (loose-substring; BC-2.7.012 ~959); each 403 case is an explicit sub-assertion with single-vs-batch distinction, not a combined catch-all; 5xx → exit 1 + stderr contains `"API error ("` (loose-substring, single-mode); network → exit 1 + stderr contains `"Could not reach"` (loose-substring per v1.3.88/v1.3.89 form, single-mode); ENOSPC → exit 1 + stderr contains `"Disk full: not enough space to write <path>"`; EACCES → exit 1 + stderr contains `"Permission denied: cannot write to <dir>"`; other write error → exit 1 only (raw OS message — unpinnable as canonical substring); each row is an explicit sub-assertion, not a combined catch-all)

---

### AC-010 (traces to BC-2.7.007 step 2 + EC-2.7.007-2: JSDCLOUD-10841 JSM uniform content URL)

Download MUST issue `GET /rest/api/3/attachment/content/{id}` (step 2 platform endpoint) regardless of whether the attachment is on a JSM issue (EC-2.7.007-2). There is no JSM-specific code path for download. The servicedeskapi `links.content` URL MUST NOT be used — JSDCLOUD-10841 confirms these URLs return 404. The platform content endpoint is authoritative for all attachment downloads.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_007_uses_platform_content_url_not_jsm_links_ec_2_7_007_2`

---

### AC-011 (traces to BC-2.7.007 postcondition: write-to-temp + atomic-rename; cleanup-on-error)

Downloads write to a `tmp_<random>` file in the SAME DIRECTORY as the final path (BC-2.7.007 ~749: process-unique random token; NOT `<final-path>.tmp` or any basename-derived suffix; NOT OS temp dir — cross-filesystem rename is not atomic; EC-2.7.007-8 concurrent same-dir downloads depend on this naming scheme), then atomically rename to the final path. On error mid-stream (EC-2.7.007-4): temp deleted, exit 1. On Ctrl+C/SIGINT (EC-2.7.007-5): best-effort temp deletion via `tokio::signal::ctrl_c()`, exit 130.

- **Tests:** `tests/attachment_download.rs::test_bc_2_7_007_atomic_rename_cleanup_on_error`; `tests/attachment_download.rs::test_bc_2_7_007_temp_file_same_dir_tmp_random_prefix` (assert temp name matches `tmp_` prefix followed by a random token — NOT `<basename>.tmp` suffix; assert temp parent dir == final-path parent dir, NOT OS temp; pins BC-2.7.007 ~749 same-dir + process-unique-random naming; EC-2.7.007-8 concurrent correctness)

---

### AC-012 (traces to BC-2.7.008 EC-2.7.008-6: JSON hint/error taxonomy — per-file failures in JSON mode)

In JSON mode (`--output json`), per-file download failures emit the canonical STDERR WARNING STRING `"warning: failed to download attachment <AID>: <reason>"` to stderr (EC-2.7.008-6/EC-2.7.008-7; not suppressed in JSON mode; errors, not hints — nothing goes to stdout as a JSON error object). The `"Downloaded N of M"` summary is NOT emitted (it is a hint, suppressed in JSON mode). Collision-skip warnings are NOT emitted in JSON mode (hint class). The final manifest `{"downloaded":[...]}` contains only successfully downloaded entries. **Exit code: partial/full batch failure in JSON mode → exit 1 (EC-2.7.008-6/7); the exit code MUST be asserted alongside manifest-only-successes + warning assertions — exit 0 with a warning would be a silent correctness bug.**

- **Test:** `tests/attachment_download.rs::test_bc_2_7_008_json_mode_error_vs_hint_taxonomy` — extend with explicit exit-code assertion: 2-attachment issue, one content-GET fails → exit 1 (EC-2.7.008-6/7) + stderr contains `"warning: failed to download attachment <AID>:"` + stdout manifest contains only the successful entry (no JSON error object in stdout). Assert `exit_code == 1` as an explicit sub-assertion, not just stderr content.

---

### AC-013 (traces to BC-2.7.007 precondition: e2e surface guard + CLAUDE.md delivery obligations)

**e2e surface guard** (GAP-AUDIT-576-001 §3.3): `tests/e2e_cli_surface_guard.rs` SURFACE table gains entries for `attachment download` paths and flags: `<KEY>` (required positional), `--id <AID>`, `--all`, `--newest <N>`, `--out <PATH>`, `--out-dir <DIR>`, `--force` (overwrite existing; EC-2.7.007-12 bypass — P1-012), `--filter <FILTER>` (repeatable), `--output json`, `--no-input`, `--profile`, `--no-color`.

**CLAUDE.md obligations** (GAP-AUDIT-576-001 §3.4 — earliest consumers for S2):
- Add §3.4(2c): filename-sanitization CWE-22 gotcha — `sanitize_attachment_filename` 5-step algorithm; containment check; mutation exemption note.
- Add §3.4(2d): redirect-behavior gotcha — `GHSA-9857-6MW7-FQ2M`, reqwest 0.13 strips Authorization/Cookie on cross-host redirect; `?redirect=false` prohibited (JRACLOUD-97046).

**docs/specs/json-output-shapes.md** (GAP-AUDIT-576-001 §3.1): add download manifest row — `{"downloaded":[{filename,id,path,size}]}` shape (P27-001 semantics noted).

- **Tests:** `tests/e2e_cli_surface_guard.rs` (SURFACE entries); `tests/claude_md_citations.rs` (CLAUDE.md citations resolve)

---

### AC-014 (traces to BC-2.7.011 VP-576-001: sanitize_attachment_filename proptest)

`prop_sanitize_attachment_filename_no_path_traversal` in `src/cli/issue/attachments.rs #[cfg(test)]`: the proptest domain MUST include non-ASCII-printable bytes (NUL, control chars) — not limited to ASCII-printable — so that step 3 (NUL → None) and step 4 (character scrub) are exercised by the random input generator.

For any arbitrary byte-string input (including NUL bytes and control characters), assert (VP-576-001 verbatim from BC-2.7.011 line 930):
1. No `Some(name)` result contains `/`, `\`, `:`, or a NUL byte.
2. `Some(name)` length in bytes is ≤ 214.
3. All `Some(name)` values are valid UTF-8 (`std::str::from_utf8` succeeds).
4. `"."`, `".."`, empty string, and NUL-byte inputs each return `None`.
5. `"../../etc/passwd"` → `Some("passwd")`; `"/etc/passwd"` → `Some("passwd")`.
6. A 214-byte ASCII prefix + 3-byte UTF-8 char returns `Some(214-byte prefix)` (char dropped, not split).
7. `"CON"` → `Some("CON")`; `"NUL"` → `Some("NUL")` (device names pass through — NOT rejected; caller escapes per SEC-576-001).

**Remove** any assertion that output "does not match Windows device names" — device names are expected `Some(name)` outputs per the BC.

Default proptest case count (~256); cap to 128 only on CI flake pressure.

- **Test:** `prop_sanitize_attachment_filename_no_path_traversal` (proptest in `src/cli/issue/attachments.rs`)

---

### AC-015 (traces to BC-2.7.010 SEC-576-011 postcondition: display_sanitize_filename required in degenerate-name warning)

When `sanitize_attachment_filename(name)` returns `None` (degenerate name — empty, `"."`, `".."`, NUL-containing, etc.) and the id-as-filename fallback is used, the degenerate-name warning emitted to stderr MUST display-sanitize the raw `<raw>` value via `display_sanitize_filename` BEFORE writing to the TTY (BC-2.7.010 ~line 870, SEC-576-011, CWE-116). The warning format is:

`"warning: using id as filename for attachment <AID> — original name '<display-sanitized-raw>' could not be sanitized."`

The `<raw>` string in the warning is the **display-sanitized** form (CWE-116 terminal injection prevention), not the verbatim Jira-supplied name. This sanitization applies in HUMAN MODE ONLY — JSON mode suppresses the warning entirely (raw value retained in the manifest's `filename` field; no TTY exposure vector, so no sanitization needed).

- **Test:** `tests/attachment_download.rs::test_bc_2_7_010_degenerate_name_warning_display_sanitized`

---

### AC-016 (traces to BC-2.7.011 SEC-576-001 postcondition: Windows device-name escape at single-id call site)

At the **single-id** bare-naming call site (the path where `sanitize_attachment_filename` returns `Some(name)` and the name is used as the output filename for `--id` without a SHA-1 prefix), the caller MUST check whether the sanitized basename (the portion before the first `.`) is a Windows reserved device name (case-insensitive match: `CON`, `NUL`, `PRN`, `AUX`, `COM1`–`COM9`, `LPT1`–`LPT9`) and, if so, prepend `_` to produce `_CON`, `_NUL`, etc. (SEC-576-001: Windows NTFS/FAT32 reserved names; BC-2.7.011 ~line 915).

**Scope restriction**: This escape is applied at the single-id call site ONLY:
- Batch paths (`--all`, `--newest`) already have a 40-hex-char SHA-1 prefix that prevents device name conflicts — no additional escape needed.
- `--out <PATH>` paths are trusted operator input (BC-2.7.011 P25-002) — not subject to this escape.

`sanitize_attachment_filename` itself does NOT perform the device-name escape (it returns `Some("CON")`, `Some("NUL")` etc. — the caller is responsible per BC-2.7.011 P18-004).

- **Test:** `tests/attachment_download.rs::test_bc_2_7_011_windows_device_name_escape_single_id_call_site`

---

### AC-017 (traces to BC-2.7.007 required-group/EC-2.7.007-9 ~769; BC-2.7.008 EC-2.7.008-3 ~803; BC-2.7.009 body ~834/EC-2.7.009-1 ~836/EC-2.7.009-2: clap structural constraints + handler N-validation for download flags)

`jr issue attachment download` structural constraints split into two layers:

**Layer 1 — Clap parse-time constraints (all exit 2; clap error to stderr; no application code reached):**
- `download <KEY> --id <AID> --all` → exit 2 (`--id` and `--all` are mutually exclusive; EC-2.7.008-3 ~803).
- `download <KEY> --id <AID> --newest 3` → exit 2 (`--id` and `--newest` are mutually exclusive; BC-2.7.009 body ~834).
- `download <KEY> --all --newest 3` → exit 2 (`--all` and `--newest` are mutually exclusive; BC-2.7.009 body ~834).
- `download <KEY> --all --out /tmp/foo` → exit 2 (`--out` requires single-mode `--id`; EC-2.7.007-9 ~769).
- `download <KEY> --newest 3 --out /tmp/foo` → exit 2 (`--out` is invalid with `--newest` as well as `--all`; EC-2.7.007-9 ~770 — discriminates `requires = "id"` from a `conflicts_with = "all"` mis-wiring).
- `download <KEY>` (no `--id`, no `--all`, no `--newest`) → exit 2 (at least one selector required; BC-2.7.007 required-group clause ~729).
- `download <KEY> --newest foo` (non-integer) → exit 2 (clap type-parse failure; EC-2.7.009-2).
- `download <KEY> --id <AID> --filter key=val` → exit 2 (`--filter` conflicts with `--id`; EC-2.7.007-10, `conflicts_with = "id"`).
- `download <KEY> --out-dir /tmp` (no `--all`/`--newest` batch selector) → exit 2 (`--out-dir` requires the `[all,newest]` ArgGroup — jr's first ArgGroup; EC-2.7.008-9 ~812).
- `download <KEY> --out-dir /tmp --id <AID>` → exit 2 (`--out-dir` requires `[all,newest]` ArgGroup; `--id` is incompatible with `--out-dir`; EC-2.7.008-9 ~812).

**Layer 2 — Handler-level N-validation (exit 64; fired before any HTTP call; `allow_negative_numbers = true` so negative values pass clap and are caught here):**
- `download <KEY> --newest 0` → exit 64, message `"--newest requires a positive integer."` (EC-2.7.009-1 ~836; N = 0 fails N ≥ 1 guard).
- `download <KEY> --newest -3` → exit 64, same message and handler path (N ≤ 0 guard; `allow_negative_numbers = true` lets `-3` reach the handler).

- **Tests:** `tests/attachment_download.rs::test_bc_2_7_download_clap_structural_constraints` (10 clap exit-2 cases); `tests/attachment_download.rs::test_bc_2_7_009_newest_nonpositive_exits_64` (2 handler exit-64 cases: N=0, N=-3)

---

### AC-018 (traces to BC-2.7.007 postcondition ~747: single-id success hint to stderr)

After a successful single-`--id` download, `jr issue attachment download <KEY> --id <AID>` MUST emit the following human-mode success hint to **stderr** (suppressed in JSON mode per §2.7 hint taxonomy):

`"Downloaded: <path> (<size_human>)."`

Where:
- `<path>` is the on-disk path as constructed by jr (NOT canonicalized/absolute; consistent with AC-006 P18-004 semantics).
- `<size_human>` is the human-readable byte size (e.g. `"1.2 MB"`, `"42.0 KB"`, `"512 B"`).
- This hint is SUPPRESSED in JSON mode (`--output json`); the machine-readable result is the `{"downloaded":[...]}` manifest on stdout.
- **DISPLAY-SANITIZATION (BC-2.7.011 ~936 every-call-site clause / SEC-576-011 / CWE-116):** The **filename portion** of `<path>` interpolated into the stderr hint string MUST be routed through `display_sanitize_filename` before emission. Raw server-supplied filenames may contain terminal-unsafe characters; the every-call-site clause applies to ALL stderr/stdout surfaces that interpolate a server-supplied filename or a path derived from one.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_007_single_id_success_hint_stderr` — extend with sub-assertion: when the server-supplied filename contains a character that `display_sanitize_filename` would replace, the echoed path contains the sanitized form, not the raw form.

---

### AC-019 (traces to EC-2.7.008-10 ~814 / EC-2.7.009-3 ~839: filtered-to-zero hint)

When `--all` or `--newest N` is combined with `--filter` flags and the filter matches **zero** attachments on a **non-empty** issue (i.e., the issue has attachments but none pass the filter), `jr` MUST:

1. Emit to **stderr** (human mode only; suppressed in JSON mode): `"No attachments matched the filter on <KEY>."`
2. Exit 0.
3. In JSON mode (`--output json`): emit `{"downloaded":[]}` to stdout (no stderr hint).

**Distinct from empty-issue case (EC-2.7.008-1 / EC-6):** EC-6 fires when the issue has zero attachments regardless of filters; AC-019 fires when the issue has attachments but the filter eliminates all of them. Both exit 0.

- **Test:** `tests/attachment_download.rs::test_bc_2_7_008_filtered_to_zero_hint`

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `handle_attachment_download` | `src/cli/issue/attachments.rs` | Effectful (HTTP + disk write) |
| `get_attachment_metadata` | `src/api/jira/attachments.rs` | Effectful (HTTP) |
| `get_attachment_content` | `src/api/jira/attachments.rs` | Effectful (HTTP streaming; ADR-0017 `stream` feature) |
| `sanitize_attachment_filename` | `src/cli/issue/attachments.rs` | Pure (CWE-22 disk-path variant) |
| `compute_default_output_path` | `src/cli/issue/attachments.rs` | Pure (SHA-1 + sanitize) |
| `display_sanitize_filename` | `src/cli/issue/attachments.rs` (from S1) | Pure (reuse; CWE-116 display variant) |

---

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-1 | `--out` parent dir doesn't exist | Exit 64 before HTTP (P32-001 pre-flight first) |
| EC-2 | `--out` path is a directory | Exit 64 before HTTP |
| EC-3 | `--out` path exists (overwrite-refuse) | Exit 64 before HTTP (EC-2.7.007-12) |
| EC-4 | Error mid-stream | Temp file deleted, exit 1 |
| EC-5 | SIGINT during download | Best-effort temp cleanup, exit 130 |
| EC-6 | Zero attachments on `--all` | Exit 0; JSON `{"downloaded":[]}` |
| EC-7 | `sanitize_attachment_filename` returns None (empty result) | Use attachment ID as basename (BC-2.7.010 degenerate fallback) |
| EC-8 | `--newest N` with fewer than N attachments | Download all available; no error |
| EC-9 | Filename collision on batch download | Collision-skip warning (hint, stderr only; not in JSON mode); filename in warning display-sanitized via `display_sanitize_filename` (BC-2.7.011 SEC-576-011 CWE-116) |
| EC-10 | Cross-host 302 redirect | Follow redirect; `Authorization` absent on target request (GHSA-9857-6MW7-FQ2M) |

---

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `src/cli/issue/attachments.rs::handle_attachment_download` | Effectful shell | HTTP + disk write + atomic rename |
| `src/api/jira/attachments.rs::get_attachment_metadata` | Effectful shell | HTTP GET; reads network |
| `src/api/jira/attachments.rs::get_attachment_content` | Effectful shell | HTTP streaming GET; writes to temp file |
| `src/cli/issue/attachments.rs::sanitize_attachment_filename` | Pure core | CWE-22 disk-path sanitization; no I/O |
| `src/cli/issue/attachments.rs::compute_default_output_path` | Pure core | SHA-1 + sanitize; no I/O |
| `src/cli/issue/attachments.rs::display_sanitize_filename` | Pure core | CWE-116 display sanitization; reused from S-576-1 |

---

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~10,000 |
| `src/cli/issue/attachments.rs` (extends S1's file, ~600 LOC total) | ~9,000 |
| `src/api/jira/attachments.rs` (extends S1's file, ~400 LOC total) | ~6,000 |
| BC-2.7.007..012 bodies (6 BCs × ~1200 tokens avg) | ~7,200 |
| ADR-0017 | ~2,500 |
| `tests/attachment_download.rs` (new file, 33 tests (22 AC tests + 11 adversarial/mutation regression pins added during Step 4.5 + CI mutation gate)) | ~7,100 |
| Tool outputs overhead | ~3,000 |
| **Total** | **~44,800** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~22%** |

---

## Tasks (MANDATORY)

### Task 0: Read BC-2.7.007..012 in full; internalize settled rulings

Read `.factory/specs/prd/bc-2-issue-read.md §2.7 BC-2.7.007..012` completely. Internalize: P27-001 (filename=RAW/path=on-disk), P31-002 (size=bytes-written), P32-001 (--out pre-flights before GET), P25-001 (JSON hint/error taxonomy), GHSA-9857-6MW7-FQ2M, JRACLOUD-97046. ADR-0017 Cargo.toml changes for this story: reqwest `stream` feature ONLY. tokio-util `io-util` dep ships in S-576-3 (upload story, earliest consumer for multipart; ADR-0017 AMENDED 2026-07-17 per P1-010).

### Task 1: Author RED test anchors in `tests/attachment_download.rs`

Write all 33 tests (22 AC tests + 11 adversarial/mutation regression pins added during Step 4.5 + CI mutation gate; 19 ACs; AC-001 has 3, AC-007 has 3, AC-011 has 2, AC-017 has 2; AC-004/AC-014 are src unit/proptest — 0 in attachment_download.rs; AC-013 uses existing test files — 0 new). Use `jr_cmd_with_xdg` + per-test `TempDir`. Confirm RED against stubs.

### Task 2: Implement `sanitize_attachment_filename` and proptest (VP-576-001); add mutants.toml entries

Pure function in `src/cli/issue/attachments.rs`. Implement BC-2.7.011 5-step algorithm for disk paths (distinct from `display_sanitize_filename` from S1). Add unit test matrix (CON, NUL, COM1, etc.) and `prop_sanitize_attachment_filename_no_path_traversal` proptest.

**mutants.toml obligation (prd-delta-576.md S2 obligation (e) — confirm-present / ADD IF ABSENT, P22-001)**: Primary delivery of `examine_globs` entries for `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` has MOVED to S-576-1 per prd-delta-576.md S1 obligation (f) / P22-001 (add-at-creation, CWE-116 rationale + S-577-1 precedent). This story now confirm-present / ADD IF ABSENT (idempotent) — do not duplicate if already present from S1. Per cargo-mutants policy §CI Integration, `--in-diff` narrows mutations to lines changed in the PR diff AND already in `examine_globs` scope.

Verify pre-fix: `cargo test --lib -- sanitize_attachment_filename` GREEN (pure implementation, no stubs involved).

### Task 3: Add reqwest `stream` feature + sha1 crate (ADR-0017 Cargo.toml — S-576-2 slot)

Add to `Cargo.toml`:
- reqwest `stream` feature ONLY. Do NOT add `tokio-util` in this story — it ships in the S-576-3 slot (multipart + io-util, ADR-0017 AMENDED 2026-07-17 P1-010).
- `sha1 = { version = "^0.10" }` — **NEW direct dependency** per ADR-0017 2026-07-18 sha1 amendment. Use the RustCrypto `sha1` crate. SHA-1's cryptographic weakness is acceptable here (path-uniqueness prefix only, not security); the 40-hex format is pinned by H-NEW-ATTACHMENT-003 — do **not** "upgrade" to SHA-256.

Run `cargo build` to confirm compile. Run `cargo deny check` (new sha1 crate in scope) — MUST pass clean; record any new `deny.toml` allowlist entry required for sha1.

### Task 4: Implement `get_attachment_metadata` and `get_attachment_content` in `src/api/jira/attachments.rs`

- `get_attachment_metadata`: `GET /rest/api/3/attachment/{id}` with partial-struct tolerance (P26-003).
- `get_attachment_content`: `GET /rest/api/3/attachment/content/{id}` with `bytes_stream()` (ADR-0017); NO `?redirect=false` param; let reqwest follow 302/303; Authorization stripped on cross-host redirect (correct behavior per GHSA-9857-6MW7-FQ2M).

### Task 5: Wire dispatch + Implement `handle_attachment_download`

Wire `AttachmentSubcommand::Download` dispatch match arm in `src/cli/issue/mod.rs` (inline match; mirrors comment-CRUD pattern; calls `handle_attachment_download`).

Wire all three selectors (`--id`, `--all`, `--newest N`) per BCs. Apply filter-before-select for `--newest`. Implement: pre-flights (P32-001 for `--out`); atomic write-to-temp + rename; fail-soft for batch; hint-vs-error taxonomy; manifest JSON shape (P27-001/P31-002).

### Task 6: AC-013 — e2e surface guard + CLAUDE.md + json-output-shapes.md + CHANGELOG-append

Add SURFACE entries. Add CLAUDE.md gotchas §3.4(2c)/(2d). Add json-output-shapes.md download manifest row.

**CHANGELOG-append (P3-018)**: Append to `CHANGELOG.md` under the appropriate `[Unreleased]` section:
```
feat(issue): attachment download single/batch/newest + streaming + CWE-22 sanitization (#576)
```
This entry is scoped to the download surface only (S-576-2). Downstream stories S-576-3, S-576-4, S-576-5 each append their own entries.

### Task 7: Confirm all 33 tests GREEN + full suite

```
cargo test --test attachment_download
cargo test --lib -- sanitize_attachment_filename prop_sanitize
cargo test
cargo clippy -- -D warnings
cargo deny check
```

### Task 8: Create PR

PR title: `feat(issue): attachment download single/batch/newest + CWE-22 sanitization (S-576-2, #576 part 2)`.
Note: depends_on: ["S-576-1"] — S-576-1 must be merged before this story is dispatched.

---

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-576-1 | `display_sanitize_filename` introduced (CWE-116, display-only); `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` created; curated attachment-object JSON shape established | `serialize_attachment_curated` helper exists in attachments.rs; BTreeMap-ordered JSON pattern | `sanitize_attachment_filename` (disk, CWE-22) is DISTINCT from `display_sanitize_filename` (display, CWE-116); do NOT merge them |
| S-ADF-CODE-MARK-1 | ADR-0017 Cargo.toml changes are deferred to S-576-3 per ADR-0017 text — BUT reqwest `stream` feature is needed for download; add stream feature here | Per-test TempDir isolation mandatory for cache/config | GHSA-9857-6MW7-FQ2M: reqwest 0.13 strips Authorization on cross-host redirect — this is CORRECT behavior, assert it in tests |

**Note on reqwest features (ADR-0017 AMENDED 2026-07-17, P1-010)**: The Cargo.toml delivery is SPLIT — reqwest `stream` feature lands in the S-576-2 delivery slot (this story, earliest consumer for streaming download); reqwest `multipart` + tokio-util `io-util` stay in the S-576-3 slot (upload story). Use `response.bytes_stream()` here — the `stream` feature is available from this story onward. Do NOT add tokio-util in this story.

---

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `?redirect=false` MUST NOT appear in the download URL | BC-2.7.007; JRACLOUD-97046 | `grep -n "redirect=false" src/api/jira/attachments.rs` must return empty |
| `Authorization` absent on redirect-target is CORRECT behavior — test MUST assert it | GHSA-9857-6MW7-FQ2M; BC-2.7.007 EC-2.7.007-3 / SEC-576-003 | wiremock test asserts header absent on redirect target (AC-001) |
| `sanitize_attachment_filename` is the DISK-PATH variant (CWE-22); distinct from `display_sanitize_filename` (CWE-116) | BC-2.7.011; DEC-184 R3.13 | Grep: two distinct functions, neither calls the other |
| P32-001: local pre-flights before step-1 GET | BC-2.7.007 | AC-002 wiremock: assert step-1 GET not issued when pre-flight fails (`.expect(0)` on GET mount) |
| P27-001: `filename` field in manifest = RAW (pre-sanitization) | BC-2.7.007 EC-2.7.007-7 | AC-006 wiremock assertion |
| P31-002: `size` = bytes written, NOT metadata size | BC-2.7.008 | AC-006 wiremock assertion |
| All `--output json` paths via `output::render_json` | #526 invariant | `grep -n "to_string_pretty" src/cli/issue/attachments.rs` empty |
| Every stderr/stdout string interpolating a server-supplied filename (or a path derived from one) routes through `display_sanitize_filename` | BC-2.7.011 ~936; SEC-576-011; CWE-116 | `grep eprintln!/println!` sites in `attachments.rs` during review |
| No unsafe code | CLAUDE.md | `cargo clippy -- -D warnings` |

---

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| reqwest `stream` feature | 0.13.x (add to Cargo.toml in S-576-2 slot) | `Response::bytes_stream()` for streaming download |
| sha1 (RustCrypto) | **NEW direct dep** — `sha1 = { version = "^0.10" }` (NOT currently in Cargo.lock); RustCrypto `sha1` per ADR-0017 2026-07-18 sha1 amendment (authorization + non-crypto-use rationale there); SHA-1's cryptographic weakness is acceptable (path-uniqueness prefix only, not security); 40-hex format pinned by H-NEW-ATTACHMENT-003 — do not "upgrade" to SHA-256 | SHA-1 prefix for default output path (BC-2.7.010) |
| proptest | as in `Cargo.lock` | VP-576-001 sanitize proptest |
| wiremock | as in `Cargo.lock` | Integration test HTTP mocking |

---

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `src/cli/issue/attachments.rs` | MODIFY (extends S1) | Add `handle_attachment_download`, `sanitize_attachment_filename`, `compute_default_output_path`, `AttachmentDownloadEntry`; VP-576-001 proptest |
| `src/api/jira/attachments.rs` | MODIFY (extends S1) | Add `get_attachment_metadata`, `get_attachment_content` (streaming); extend `AttachmentObject` if needed |
| `Cargo.toml` | MODIFY | Add reqwest `stream` feature ONLY (ADR-0017 S-576-2 slot; tokio-util `io-util` ships at S-576-3, not here — ADR-0017 AMENDED 2026-07-17 P1-010) |
| `tests/attachment_download.rs` | CREATE | 33 integration tests (22 AC tests + 11 adversarial/mutation regression pins added during Step 4.5 + CI mutation gate; 19 ACs; AC-001 has 3, AC-007 has 3, AC-011 has 2, AC-017 has 2; AC-004/AC-014 are src unit/proptest — 0 here; AC-013 uses existing test files — 0 new) |
| `tests/e2e_cli_surface_guard.rs` | MODIFY | SURFACE entries for `attachment download` paths/flags (AC-013) |
| `CLAUDE.md` | MODIFY | §3.4(2c) CWE-22 gotcha; §3.4(2d) redirect-behavior gotcha (AC-013) |
| `docs/specs/json-output-shapes.md` | MODIFY | Download manifest shape row (AC-013) |

| `src/cli/mod.rs` | MODIFY | Add `AttachmentSubcommand::Download` clap VARIANT definition (P1-011: S-576-1 defines the `Attachment` group; S-576-2 adds the Download variant with flags `--id`/`--all`/`--newest`/`--filter`/`--out`/`--force`/`--output`) |
| `src/cli/issue/mod.rs` | MODIFY | Add `AttachmentSubcommand::Download` dispatch match arm (inline match; mirror comment-CRUD pattern in `src/cli/issue/mod.rs`; calls `handle_attachment_download`) |
| `.cargo/mutants.toml` | MODIFY | confirm-present / ADD IF ABSENT `examine_globs` entries for `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` (prd-delta-576.md S2 obligation (e): primary delivery moved to S-576-1 per S1 obligation (f) / P22-001; S2 confirms present, idempotent) |

**Additive-only coordination:** `AttachmentSubcommand` (`src/cli/mod.rs`) and its dispatch match (`src/cli/issue/mod.rs`) are edited ADDITIVELY by S2/S3/S4/S5 — append your variant + arm only; never remove or reorder a sibling story's variant or arm; expect a rebase if a sibling merges first.

**MUST NOT change**: `src/cli/issue/interactions.rs`.

---

## Out-of-Scope

- `jr issue attachment upload` (S-576-3): multipart feature.
- `jr issue attachment delete` (S-576-4).
- JSM --public/--internal (S-576-5).
- Unicode bidi-control sanitization: discharged @ v1.3.94 — scope covered by `display_sanitize_filename` per BC-2.7.011 display-sanitization primary clause (S-576-1 implements the extended set).

---

## Spec Reference Links

- BC-2.7.007..012: `.factory/specs/prd/bc-2-issue-read.md §2.7`
- VP-576-001: `.factory/specs/prd/bc-2-issue-read.md §BC-2.7.011`
- ADR-0017: `.factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md`
- prd-delta-576.md Scope S2 row: `.factory/phase-f2-spec-evolution/prd-delta-576.md`
