---
document_type: story
level: ops
story_id: "S-576-3"
epic_id: "SOH-ATTACHMENTS-1"
title: "jr issue attachment upload — platform POST, --replace-existing, --dry-run (path-c), ADR-0017 Cargo.toml (issue #576)"
wave: feature-followup
status: ready
intent: attachment-write
feature_type: feature
mode: feature
scope: standard
severity: HIGH
trivial_scope: false
points: 13
priority: P1
tdd_mode: strict
producer: story-writer
timestamp: "2026-07-17T00:00:00"
phase: 3
inputs:
  - ".factory/phase-f2-spec-evolution/prd-delta-576.md"
  - ".factory/specs/prd/bc-3-issue-write.md"
  - ".factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md"
  - ".factory/phase-f1-delta-analysis/impact-boundary-576.md"
input-hash: "fce41b3"
traces_to: ".factory/specs/prd/bc-3-issue-write.md"
cycle: cycle-attachment-576
estimated_effort: x-large
estimated_days: 5
target_module: src/cli/issue/attachments.rs
subsystems: ["SS-03", "SS-09"]
depends_on: ["S-576-1"]
blocks: ["S-576-5"]
behavioral_contracts:
  - "BC-3.9.001"
  - "BC-3.9.002"
  - "BC-3.9.009"
  - "BC-3.9.012"
  - "BC-3.9.014"
  - "BC-3.9.017"
  - "BC-3.9.018"
  - "BC-3.9.020"
bcs:
  - "BC-3.9.001"
  - "BC-3.9.002"
  - "BC-3.9.009"
  - "BC-3.9.012"
  - "BC-3.9.014"
  - "BC-3.9.017"
  - "BC-3.9.018"
  - "BC-3.9.020"
verification_properties:
  - "VP-576-003"
  - "VP-576-004"
holdout_anchors:
  - "H-NEW-ATTACHMENT-004"
  - "H-NEW-ATTACHMENT-010"
nfr_anchors: []
adr_refs: ["ADR-0017"]
sd_refs: []
parent_phase: F3-incremental-stories
spec_source: ".factory/phase-f2-spec-evolution/prd-delta-576.md"
implementation_strategy: tdd
module_criticality: HIGH
acceptance_criteria_count: 18
assumption_validations: []
risk_mitigations: []
created: "2026-07-17"
version: "1.45"
last_updated: "2026-07-20"
breaking_change: false
retroactive: false
origin: >
  Issue #576 (attachment upload surface). Covers platform POST multipart upload,
  --replace-existing (delete-then-upload), and --dry-run path-c (upload dry-run).
  Introduces reqwest 'multipart' feature + tokio-util direct dep per ADR-0017
  (Cargo.toml delivery slot). Gate mechanics (eprint!+read_line) for --replace-existing
  confirmation prompt. VP-576-003 DELETE-before-POST ordering invariant.
  VP-576-004 upload half + full cross-path curated-JSON test (depends_on S-576-1
  list half for plumbing). display_sanitize_filename reused in confirmation prompts
  (DEC-184 R3.13). Sanitization scope per BC-2.7.011 primary clause v1.3.94 (PRE-F4-UNICODE discharged). Blocks S-576-5
  (JSM upload needs gate mechanics established here). F2 spec converged at v1.3.81
  (DEC-184 authorized story decomposition).
changelog:
  - "1.45 (2026-07-20): Kill-test round 3ea9cca0 count sync 22→26 — worktree verified at 26 test fns (+4 mutation-kill integration tests). 8 sites updated: (1) AC-006 Tests: added test_bc_3_9_017_delete_403_aborts_flow (DELETE 403 aborts flow); (2) AC-007 Tests: added test_bc_3_9_014_gate_confirm_uppercase_y_proceeds (uppercase Y gate); (3) AC-008 Test→Tests: added test_bc_3_9_020_dry_run_table_output_strings (table dry-run exact-string pins); (4) AC-011 Test→Tests: added test_bc_3_9_001_persistent_429_exhausts_retries (persistent-429 exhaustion, BC-3.9.012 terminal failure row); (5) Token Budget ~548: 22→26; (6) Budget note ~555: 22→26; (7) Task 2 ~571: 22→26, AC-006 has 3→4, AC-007 has 3→4, AC-008 has 1→2, AC-011 has 1→2; (8) File Structure ~663: same. input-hash updated."
  - "1.44 (2026-07-20): P3-001 (MEDIUM) count sync 20→22 — worktree verified at 22 test fns. 6 sites updated: (1) AC-006 Tests ~267: added test_bc_3_9_017_delete_404_is_benign_skip (P2-003 pin; AC-006 now has 3 tests); (2) AC-018 Tests ~489: added test_ac_018_double_quote_filename_well_formed_content_disposition (P2-001 pin; #[cfg(unix)]); (3) Token Budget row ~547: 20→22; (4) Budget note ~554: '20 test functions'→'22 test functions'; (5) Task 2 body ~570: 20→22, AC-006 has 2→3, AC-018 has 1→2 [#[cfg(unix)] noted]; (6) File Structure ~662: 20→22, same AC breakdown. Historical changelog entry 1.13 left intact. input-hash updated."
  - "1.43 (2026-07-20): P2-002 (MEDIUM) — allow_hyphen_values correction at 3 live sites; historical changelog entry 1.8 left intact. (1) AC-001 ~191: replaced 'allow_hyphen_values = true on FILE clap arg' with statement that allow_hyphen_values is intentionally ABSENT on multi-value positional (clap flag-consumption hazard; cross-ref CLAUDE.md gotcha). (2) Task 5 ~581: updated clap-wiring step to 'no allow_hyphen_values' with rationale. (3) File Structure src/cli/mod.rs row ~667: same correction. Rationale: on a multi-value positional, allow_hyphen_values causes clap to consume subsequent flags (e.g. --output json) as file path arguments; shipped code correctly omits it; CLAUDE.md gotcha documents this. input-hash updated."
  - "1.42 (2026-07-19): Propagation sweep v1.3.95/96 — P1-002 author-curated-form ruling — 1 site updated: AC-015 VP-576-004 test note gains assertion 7 — serialize_attachment_curated strips extra Jira author fields: full-author fixture (Jira response includes self/avatarUrls/accountType) → output author has ONLY {accountId, displayName}; partial-author fixture (both subfields null/absent) → output author is {\"accountId\": null, \"displayName\": null} (not null whole-field). Discharges BC-2.7.002 v1.3.95 curated-form ruling to VP-576-004 cross-path contract. No AC or test count change; input-hash updated 52b90dc→fce41b3 (bc-3-issue-write.md updated for v1.3.95/96 curated-form authority)."
  - "1.41 (2026-07-19): Status draft→ready per DEC-185 F3 gate approval (mechanical flip; no content change)."
  - "1.40 (2026-07-18): Fix changelog label RV-576-001 — v1.39 propagation-site (3) corrected: 'AC-003 note ~227' → 'AC-004 note ~228' (banner lives in AC-004 at line 228, not AC-003). No AC, test, or body content change; input-hash unchanged (52b90dc)."
  - "1.39 (2026-07-18): PRE-F4-UNICODE propagation @ v1.3.94 — 5 sites updated (pointer style, obligation discharged): (1) origin narrative ~82: 'PRE-F4-UNICODE note applies to prompt display' → 'Sanitization scope per BC-2.7.011 primary clause v1.3.94 (PRE-F4-UNICODE discharged)'; (2) SETTLED RULINGS ~138: PRE-F4-UNICODE note replaced with discharged pointer; (3) AC-004 note ~228: PRE-F4-UNICODE banner replaced with discharged scope notice; (4) prompt note ~288: PRE-F4-UNICODE banner replaced with discharged pointer; (5) Out-of-Scope ~676: updated to discharged pointer. No AC or test count change; input-hash updated 528bf23→52b90dc (v1.3.94: bc-2, bc-3, BC-INDEX, prd-delta changed)."
  - "1.38 (2026-07-18): Pre-gate hash refresh @ v1.3.93 — no content change; input-hash updated 7480c29 → 528bf23 (v1.3.93 cosmetics: error-taxonomy 401 row, prd-delta enumeration lines 84/85, ADR-0017 command-name + ID-type)."
  - "1.37 (2026-07-18): Fix round 74, adversary pass 74 — P74-001 (MEDIUM): AC-015 test note ~431 gains one-line S1 pub-visibility obligation note: requires pub fn serialize_attachment_curated + pub mod attachments; in src/cli/issue/mod.rs (set in S1 per P74-001) + all AttachmentObject fields pub. Cross-references S1 Task 5 Visibility obligation paragraph. No AC or test count change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.36 (2026-07-18): Fix round 72, adversary pass 72 — P72-001 (LOW): AC-017 test note ~460 extended with NONEXISTENT_FILE discriminating sub-assertion pinning interim-rejection-precedes-file-pre-check: upload <KEY> <NONEXISTENT_FILE> --public → exit 64 + stderr contains VERBATIM interim-rejection message (NOT 'file not found:') + ZERO HTTP mounts — proves rejection fires before any file pre-check. REMOVAL OBLIGATION ~458 updated to note this sub-assertion is also removed together with the guard at S-576-5. No AC or test count change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.35 (2026-07-18): Fix round 68, adversary pass 68 — P68-001 (MEDIUM): EC-3.9.020-6 (--dry-run WITHOUT --replace-existing → exit 2) had no dedicated test path — the existing dry-run test supplies BOTH flags and would pass silently if the requires annotation were absent. Fixed at 2 sites: (1) EC-3.9.020-6 paragraph ~314: sub-assertion note added (CRITICAL pin + regression-vector explanation); (2) test note ~322: extended test_bc_3_9_020_dry_run_path_c_guards_not_suppressed_gates_suppressed with sub-assertion (ii) — upload <KEY> <FILE> --dry-run (no --replace-existing) → exit 2 + clap error on stderr + ZERO HTTP mounts fire. No new test fn; no AC or test count change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.34 (2026-07-18): Fix round 64, adversary pass 64 — P64-001 (LOW): BC-3.9.018 mislabeled as '(multi-file)' in 2 sites. (1) BC-anchors line ~139: 'BC-3.9.018 (multi-file)' → 'BC-3.9.018 (--replace-existing zero-match idempotent)'. (2) AC-002 body ~194: 'For multi-file invocations (BC-3.9.018)' → 'For multi-file invocations (BC-3.9.001 EC-3.9.001-4)' (multi-file pre-check is governed by BC-3.9.001 EC-3.9.001-4, matching AC-002 header trace; BC table ~163 and AC-005/AC-012 verified already-correct). No AC or test count change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.33 (2026-07-18): Fix round 59, adversary pass 59 — P59-001 (LOW): Token Budget Total absolute-sum reconciliation: itemized rows sum 64,600 vs stated ~63,600; corrected Total ~63,600 → ~64,600. Budget usage ~32% unchanged (64,600/200K). No AC, test, or body content change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.32 (2026-07-18): Fix round 57, adversary pass 57 — P57-001 (LOW): EC-2 ~497 Expected Behavior cell canonical string corrected: 'stdin is not supported' → 'stdin upload is not supported; provide a file path.' (EC-3.9.001-6 ~3279; SETTLED RULINGS/AC-001/AC-002 already correct — EC table was the lone offender). No AC or test count change; input-hash unchanged (spec v1.3.92, 7480c29)."
  - "1.31 (2026-07-18): Fix round 53 — prd-delta v1.3.92 reconciliation; input-hash refresh a285edc→7480c29 (prd-delta v1.3.91→v1.3.92). 1 body citation updated: AC-008 ownership note ~316 'prd-delta-576.md S5 row v1.3.91' → 'v1.3.92'. S-576-3 wording (BC table ~161, AC-008) reads consistently with amended prd-delta — prd-delta was aligned TO the stories. No AC or test count change."
  - "1.30 (2026-07-18): Fix round 48, adversary pass 48 — P48-001 (LOW): EC-3.9.020-7 is composite; only its visibility-annotation clause is S5-realized; its gate-suppression clause is S3-realized (AC-008). Fixed at 2 sites: (1) BC table Title cell ~160: 'EC-3.9.020-7/8 S5-realized; excluded from S3 scope' → 'EC-3.9.020-7 visibility-annotation clause + EC-3.9.020-8 S5-realized; EC-3.9.020-7 gate-suppression clause S3-realized (AC-008)'; (2) ownership note ~315: 'both EC-3.9.020-7 and EC-3.9.020-8 are S5-realized' → same clause-scoped wording. P48-002 (LOW): AC-008 ~306 stale 'dry-run MAY still fetch the list' → 'after fetching the list (mandatory — populates wouldDelete; see Category 1)' (R47 made list GET mandatory). No AC or test count change; input-hash unchanged (spec v1.3.91, a285edc)."
  - "1.29 (2026-07-18): Fix round 47, adversary pass 47 — P47-001 (MEDIUM): AC-008 ~297-299 Category 1 bullet contradicted wouldDelete shape/simulated-list-fetch note/EC-7/BC-3.9.020 path(c). Fixed at 3 sites: (1) Category-1 header ~297: '(EC-3.9.020-9 sub-clause)' → '(EC-3.9.020-7)' (gate suppression is EC-3.9.020-7, not a sub-clause of EC-3.9.020-9); (2) Category-1 HTTP bullet ~299: 'All HTTP requests: suppressed (no POST, no DELETE, no GET for list)' → 'Destructive HTTP requests suppressed (no POST, no DELETE). The read-only list GET (GET /rest/api/3/issue/{key}?fields=attachment) still fires to populate wouldDelete preview (BC-3.9.020 path (c))'; (3) AC-008 test note ~316: sub-assertions added — wiremock list GET MUST fire; DELETE and POST mounts MUST NOT fire; wouldDelete reflects list response. No AC or test count change; input-hash unchanged (spec v1.3.91, a285edc)."
  - "1.28 (2026-07-18): Fix round 46, adversary pass 46 — P46-001 (LOW): AC-008 Category 3 bullet ~294 mislabeled positional KEY/FILE validation as '(category 2 eligibility guard)' — clap required-argument checks exit 2 at parse time and are outside the EC-3.9.020-9 three-category taxonomy. Parenthetical replaced with '(clap parse-time required-argument check → exit 2; NOT an eligibility guard — outside the three-category taxonomy)'. No AC or test count change; input-hash unchanged (spec v1.3.91, a285edc)."
  - "1.27 (2026-07-18): Fix round 43, adversary pass 43 — P43-001 (LOW): AC-008 header ~282 'EC-3.9.020-7/8 distinction' → 'EC-3.9.020-9 three-category dry-run taxonomy' (S3 path-c scope; EC-3.9.020-7/8 are S5-realized per R42). P43-002 (LOW): 3 CLI command examples missing --replace-existing: (1) AC-008 command ~284 'upload <KEY> <FILE> --dry-run' → '... --replace-existing --dry-run'; (2) EC-6 ~496 '--dry-run with non-existent FILE' → '--replace-existing --dry-run with non-existent FILE'; (3) ACR enforcement cell ~617 'dry-run + non-existent file' → 'replace-existing --dry-run + non-existent file'. Sweep: lines 157 (BC table description) and 564 (task prose reference) left as-is — not CLI command examples. No AC or test count change; input-hash unchanged (spec v1.3.91, a285edc)."
  - "1.26 (2026-07-18): Fix round 42, adversary pass 42 — P42-001 (LOW): spec moved v1.3.90→v1.3.91; S3 prd-delta cell gained path-c scope note excluding EC-3.9.020-7/8 (S5-realized). (a) BC table row updated: 'EC-3.9.020-7/8 S5-realized; excluded from S3 scope per amended prd-delta v1.3.91'; (b) AC-008 ownership note at ~311 updated: both EC-3.9.020-7 (visibility annotation) AND EC-3.9.020-8 (eligibility-guard precedence on --replace-existing --dry-run --public path) are now spec-recorded as S5-realized, not just changelog lore. No AC or test count change; input-hash refreshed: a9d7cfd→a285edc (prd-delta v1.3.91)."
  - "1.25 (2026-07-18): Fix round 40, adversary pass 40 — P40-001 (LOW): AC-018 ~457 'A named unit test' → 'A named integration (wiremock) test' + one-line rationale (reqwest does not expose serialized multipart Content-Disposition for src #[cfg(test)] assertion; BC 'unit test' wording satisfied at integration level). No AC or test count change; input-hash unchanged (spec v1.3.90, a9d7cfd)."
  - "1.24 (2026-07-18): Fix round 38, adversary pass 38 — P38-001 (MEDIUM): AC-004 ~214, EC-8 ~495, EC-9 ~496, Task 5 ~561 retained per-file fail-soft language contradicting AC-005 atomic semantics. Fixed at 4 sites: AC-004 now states on-2xx echoes all files + request-level error only (no partial-success state); EC-8 changed to 'whole request fails atomically'; EC-9 updated to 'Multi-file POST fails (e.g., 400) → exit 1 per BC-3.9.012'; Task 5 'Multi-file fail-soft (BC-3.9.018)' → 'Multi-file = single atomic POST (BC-3.9.001 EC-3.9.001-2); all-or-nothing success/failure per BC-3.9.012'. No AC or test count change; input-hash unchanged (spec v1.3.90, a9d7cfd)."
  - "1.23 (2026-07-18): Fix round 36, adversary pass 36 — P36-001 (LOW [process-gap]): test_bc_3_9_003_public_internal_interim_rejection_exits_64 renamed → test_bc_3_9_001_public_internal_interim_rejection_exits_64 at both live sites (AC-017 REMOVAL OBLIGATION ~445 + AC-017 Test bullet ~447); P33-003 anchored formal trace to BC-3.9.001 but left test name at bc_3_9_003 — P15-001 precedent (rename, not exception). Changelog historical reference at 1.5 preserved (quoting original name as authored). No AC or test count change; input-hash unchanged (spec v1.3.90, a9d7cfd)."
  - "1.22 (2026-07-18): Fix round 34, adversary pass 34 — P34-001 (LOW): AC-011 test note (~361) 5xx sub-assertion corrected from LITERAL 'API error (N)' to loose-substring 'API error (' (src/error.rs renders 'API error (500): <message>'; literal match fails); TABLE cell (~353) 'API error (N)' preserved verbatim (frozen BC cell). No AC or test count change; input-hash unchanged (spec v1.3.90, a9d7cfd)."
  - "1.21 (2026-07-18): Fix round 33, adversary pass 33 — P33-003 (LOW): AC-017 header retrace corrected — 'traces to BC-3.9.003/BC-3.9.004 precondition' violated AC∈bcs invariant (neither BC is in S3's bcs; double-ownership forbidden per prd-delta). Reanchored formally to BC-3.9.001 (clap-surface precondition: S3 defines --public/--internal flags) and moved BC-3.9.003/004 to authority-prose-only citation: 'licensed by prd-delta-576.md S3 DECOMPOSITION SEAM note as authority for interim rejection until S-576-5 wires JSM visibility per BC-3.9.003/004 — NOT a formal trace to those BCs'. TEMPORARY marker and REMOVAL OBLIGATION unchanged. No AC or test count change; input-hash unchanged (spec v1.3.90, a9d7cfd)."
  - "1.20 (2026-07-18): Fix round 31, adversary pass 31 — P31-001 (LOW): ACR Rule cell corrected: 'EC-3.9.020-8 guards NOT suppressed on dry-run' → 'EC-3.9.020-9 pre-flight checks NOT suppressed on dry-run' (the enforcement example — dry-run + non-existent file exits 64 — is a category-3 PRE-FLIGHT check per AC-008/EC-6; EC-3.9.020-8 eligibility guards are not reachable in S3 behind the AC-017 interim rejection). P31-002 (LOW): ACR Source cell corrected: 'ADR-0012 module boundary' → 'S3 interim scope (superseded by S-576-5); prd-delta-576.md S3 DECOMPOSITION SEAM' (ADR-0012 is the LOC shard rule, not an import boundary; P5-010 reframed AC-009 but Source cell was never swept). No AC or test count change; input-hash unchanged (spec v1.3.89, 063007d)."
  - "1.19 (2026-07-18): Fix round 27, adversary pass 27 — P27-003 (LOW): ACR 'No JSM API calls' enforcement cell tightened from bare 'grep -n \"jsm\" src/cli/issue/attachments.rs' (over-broad; false-blocks doc mentions) to import-scoped pattern 'grep -n \"api::jsm\\|api/jsm\" src/cli/issue/attachments.rs'; no AC or test count change; input-hash unchanged (spec v1.3.89, 063007d)."
  - "1.18 (2026-07-18): Fix round 26, adversary pass 26 — P26-002 (LOW): Additive-only coordination guard added to File Structure section (after src/cli/issue/mod.rs row, before MUST NOT change): 'AttachmentSubcommand (src/cli/mod.rs) and its dispatch match (src/cli/issue/mod.rs) are edited ADDITIVELY by S2/S3/S4/S5 — append your variant + arm only; never remove or reorder a sibling story's variant or arm; expect a rebase if a sibling merges first'; no AC or test count change; input-hash unchanged (spec v1.3.89, 063007d)."
  - "1.17 (2026-07-18): Fix round 25, adversary pass 25 — P25-002 (LOW): AC-011 test note extended with explicit per-row sub-assertion enumeration matching S2 AC-009 / S4 AC-016 hardened pattern: FILE not-found/stdin/KEY-404/401/403/413/400-generic/5xx/network rows each listed as an explicit sub-assertion of test_bc_3_9_012_error_taxonomy (not a combined catch-all); no new test function; count stays 20; input-hash unchanged (spec v1.3.89, 1435172)."
  - "1.16 (2026-07-18): Fix round 23, adversary pass 23 — P23-001 (MEDIUM): File Structure gains src/cli/issue/mod.rs MODIFY row (Add AttachmentSubcommand::Upload dispatch match arm; inline match mirrors comment-CRUD pattern at mod.rs:75); Task 5 body updated with explicit 'Also wire AttachmentSubcommand::Upload dispatch match arm in src/cli/issue/mod.rs'; no AC or test count change; input-hash unchanged (spec v1.3.89, 1435172)."
  - "1.15 (2026-07-18): Fix round 22, adversary pass 22 — P22-001 (LOW): spec moved v1.3.88→v1.3.89; input-hash refreshed (ecdb9f0→1435172). AC-010 updated: S-576-1 owns primary mutants.toml examine_globs delivery per prd-delta-576.md S1 obligation (f) / P22-001; migration chain noted (S3→S2 per P3-009→S1 per P22-001); File Structure row updated; no AC or test count change."
  - "1.14 (2026-07-18): Fix round 20, adversary pass 20 — P20-002 (LOW): spec moved v1.3.87→v1.3.88; input-hash refreshed (cd97c6b→ecdb9f0). AC-011 (~341/346) error taxonomy table updated to BC-3.9.012 amended cells: 401 row from bare 'stderr' to loose-substring form (stderr contains \"Not authenticated\" AND \"jr auth login\"; full literal: Not authenticated. Run \"jr auth login\" to connect. — src/error.rs::JrError + client.rs); network row from 'Could not reach <instance>: <reason>' to loose-substring form (stderr contains \"Could not reach\"; full literal: Could not reach <host> — check your connection — src/error.rs::JrError::NetworkError); no AC or test count change."
  - "1.13 (2026-07-18): Fix round 16, adversary pass 16 — P16-002 (LOW): test-function count reconciled — S3 has 18 ACs but 20 test functions (AC-001:3, AC-003:2, AC-006:2, AC-007:3; AC-015 shares AC-003's VP test = 0 new; AC-009/010/013 use existing test files = 0 new); 3-surface sweep: Token Budget 18→20 tests (8,000→8,900; Total 62,700→63,600; budget 31%→32%); Task 2 body 'Write all 18 AC tests'→'Write all 20 tests (18 ACs; AC-001 has 3 …)'; File Structure '18 integration tests'→'20 integration tests (18 ACs; …)'; budget note 31→32%; acceptance_criteria_count unchanged at 18; input-hash unchanged (spec v1.3.87, cd97c6b)."
  - "1.12 (2026-07-18): Fix round 15, adversary pass 15 — P15-001 (LOW): AC-002 test renamed: test_bc_3_9_012_file_prechecks_before_http → test_bc_3_9_001_file_prechecks_before_http (new-sweep-rule: AC-002 primary trace is BC-3.9.001 EC-3.9.001-4, re-anchored in v1.10/P12-001; test name was left behind with 012 in the P12-001 pass); no AC count change; input-hash unchanged (spec v1.3.87, cd97c6b)."
  - "1.11 (2026-07-18): Fix round 14, adversary pass 14 — P14-003 (LOW): AC-012 test renamed: test_bc_3_9_017_replace_existing_no_match_direct_upload → test_bc_3_9_018_replace_existing_no_match_direct_upload (new-sweep-rule: test name must match traced BC id; AC-012 header was re-anchored to BC-3.9.018 in v1.2 but test name was left behind with 017); input-hash unchanged (spec v1.3.87, cd97c6b)."
  - "1.10 (2026-07-18): Fix round 12, adversary pass 12 — P12-001 (HIGH): BC-3.9.012 real title corrected in BC table + BC anchor line (was 'FILE arg pre-checks...', now 'Upload error taxonomy — file-not-found exit 64; 413 actionable message; 401/5xx/network standard exits'); AC-002 header re-anchored from BC-3.9.012 to BC-3.9.001 EC-3.9.001-4 (file pre-checks trace to BC-3.9.001, not BC-3.9.012); AC-011 re-anchored from BC-3.9.009 to BC-3.9.012 and extended with full error taxonomy rows (413 EC-3.9.012-3 actionable string, 400 generic, 5xx, network, two --public/--internal rows deferred to S5), test renamed test_bc_3_9_012_error_taxonomy. P12-002 (MEDIUM): AC-018 added for SEC-576-004 CWE-93 Content-Disposition CRLF injection guard (BC-3.9.001 verbatim invariant; test_sec_576_004_content_disposition_crlf_injection_guard; filenames with ';'/quote/CRLF vectors); 4-surface sweep 17→18 ACs. P12-003 (LOW): SETTLED RULING + AC-001 aligned to v1.3.87 hedged wording — dropped 'XSRF check failed' as asserted server string; now 'XSRF-related rejection; exact body varies by deployment; test asserts header presence, not 403 body'; input-hash refreshed to spec v1.3.87 (cd97c6b)."
  - "1.9 (2026-07-18): Fix round 11, adversary pass 11 — P11-002: holdout_anchors populated: ['H-NEW-ATTACHMENT-004','H-NEW-ATTACHMENT-010'] (Group 19 BC-3.9.001/017/018 upload+replace-existing, BC-3.9.017 EC-3.9.017-9 non-interactive no-yes exit-64); P11-004: AC-008 dry-run base strings 'Would upload: <FILE> → <KEY>' and 'Would replace N existing...' marked ILLUSTRATIVE (non-normative) — BC-3.9.020 mandates only the [public] annotation + abstract sections, not specific string format; input-hash refreshed to spec v1.3.86 (737be5d)."
  - "1.8 (2026-07-17): Fix round 9, adversary pass 9 — P9-002: File Structure src/cli/mod.rs row expanded to full AttachmentSubcommand::Upload variant spec (positional <KEY>, <FILE...> allow_hyphen_values; flags --replace-existing/--yes/--dry-run(requires)/--public/--internal(mutually conflicting); BC-3.9.001 CLI-flags clause ~3285); Task 5 header updated to 'Wire src/cli/mod.rs + ...' with explicit clap-wiring step prepended; input-hash 42e8610 (spec v1.3.85)."
  - "1.7 (2026-07-17): Fix round 7, adversary pass 7 — P7-003: SETTLED RULINGS + Arch Mapping + Task 3 header + File Structure: upload_attachment → upload_attachments (plural; BC-3.9.001 Source); P7-004: AC-008 eligibility guards BC-3.9.009 non-JSM check → BC-3.9.005 non-JSM check (BC-3.9.009 is upload JSON shape, not eligibility; EC-3.9.020-9 verbatim); P7-005: SETTLED RULINGS EC-3.9.020-7/8 stale two-category ruling rewritten to three-category taxonomy: confirmation gates (EC-3.9.020-7, SUPPRESSED) / eligibility guards incl. BC-3.9.005 (EC-3.9.020-8, NOT suppressed) / pre-flight file checks (EC-3.9.020-9, NOT suppressed); 'eligibility gates' mis-term corrected to 'confirmation gates'; input-hash refreshed to spec v1.3.85 (5a0b1d0)."
  - "1.6 (2026-07-17): Fix round 6, adversary pass 6 — P6-003: Task 10 depends_on corrected from ['S-576-1','S-576-2'] to ['S-576-1'] only (ADR-0017 stream feature is idempotent — no S-576-2 dep; contradicted frontmatter/index/Out-of-Scope); P6-004: AC-008 dry-run note gains prd-delta-576.md S5 row citation ('per amended prd-delta-576.md S5 row — S5 owns this end-to-end'); P6-009: AC-017 header + body gain prd-delta-576.md S3 DECOMPOSITION SEAM citation (licenses the interim-rejection; message must be reproduced byte-for-byte); input-hash refreshed to spec v1.3.84 (754f22f)."
  - "1.5 (2026-07-17): Fix round 5, adversary pass 5 — P5-010: AC-009 forbidden-dependency reworded from 'ADR-0012 module boundary + build enforcement via architecture tests' to story-local review-time grep constraint (superseded by S-576-5); P5-011 (AC-008): removed 'When --public supplied: wouldUpload gain visibility:public per EC-3.9.020-7' from dry-run JSON shape (moved to S-576-5); AC-013: added INTERIM REJECTION note (--public/--internal rejected with exit 64 TEMPORARY message until S5 wires JSM behavior); NEW AC-017: interim rejection guard AC (test: test_bc_3_9_001_public_internal_interim_rejection_exits_64); Task 10: Create PR added; 4-surface sweep: acceptance_criteria_count 16→17, Task 2 16→17 ACs, Token Budget 16→17 tests (~7,000→~7,500), File Structure 16→17 integration tests; input-hash refreshed to spec v1.3.83 (e5dc680)."
  - "1.4 (2026-07-17): Fix round 4, adversary pass 4 — P4-006: AC-004 test renamed 'test_bc_3_9_002_human_table_display' → 'test_bc_3_9_001_human_table_display' (BC-3.9.001 is the correct anchor for the upload echo table; BC-3.9.002 is JSM no-flag postcondition); P4-008: AC-010 mutants.toml wording updated to 'ADD IF ABSENT (idempotent; S-576-2 owns primary delivery per prd-delta S2 (e))' replacing 'IDEMPOTENT: verify entries added by S-576-2 are still present' — wording now consistent with S-576-4 AC-013."
  - "1.3 (2026-07-17): Fix round 3, adversary pass 3 — P3-007: AC-008 gains EC-3.9.020-9 THREE-CATEGORY taxonomy (pre-flight checks = THIRD category, distinct from eligibility guards; file-existence/is_file() pre-flights are third category, NOT eligibility guards); EC-6 updated to cite EC-3.9.020-9 not EC-3.9.020-8; P3-008: AC-013 SURFACE entries gain --public/--internal flags; File Structure gains src/cli/mod.rs MODIFY row (define --public/--internal on upload subcommand); P3-009: AC-010 corrected to 'confirm present/idempotent — entries moved to S-576-2 per prd-delta amendment'; mutants.toml File Structure row updated to idempotent-confirm; P3-012: Task 2 '(15 ACs)' corrected to '(16 ACs)'; P3-018: CHANGELOG-append task added; 4-surface sweep complete."
  - "1.2 (2026-07-17): Fix round 2 P2-004/P2-008/P2-014 — BC-3.9.002 table row title corrected to JSM no-flag platform POST postcondition; AC-004 trace corrected BC-3.9.002→BC-3.9.001; AC-012 trace corrected BC-3.9.017→BC-3.9.018 zero-match; AC-008 guard citation fixed to EC-3.9.020-7 GATES vs ELIGIBILITY GUARDS; added AC-016 asserting BC-3.9.002 JSM no-flag = platform POST, zero servicedeskapi calls."
  - "1.1 (2026-07-17): Fix round 1, adversary pass 1 P1-006/P1-007/P1-008/P1-013/P1-016/P1-024 — single multipart POST for multi-file (not per-file loop), bare curated array JSON (no envelope), camelCase dryRun JSON, BC-3.9.018 table row corrected (zero-match idempotent), BC-3.9.014 consumer-2 prompt verbatim, cancel='Upload cancelled.' (not 'Cancelled.'), non-interactive message verbatim, lowercase precheck strings, EC-3.9.020-6 dry-run requires --replace-existing; added Purity Classification section."
  - "1.0 (2026-07-17): Initial F3 story authored per F2 CONVERGED spec v1.3.81."
---

> **tdd_mode:** strict — full TDD Iron Law enforced (todo!() + Red Gate ≥0.5 required).

> **SETTLED RULINGS — encode these exactly, do not re-litigate:**
> - **ADR-0017 Cargo.toml delivery slot**: `Cargo.toml` gains reqwest `multipart` feature and `tokio-util = { version = "^0.7", features = ["io-util"] }` direct dep in THIS story (S-576-3). The `stream` feature may already be present from S-576-2; if so, adding it again is idempotent. S-576-3 is the canonical delivery slot per ADR-0017.
> - **X-Atlassian-Token: no-check IS MANDATORY on upload**: Jira Cloud triggers an XSRF-related rejection when this header is absent on multipart POST. Never omit it. Atlassian's exact body text varies by deployment and is not load-bearing — jr only guarantees the header is always sent; the test asserts header presence, not the server's 403 body (BC-3.9.001 v1.3.87 hedge, P12-003).
> - **Retry requires fresh ReaderStream per attempt**: `Request::try_clone()` returns `None` for multipart requests (ADR-0017). On retry (rate-limit 429), the entire request including the `ReaderStream` must be rebuilt. This is a STREAMING-ONLY constraint — the stream is consumed on the first attempt.
> - **stdin `-` rejected (EC-3.9.001-6)**: `jr issue attachment upload <KEY> -` exits 64 with `"stdin upload is not supported; provide a file path."` (BC-3.9.001 EC-3.9.001-6 canonical). Do NOT implement stdin upload.
> - **BC-3.9.014 gate mechanics**: Confirmation prompt uses `eprint!` (NOT `eprintln!`, NOT `dialoguer::Confirm`). Three-way branch: `y`/`yes` (case-insensitive) → proceed; other input → cancel, exit 0; EOF (read_line Ok(0)) or Err(_) → exit 130 (P14-001). Prompt ends with `[y/N] ` (no newline after `] `).
> - **EC-3.9.020-7 / EC-3.9.020-8 / EC-3.9.020-9 dry-run three-category taxonomy**: `--dry-run` SUPPRESSES **confirmation gates** (EC-3.9.020-7) — guard destructive actions ("proceed with `--replace-existing`?"). Does NOT suppress **eligibility guards** (EC-3.9.020-8) — flag-combination validity incl. BC-3.9.005 non-JSM check, BC-3.9.017 step-0 issue validity. Does NOT suppress **pre-flight file checks** (EC-3.9.020-9) — resource reachability ("is this a file? does it exist?"). Pre-flight checks are a THIRD distinct category; do not conflate with eligibility guards. EC-3.9.020-9 verbatim taxonomy is authoritative.
> - **VP-576-003 ordering invariant**: `--replace-existing` MUST issue all DELETEs for same-filename attachments BEFORE issuing the new POST. The wiremock test MUST assert ordering.
> - **VP-576-004 upload half**: Upload response JSON shape must be structurally identical to list response (same `serialize_attachment_curated` helper from S-576-1). The full cross-path test (list + upload) is in THIS story's test file.
> - **display_sanitize_filename in prompts**: When showing filenames in confirmation prompts (`--replace-existing`), use `display_sanitize_filename` from S-576-1 (CWE-116 terminal display). Sanitization scope per BC-2.7.011 display-sanitization primary clause (v1.3.94) — PRE-F4-UNICODE obligation discharged.

# S-576-3: `jr issue attachment upload` — platform POST + --replace-existing + --dry-run path-c

**Bundle**: SOH-ATTACHMENTS-1
**GitHub issue**: #576
**BC anchors**: BC-3.9.001 (upload POST), BC-3.9.002 (upload human output), BC-3.9.009 (upload JSON shape), BC-3.9.012 (upload error taxonomy), BC-3.9.014 (gate mechanics), BC-3.9.017 (--replace-existing), BC-3.9.018 (--replace-existing zero-match idempotent), BC-3.9.020 (--dry-run path-c)
**VPs**: VP-576-003 (DELETE-before-POST ordering), VP-576-004 (upload half + full cross-path test)
**ADR**: ADR-0017 (multipart + tokio-util Cargo.toml delivery)

---

## Narrative

- **As a** developer or CI pipeline user
- **I want** `jr issue attachment upload <KEY> <FILE...>` to upload one or more files to a Jira issue via the platform REST API
- **So that** I can programmatically attach files to issues, optionally replacing existing same-named attachments (`--replace-existing`) and previewing the operation without side effects (`--dry-run`)

---

## Behavioral Contracts

| BC | Title | This Story's Scope |
|----|-------|--------------------|
| BC-3.9.001 | Platform upload: multipart POST; `X-Atlassian-Token: no-check`; ReaderStream; no size cap; retry rebuild; stdin rejected | Full |
| BC-3.9.002 | Upload to JSM issue with no visibility flag → platform POST, internal by default; zero servicedeskapi calls; behavior identical to non-JSM upload; S3 scope | Full |
| BC-3.9.009 | Upload response JSON shape; curated attachment-object (`"self"` omitted, `"content"` → `"contentUrl"`); VP-576-004 | Full |
| BC-3.9.012 | Upload error taxonomy — file-not-found exit 64; 413 actionable message; 401/5xx/network standard exits | Full |
| BC-3.9.014 | Gate mechanics: `eprint!` + read_line; three-way branch; P14-001 EOF→exit 130; consumer-2: --replace-existing | Full (consumer 2; consumer 1 = --public at S5) |
| BC-3.9.017 | `--replace-existing`: list→match by filename→DELETE ALL→POST; non-atomic race documented; confirmation gate | Full |
| BC-3.9.018 | `--replace-existing` zero-match — idempotent plain upload; zero-match is silent; `--replace-existing` no-op on delete phase | Full |
| BC-3.9.020 | `--dry-run` path-c (upload): lists would-upload files; gates suppressed; guards NOT suppressed (EC-3.9.020-7 visibility-annotation clause + EC-3.9.020-8 S5-realized; EC-3.9.020-7 gate-suppression clause S3-realized (AC-008)) | Full (path-c only; EC-3.9.020-7 visibility annotation + EC-3.9.020-8 guard precedence S5-realized; delete dry-run paths a/b at S4) |

---

## Acceptance Criteria

### AC-001 (traces to BC-3.9.001 postcondition: multipart POST; X-Atlassian-Token mandatory; ReaderStream)

`jr issue attachment upload <KEY> <FILE>` issues:
`POST /rest/api/3/issue/{key}/attachments` with:
- `Content-Type: multipart/form-data` (reqwest `multipart` feature; field name `"file"`).
- `X-Atlassian-Token: no-check` header MANDATORY on every upload request (XSRF-related rejection when absent; Atlassian's exact body text varies by deployment and is not load-bearing — jr only guarantees the header is always sent; the test asserts header presence, not the server's 403 body).
- File streamed via `tokio_util::io::ReaderStream` (ADR-0017 `io-util` feature). No client-side size cap.

On 429 (rate-limit): retry requires rebuilding the entire multipart request (including a fresh `ReaderStream` from a fresh `tokio::fs::File::open` call — `Request::try_clone()` returns `None` for multipart; ADR-0017 constraint).

`stdin -` as FILE value → exit 64 before HTTP: `"stdin upload is not supported; provide a file path."` (EC-3.9.001-6 canonical).

`allow_hyphen_values` is **intentionally ABSENT** on the `<FILE...>` positional. On a multi-value positional, setting `allow_hyphen_values = true` causes clap to consume subsequent flags (e.g. `--output json`) as file path arguments. The shipped code omits it; filenames starting with `-` are not supported (cross-ref CLAUDE.md §allow_hyphen_values gotcha and the S-576-3 CLAUDE.md gotcha added in the diff).

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_001_multipart_post_x_atlassian_token_mandatory`; `tests/attachment_upload.rs::test_bc_3_9_001_stdin_rejected`; `tests/attachment_upload.rs::test_bc_3_9_001_rate_limit_retry_rebuilds_request`

---

### AC-002 (traces to BC-3.9.001 EC-3.9.001-4 precondition: FILE arg pre-checks before HTTP; exit 64; BC-3.9.012 EC-3.9.012-1 for file-not-found taxonomy row)

FILE arg pre-checks fire BEFORE any HTTP call (BC-3.9.001 EC-3.9.001-4; BC-3.9.012):
- File path does not exist or is not a regular file (checked via `is_file()`) → exit 64: `"file not found: <path>"` (missing) or `"not a regular file: <path>"` (exists but not a regular file — directory, symlink-to-dir, FIFO, block device, etc.).
- Stdin `-` as FILE → exit 64: `"stdin upload is not supported; provide a file path."` (EC-3.9.001-6 canonical).

For multi-file invocations (BC-3.9.001 EC-3.9.001-4), ALL file pre-checks run before any HTTP is issued (fail-fast-all).

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_001_file_prechecks_before_http`

---

### AC-003 (traces to BC-3.9.009 postcondition: upload JSON shape; VP-576-004 upload half)

`jr issue attachment upload <KEY> <FILE> --output json` returns a **bare JSON array** (BC-3.9.009 — no envelope wrapper):
```json
[{"author": {"accountId": "<id>", "displayName": "<name>"}, "contentUrl": "<url>", "created": "<iso>", "filename": "<name>", "id": "<AID>", "mimeType": "<type>", "size": <bytes>}]
```

Shape: BC-2.7.002 curated form `{author, contentUrl, created, filename, id, mimeType, size}` — BTreeMap-alphabetical key order. `"self"` field is OMITTED. `"content"` from Jira response is renamed to `"contentUrl"` (VP-576-004 curated-serialization invariant — same `serialize_attachment_curated` helper established in S-576-1). Multi-file: array with one element per file in upload order (EC-3.9.009-2).

**VP-576-004 upload half**: The upload response uses the identical curated shape as the list response (both via `serialize_attachment_curated`). A dedicated cross-path integration test (`test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical`) MUST assert that both paths use the same shape by constructing both output types from the same fixture data and comparing key sets.

Output routed through `output::render_json` (#526 invariant).

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_009_upload_json_shape_self_omitted_content_renamed`; `tests/attachment_upload.rs::test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical`

---

### AC-004 (traces to BC-3.9.001 postcondition: human-mode upload echo; P19-I1 4-column table)

`jr issue attachment upload <KEY> <FILE>` (no `--output json`) prints:
- On success: a table row per uploaded file with columns: **Filename** (via `display_sanitize_filename` — CWE-116 terminal display), **Size** (human-readable: `1.2 MB`, `42 KB`, `815 B`), **ID**, **Created** (BC-3.9.001 P19-I1 note: 4-column upload echo table — deliberately differs from the 6-column list table; no Type/MIME column on the upload echo surface).

> **DISPLAY-SANITIZATION SCOPE (BC-2.7.011 v1.3.94):** `display_sanitize_filename` covers the full extended set per BC-2.7.011 display-sanitization primary clause. PRE-F4-UNICODE obligation discharged @ v1.3.94.

On 2xx: ALL uploaded files are echoed (one table row each), exit 0. On failure: request-level error only (one atomic POST — no partial-success state exists); exit per BC-3.9.012 taxonomy.

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_001_human_table_display`

---

### AC-005 (traces to BC-3.9.001 EC-3.9.001-2 postcondition: multi-file = single multipart POST)

`jr issue attachment upload <KEY> <FILE1> <FILE2> ...` sends a **single multipart POST** (`POST /rest/api/3/issue/{key}/attachments`) with multiple `file`-named parts, one per FILE, in the order supplied on the command line (BC-3.9.001 EC-3.9.001-2). This is NOT one POST per file.

Server returns an array with one element per uploaded file. The entire POST either succeeds or fails atomically from the client perspective. Exit 0 on success; exit per BC-3.9.012 taxonomy on failure.

JSON output: bare curated array per AC-003 (one element per file, upload order). Human: one table row per file per AC-004.

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_001_multi_file_single_multipart_post`

---

### AC-006 (traces to BC-3.9.017 postcondition: --replace-existing: list→match→delete all→upload; VP-576-003)

`jr issue attachment upload <KEY> <FILE> --replace-existing`:
1. Fetch attachment list via `GET /rest/api/3/issue/{key}?fields=attachment`.
2. Find ALL existing attachments whose filename matches the FILE basename (case-sensitive string equality; JRACLOUD-96384 — multiple attachments CAN share the same filename; all matches are deleted). Match by filename, NOT by AID.
3. If ≥1 match found: confirmation gate (BC-3.9.014 consumer 2 — see AC-007). On cancel: exit 0, no deletion, no upload.
4. Issue DELETE for each matching AID (one per AID, sequential). 404 on a delete = benign skip (stale IDs tolerated per race-window documentation).
5. After ALL deletes complete: issue the POST upload.

**VP-576-003 ordering invariant**: Wiremock test MUST assert DELETE(s) precede POST in the request log. No interleaving allowed. The test constructs a fixture with two same-named attachments and asserts: 2 × DELETE, then 1 × POST, in that order.

No matches found: proceeds directly to POST without confirmation gate (no-op replacement).

Non-atomic race window: documented in BC-3.9.017. A new same-named attachment created between step 1 and step 4 is not deleted. This is expected behavior.

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_017_replace_existing_delete_then_post`; `tests/attachment_upload.rs::test_vp_576_003_delete_before_post_ordering_invariant`; `tests/attachment_upload.rs::test_bc_3_9_017_delete_404_is_benign_skip` (P2-003 regression pin — 404 on delete step is benign and must not abort the upload); `tests/attachment_upload.rs::test_bc_3_9_017_delete_403_aborts_flow` (mutation-kill pin — DELETE 403 is non-benign and MUST abort with exit 1)

---

### AC-007 (traces to BC-3.9.014 postcondition: gate mechanics consumer 2; eprint! not eprintln!)

`--replace-existing` confirmation gate (BC-3.9.014 consumer 2):

Prompt (BC-3.9.014 consumer 2 verbatim — `eprint!`, NOT `eprintln!`, NOT `dialoguer::Confirm`):
```
Replace existing attachment(s) on <KEY>:
  <filename1> (id: <AID1>)
  <filename2> (id: <AID2>)
Continue? [y/N] 
```
(lists ALL would-delete entries; no `"N items"` summary — exact count is meaningful for destructive ops; trailing space, no newline after `] `)

All `<filenameN>` display-sanitized per BC-2.7.011 before writing to TTY.

Three-way branch per DEC-174/P14-001:
- Input `y` or `yes` (case-insensitive): proceed with deletes + upload.
- Any other non-EOF (including empty-Enter, `n`, `no`): cancel, print `"Upload cancelled."` to **stderr**; JSON stdout: `{"cancelled":true,"uploaded":false}`; exit 0.
- EOF (`read_line` returns `Ok(0)`) or I/O error (`Err(_)`): exit 130 (`JrError::Interrupted`) — NOT cancel path.

`--yes` flag suppresses the gate (non-interactive bypass). `--no-input` or stdin not a TTY suppresses the gate if `--yes` was also passed; otherwise exits 64 if would prompt in non-interactive mode (EC-3.9.014-3).

> **DISPLAY-SANITIZATION SCOPE (BC-2.7.011 v1.3.94):** `display_sanitize_filename` covers the extended set per BC-2.7.011 display-sanitization primary clause. PRE-F4-UNICODE discharged @ v1.3.94.

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_014_gate_confirm_proceeds`; `tests/attachment_upload.rs::test_bc_3_9_014_gate_cancel_exits_0`; `tests/attachment_upload.rs::test_bc_3_9_014_gate_eof_exits_130` (uses `JR_STDIN_IS_TTY=1` seam); `tests/attachment_upload.rs::test_bc_3_9_014_gate_confirm_uppercase_y_proceeds` (mutation-kill pin — gate must accept uppercase "Y" via eq_ignore_ascii_case)

---

### AC-008 (traces to BC-3.9.020 postcondition: --dry-run path-c upload; EC-3.9.020-9 three-category dry-run taxonomy)

`jr issue attachment upload <KEY> <FILE> --replace-existing --dry-run`:

**EC-3.9.020-9 THREE-CATEGORY DRY-RUN TAXONOMY (DO NOT RE-LITIGATE)**:
1. **Confirmation gates** (BC-3.9.014): **SUPPRESSED** on `--dry-run`. These guard destructive actions (prompts for `--replace-existing`).
2. **Eligibility guards** (BC-3.9.005 non-JSM check; BC-3.9.017 step-0 issue validity): **NOT suppressed**. These validate flag-combination validity.
3. **Pre-flight checks** (BC-3.9.012 file-existence/is_file() validation): **NOT suppressed**. These validate resource reachability. "Pre-flight checks differ from eligibility guards: they validate resource reachability, not flag-combination validity." — EC-3.9.020-9 verbatim.

**Category 3 (Pre-flight checks) NOT suppressed** (EC-3.9.020-9):
- FILE pre-checks (AC-002): still apply. Non-existent file exits 64 even on dry-run. This is a **PRE-FLIGHT CHECK** (category 3), NOT an eligibility guard — it validates whether the file resource is reachable.
- Selector validation (required positional KEY and FILE): still applies (clap parse-time required-argument check → exit 2; NOT an eligibility guard — outside the three-category taxonomy).

**Category 1 (Gates) ARE suppressed** (EC-3.9.020-7):
- `--replace-existing` confirmation gate (AC-007): suppressed on `--dry-run`.
- Destructive HTTP requests suppressed (no POST, no DELETE). The read-only list GET (`GET /rest/api/3/issue/{key}?fields=attachment`) still fires to populate the `wouldDelete` preview (BC-3.9.020 path (c)).

**Dry-run output** (human mode — ILLUSTRATIVE, non-normative; BC-3.9.020 does not mandate specific base string format):
```
Would upload: <FILE> → <KEY>
```
For `--replace-existing`, after fetching the list (mandatory — populates `wouldDelete`; see Category 1): `"Would replace N existing attachment(s) named '<name>'"`.
> **Non-normative note:** The human-mode base strings above are illustrative. The only spec-mandated human-mode annotation is `[public]` when `--public` is set (EC-3.9.020-7, owned by S-576-5). Implementers MAY use different phrasing; tests MUST NOT pin the exact wording beyond the `[public]` marker.

**EC-3.9.020-6 CRITICAL**: `--dry-run` WITHOUT `--replace-existing` → exit 2 (clap `requires` constraint enforced at parse time). The `--dry-run` flag is only meaningful for `--replace-existing` (no deletes to preview = no point). No application code reached. **Sub-assertion required in `test_bc_3_9_020_dry_run_path_c_guards_not_suppressed_gates_suppressed`**: `upload <KEY> <FILE> --dry-run` (no `--replace-existing`) → exit 2 + clap error on stderr + ZERO HTTP mounts fire (list GET suppressed at parse time, before handler is invoked). This pin is CRITICAL: the existing dry-run test supplies BOTH flags and would pass even if the `requires` annotation is absent — a silent regression vector if EC-3.9.020-6 is the only test path.

**Dry-run output** (JSON mode via `output::render_json`; BC-3.9.020 camelCase shape):
```json
{"dryRun": true, "wouldDelete": [{"filename": "<name>", "id": "<AID>"}], "wouldUpload": [{"filename": "<name>"}]}
```
The `--public`/`--internal` dry-run visibility annotation (`wouldUpload` entries gain `"visibility":"public"`) AND EC-3.9.020-8 eligibility-guard precedence on dry-run (non-JSM check on `--replace-existing --dry-run --public` path) are out of scope here (per amended prd-delta-576.md S5 row v1.3.92 — EC-3.9.020-7 visibility-annotation clause + EC-3.9.020-8 S5-realized; EC-3.9.020-7 gate-suppression clause S3-realized (AC-008); the interim rejection guard (AC-017) fires before the --public dry-run visibility path is reached in S3). S-576-5 adds both the annotation and the non-JSM guard precedence when wiring full JSM visibility.

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_020_dry_run_path_c_guards_not_suppressed_gates_suppressed`; `tests/attachment_upload.rs::test_bc_3_9_020_dry_run_table_output_strings` (mutation-kill pin — exact-string pins for table dry-run arm OutputFormat::Table) (sub-assertions: (i) wiremock list GET mount MUST fire — dry-run does NOT suppress the read-only list fetch; DELETE and POST mounts MUST NOT fire; `wouldDelete` array in output reflects list response; (ii) EC-3.9.020-6 pin: `upload <KEY> <FILE> --dry-run` with NO `--replace-existing` → exit 2 + clap error on stderr + ZERO HTTP mounts fire — this sub-assertion is CRITICAL to verify the `requires` clap annotation is present; the success-path sub-assertion supplies both flags and would pass silently if the annotation were missing)

---

### AC-009 (traces to BC-3.9.001 precondition: ADR-0017 Cargo.toml delivery; cargo deny passes)

`Cargo.toml` gains in this story (ADR-0017 delivery slot):
- reqwest `multipart` feature enabled (in addition to existing features).
- `tokio-util = { version = "^0.7", features = ["io-util"] }` as a DIRECT dependency.
- `stream` feature on reqwest: idempotent if already added by S-576-2; ensure it is present.

`cargo build` MUST compile cleanly. `cargo deny check` MUST pass with no new license/vulnerability issues. Run `cargo deny check` in AC test obligation.

**Dependency constraint (S3 interim scope — superseded by S-576-5):** `src/cli/issue/attachments.rs` MUST NOT import from `src/api/jsm/` in this story. Review-time enforcement: `grep -n "api::jsm\|api/jsm" src/cli/issue/attachments.rs` must return empty at PR submission. S-576-5 removes this constraint when wiring JSM visibility behavior.

- **Task verification:** `cargo build && cargo deny check && cargo clippy -- -D warnings`

---

### AC-010 (traces to BC-3.9.001 precondition: mutants.toml examine_globs; ADR-0012 shard rule)

`.cargo/mutants.toml` `examine_globs`: ensure entries for `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` are present — **ADD IF ABSENT** (idempotent; S-576-1 owns primary delivery per prd-delta-576.md S1 obligation (f) / P22-001 — add-at-creation, CWE-116 rationale + S-577-1 precedent; S-576-2 Task 2 confirms present per S2 obligation (e); do not duplicate if already present). Migration chain: originally S3 → moved to S2 per P3-009 → moved to S1 per P22-001.

Per ADR-0012: new modules extracted at ≥1,000 LOC become shard candidates. `attachments.rs` is expected to approach this threshold by S-576-3. The story notes this for the implementer; no shard action required at this delivery (threshold will be evaluated at S-576-3 + S-576-4 + S-576-5 completion).

`tests/mutants_glob_existence.rs` MUST remain GREEN (no orphaned glob entries). Verify: `cargo test --test mutants_glob_existence`.

- **Test:** `cargo test --test mutants_glob_existence`

---

### AC-011 (traces to BC-3.9.012 postcondition: upload error taxonomy — file-not-found exit 64; 413/400/5xx/network standard exits)

Full error taxonomy for `attachment upload` (BC-3.9.012):

| Condition | Exit | Channel |
|-----------|------|---------|
| FILE not found / not readable | 64 | stderr (before HTTP) |
| stdin `-` supplied | 64 | stderr (before HTTP) |
| KEY 404 (issue not found) | 64 | stderr |
| 401 | 2 | stderr contains `"Not authenticated"` AND `"jr auth login"` (full literal: `Not authenticated. Run "jr auth login" to connect.` — `src/error.rs::JrError` + `client.rs`) |
| 403 | 1 | stderr |
| 413 (file too large) | 1 | stderr: `"Attachment too large: the file exceeds the server-configured limit."` (EC-3.9.012-3) |
| 400 generic | 1 | stderr (Jira error body surfaced) |
| 5xx | 1 | stderr: `"API error (N)"` |
| Network error | 1 | stderr contains `"Could not reach"` (full literal: `Could not reach <host> — check your connection` — `src/error.rs::JrError::NetworkError`) |
| `--replace-existing` list 404 | 64 | stderr |
| `--public` flag (JSM visibility = public) | — | Deferred to S-576-5 per prd-delta-576.md S3 row; interim behavior: exit 64 (AC-017) |
| `--internal` flag (JSM visibility = internal) | — | Deferred to S-576-5 per prd-delta-576.md S3 row; interim behavior: exit 64 (AC-017) |

JRACLOUD-96384 note (BC-3.9.017): multiple attachments CAN share the same filename in Jira. `--replace-existing` deletes ALL matches by filename, not just the first. This is deliberate and load-bearing.

- **Tests:** `tests/attachment_upload.rs::test_bc_3_9_012_error_taxonomy`; `tests/attachment_upload.rs::test_bc_3_9_001_persistent_429_exhausts_retries` (mutation-kill pin — persistent 429 exhausts retries and exits 1; BC-3.9.012 terminal failure taxonomy row) (assertions MUST cover ALL taxonomy rows as sub-cases: FILE not found → exit 64 + no HTTP; stdin `-` supplied → exit 64 + no HTTP; KEY 404 → exit 64; 401 → exit 2 + stderr contains `"Not authenticated"` AND `"jr auth login"`; 403 → exit 1; 413 → exit 1 + stderr contains `"Attachment too large: the file exceeds the server-configured limit."`; 400 generic → exit 1 + Jira error body surfaced; 5xx → exit 1 + stderr contains `"API error ("` (loose-substring — src/error.rs renders `"API error (500): <message>"`; do NOT literal-match `"API error (N)"`); network → exit 1 + stderr contains `"Could not reach"`; each row is an explicit sub-assertion, not a combined catch-all)

---

### AC-012 (traces to BC-3.9.018 postcondition: --replace-existing zero-match idempotent plain upload)

`jr issue attachment upload <KEY> <FILE> --replace-existing` when NO existing attachment matches the filename: proceeds directly to POST without confirmation gate. No delete issued. This is the happy-path no-op replacement scenario.

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_018_replace_existing_no_match_direct_upload`

---

### AC-013 (traces to BC-3.9.001 precondition: e2e surface guard + CLAUDE.md delivery obligations)

**e2e surface guard** (GAP-AUDIT-576-001 §3.3): `tests/e2e_cli_surface_guard.rs` SURFACE table gains entries for `attachment upload` paths and flags: `<KEY>` (required positional), `<FILE...>` (required repeatable positional), `--replace-existing`, `--yes`, `--dry-run`, `--public`, `--internal`, `--output json`, `--no-input`, `--profile`, `--no-color`. (`--public` and `--internal` are defined on the upload subcommand in this story; S-576-5 exercises the JSM wire behavior — but the flags themselves belong to this story's clap surface.)

> **INTERIM REJECTION (TEMPORARY — removed by S-576-5):** The `--public` and `--internal` flags are defined in clap here but the handler in this story REJECTS them with exit 64 and a TEMPORARY message: `"--public and --internal are not yet supported. JSM visibility will be shipped in a follow-on story."` This rejection is formally specified in AC-017. S-576-5 removes the rejection and wires full JSM behavior.

**CLAUDE.md delivery obligations** (S3 scope per prd-delta-576.md):
- Add §3.x: `X-Atlassian-Token: no-check` mandatory on attachment upload (Jira Cloud XSRF protection; 403 without it).
- Add §3.x: reqwest multipart retry must rebuild entire request + fresh `tokio::fs::File::open` (ADR-0017: `Request::try_clone()` returns `None` for multipart).
- Add §3.x: `--replace-existing` matches by filename (NOT AID) to handle JRACLOUD-96384 multiple same-named attachments.

**docs/specs/json-output-shapes.md**: add upload manifest row — bare array `[{author,contentUrl,created,filename,id,mimeType,size}]` (BC-3.9.009; no envelope wrapper) and dry-run shape `{"dryRun":true,"wouldDelete":[{filename,id}],"wouldUpload":[{filename}]}` (BC-3.9.020 camelCase).

**docs/specs/attachments.md**: create new spec file (F4 obligation) documenting: platform vs servicedeskapi URL choice; JSDCLOUD-10841; X-Atlassian-Token; --replace-existing race window; stdin rejection; CWE-22 vs CWE-116 sanitization split.

- **Tests:** `tests/e2e_cli_surface_guard.rs` (SURFACE entries); `tests/claude_md_citations.rs` (CLAUDE.md citations resolve)

---

### AC-014 (traces to BC-3.9.014 precondition: gate mechanics non-interactive without --yes exits 64; DEC-174)

**DEC-174** (interactive prompt mechanism ruling): `eprint!` + `io::stdin().read_line(...)` pattern only. NOT `dialoguer::Confirm`. NOT `dialoguer::Select`. This is the same pattern used in `cli/issue/workflow.rs::handle_move` for the resolution prompt.

When `--no-input` or stdin is not a TTY (auto-`--no-input` via `src/main.rs`) and `--yes` was NOT supplied: the `--replace-existing` gate exits 64 with `"Use --yes to confirm deletion of existing same-filename attachments."` (BC-3.9.014 consumer 2 non-interactive canonical message — substring-matchable by tests).

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_014_non_interactive_without_yes_exits_64`

---

### AC-015 (traces to BC-3.9.009 VP-576-004: full cross-path curated-JSON test)

**VP-576-004 full cross-path test** (this story's scope — the cross-path assertion combining S1 list + S3 upload):

`tests/attachment_upload.rs::test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical`:
1. Constructs a mock `AttachmentObject` fixture with `self`, `content`, and all other standard fields.
2. Calls `serialize_attachment_curated(&fixture)` (the function from `src/cli/issue/attachments.rs` established in S-576-1).
3. Asserts: `"self"` key absent from output.
4. Asserts: `"contentUrl"` key present (renamed from `"content"`).
5. Asserts: both list and upload serialization paths use the SAME function (structural identity by inspecting key sets).
6. Asserts: BTreeMap ordering (alphabetical key order in JSON output).
7. Asserts: author curated form (BC-2.7.002 v1.3.95) — full-author fixture (Jira response includes `self`/`avatarUrls`/`accountType`) → serialized `author` has ONLY `{accountId, displayName}` (no extra fields); partial-author fixture (both `accountId` and `displayName` null/absent) → serialized `author` is `{"accountId": null, "displayName": null}` (NOT null whole-field).

This test is LOAD-BEARING for the VP-576-004 contract. It must not be removed or weakened.

**S1 pub-visibility obligation (P74-001):** This test requires `pub fn serialize_attachment_curated` and `pub mod attachments;` in `src/cli/issue/mod.rs` (both set in S-576-1 Task 5 / File Structure per P74-001), plus all `AttachmentObject` fields `pub` for direct fixture construction.

- **Test:** `tests/attachment_upload.rs::test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical`

---

### AC-016 (traces to BC-3.9.002 postcondition: JSM issue upload with no visibility flag = platform POST; zero servicedeskapi calls; internal by default)

`jr issue attachment upload <KEY> <FILE>` against a JSM issue, with NO `--public` or `--internal` flag supplied:
- Issues the platform POST endpoint (`POST /rest/api/3/issue/{key}/attachments`) — NOT the JSM servicedeskapi endpoint.
- Zero calls to any `/rest/servicedeskapi/` endpoint during this operation.
- The attachment is internal by default on JSM (P2-4a confirmed — platform POST semantics apply).
- Behavior is byte-for-byte identical to uploading to a non-JSM issue (BC-3.9.002 postcondition: behavior identical to non-JSM upload).

> **Note**: JSM visibility control (`--public` / `--internal`) is implemented in S-576-5 (BC-3.9.004). This story's scope is the zero-flag path: no servicedeskapi calls, platform POST only.

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_002_jsm_no_flag_uses_platform_post_zero_servicedeskapi_calls`

---

### AC-017 (TEMPORARY — traces to BC-3.9.001 precondition: --public/--internal clap-surface definition (S3 defines these flags); licensed by prd-delta-576.md S3 DECOMPOSITION SEAM note as authority for interim rejection until S-576-5 wires JSM visibility per BC-3.9.003/004 — NOT a formal trace to those BCs)

`jr issue attachment upload <KEY> <FILE> --public` or `jr issue attachment upload <KEY> <FILE> --internal` → exit 64 with TEMPORARY message (verbatim per prd-delta-576.md S3 DECOMPOSITION SEAM note):

```
--public and --internal are not yet supported. JSM visibility will be shipped in a follow-on story.
```

This rejection fires before any HTTP call or file pre-check. The `--public` and `--internal` flags are defined in clap in this story (clap surface guard requires them — P3-008), but the handler rejects them unconditionally until S-576-5 wires the JSM servicedeskapi behavior. The prd-delta-576.md S3 DECOMPOSITION SEAM note explicitly licenses this interim rejection with the message quoted above — the message string must be reproduced byte-for-byte.

> **REMOVAL OBLIGATION (S-576-5):** S-576-5 MUST remove this rejection guard from `handle_attachment_upload` and wire actual JSM visibility behavior (BC-3.9.003/BC-3.9.004). The test `test_bc_3_9_001_public_internal_interim_rejection_exits_64` is superseded by S-576-5's AC-NNN visibility tests. The sub-assertion pinning interim-rejection-precedes-file-pre-check (NONEXISTENT_FILE path — see test note below) is also removed together with the guard; S-576-5 Task 5 REMOVAL OBLIGATION notes this explicitly.

- **Test:** `tests/attachment_upload.rs::test_bc_3_9_001_public_internal_interim_rejection_exits_64` — extend with discriminating sub-assertion pinning the ordering claim: `upload <KEY> <NONEXISTENT_FILE> --public` → exit 64 + stderr contains the VERBATIM interim-rejection message `"--public and --internal are not yet supported. JSM visibility will be shipped in a follow-on story."` (NOT `"file not found:"`) + ZERO HTTP mounts fire. This pins that the interim rejection fires before any file pre-check (AC-017 prose: "this rejection fires before any HTTP call or file pre-check"). **NOTE:** this sub-assertion is removed together with the guard at S-576-5 (REMOVAL OBLIGATION above + S-576-5 Task 5).

---

### AC-018 (traces to BC-3.9.001 invariant: SEC-576-004 CWE-93 multipart Content-Disposition CRLF injection guard)

**SEC-576-004 (CWE-93 — CRLF Injection in multipart Content-Disposition)** — BC-3.9.001 invariant (verbatim): _"reqwest 0.13's `multipart::Part` applies percent-encoding to the filename value in the `Content-Disposition` header. The implementer MUST include a unit test with filenames containing `;`, `"`, and `\r\n` and assert the resulting multipart POST body has a well-formed `Content-Disposition` header (no CRLF injection, no boundary escape). This resolves SQ-6 from `.factory/phase-f1-delta-analysis/impact-boundary-576.md`, to be verified at Story 3 delivery."_

A named integration (wiremock) test MUST exercise all three injection vectors in a single test function (reqwest does not expose the serialized multipart Content-Disposition for a `src` `#[cfg(test)]` assertion, so a wiremock body-capture test is required — the BC's "unit test" wording is satisfied by this integration-level assertion):

| Filename Input | Vector | Assertion |
|---------------|--------|-----------|
| `file;name.txt` | Semicolon — boundary separator | `Content-Disposition` value does NOT contain unquoted `;` that breaks the boundary |
| `file"name.txt` | Double-quote — value delimiter | Filename parameter is properly escaped; no unmatched `"` |
| `file\r\nX-Injected: hdr` | CRLF — header injection | No raw `\r\n` in the `Content-Disposition` value; header value is one line |

For each input: assert the resulting multipart body has a well-formed `Content-Disposition` header per RFC 2183 (no CRLF injection, no boundary escape). reqwest 0.13's `multipart::Part::bytes()` with `.file_name()` applies percent-encoding — the test validates this percent-encoding is present and well-formed.

- **Tests:** `tests/attachment_upload.rs::test_sec_576_004_content_disposition_crlf_injection_guard`; `tests/attachment_upload.rs::test_ac_018_double_quote_filename_well_formed_content_disposition` (P2-001 regression pin; `#[cfg(unix)]` — double-quote in filename; validates percent-encoding is present and well-formed)

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `handle_attachment_upload` | `src/cli/issue/attachments.rs` | Effectful (HTTP + disk read) |
| `upload_attachments` | `src/api/jira/attachments.rs` | Effectful (HTTP multipart POST) |
| `replace_existing_attachments` | `src/cli/issue/attachments.rs` | Effectful (HTTP list + delete + post) |
| `dry_run_upload` | `src/cli/issue/attachments.rs` | Effectful (HTTP list for --replace-existing preview; output only) |
| `attachment_replace_confirmation_gate` | `src/cli/issue/attachments.rs` | Effectful (stdin read_line) |
| `serialize_attachment_curated` | `src/cli/issue/attachments.rs` (from S1) | Pure (reuse; BTreeMap JSON) |
| `display_sanitize_filename` | `src/cli/issue/attachments.rs` (from S1) | Pure (reuse; CWE-116 terminal display) |

---

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-1 | Multipart retry on 429 | Rebuild request from fresh `File::open`; fresh `ReaderStream` |
| EC-2 | `stdin -` supplied as FILE | Exit 64 before HTTP; `"stdin upload is not supported; provide a file path."` |
| EC-3 | `--replace-existing` with 2 same-named attachments (JRACLOUD-96384) | Delete BOTH; VP-576-003 asserts order: DELETE×2 then POST |
| EC-4 | `--replace-existing` with no matches | Skip gate; proceed directly to POST |
| EC-5 | Confirmation gate EOF (Ctrl+D) | Exit 130 (`JrError::Interrupted`) |
| EC-6 | `--replace-existing --dry-run` with non-existent FILE | Exit 64 (PRE-FLIGHT CHECK NOT suppressed per EC-3.9.020-9 — file-existence pre-flight is third category, distinct from eligibility guards) |
| EC-7 | `--dry-run` with `--replace-existing` | Gate suppressed; shows "would replace N" |
| EC-8 | Multi-file POST rejected by server | Whole request fails atomically; exit per BC-3.9.012; no partial attach. |
| EC-9 | Multi-file POST fails (e.g., 400) | Exit 1 per BC-3.9.012. |
| EC-10 | Non-interactive + no `--yes` on `--replace-existing` | Exit 64 (EC-3.9.014-3) |

---

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `src/cli/issue/attachments.rs::handle_attachment_upload` | effectful-shell | HTTP I/O + disk read + stdin read_line |
| `src/api/jira/attachments.rs::upload_attachments` | effectful-shell | HTTP multipart POST |
| `src/cli/issue/attachments.rs::replace_existing_attachments` | effectful-shell | HTTP list GET + DELETE(s) + POST |
| `src/cli/issue/attachments.rs::attachment_replace_confirmation_gate` | effectful-shell | stdin read_line |
| `src/cli/issue/attachments.rs::dry_run_upload` | effectful-shell | optional HTTP list GET for --replace-existing preview |
| `src/cli/issue/attachments.rs::serialize_attachment_curated` | pure-core | BTreeMap JSON serialization; no I/O |
| `src/cli/issue/attachments.rs::display_sanitize_filename` | pure-core | CWE-116 display sanitization; no I/O |

---

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~14,000 |
| `src/cli/issue/attachments.rs` (extends S1+S2, ~1,000 LOC) | ~15,000 |
| `src/api/jira/attachments.rs` (extends S1+S2, ~600 LOC) | ~9,000 |
| BC-3.9.001, 002, 009, 012, 014, 017, 018, 020 (8 BCs × ~1,400 avg) | ~11,200 |
| ADR-0017 | ~2,500 |
| `tests/attachment_upload.rs` (new file, 26 tests) | ~8,900 |
| `Cargo.toml` / `cargo deny` overhead | ~1,000 |
| Tool outputs overhead | ~3,000 |
| **Total** | **~64,600** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~32%** |

> **Note:** 32% slightly exceeds the 20-30% guideline for strict-mode stories. However, this story was authorized at 13 points by DEC-184 with full knowledge of its scope. The 32% is a natural consequence of the story's required scope (26 test functions across 18 ACs). If implementation proves difficult in a single agent turn, split at AC-010 (Task 5 boundary): Tasks 1-5 in one sub-turn, Tasks 6-9 in a second sub-turn.

---

## Tasks (MANDATORY)

### Task 0: Read BC-3.9.001/002/009/012/014/017/018/020 in full

Read `.factory/specs/prd/bc-3-issue-write.md §3.9 BC-3.9.001,002,009,012,014,017,018,020` completely. Internalize settled rulings: X-Atlassian-Token, retry rebuild, gate mechanics (eprint!+read_line), EC-3.9.020-7/8 distinction, VP-576-003 ordering, VP-576-004 cross-path. Also read ADR-0017 fully.

### Task 1: Update Cargo.toml (ADR-0017 delivery slot)

Add reqwest `multipart` feature; `tokio-util = { version = "^0.7", features = ["io-util"] }` direct dep; ensure `stream` feature present. Run `cargo build && cargo deny check`. Verify `tests/mutants_glob_existence` GREEN.

### Task 2: Author RED test anchors in `tests/attachment_upload.rs` (18 ACs)

Write all 26 tests (18 ACs; AC-001 has 3, AC-003 has 2, AC-006 has 4, AC-007 has 4, AC-008 has 2, AC-011 has 2, AC-018 has 2 [second is `#[cfg(unix)]`]; AC-015 shares AC-003's VP test — 0 new; AC-009/010/013 use existing test files — 0 new each). Use `jr_cmd_with_xdg` + per-test `TempDir`. Confirm RED against todo!() stubs. Verify Red Gate density ≥0.5 across the file.

### Task 3: Implement `upload_attachments` in `src/api/jira/attachments.rs`

Multipart POST with `X-Atlassian-Token: no-check`. `ReaderStream` from `tokio::fs::File`. Retry rebuilds from fresh `File::open` (ADR-0017). Handle 429 retry-after. Deserialize response to `Vec<AttachmentObject>` using `serialize_attachment_curated` shape.

### Task 4: Implement `attachment_replace_confirmation_gate`

`eprint!` + `io::stdin().read_line`. Three-way branch per P14-001. Non-interactive exit-64 path (EC-3.9.014-3). `--yes` bypass. Tests AC-007/AC-014 GREEN.

### Task 5: Wire `src/cli/mod.rs` + Implement `replace_existing_attachments` and `handle_attachment_upload`

First wire `AttachmentSubcommand::Upload` in `src/cli/mod.rs`: positional `<KEY>`, `<FILE...>` (**no** `allow_hyphen_values` — intentionally absent on multi-value positional; clap would consume subsequent flags as file paths; see CLAUDE.md gotcha); flags `--replace-existing` / `--yes` / `--dry-run` (requires `--replace-existing`) / `--public` / `--internal` (mutually conflicting); per BC-3.9.001 CLI-flags clause (~3285). Also wire `AttachmentSubcommand::Upload` dispatch match arm in `src/cli/issue/mod.rs` (inline match; mirrors comment-CRUD pattern; calls `handle_attachment_upload`).

Full flow: file pre-checks → list → match-by-filename → gate → delete-all → POST (VP-576-003 ordering). Multi-file = single atomic POST (BC-3.9.001 EC-3.9.001-2); all-or-nothing success/failure per BC-3.9.012. --dry-run path-c (AC-008). Tests AC-005/AC-006/AC-007/AC-008/AC-012 GREEN.

### Task 6: VP-576-004 cross-path test + VP-576-003 ordering test

`test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical` and `test_vp_576_003_delete_before_post_ordering_invariant` both GREEN.

### Task 7: AC-013 — e2e surface guard + CLAUDE.md + docs/specs/ + CHANGELOG-append

Add SURFACE entries. Add CLAUDE.md gotchas (X-Atlassian-Token, retry rebuild, JRACLOUD-96384 match-by-filename). Add json-output-shapes.md upload rows. Create `docs/specs/attachments.md`.

**CHANGELOG-append (P3-018)**: Append to `CHANGELOG.md` under the appropriate `[Unreleased]` section:
```
feat(issue): attachment upload platform POST + --replace-existing + --dry-run path-c (#576)
```
This entry is scoped to the upload surface only (S-576-3).

### Task 8: mutants.toml examine_globs + ADR-0012 shard check

Confirm `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` in examine_globs. Run `cargo test --test mutants_glob_existence`.

### Task 9: Full suite green

```
cargo test --test attachment_upload
cargo test
cargo clippy -- -D warnings
cargo deny check
```

### Task 10: Create PR

PR title: `feat(issue): attachment upload platform POST + --replace-existing + --dry-run path-c (S-576-3, #576 part 3)`.
Note: depends_on: ["S-576-1"] — S-576-1 must be merged first (ADR-0017 `stream` feature is idempotent; no S-576-2 dependency). This story does NOT close #576 (S-576-5 closes it). AC-017 interim rejection guard ships here; S-576-5 removes it.

---

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-576-1 | `serialize_attachment_curated`, `display_sanitize_filename` established; `src/cli/issue/attachments.rs` + `src/api/jira/attachments.rs` created; VP-576-004 list half | `output::render_json` pattern; curated BTreeMap JSON shape | `"self"` OMITTED and `"content"` renamed to `"contentUrl"` — do NOT use default serde derive on AttachmentObject |
| S-576-2 | `sanitize_attachment_filename` (disk/CWE-22) distinct from `display_sanitize_filename` (display/CWE-116); `stream` feature may already be in Cargo.toml | streaming download via `bytes_stream()`; atomic temp+rename | reqwest `stream` and `multipart` are separate features; check Cargo.toml before adding `multipart` |

---

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `X-Atlassian-Token: no-check` on every upload POST | BC-3.9.001; CLAUDE.md gotcha | AC-001 wiremock asserts header present (`.expect(1)` on matched path) |
| stdin `-` rejected before HTTP | BC-3.9.001 EC-3.9.001-6 | AC-001 test; no HTTP mounts match |
| DELETE(s) before POST on --replace-existing | VP-576-003 | AC-006 wiremock request log ordering assertion |
| Gate uses `eprint!` not `eprintln!` or `dialoguer` | BC-3.9.014; DEC-174 | Code review; grep: `dialoguer::Confirm` must be absent from attachments.rs |
| EC-3.9.020-9 pre-flight checks NOT suppressed on dry-run | BC-3.9.020 | AC-008 test: replace-existing --dry-run + non-existent file exits 64 |
| No JSM API calls | S3 interim scope (superseded by S-576-5); prd-delta-576.md S3 DECOMPOSITION SEAM | `grep -n "api::jsm\|api/jsm" src/cli/issue/attachments.rs` empty |
| All `--output json` via `output::render_json` | #526 invariant | `grep -n "to_string_pretty" src/cli/issue/attachments.rs` empty |

---

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| reqwest `multipart` feature | 0.13.x (add to Cargo.toml) | `multipart::Form` for upload |
| reqwest `stream` feature | 0.13.x (ensure present; idempotent from S2) | `ReaderStream` for streaming |
| tokio-util `io-util` | `^0.7` (add to Cargo.toml) | `tokio_util::io::ReaderStream` |
| proptest | as in `Cargo.lock` | proptest support (re-used from S2) |
| wiremock | as in `Cargo.lock` | Integration test HTTP mocking |

---

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `Cargo.toml` | MODIFY | reqwest `multipart`; tokio-util direct dep (ADR-0017) |
| `src/cli/issue/attachments.rs` | MODIFY (extends S1+S2) | Add upload handler, gate, replace-existing, dry-run path-c |
| `src/api/jira/attachments.rs` | MODIFY (extends S1+S2) | Add `upload_attachments` multipart POST |
| `tests/attachment_upload.rs` | CREATE | 26 integration tests (18 ACs; AC-001 has 3, AC-003 has 2, AC-006 has 4, AC-007 has 4, AC-008 has 2, AC-011 has 2, AC-018 has 2 [second is `#[cfg(unix)]`]; AC-015 shares AC-003's VP test; AC-009/010/013 use existing test files) |
| `tests/e2e_cli_surface_guard.rs` | MODIFY | SURFACE entries for upload flags |
| `CLAUDE.md` | MODIFY | X-Atlassian-Token gotcha; multipart retry gotcha; JRACLOUD-96384 match-by-filename |
| `docs/specs/json-output-shapes.md` | MODIFY | Upload manifest + dry-run shapes |
| `docs/specs/attachments.md` | CREATE | Attachment surface reference doc (F4 obligation) |
| `.cargo/mutants.toml` | MODIFY (idempotent) | confirm examine_globs entries for attachments.rs are present (primary delivery moved to S-576-1 per prd-delta-576.md S1 obligation (f) / P22-001; S2 obligation (e) confirms present; this story ADD IF ABSENT per S3 obligation (a)) |
| `src/cli/mod.rs` | MODIFY | Add `AttachmentSubcommand::Upload` variant — positional `<KEY>`, `<FILE...>` (**no** `allow_hyphen_values` — intentionally absent; multi-value positional + `allow_hyphen_values` causes clap to consume subsequent flags as file paths; see CLAUDE.md gotcha); flags `--replace-existing` / `--yes` / `--dry-run` (requires `--replace-existing`) / `--public` / `--internal` (mutually conflicting); per BC-3.9.001 CLI-flags clause (~3285); S-576-5 exercises JSM wire behavior, flag definitions live here (P3-008) |
| `src/cli/issue/mod.rs` | MODIFY | Add `AttachmentSubcommand::Upload` dispatch match arm (inline match; mirror comment-CRUD pattern in `src/cli/issue/mod.rs`; calls `handle_attachment_upload`) |

**Additive-only coordination:** `AttachmentSubcommand` (`src/cli/mod.rs`) and its dispatch match (`src/cli/issue/mod.rs`) are edited ADDITIVELY by S2/S3/S4/S5 — append your variant + arm only; never remove or reorder a sibling story's variant or arm; expect a rebase if a sibling merges first.

**MUST NOT change**: `src/cli/issue/interactions.rs`; `src/api/jsm/` any file.

---

## Out-of-Scope

- JSM `--public`/`--internal` (S-576-5 builds on this story's gate mechanics).
- `jr issue attachment delete` (S-576-4).
- `jr issue attachment download` (S-576-2, independent track).
- Unicode bidi-control sanitization: discharged @ v1.3.94 — scope covered by `display_sanitize_filename` per BC-2.7.011 display-sanitization primary clause.

---

## Spec Reference Links

- BC-3.9.001..020: `.factory/specs/prd/bc-3-issue-write.md §3.9`
- VP-576-003/004: `.factory/specs/prd/bc-3-issue-write.md`
- ADR-0017: `.factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md`
- prd-delta-576.md Scope S3 row: `.factory/phase-f2-spec-evolution/prd-delta-576.md`
