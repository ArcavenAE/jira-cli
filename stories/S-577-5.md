---
document_type: story
level: ops
story_id: "S-577-5"
epic_id: "none"
title: "comment edit visibility flags + public confirmation + e2e probe"
wave: feature-followup
status: draft
intent: feature
feature_type: backend-cli
mode: feature
scope: standard
severity: HIGH
trivial_scope: false
points: 4
priority: P0
tdd_mode: strict
producer: story-writer
timestamp: "2026-07-11T00:00:00"
phase: 3
inputs:
  - ".factory/specs/prd/bc-3-issue-write.md"
  - ".factory/phase-f1-delta-analysis/delta-analysis-577-comment-crud.md"
  - ".factory/phase-f2-spec-evolution/f2-convergence-record-577.md"
input-hash: "7131b40"
traces_to: "src/cli/issue/interactions.rs::handle_comment_edit"
cycle: SOH-COMMENT-CRUD-1
estimated_effort: medium-high
estimated_days: 2
target_module: src/cli/issue/interactions.rs
subsystems: ["issue-comment", "cli-dispatch"]
depends_on: [S-577-1, S-577-2, S-577-3, S-577-4]
blocks: []
behavioral_contracts:
  [BC-3.5.006, BC-3.5.007, BC-3.5.008, BC-3.5.011]
verification_properties:
  [VP-577-002, VP-577-003, VP-577-006, VP-577-010, VP-577-017, VP-577-025, VP-577-026, VP-577-028, VP-577-029, VP-577-030]
holdout_anchors: [H-NEW-COMMENT-004]
nfr_anchors: []
adr_refs: []
sd_refs: []
parent_phase: F2-approved-standard-feature
spec_source: ".factory/specs/prd/bc-3-issue-write.md"
implementation_strategy: tdd
module_criticality: HIGH
acceptance_criteria_count: 12
assumption_validations: []
risk_mitigations: []
created: "2026-07-11"
version: "1.0"
last_updated: "2026-07-11"
breaking_change: false
retroactive: false
origin: >
  Issue #577 SOH-COMMENT-CRUD-1: handle_comment_edit visibility flag paths.
  BC-3.5.006: --internal properties PUT (sd.public.comment={internal:true}); JSDCLOUD-6050
  hint. BC-3.5.007: --public properties PUT (sd.public.comment={internal:false}).
  BC-3.5.008: --public confirmation gate (--yes bypass; non-TTY without --yes → exit 64;
  interactive y/N; dialoguer::Error → exit 130). BC-3.5.011: --internal/--public
  conflicts_with → exit 2. EC-3.5.008-3: --stdin implies no_input. EC-3.5.008-4: --yes
  without --public accepted silently. VP-577-026 variants 1+2 (jsm_internal Bool present).
  BC-3.5.006 delivery obligations: (a) CLAUDE.md MERGE gotcha; (b) 3-scenario gated e2e test.
  VP-577-030 variant 2 (--public EOF → exit 130).
changelog:
  - "1.0 (2026-07-11): Initial story authored; F2 gate convergence at spec v1.3.40; bundle SOH-COMMENT-CRUD-1."
---

> **tdd_mode:** strict — Red Gate required. Extend `tests/comment_edit.rs` from S-577-4
> with 12 new AC tests. After S-577-4 merges, `handle_comment_edit` has a working body-only
> implementation — no `todo!()` stub. Tests fail because the VISIBILITY BRANCHES are absent
> (feature-absence failures, not compilation panics). Red Gate:
> VP-577-002/003/006/010/017/025/026/028/029 tests FAIL (unimplemented branch) → all PASS.
> VP-577-030 variant 2 (--public EOF) also FAIL → PASS.
> **Pre-satisfied ACs** (remain as regression assertions, not fail-first):
> - AC-010 (--yes silent no-op) is pre-satisfied by S-577-4's body-only path (clap accepts --yes)
> - AC-011 (--internal + --public clap exit 2) is pre-satisfied by S-577-1's clap conflicts_with
> These pre-satisfied tests MUST remain in the test suite as regression guards.

> **Execute:** `/vsdd-factory:deliver-story S-577-5`

# S-577-5: `comment edit` visibility flags + public confirmation + e2e probe

**Bundle**: SOH-COMMENT-CRUD-1  
**GitHub issue**: #577  
**BC anchors**: BC-3.5.006 (--internal PUT + JSDCLOUD-6050 + e2e obligations),
BC-3.5.007 (--public PUT), BC-3.5.008 (--public confirmation gate),
BC-3.5.011 (mutual exclusion)  
**VPs**: VP-577-002, VP-577-003, VP-577-006, VP-577-010, VP-577-017, VP-577-025,
VP-577-026(variants 1+2), VP-577-028, VP-577-029, VP-577-030(variant 2)  
**Routing**: standard feature  
**Sequencing**: depends on S-577-3 (delete; JR_STDIN_IS_TTY seam) + S-577-4 (edit core);
no story blocks this one (final edit story in bundle)

---

## Narrative

- **As a** developer managing JSM Jira comments from the CLI
- **I want** `jr issue comment edit KEY --id ID [--internal | --public [--yes]]` to change
  a comment's visibility atomically with its body update
- **So that** I can make internal agent notes truly internal and make restricted comments
  public, with safety confirmation for the --public direction that can be bypassed
  non-interactively with --yes

---

## Behavioral Contracts

| BC | Summary | Clauses Covered |
|----|---------|----------------|
| BC-3.5.006 | --internal: adds `properties:[{key:"sd.public.comment",value:{internal:true}}]` to PUT; JSDCLOUD-6050 hint on stderr before PUT; delivery obligations (a) CLAUDE.md, (b) gated e2e | EC-3.5.006-1/2; MERGE semantics |
| BC-3.5.007 | --public: adds `properties:[{key:"sd.public.comment",value:{internal:false}}]` to PUT; JSDCLOUD-6050 hint after confirmation, before PUT | EC-3.5.007-1 |
| BC-3.5.008 | --public confirmation: --yes bypass; --no-input/non-TTY → exit 64 without --yes; interactive y/N; dialoguer::Err → exit 130; --stdin without --yes → exit 64 | EC-3.5.008-1/2/3/4/5 |
| BC-3.5.011 | `--internal` + `--public` → clap exit 2 (conflicts_with) | EC-3.5.011-1 |

---

## Acceptance Criteria

### AC-001: --internal PUT wire format (VP-577-002)
(traces to BC-3.5.006 postcondition — --internal adds properties to PUT request body)

`jr issue comment edit FOO-1 --id 10001 "body" --internal --output json` against wiremock
→ exit 0; inspect `received_requests()[0]` PUT body parsed as JSON:
- `body` key present (ADF value)
- `properties` key present; value is array with exactly one element:
  `{"key":"sd.public.comment","value":{"internal":true}}`

**Test**: `test_bc_3_5_006_internal_puts_properties_true` in `tests/comment_edit.rs`.

---

### AC-002: --internal response JSON includes jsm_internal=true (VP-577-026 variant 1)
(traces to BC-3.5.006 postcondition — changed_fields.jsm_internal is true for --internal)

`jr issue comment edit FOO-1 --id 10001 "body" --internal --output json` → exit 0;
parsed `changed_fields` contains key `"jsm_internal"` with value `true` (Boolean).

**Test**: `test_bc_3_5_006_changed_fields_jsm_internal_true` in `tests/comment_edit.rs`.

---

### AC-003: --public PUT wire format (VP-577-003)
(traces to BC-3.5.007 postcondition — --public adds properties internal:false to PUT)

`jr issue comment edit FOO-1 --id 10001 "body" --public --yes --output json` against
wiremock → exit 0; inspect PUT request body:
- `properties` key present; first element: `{"key":"sd.public.comment","value":{"internal":false}}`

**Test**: `test_bc_3_5_007_public_puts_properties_false` in `tests/comment_edit.rs`.

---

### AC-004: --public response JSON includes jsm_internal=false (VP-577-026 variant 2)
(traces to BC-3.5.007 postcondition — changed_fields.jsm_internal is false for --public+yes)

`jr issue comment edit FOO-1 --id 10001 "body" --public --yes --output json` → exit 0;
parsed `changed_fields` contains key `"jsm_internal"` with value `false` (Boolean).

**Test**: `test_bc_3_5_007_changed_fields_jsm_internal_false` in `tests/comment_edit.rs`.

---

### AC-005: JSDCLOUD-6050 hint fires before PUT on --internal path (VP-577-025 variant 1)
(traces to BC-3.5.006 postcondition EC-3.5.006-1 — JSDCLOUD-6050 hint timing)

`jr issue comment edit FOO-1 --id 10001 "body" --internal` → exit 0; stderr contains
`"JSDCLOUD-6050"` (load-bearing substring) AND `"(marked internal)"` (BC-3.5.005 human-channel
echo marker; VP-577-025 variant 1 purpose). Validate timing by asserting both substrings
appear in stderr regardless of output format.

**Test**: `test_bc_3_5_006_jsdcloud_hint_appears_on_internal` in `tests/comment_edit.rs`.

---

### AC-006: JSDCLOUD-6050 hint fires after confirmation on --public path (VP-577-025 variant 2)
(traces to BC-3.5.007 EC-3.5.007-1 — hint appears after --yes bypass, before PUT; also proves EC-3.5.008-1 hint-not-suppressed)

`jr issue comment edit FOO-1 --id 10001 "body" --public --yes` → exit 0; stderr contains
`"JSDCLOUD-6050"` AND `"(marked public)"` (BC-3.5.005 human-channel echo marker; VP-577-025
variant 2 purpose). The hint fires AFTER the confirmation step (after --yes) and BEFORE
the HTTP PUT. Simultaneously proves EC-3.5.008-1: `--yes` does NOT suppress the
JSDCLOUD-6050 hint — hint fires on every `--public` PUT path regardless of `--yes`.

**Test**: `test_bc_3_5_007_jsdcloud_hint_appears_on_public_yes` in `tests/comment_edit.rs`.

---

### AC-007: --public without --yes, non-interactive → exit 64, no PUT (VP-577-006)
(traces to BC-3.5.008 precondition — confirmation required in non-interactive mode)

`jr issue comment edit FOO-1 --id 10001 "body" --public --no-input` (no `--yes`) → exit 64;
stderr contains BOTH `"visibility to public"` AND `"--yes"` (VP-577-006 dual-pin: proves
exit originates from the step-3 `--public` gate, not the step-2 body gate — the non-empty
body `"body"` is REQUIRED for this test to be meaningful); wiremock PUT has `.expect(0)`.

**Test**: `test_bc_3_5_008_public_no_input_without_yes_exits_64` in `tests/comment_edit.rs`.

---

### AC-008: --public interactive cancel → exit 0, JSON cancel envelope (VP-577-029)
(traces to BC-3.5.008 EC-3.5.008-2 — interactive N → cancelled, no PUT)

`jr issue comment edit FOO-1 --id 10001 "body" --public --output json` with
`JR_STDIN_IS_TTY=1` and stdin fed `"N\n"` → exit 0; stdout parses as JSON:
- Top-level key set == `BTreeSet::from(["cancelled", "updated"])` (exact; no `"id"` or `"key"`)
- `cancelled` == `true`
- `updated` == `false` (boolean false — NOT a timestamp or empty string; VP-577-029 exact pin)
- Wiremock PUT has `.expect(0)`

**Test**: `test_bc_3_5_008_public_interactive_cancel_json_key_set` in `tests/comment_edit.rs`.

---

### AC-009: --stdin without --yes → exit 64 (VP-577-017, EC-3.5.008-3)
(traces to BC-3.5.008 EC-3.5.008-3 — --stdin implies no_input gate)

`jr issue comment edit FOO-1 --id 10001 --stdin --public` with stdin fed non-empty body
(e.g. `"body"`) and stdin is a pipe, not a TTY → exit 64; stderr contains BOTH `"--stdin"`
AND `"--yes"` (VP-577-017 dual-pin: the EC-3.5.008-3 targeted hint must contain both
substrings); no HTTP PUT; wiremock PUT has `.expect(0)`.

**VP-577-017 second variant (prescriptive-rule pin)**: same invocation with `JR_STDIN_IS_TTY=1`
set (seam active, auto-flip suppressed) → STILL exit 64; same stderr assertions (`"--stdin"`
AND `"--yes"`); zero PUT calls. This variant proves the `--stdin` flag-based branch fires
independently of TTY-detection state per EC-3.5.008-3.

**Setup note**: stdin MUST contain a NON-EMPTY body (so EC-3.5.009-5 body-empty gate passes);
the EC-3.5.008-3 targeted message fires at step-3, not as a handler-start short-circuit.

**Test**: `test_bc_3_5_008_ec3_stdin_without_yes_public_exits_64` in `tests/comment_edit.rs`.

---

### AC-010: --yes without --public accepted silently (VP-577-028, EC-3.5.008-4)
(traces to BC-3.5.008 EC-3.5.008-4 — --yes is silent no-op on non-public paths)

**Variant 1 (VP-577-028 variant 1 — silent no-op success):**
`jr issue comment edit FOO-1 --id 10001 "body" --yes --output json` (no `--public`) →
exit 0; body-only PUT succeeds; `--yes` is a silent no-op; no error, no clap rejection.
Clap MUST NOT have `requires("public")` on `--yes`.

**Variant 2 (VP-577-028 variant 2 — runtime clap-requires probe):**
`jr issue comment edit FOO-1 --id 10001 "" --yes` (empty-string positional, no `--public`) →
exit 64; stderr contains `"comment body cannot be empty"`.
**Rationale:** exit 64 (handler-level empty-body guard) NOT exit 2 (clap-level) proves
that `requires("public")` is absent from `--yes` at parse time — if `requires("public")`
were present, clap would reject the invocation with exit 2 before reaching the empty-body
guard. This exit-64-vs-2 discrimination is the operative test signal.

**Test**: `test_bc_3_5_008_ec4_yes_without_public_is_silent_noop` (variant 1) and
`test_bc_3_5_008_ec4_yes_without_public_runtime_probe_exit64` (variant 2) in `tests/comment_edit.rs`.

---

### AC-011: --internal + --public → exit 2 (VP-577-010, BC-3.5.011)
(traces to BC-3.5.011 — clap conflicts_with enforcement)

`jr issue comment edit FOO-1 --id 10001 "body" --internal --public` → exit 2 (clap error).
This is a clap-enforced constraint, not a runtime guard.

**Test**: `test_bc_3_5_011_internal_and_public_clap_exit_2` in `tests/comment_edit.rs`.

---

### AC-012: EOF during --public prompt → exit 130 (VP-577-030 variant 2)
(traces to BC-3.5.008 EC-3.5.008-5 — dialoguer::Error → JrError::Interrupted exit 130)

`jr issue comment edit FOO-1 --id 10001 "body" --public` with `JR_STDIN_IS_TTY=1` and
stdin fed EOF → exit 130 (`JrError::Interrupted`); wiremock PUT has `.expect(0)`.

**Test**: `test_bc_3_5_008_ec5_public_prompt_eof_exits_130` in `tests/comment_edit.rs`.

---

## Architecture Mapping

| Component | File | Pure/Effectful | Change |
|-----------|------|---------------|--------|
| handle_comment_edit (visibility path) | `src/cli/issue/interactions.rs` | Effectful (HTTP + prompt) | Extend default path with --internal/--public branches |
| update_comment (with visibility) | `src/api/jira/issues.rs` | Effectful (HTTP) | Called with `Some(true)`/`Some(false)` — no code change needed |
| comment_edit tests | `tests/comment_edit.rs` | Effectful (wiremock) | Extend file from S-577-4 with 12 new tests |
| E2E probe | `tests/e2e_live.rs` | Live API | New gated e2e test function (3 scenarios) |

### Visibility Flag Branching in handle_comment_edit

Full pipeline (per BC-3.5.005 edit pipeline ordering pin at bc-3-issue-write.md ~line 2264;
steps 1–2 execute in S-577-4's body-only implementation, steps 3–5 are new here):

```
(1) --id regex validation: validate_comment_id(id) → exit 64 on mismatch
(2) Body-source resolution + empty/whitespace guard (EC-3.5.009-5):
    --file / --stdin / positional → body string; trim().is_empty() → exit 64
    (stash raw pre-trim body for JSON echo BEFORE any trim)

(3) --public confirmation gate (fires BEFORE ADF conversion, step 4):
    if --public:
      if stdin_flag set → mutate no_input = true at handler-start (EC-3.5.008-3; check the
        `stdin` clap flag DIRECTLY — do NOT call is_terminal(); flag check is TTY-agnostic)
      if no_input && !yes → exit 64 + targeted hint:
        stdin_flag: "--stdin disables interactive prompts — pass --yes to confirm the visibility change."
        otherwise:  "This will set the comment's visibility to public. Use --yes to confirm."
        OTHERWISE hint MUST contain "visibility to public" AND "--yes" (VP-577-006 pin).
        STDIN hint MUST contain "--stdin" AND "--yes" (VP-577-017 pin) — EC-3.5.008-3 verbatim message.
      if yes → proceed (skip interactive prompt)
      else (interactive) → dialoguer::Confirm::interact_on(&Term::stderr())
        Err(_) → JrError::Interrupted (exit 130)
        Ok(false) → return cancel envelope {"cancelled":true,"updated":false}
    (--yes without --public: silent no-op, gate not reached — EC-3.5.008-4)

    NOTE: Test `no_input` ALONE for the non-interactive branch. Do NOT call
    `is_terminal()` or `refuse_noninteractive(no_input, is_terminal(&stdin()))` inside
    the handler. See Architecture Compliance Rule 8.

(4) ADF conversion: text_to_adf(body) or markdown_to_adf(body) depending on --markdown

(4.x) JSDCLOUD-6050 hint (fires AFTER step 4 ADF success, BEFORE step 5 PUT):
      --internal → eprintln!("note: … JSDCLOUD-6050 …") BEFORE PUT
      --public → eprintln!("note: … JSDCLOUD-6050 …") AFTER confirmation + AFTER ADF, BEFORE PUT
      DOES NOT fire if step 4 ADF fails (handler exits with ADF error instead)
      DOES NOT fire if --public confirmation was cancelled (already returned at step 3)

(5) HTTP PUT: client.update_comment(key, id, adf_body, visibility_flag).await
    --internal → visibility_flag = Some(true)
    --public   → visibility_flag = Some(false)
    neither    → visibility_flag = None (body-only; handled in S-577-4)

(6) Build response JSON from local state:
    changed_fields.body = raw pre-trim body (stashed at step 2; same as S-577-4)
    changed_fields.jsm_internal = true  when --internal (boolean, not string)
    changed_fields.jsm_internal = false when --public confirmed (boolean, not string)
    (absent when neither — S-577-4's responsibility)
    id = --id argument; key = KEY argument; updated = literal true (boolean)
```

### Cancelled Envelope (--public interactive cancel)

When the user answers N to the `--public` confirmation:

```json
{
  "cancelled": true,
  "updated": false
}
```

Key set: exactly `["cancelled", "updated"]`. No `"id"` or `"key"` keys (not confirmed
server-side). `"updated"` is JSON boolean `false` — VP-577-029 exact pin. NOT a timestamp
or empty string. The key MUST be present for BTreeSet assertion consistency.

### JSDCLOUD-6050 Hint Text

```
note: visibility change is best-effort on JSM projects — verify in the portal (JSDCLOUD-6050); no-op on non-JSM projects.
```

The hint text MUST be byte-for-byte as above (spec-verbatim from EC-3.5.006-1, preserving
em-dash). `"JSDCLOUD-6050"` MUST appear in the stderr output (VP-577-025 variant 1 and
variant 2 key substring check). This hint fires even with `--output json` (it goes
to stderr, not stdout).

### BC-3.5.006 Delivery Obligation (a): CLAUDE.md MERGE Verdict

Add to CLAUDE.md under `## Gotchas` (or `## Key Decisions`):

```
- **`jr issue comment edit --internal/--public` uses MERGE semantics on Jira's properties
  endpoint (BC-3.5.006, JSDCLOUD-6050):** A PUT to `/rest/api/3/issue/{key}/comment/{id}`
  with a `properties` array MERGES with existing properties — it does NOT replace them.
  A body-only PUT (no `--internal`/`--public`) leaves visibility UNCHANGED (PRESERVED
  semantics). Confirmed by `.factory/research/issue-577-properties-merge-replace-2026-07-09.md`.
  Do NOT attempt to clear visibility by omitting properties; use `--public` explicitly.
```

This CLAUDE.md entry is a mandatory delivery obligation for this story's PR.

### BC-3.5.006 Delivery Obligation (b): Gated E2E Test (3 Scenarios)

Add to `tests/e2e_live.rs` a new `#[ignore]` test function gated on `JR_RUN_E2E=1`.

Gate conditions: requires `JR_RUN_E2E=1` + `JR_E2E_JSM_PROJECT` (set to `EJ` in canonical CI).
Clean-skip (return early) when `JR_E2E_JSM_PROJECT` is unset.

**Teardown (mandatory):** Each scenario self-cleans with
`jr issue comment delete <KEY> --id <CID> --yes` per probe comment. Do NOT use
`jsm_self_close` (closes the parent issue; EJ fixture must remain open).
**Restricted-fixture creation:** Use `jr api POST` (not `jr issue comment add`) for
comments requiring role/group visibility restriction or extra `properties`.

The three scenarios are (BC-3.5.006 obligation b, spec v1.3.40):

**Scenario 1 (5-step MERGE probe):**
(1) Create JSM comment via `jr api POST` with `sd.public.comment={internal:true}`.
(2) GET; assert internal=true. (3) `jr issue comment edit KEY --id CID "body" --internal`.
(4) GET; assert internal=true (MERGE preserved). (5) Second `--internal` PUT; GET; assert stable.

**Scenario 2 (PRESERVED base — 2-step):**
(1) Create JSM comment via `jr api POST` WITH a role/group visibility restriction.
(2) `jr issue comment edit KEY --id CID "body-only"` (no `--internal`/`--public`).
(3) GET; assert restriction SURVIVES (confirms PRESERVED verdict from
`.factory/research/issue-577-visibility-put-semantics-2026-07-09.md`).

**Scenario 3 (compound cell — ADDITION to Scenario 2, NOT a substitute):**
(1) Create JSM comment via `jr api POST` carrying BOTH a role/group restriction AND
a `jr.test.marker` property.
(2) PUT body = `{"body": <adf>, "properties": [{"key": "sd.public.comment", "value": {"internal": false}}]}`
with NO `visibility` key.
(3) GET; assert BOTH the restriction AND `jr.test.marker` SURVIVE.
Closes the weakest safety-table cell: MERGE for properties does not interfere with
PRESERVED for visibility when both are simultaneously present.
Scenario 3 is an explicit ADDITION to Scenario 2 — both MUST run.

Full pseudocode lives in Task 4 below.

Add to `tests/e2e_cli_surface_guard.rs` surface table entries for any new subcommand paths
exercised by these scenarios.

### Forbidden Dependencies

`interactions.rs` MUST NOT use `serde_json::to_string_pretty` directly. All JSON output
routes through `output::render_json`.

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `--internal --public` on one command | clap exit 2 (BC-3.5.011; AC-011) |
| EC-002 | `--public --no-input` without `--yes` | exit 64, "Use --yes to confirm" (AC-007) |
| EC-003 | `--stdin --public` (stdin is pipe) | exit 64, "--stdin disables interactive" (EC-3.5.008-3; AC-009) |
| EC-004 | `--yes` without `--public` | silent no-op; body-only PUT proceeds (EC-3.5.008-4; AC-010) |
| EC-005 | EOF during `--public` interactive prompt | exit 130 (EC-3.5.008-5; AC-012) |
| EC-006 | Interactive cancel on `--public` | exit 0, `{"cancelled":true,"updated":false}` (VP-577-029 exact pin; AC-008) |
| EC-007 | `--internal` changes wire format | PUT includes `"properties"` array with `internal:true` (AC-001) |
| EC-008 | `--public --yes` changes wire format | PUT includes `"properties"` array with `internal:false` (AC-003) |
| EC-009 | `jsm_internal` in changed_fields | `true` for --internal, `false` for --public, absent for neither (VP-577-026) |
| EC-010 | JSDCLOUD-6050 hint on both paths | Fires on `--internal` (before PUT) and `--public --yes` (after confirm, before PUT) |

---

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `src/cli/issue/interactions.rs::handle_comment_edit` (visibility path) | effectful-shell | HTTP PUT + dialoguer interactive prompt; stdin TTY check via no_input |
| `tests/comment_edit.rs` (extended, 12 new tests) | effectful-shell (test boundary) | Subprocess via jr_cmd_with_xdg; wiremock; JR_STDIN_IS_TTY env |
| `tests/e2e_live.rs` (gated e2e test) | effectful-shell (test boundary) | Live Jira API; gated behind JR_RUN_E2E + JR_E2E_JSM_PROJECT |
| `CLAUDE.md` (MERGE gotcha entry) | N/A (docs) | Documentation obligation; no executable code |

---

## Token Budget Estimate

| Item | Est. Tokens |
|------|------------|
| Story spec (this file) | ~7 k |
| `src/cli/issue/interactions.rs` (edit handler from S-577-4, ~100 LOC) | ~2 k |
| `src/api/jira/issues.rs` (update_comment method from S-577-2) | ~1 k |
| `src/cli/issue/workflow.rs` (refuse_noninteractive + dialoguer pattern) | ~1 k |
| BC-3.5.006/007/008/011 relevant sections | ~8 k |
| `tests/comment_edit.rs` (existing from S-577-4 + 12 new tests) | ~8 k |
| `tests/e2e_live.rs` (existing structure + new test function) | ~3 k |
| **Total** | **~30 k** |

Well under 20% of 200 k context window.

---

## Tasks

**Red Gate protocol**: Extend `tests/comment_edit.rs` with 12 new AC tests; they MUST fail
because visibility branches are absent (feature-absence failures, not todo!() panics — S-577-4
leaves a working body-only implementation, not a stub). Then implement the visibility paths.
Pre-satisfied ACs (AC-010, AC-011) will already pass; the other 10 will fail until implementation.

### Task 0 — Read source context

Read:
- `src/cli/issue/interactions.rs` (from S-577-4 — edit handler with body-only default path)
- `src/cli/issue/workflow.rs` — `handle_move` interactive prompt pattern (dialoguer use)
- BC-3.5.006/007/008/011 from `.factory/specs/prd/bc-3-issue-write.md` (§3.5 visibility sections)
- `tests/comment_edit.rs` (from S-577-4 — existing 9 tests to understand pattern)
- `tests/e2e_live.rs` — `e2e_enabled()` helper, existing ignore-test pattern, `jsm_self_close` usage

### Task 1 — Extend tests/comment_edit.rs (Red Gate tests, 12 new tests)

Add all 12 ACs as tests. For AC-008/012 (interactive mode), set `JR_STDIN_IS_TTY=1`.
Confirm that AC-010 (--yes silent no-op) and AC-011 (--internal+--public clap exit 2) already
pass (pre-satisfied by S-577-4 and S-577-1 respectively). Confirm the other 10 tests fail
due to missing visibility branch implementation.

### Task 2 — Implement visibility flag paths in handle_comment_edit

Extend the pipeline from S-577-4 with:
- Confirmation gate (BC-3.5.008): test `no_input` ALONE (NOT `is_terminal()`) — if
  `no_input && !yes` → exit 64 with hint containing `"visibility to public"` AND `"--yes"`;
  if `--stdin` flag set → mutate `no_input = true` at handler-start (EC-3.5.008-3);
  interactive y/N via `dialoguer::Confirm::interact_on(&Term::stderr())`; EOF → exit 130.
  WARNING: Do NOT use `refuse_noninteractive(no_input, is_terminal(&stdin()))` — see
  Architecture Compliance Rule 8.
- JSDCLOUD-6050 hint emission (BC-3.5.006/007)
- Pass `Some(true)`/`Some(false)` to `client.update_comment(...)`
- Build `changed_fields.jsm_internal` in the response JSON

### Task 3 — Confirm all 12 new tests pass

```bash
cargo test --test comment_edit -- --nocapture
```

All 22 tests (10 from S-577-4 + 12 from this story) pass.

### Task 4 — Add gated e2e test (BC-3.5.006 delivery obligation b)

Add `test_e2e_comment_edit_visibility_merge_semantics` to `tests/e2e_live.rs`.
Gate: `if !e2e_enabled() { return; }` + `JR_E2E_JSM_PROJECT` clean-skip.

**Teardown pattern (per-comment DELETE — mandatory):**
Each probe comment created in these scenarios MUST be self-cleaned via:
```bash
jr issue comment delete <KEY> --id <CID> --yes
```
Do NOT use `jsm_self_close` — that closes the parent issue and would consume the
reusable EJ test fixture. Each scenario deletes its own probe comment immediately
after its assertions pass.

**Restricted-fixture creation:** Use `jr api POST /rest/api/3/issue/<KEY>/comment`
(not `jr issue comment add`) for any comment that must carry a role/group visibility
restriction or an additional `properties` key, since `jr issue comment add` does not
yet support those options.

```rust
#[tokio::test]
#[ignore]
async fn test_e2e_comment_edit_visibility_merge_semantics() {
    if !e2e_enabled() { return; }
    let jsm_project = match std::env::var("JR_E2E_JSM_PROJECT").ok() {
        Some(p) if !p.is_empty() => p,
        _ => return, // clean skip when JSM project not configured
    };
    // KEY is a pre-existing EJ issue (reusable fixture; do NOT close it)

    // ── Scenario 1 (5-step MERGE probe) ──────────────────────────────────────
    // (1) Create probe comment via jr api POST with sd.public.comment={internal:true}
    // (2) GET; assert comment is internal (internal=true)
    // (3) jr issue comment edit KEY --id CID "new body" --internal
    // (4) GET; assert comment is still internal (MERGE: existing sd.public.comment PRESERVED)
    // (5) jr issue comment edit KEY --id CID "body again" --internal (second identical PUT)
    //     GET; assert internal=true still (MERGE is stable on repeated --internal)
    // Teardown: jr issue comment delete KEY --id CID --yes

    // ── Scenario 2 (PRESERVED base — 2-step) ─────────────────────────────────
    // (1) Create probe comment via jr api POST carrying a role/group visibility restriction
    // (2) jr issue comment edit KEY --id CID "body-only edit" (no --internal/--public)
    //     This is a body-only PUT: {"body": <adf>} with no "properties" key
    // (3) GET comment; assert the role/group restriction survives
    //     (confirms the PRESERVED verdict: body-only PUT does not touch visibility)
    // Teardown: jr issue comment delete KEY --id CID --yes

    // ── Scenario 3 (compound cell — ADDITION to Scenario 2, NOT a substitute) ─
    // (1) Create probe comment via jr api POST carrying BOTH:
    //       - a role/group visibility restriction, AND
    //       - a "jr.test.marker" property in the comment's properties array
    // (2) Execute: jr api POST PUT body =
    //       {"body": <adf>, "properties": [{"key": "sd.public.comment", "value": {"internal": false}}]}
    //       with NO "visibility" key in the PUT body
    // (3) GET comment; assert BOTH survive:
    //       - the role/group visibility restriction is still present (PRESERVED)
    //       - the "jr.test.marker" property is still present (MERGE: not clobbered)
    //     This closes the weakest safety-table cell: MERGE for properties does not
    //     interfere with PRESERVED for visibility when both are simultaneously present.
    // Teardown: jr issue comment delete KEY --id CID --yes
    //
    // NOTE: Scenario 3 is an explicit ADDITION to Scenario 2 — NOT a substitute for it;
    // both MUST run (BC-3.5.006 delivery obligation b, F2 spec v1.3.40).
}
```

Update `tests/e2e_cli_surface_guard.rs` surface table if new `jr` invocations are added.
Run `cargo test --test e2e_cli_surface_guard` to confirm surface guard passes.

### Task 5 — Add CLAUDE.md MERGE gotcha entry (BC-3.5.006 delivery obligation a)

Add the MERGE semantics gotcha entry to CLAUDE.md under `## Gotchas`.

### Task 6 — Full suite green + lint

```bash
cargo test
cargo clippy -- -D warnings
```

### Task 7 — PR creation

Create PR to `develop`:
- Title: `feat(comment): comment edit visibility flags + public confirmation + e2e probe (#577 part 5)`
- Reference #577; include Red Gate evidence (22 tests: 10+12); note BC-3.5.006 delivery
  obligations (a) CLAUDE.md + (b) gated e2e in same PR

---

## Previous Story Intelligence

**S-577-3 (delete handler):** Implemented `JR_STDIN_IS_TTY` seam and `validate_comment_id`.
This story's interactive tests depend on both being present. Ensure S-577-3 is merged before
this story's PR (or stacked feature branches).

**S-577-4 (edit core):** Implemented the body-only default path and created
`tests/comment_edit.rs`. This story EXTENDS the existing test file with 12 more tests.
Do not delete or modify S-577-4's tests; the full 22-test run validates both stories together.

**ADR-0015 (proactive resolution enforcement):** The `--public` confirmation gate is
structurally similar to `handle_move` resolution confirmation, but do NOT call
`refuse_noninteractive` from `workflow.rs` — that function re-derives TTY state via
`is_terminal()` (see `workflow.rs:207-209`), defeating the `JR_STDIN_IS_TTY` seam.
Test `no_input` directly: `if no_input && !yes { … }`. The `dialoguer::Confirm` interactive
prompt pattern from `handle_move` is still a good reference for the prompt structure and
`interact_on(&Term::stderr())` usage.

**CLAUDE.md §`--yes silent no-op`:** The `--yes` silent no-op on non-public paths (EC-3.5.008-4)
was ratified in DEC-169 after a 9-CLI survey. Do NOT add `requires("public")` to the `--yes`
flag. Tested by AC-010.

**F2 convergence research:** `.factory/research/issue-577-properties-merge-replace-2026-07-09.md`
is the authoritative source for the MERGE/PRESERVED semantics verdicts. The CLAUDE.md gotcha
entry must cite this file.

---

## Architecture Compliance Rules

(Extracted from CLAUDE.md, BC-3.5.006/007/008, DEC-169)

1. **`interact_on(&Term::stderr())` is unconditional (all builds).** Same constraint as
   S-577-3. The `--public` confirmation prompt uses `dialoguer::Confirm` with
   `interact_on(&Term::stderr())`. Not gated by any cfg attribute.

2. **`dialoguer::Error` → `JrError::Interrupted` (exit 130).** EC-3.5.008-5. Must not be
   swallowed or mapped to the cancel path.

3. **--yes MUST NOT have `requires("public")` in clap.** EC-3.5.008-4, DEC-169. The
   `--yes` flag is a general confirmation bypass accepted silently on non-public paths.
   If clap `requires("public")` is present, `--yes` without `--public` will clap-exit 2,
   breaking the silent no-op contract. Tested by AC-010.

4. **JSDCLOUD-6050 hint goes to stderr, not stdout.** Even with `--output json`, the hint
   is `eprintln!()` to stderr. It must NOT appear in the stdout JSON envelope.

5. **MERGE semantics are documented in CLAUDE.md.** BC-3.5.006 delivery obligation (a) is
   not optional. The CLAUDE.md entry ships in the SAME PR as the implementation. This is a
   correctness-critical gotcha (a body-only PUT PRESERVES visibility — the wrong mental
   model leads to unintentional visibility changes).

6. **E2E test is gated.** The `test_e2e_comment_edit_visibility_merge_semantics` function
   MUST have `#[ignore]` + `if !e2e_enabled() { return; }` early-return per the two-layer
   gate pattern. Clean-skip when `JR_E2E_JSM_PROJECT` is unset.

7. **Cancelled envelope key set is exact.** `{"cancelled":true,"updated":false}` has exactly
   2 keys. `"id"` and `"key"` MUST NOT appear (not confirmed server-side on cancel).
   `"updated"` is boolean `false` (VP-577-029 exact pin — NOT a timestamp or empty string).

8. **WARNING: Do NOT call `is_terminal()` inside the handler for the `--public` gate.**
   The `--public` confirmation gate MUST test `no_input` ALONE — NOT re-derive
   `is_terminal(&stdin())` or call `refuse_noninteractive(no_input, is_terminal(&stdin()))`.
   Re-deriving TTY state at handler call time defeats the `JR_STDIN_IS_TTY` seam (which
   only gates the `src/main.rs` auto-`--no-input` flip). See `workflow.rs:207-209` for the
   pattern to AVOID: that function re-checks TTY state and breaks AC-008 and AC-012 when
   `JR_STDIN_IS_TTY=1` is set with piped stdin. Use `if no_input && !yes { … }` directly.
   The `--stdin` implies `no_input=true` mutation (EC-3.5.008-3) also happens at handler-start
   by checking the `stdin` flag directly, not by re-reading the terminal fd.

---

## Library & Framework Requirements

| Library | Version | Constraint |
|---------|---------|------------|
| dialoguer | 0.12 | `dialoguer::Confirm::new().interact_on(&Term::stderr())`; `Err` → `JrError::Interrupted` |
| serde_json | current | `Value`, `BTreeMap` for response shape |
| wiremock | 0.6 | `received_requests()` for wire-level PUT body inspection |
| tokio | current | `#[tokio::test]` |

---

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `src/cli/issue/interactions.rs` | Modify — extend handle_comment_edit with visibility paths | Core handler |
| `CLAUDE.md` | Modify — add MERGE gotcha entry | BC-3.5.006 delivery obligation (a) |
| `tests/comment_edit.rs` | Modify — add 12 new tests | Extends S-577-4's file |
| `tests/e2e_live.rs` | Modify — add gated 3-scenario e2e test | BC-3.5.006 delivery obligation (b) |
| `tests/e2e_cli_surface_guard.rs` | Modify if new surface entries needed | Surface guard for new e2e invocations |

**Files that MUST NOT change in this story:**
- `src/api/jira/issues.rs` — update_comment already handles visibility flags via parameters
- `src/cli/mod.rs` — CommentSubcommand fully defined in S-577-1
- `src/main.rs` — JR_STDIN_IS_TTY seam added in S-577-3
- Any `.factory/specs/prd/` BC file
