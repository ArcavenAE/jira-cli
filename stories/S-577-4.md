---
document_type: story
level: ops
story_id: "S-577-4"
epic_id: "none"
title: "comment edit core — body sources + body-only PUT"
wave: feature-followup
status: draft
intent: feature
feature_type: backend-cli
mode: feature
scope: standard
severity: HIGH
trivial_scope: false
points: 3
priority: P0
tdd_mode: strict
producer: story-writer
timestamp: "2026-07-11T00:00:00"
phase: 3
inputs:
  - ".factory/specs/prd/bc-3-issue-write.md"
  - ".factory/phase-f1-delta-analysis/delta-analysis-577-comment-crud.md"
  - ".factory/phase-f2-spec-evolution/f2-convergence-record-577.md"
input-hash: "7131b40"
traces_to: "src/cli/issue/interactions.rs::handle_comment_edit"
cycle: SOH-COMMENT-CRUD-1
estimated_effort: medium
estimated_days: 1.5
target_module: src/cli/issue/interactions.rs
subsystems: ["issue-comment", "cli-dispatch"]
depends_on: [S-577-1, S-577-2, S-577-3]
blocks: [S-577-5]
behavioral_contracts:
  [BC-3.5.005, BC-3.5.009]
verification_properties:
  [VP-577-001, VP-577-011, VP-577-012, VP-577-022, VP-577-023, VP-577-024, VP-577-026]
holdout_anchors: [H-NEW-COMMENT-002]
nfr_anchors: []
adr_refs: []
sd_refs: []
parent_phase: F2-approved-standard-feature
spec_source: ".factory/specs/prd/bc-3-issue-write.md"
implementation_strategy: tdd
module_criticality: HIGH
acceptance_criteria_count: 10
assumption_validations: []
risk_mitigations: []
created: "2026-07-11"
version: "1.0"
last_updated: "2026-07-11"
breaking_change: false
retroactive: false
origin: >
  Issue #577 SOH-COMMENT-CRUD-1: handle_comment_edit body-only PUT path.
  BC-3.5.005: PUT with only "body" key in request — no "properties" key.
  BC-3.5.009: four body sources (--file/--stdin/positional/--markdown) with
  mutual exclusion via clap conflicts_with. EC-3.5.009-1: file-not-found →
  exit 64 (explicit open, NOT bare ? propagation). EC-3.5.009-5: empty/whitespace
  body → exit 64. Response JSON: {changed_fields: {body: <raw-pre-trim>}, id, key,
  updated}. VP-577-001 exact key-set assertion. VP-577-023 raw-body echo.
  VP-577-024 empty-body guard. VP-577-026 variant 3 (changed_fields.jsm_internal absent).
changelog:
  - "1.0 (2026-07-11): Initial story authored; F2 gate convergence at spec v1.3.40; bundle SOH-COMMENT-CRUD-1."
---

> **tdd_mode:** strict — Red Gate required. Write all tests in `tests/comment_edit.rs`
> first (they MUST fail because the edit stub is `todo!()`); then replace the stub with
> the body-only PUT implementation. Red Gate: all VP-577-011/012/022(b)/023/024 tests
> FAIL → all PASS after implementing the default (no-visibility-flag) edit path.
> VP-577-026 variant 3 (jsm_internal absent) is also validated here.

> **Execute:** `/vsdd-factory:deliver-story S-577-4`

# S-577-4: `comment edit` core — body sources + body-only PUT

**Bundle**: SOH-COMMENT-CRUD-1  
**GitHub issue**: #577  
**BC anchors**: BC-3.5.005 (body-only PUT invariant + output shapes),
BC-3.5.009 (body sources + guards)  
**VPs**: VP-577-011, VP-577-012, VP-577-022(b), VP-577-023, VP-577-024, VP-577-026(variant 3)  
**Routing**: standard feature  
**Sequencing**: depends on S-577-1 (interactions.rs stub) + S-577-2 (update_comment API method);
S-577-5 (visibility flags) depends on this story

---

## Narrative

- **As a** developer managing Jira comments from the CLI
- **I want** `jr issue comment edit KEY --id ID [BODY | --file F | --stdin | --markdown]` to
  update a comment's body using a simple PUT call
- **So that** I can correct or update comment content precisely, knowing the API call only
  modifies the body text and never touches visibility properties

---

## Behavioral Contracts

| BC | Summary | Clauses Covered |
|----|---------|----------------|
| BC-3.5.005 | PUT with only "body" key; response JSON `{changed_fields:{body:"<raw>"},id,key,updated}`; changed_fields.body is raw pre-trim input | VP-577-001 exact key-set; EC-3.5.005-1 (no "properties" key unless --internal/--public) |
| BC-3.5.009 | Four body sources; mutual exclusion via clap conflicts_with; empty-body guard | EC-3.5.009-1 (file not found exit 64); EC-3.5.009-2/3/4 (conflicts_with); EC-3.5.009-5 (empty body exit 64) |

---

## Acceptance Criteria

### AC-001: body-only PUT — exact response key-set (VP-577-023)
(traces to BC-3.5.005 postcondition — response JSON exact key-set assertion)

`jr issue comment edit FOO-1 --id 10001 "new body" --output json` against wiremock
returning 200 with a minimal comment JSON → exit 0; stdout parses as JSON; parsed
top-level key set == `BTreeSet::from(["changed_fields", "id", "key", "updated"])` (exact);
`changed_fields` has exactly 1 sub-key: `"body"` (no `"jsm_internal"` key present).

**Test**: `test_bc_3_5_005_edit_response_exact_key_set` in `tests/comment_edit.rs`.

---

### AC-002: body-only PUT — raw pre-trim body echo (VP-577-023)
(traces to BC-3.5.005 postcondition — changed_fields.body is raw input, not trimmed)

`jr issue comment edit FOO-1 --id 10001 "  hello world  " --output json` → exit 0;
parsed `changed_fields.body` == `"  hello world  "` (leading/trailing spaces PRESERVED in
JSON echo). The PUT request body sent to Jira may be trimmed (safe for ADF conversion),
but `changed_fields.body` in the JSON response MUST be the raw pre-trim input.

**Test**: `test_bc_3_5_005_edit_changed_fields_body_is_raw_pre_trim` in `tests/comment_edit.rs`.

---

### AC-003: PUT request body contains only "body" key (VP-577-001)
(traces to BC-3.5.005 precondition EC-3.5.005-1 — no "properties" key in request)

`jr issue comment edit FOO-1 --id 10001 "body text" --output json` against a wiremock
with a body matcher — inspect `received_requests()[0].body`, parse as JSON, assert the
parsed PUT request object has exactly key `"body"` and does NOT contain `"properties"`.

**Test**: `test_bc_3_5_005_ec1_put_request_has_only_body_key` in `tests/comment_edit.rs`.

---

### AC-004: --file body source (EC-3.5.009-1)
(traces to BC-3.5.009 postcondition — --file body source happy path)

Create a temp file containing `"body from file\n"`. `jr issue comment edit FOO-1 --id 10001 --file <path> --output json`
→ exit 0; `changed_fields.body` in response is `"body from file\n"` (raw file content).

**Test**: `test_bc_3_5_009_edit_file_body_source` in `tests/comment_edit.rs`.

---

### AC-005: --stdin body source (EC-3.5.009-2)
(traces to BC-3.5.009 postcondition — --stdin body source happy path)

`jr issue comment edit FOO-1 --id 10001 --stdin --no-input --output json` with stdin
fed `"body from stdin"` → exit 0; `changed_fields.body` contains `"body from stdin"`.

**Test**: `test_bc_3_5_009_edit_stdin_body_source` in `tests/comment_edit.rs`.

---

### AC-006: file not found → exit 64 (not panic) (VP-577-011)
(traces to BC-3.5.009 EC-3.5.009-1 — explicit open, NOT bare ? propagation)

`jr issue comment edit FOO-1 --id 10001 --file /no/such/file.md` → exit 64; stderr
contains `"file not found"` or path mention; MUST NOT exit 1 (default ApiError path).
No HTTP call should be made (wiremock expects 0 PUT calls).

**Test**: `test_bc_3_5_009_ec1_file_not_found_exits_64` in `tests/comment_edit.rs`.

---

### AC-007: empty/whitespace body → exit 64 (VP-577-012)
(traces to BC-3.5.009 EC-3.5.009-5 — empty body guard)

`jr issue comment edit FOO-1 --id 10001 "   "` (all-whitespace positional arg) → exit 64;
stderr contains `"comment body cannot be empty"` (load-bearing substring); no HTTP PUT.

**Test**: `test_bc_3_5_009_ec5_empty_whitespace_body_exits_64` in `tests/comment_edit.rs`.

---

### AC-008: invalid --id regex → exit 64 (VP-577-022 variant b)
(traces to BC-3.5.002 EC-3.5.002-1 — shared validate_comment_id guard, edit path)

`jr issue comment edit FOO-1 --id "not valid!" "body"` → exit 64; stderr contains
`"invalid comment id"`; wiremock PUT has `.expect(0)`.

**Test**: `test_bc_3_5_002_ec1_edit_invalid_id_regex_exits_64` in `tests/comment_edit.rs`.

---

### AC-009: jsm_internal absent in changed_fields (default path) (VP-577-026 variant 3)
(traces to BC-3.5.005 postcondition — no jsm_internal in default path response)

`jr issue comment edit FOO-1 --id 10001 "body" --output json` → exit 0; parsed
`changed_fields` does NOT contain key `"jsm_internal"`. (The `jsm_internal` key is
only present when `--internal` or `--public` flags are used — that is S-577-5's scope.)

**Test**: `test_bc_3_5_005_jsm_internal_absent_in_default_path` in `tests/comment_edit.rs`.

---

### AC-010: PUT 404 → exit 64 with dual-line stderr (VP-577-024)
(traces to BC-3.5.005 postcondition — PUT returns 404 → exit 64; stderr contains BOTH preamble AND Jira body text on separate lines)

`jr issue comment edit FOO-1 --id 10001 "body"` against a wiremock returning
`404 {"errorMessages":["comment not found"]}` → exit 64; stderr contains BOTH:
(a) the preamble `"comment not found or permission denied"` AND
(b) the Jira error text `"comment not found"` on a SEPARATE line.
Mirrors VP-577-004 (delete 404 handling) applied to the PUT route.
No stdout output.

**Test**: `test_bc_3_5_005_put_404_exits_64_with_dual_stderr` in `tests/comment_edit.rs`.

---

## Architecture Mapping

| Component | File | Pure/Effectful | Change |
|-----------|------|---------------|--------|
| handle_comment_edit (default path) | `src/cli/issue/interactions.rs` | Effectful (HTTP) | Replace todo!() stub with body-sources + body-only PUT |
| update_comment | `src/api/jira/issues.rs` | Effectful (HTTP) | Already added in S-577-2 |
| comment_edit tests | `tests/comment_edit.rs` | Effectful (wiremock) | NEW test file (shared with S-577-5 additions) |

### Edit Pipeline Order (BC-3.5.009 edit-pipeline ordering pin)

```
(1) --id regex validation (EC-3.5.002-1): validate_comment_id(id) → exit 64 on mismatch
(2) Body source resolution (BC-3.5.009):
      --file  → fs::read_to_string(path) — explicit error: file not found → exit 64 (EC-3.5.009-1)
      --stdin → std::io::Read::read_to_string from stdin
      positional BODY → use as-is
      (--markdown is a modifier: applies markdown_to_adf conversion to selected body source)
(3) Empty/whitespace guard (EC-3.5.009-5): body.trim().is_empty() → exit 64
(4) ADF conversion: text_to_adf(body) or markdown_to_adf(body) depending on --markdown
(5) HTTP PUT: client.update_comment(key, id, adf_body, None).await
    (None = body-only; this story implements the default no-visibility-flag path)
    200 → success output
    404/403 → exit 64 + body surface
    other 4xx/5xx (except 401) → exit 1
    401 → exit 2
(6) Output: --output json → stdout JSON; human → stderr success message
```

### Body-only PUT wire format (BC-3.5.005 body invariant)

The `update_comment` API method (from S-577-2) accepts a single `visibility_flag: Option<bool>`
parameter. When `None` (this story's scope), the implementation MUST produce a PUT request
body of:

```json
{"body": <adf_value>}
```

The `"properties"` key MUST NOT be present in the request when neither `--internal` nor
`--public` is passed. This is the body-only invariant (EC-3.5.005-1, DEC-168 ruling 1).

Validated by AC-003 (wire-level inspection of PUT request body).

### Response JSON format (BC-3.5.005 output shape)

On 200 success with `--output json`:

```json
{
  "changed_fields": {
    "body": "<raw pre-trim user input>"
  },
  "id": "<comment id>",
  "key": "<issue key>",
  "updated": true
}
```

`changed_fields.body` is the raw user-supplied input string (pre-trim), NOT the
ADF-converted value and NOT `"(updated)"`. This mirrors the `issue edit --description`
asymmetry documented in CLAUDE.md §"issue edit description echo asymmetry (issue #398)".

### Forbidden Dependencies

`interactions.rs` must NOT import `serde_json::to_string_pretty` directly. All JSON
output MUST route through `output::render_json` or `output::print_output`. No compact
JSON printing allowed (JSON render invariant #526 in CLAUDE.md).

---

## Edge Cases

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | `--id "bad id"` (fails regex) | exit 64, `"invalid comment id"` (VP-577-022b) |
| EC-002 | `--file /nonexistent/path.md` | exit 64, explicit `JrError::UserError` (NOT bare ?) |
| EC-003 | Positional body is empty string `""` | exit 64, `"comment body cannot be empty"` (EC-3.5.009-5) |
| EC-004 | Positional body is whitespace-only `"   "` | exit 64, same as EC-003 |
| EC-005 | `--file /dev/null` (empty file) | exit 64 (file exists but body empty after read) |
| EC-006 | `--file X --stdin` (mutual exclusion) | clap exits 2 (conflicts_with) (EC-3.5.009-2) |
| EC-007 | `--stdin X positional` (mutual exclusion) | clap exits 2 (EC-3.5.009-4) |
| EC-008 | `--file X positional` (mutual exclusion) | clap exits 2 (EC-3.5.009-3) |
| EC-009 | PUT returns 404 | exit 64 + body surface (BC-3.5.004 applies) |
| EC-010 | changed_fields.jsm_internal absent | confirmed by VP-577-026 variant 3 (AC-009) |

---

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `src/cli/issue/interactions.rs::handle_comment_edit` (default path) | effectful-shell | HTTP PUT + body-source file I/O; no pure-core logic beyond empty guard |
| `src/api/jira/issues.rs::update_comment` | effectful-shell | HTTP PUT; imported from S-577-2 |
| `tests/comment_edit.rs` | effectful-shell (test boundary) | Subprocess via jr_cmd_with_xdg; wiremock for HTTP; wire-level inspection |

---

## Token Budget Estimate

| Item | Est. Tokens |
|------|------------|
| Story spec (this file) | ~6 k |
| `src/cli/issue/interactions.rs` (stub from S-577-1, ~50 LOC) | ~1 k |
| `src/api/jira/issues.rs` (update_comment method from S-577-2) | ~1 k |
| `src/adf.rs` (text_to_adf + markdown_to_adf signatures, ~30 LOC) | ~1 k |
| `src/error.rs` (JrError::UserError) | ~0.5 k |
| BC-3.5.005/009 relevant sections | ~6 k |
| `tests/comment_edit.rs` (9 tests) | ~5 k |
| Output module pattern reference | ~0.5 k |
| **Total** | **~21 k** |

Well under 20% of 200 k context window.

---

## Tasks

**Red Gate protocol**: Write all 9 AC tests first; they MUST fail on `todo!()` stub. Then
implement handle_comment_edit (body-only path). Then all tests pass.

### Task 0 — Read source context

Read:
- `src/cli/issue/interactions.rs` (from S-577-1 — stub implementations, including
  validate_comment_id if S-577-3 has already implemented it)
- `src/api/jira/issues.rs` — `update_comment` signature (from S-577-2)
- `src/adf.rs` — `text_to_adf` and `markdown_to_adf` function signatures
- `src/output.rs` — `render_json` / `print_output` signatures
- `src/error.rs` — `JrError::UserError` variant

### Task 1 — Create/extend tests/comment_edit.rs (Red Gate tests)

Write all 9 ACs as tests. Use `jr_cmd_with_xdg` subprocess pattern.
For AC-004 (--file), create a `tempfile::NamedTempFile` with content and pass path.
For AC-005 (--stdin), pipe stdin bytes via `std::process::Stdio::piped()`.

Confirm all tests fail with panics on `todo!()` stub.

### Task 2 — Implement handle_comment_edit (body-only default path)

Replace the `todo!()` stub in `interactions.rs` with the full implementation:

1. `validate_comment_id(id)?` — first (from S-577-3, re-use the shared helper)
2. Body source resolution:
   - `--file path` → `std::fs::read_to_string(path).map_err(|_| JrError::UserError(format!("file not found: {}", path)))?`
   - `--stdin` → read from stdin
   - positional `body` → use as-is
3. Empty guard: `if body.trim().is_empty() { return Err(JrError::UserError("comment body cannot be empty.".into()).into()); }`
4. ADF: if `--markdown` flag set, `markdown_to_adf(&body)?` else `text_to_adf(&body)`
5. Call `client.update_comment(key, id, adf_body, None).await?` (None = body-only, no visibility flag)
6. Build response JSON from local state (NOT from Jira PUT response body — `update_comment` returns `Result<()>`):
   - `changed_fields.body` = raw pre-trim input string (stash BEFORE any `.trim()` call)
   - `id` = the `--id` argument
   - `key` = the issue KEY argument
   - `updated` = literal `true` (boolean; spec-pinned per BC-3.5.005; NOT a timestamp from API)
7. Route through `output::render_json` for JSON output

**Note on raw pre-trim body echo**: stash the raw body string BEFORE any `.trim()` call.
The JSON response echoes the raw input; the ADF conversion may use `.trim()` internally —
that's fine — but the stored echo must be the original string.

**Note on `updated: true`**: `update_comment` returns `Result<()>` — the Jira PUT response
body is discarded. The `"updated": true` in the response JSON is a LITERAL boolean constant,
built from local state. Do NOT attempt to parse it from the Jira API response.

### Task 3 — Verify wire-level PUT body (AC-003)

This requires inspecting wiremock's received requests. Pattern:
```rust
let reqs = mock_server.received_requests().await.unwrap();
let body_json: serde_json::Value = serde_json::from_slice(&reqs[0].body).unwrap();
assert!(body_json.get("properties").is_none(), "properties must not be present");
assert!(body_json.get("body").is_some(), "body must be present");
let keys: Vec<&str> = body_json.as_object().unwrap().keys().map(|k| k.as_str()).collect();
assert_eq!(keys.len(), 1);
```

### Task 4 — Run tests

```bash
cargo test --test comment_edit -- --nocapture
```

All 9 ACs pass.

### Task 5 — Full suite green + lint

```bash
cargo test
cargo clippy -- -D warnings
```

### Task 6 — PR creation

Create PR to `develop`:
- Title: `feat(comment): comment edit core — body sources + body-only PUT (#577 part 4)`
- Reference #577; include Red Gate evidence; note that S-577-5 (visibility flags) extends
  this story's test file (`tests/comment_edit.rs`)

---

## Previous Story Intelligence

**S-577-1 (CLI refactor):** interactions.rs stub created here. This story fills in the
edit stub. Follow the exact function signature that S-577-1 placed as `todo!()`.

**S-577-2 (API layer):** `update_comment(key, id, body, visibility_flag: Option<bool>)` method
implemented there. This story calls it with `None` (body-only path). S-577-5 will call it with
`Some(true)` (--internal) or `Some(false)` (--public confirmed).

**S-577-3 (delete handler):** Implements `validate_comment_id` shared helper and the
`JR_STDIN_IS_TTY` seam. This story calls `validate_comment_id` from S-577-3's implementation.
Ensure S-577-3 is merged first OR that validate_comment_id is defined early in interactions.rs
so this story can call it.

**CLAUDE.md §issue edit description echo asymmetry**: The `changed_fields.body` raw echo
pattern is the SAME pattern as `changed_fields.description` on `issue edit` (BC-3.4.012/013).
The test names follow the same VP-NNN-0NN convention.

---

## Architecture Compliance Rules

(Extracted from CLAUDE.md, BC-3.5.005, BC-3.5.009)

1. **Body-only PUT invariant (EC-3.5.005-1):** When neither `--internal` nor `--public`
   is passed, the PUT request body MUST contain only the `"body"` key. No `"properties"`
   key. This is load-bearing — a MERGE semantics footgun (see S-577-5 for the CLAUDE.md
   gotcha note). Validated by wire-level inspection (AC-003).

2. **`changed_fields.body` is raw pre-trim input.** Store the raw input string BEFORE
   any trim or ADF conversion. Do NOT echo the ADF value or `"(updated)"` in the JSON
   channel. The human channel may say `"(updated)"` — the JSON channel MUST be lossless.

3. **EC-3.5.009-1: file not found is exit 64, NOT exit 1.** Use explicit error wrapping:
   `map_err(|e| JrError::UserError(format!("…")))`. Bare `?` propagation would produce
   exit 1 (ApiError-like). Deliberate deviation from add_comment file handling.

4. **JSON output invariant (#526):** ALL JSON must route through `output::render_json`.
   No `serde_json::to_string_pretty` calls, no compact prints.

5. **Pipeline ORDER is non-negotiable.** Steps (1) id validation → (2) body source →
   (3) empty guard → (4) ADF → (5) HTTP PUT → (6) output. No reordering.

6. **`--markdown` is a modifier, not a source.** It applies to whichever body source is
   selected. `--markdown` + `--file` is allowed (converts file content via markdown_to_adf).
   `--markdown` + `--stdin` is allowed. `--markdown` + positional is allowed.

---

## Library & Framework Requirements

| Library | Version | Constraint |
|---------|---------|------------|
| serde_json | current | `Value` type for ADF body; `BTreeMap` for output shape |
| wiremock | 0.6 | `Mock::given(method("PUT"))`, `received_requests()` for wire inspection |
| tempfile | current in dev-deps | `NamedTempFile` for AC-004 test |
| tokio | current | `#[tokio::test]` on async tests |

---

## File Structure Requirements

| File | Action | Notes |
|------|--------|-------|
| `src/cli/issue/interactions.rs` | Modify — replace edit stub | Core handler (body-only default path) |
| `tests/comment_edit.rs` | CREATE — 9 tests | New integration test file (S-577-5 will extend it) |

**Files that MUST NOT change in this story:**
- `src/api/jira/issues.rs` — update_comment already added in S-577-2
- `src/cli/mod.rs` — CommentSubcommand already defined in S-577-1
- `src/main.rs` — JR_STDIN_IS_TTY already added in S-577-3
- Any `.factory/specs/prd/` BC file

**Note on test file sharing:** S-577-5 will ADD tests to `tests/comment_edit.rs` (the
`--internal`/`--public` test cases). This story CREATES the file; S-577-5 EXTENDS it.
The file path is `tests/comment_edit.rs` in both stories.
