---
document_type: story
level: ops
story_id: "S-576-1"
epic_id: "SOH-ATTACHMENTS-1"
title: "jr issue attachment list — table + JSON output + client-side filters (issues #576 #585)"
wave: feature-followup
status: ready
intent: attachment-read
feature_type: feature
mode: feature
scope: standard
severity: HIGH
trivial_scope: false
points: 5
priority: P1
tdd_mode: strict
producer: story-writer
timestamp: "2026-07-17T00:00:00"
phase: 3
inputs:
  - ".factory/phase-f2-spec-evolution/prd-delta-576.md"
  - ".factory/specs/prd/bc-2-issue-read.md"
  - ".factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md"
  - ".factory/phase-f1-delta-analysis/impact-boundary-576.md"
input-hash: "3994226"
traces_to: ".factory/specs/prd/bc-2-issue-read.md"
cycle: cycle-attachment-576
estimated_effort: medium
estimated_days: 2
target_module: src/cli/issue/attachments.rs
subsystems: ["SS-03", "SS-09"]
depends_on: []
blocks: ["S-576-2","S-576-3","S-576-4"]
behavioral_contracts:
  - "BC-2.7.001"
  - "BC-2.7.002"
  - "BC-2.7.003"
  - "BC-2.7.004"
  - "BC-2.7.005"
  - "BC-2.7.006"
bcs:
  - "BC-2.7.001"
  - "BC-2.7.002"
  - "BC-2.7.003"
  - "BC-2.7.004"
  - "BC-2.7.005"
  - "BC-2.7.006"
verification_properties:
  - "VP-576-004"
holdout_anchors: ["H-NEW-ATTACHMENT-001"]
nfr_anchors: []
adr_refs: []
sd_refs: []
parent_phase: F3-incremental-stories
spec_source: ".factory/phase-f2-spec-evolution/prd-delta-576.md"
implementation_strategy: tdd
module_criticality: HIGH
acceptance_criteria_count: 11
assumption_validations: ["BC-2.7.001:fields-attachment-completeness-probe"]
risk_mitigations: []
created: "2026-07-17"
version: "1.23"
last_updated: "2026-07-28"
breaking_change: false
retroactive: false
origin: >
  Issue #576 (attachment read surface). Issue #585 (contentUrl field) is absorbed
  into BC-2.7.002. S1 is the first-landing story of bundle SOH-ATTACHMENTS-1:
  it creates src/cli/issue/attachments.rs and src/api/jira/attachments.rs, introduces
  the display_sanitize_filename helper (earliest consumer per SEC-576-011/DEC-184 R3.13),
  and establishes the curated attachment-object JSON serialization shape (BC-2.7.002)
  that S3 depends on for VP-576-004 cross-path verification. F2 spec converged at
  v1.3.81 (DEC-184 authorized story decomposition).
changelog:
  - "1.23 (2026-07-28): AX-004 fix — BC table titles synced to BC-INDEX v6.74: all 6 BCs (BC-2.7.001..006) updated. BC-2.7.001 adds 'renders table' framing, 'size (human-readable)', and output channel profile 2 note. BC-2.7.002 syncs to full index title including BTreeMap key-order note (P19-001). BC-2.7.003 adds glob case-insensitive/AND-combined/EC-2.7.003-2 pre-flight note. BC-2.7.004 adds JRACLOUD-96384 match-by-id note. BC-2.7.005 adds no-hard-coded-cap and non-numeric exit 64. BC-2.7.006 adds full error taxonomy framing and P18-002 403 note. input-hash updated."
  - "1.22 (2026-07-19): Input-hash refresh @ v1.3.96 — EC-2.7.001-3 empty-string ratification (P2-001); no body citation sites in S1. input-hash updated 764fea5→d6e6eb3."
  - "1.21 (2026-07-19): Step 4.5 fix round 1 propagation @ v1.3.95 (P1-002 author-curated-form ruling + adversary P1-001/P1-005 glob semantics). 5 sites changed: (1) AC-003 author bullet extended — present author is ALWAYS curated {accountId, displayName} only; no self/avatarUrls/accountType/timeZone/emailAddress/active; partial-author (present, both subfields null/absent) → {\"accountId\": null, \"displayName\": null}; BTreeMap ordering accountId < displayName within author; test_bc_2_7_002_json_shape_curated_form gains sub-assertions: full-author fixture (Jira response has self/avatarUrls/accountType) → emitted author has exactly {accountId, displayName}; partial-author fixture → {\"accountId\": null, \"displayName\": null} not null. (2) EC-6 row: 'JSON emits author object as received' → 'JSON emits curated {accountId, displayName} (v1.3.95)'. (3) AC-005: '? matches any single character' added to glob semantics; test note extended with star-crosses-slash assertion (glob 'image*' matches 'image/png') and ?-glob case; no new test functions. (4) AC-006: '? matches any single character' added to glob semantics; test note extended with ?-glob sub-assertion (report-?.pdf matches report-1.pdf not report-10.pdf); no new test function. acceptance_criteria_count stays 11; test count stays 14. input-hash updated 71f97e7→764fea5 (v1.3.95)."
  - "1.20 (2026-07-19): Status draft→ready per DEC-185 F3 gate approval (mechanical flip; no content change)."
  - "1.19 (2026-07-18): PRE-F4-UNICODE propagation @ v1.3.94 — BC-2.7.011 extended display-sanitization scope propagated at 5 sites in S1 (primary story): (1) PRE-F4-UNICODE banner ~102 replaced with DISCHARGED scope notice (extended set: ASCII 0x00–0x1F/0x7F + bidi U+202A..U+202E/U+2066..U+2069 + U+2028/U+2029 + NEL U+0085; char-based; each → ?); (2) AC-001 algorithm description ~140 updated to extended set + pointer to BC-2.7.011 primary clause; (3) Task 0 spec-read note ~300 updated to extended set pointer; (4) Task 3 ~325: algorithm bullet-list updated to extended set + 3 mandated unit-test cases (U+202E→?, U+2028→?, U+0085→?) added to existing #[cfg(test)] suite (no new integration tests; no 4-surface count change); (5) Out-of-Scope ~442: entry changed from FROZEN residual to IN-SCOPE discharged note. input-hash updated 5c14ae6→71f97e7 (v1.3.94: bc-2, bc-3, BC-INDEX, prd-delta changed)."
  - "1.18 (2026-07-18): Pre-gate hash refresh @ v1.3.93 — no content change; input-hash updated 7e3a22e → 5c14ae6 (v1.3.93 cosmetics: error-taxonomy 401 row, prd-delta enumeration lines 84/85, ADR-0017 command-name + ID-type)."
  - "1.17 (2026-07-18): Fix round 74, adversary pass 74 — P74-001 (MEDIUM): compile trap: S3 integration test calls serialize_attachment_curated directly (VP-576-004 cross-path) but S1 mandated private `mod attachments;`. Fixed at 4 sites: (1) Architecture Mapping ~243: `serialize_attachment_curated` → `pub fn serialize_attachment_curated`; (2) Purity Classification ~271: same rename + pub note; (3) Task 5 ~346: Visibility obligation paragraph added naming 8 `pub` AttachmentObject fields (self_url, id, filename, author, created, size, mime_type, content) + pub fn + pub mod requirement; (4) File Structure ~417: `mod attachments;` → `pub mod attachments;` with mirror-precedent note. No AC or test count change; input-hash unchanged (spec v1.3.92, 7e3a22e)."
  - "1.16 (2026-07-18): Fix round 62, adversary pass 62 — P62-001 (LOW): AC-004 test note ~169 extended with explicit EC-2.7.001-2 sub-assertions for the DELIBERATE ASYMMETRY (hint fires in BOTH modes): (a) --output json + reducing filter → filtered JSON stdout AND stderr 'Showing N of M'; (b) N==M → NO hint in either mode; (c) human N<M → hint (existing coverage). Extended existing test fn, no new fn; no test count change; acceptance_criteria_count unchanged at 11; input-hash unchanged (spec v1.3.92, 7e3a22e)."
  - "1.15 (2026-07-18): Fix round 55, adversary pass 55 — P55-001 (MEDIUM): BC-2.7.001's ASSUMPTION clause (~554) assigned a MUST delivery obligation to the S1 implementer that was unrecorded in the story. Fixed at 3 sites: (1) assumption_validations frontmatter [] → ['BC-2.7.001:fields-attachment-completeness-probe']; (2) AC-011 gains item 5: PRE-DELIVERY GATE with verbatim-derived obligation (live-verify >100 attachments OR document Atlassian bound; PR merge gate; downstream-dependency note S-576-2 --all/--newest + S-576-4 BC-3.9.019); (3) Task 0 gains blockquote PRE-DELIVERY GATE note. No new AC; acceptance_criteria_count unchanged at 11; test count unchanged at 14; input-hash unchanged (spec v1.3.92, 7e3a22e)."
  - "1.14 (2026-07-18): Fix round 53 — prd-delta v1.3.92 reconciliation; input-hash refresh 3134afd→7e3a22e (prd-delta v1.3.91→v1.3.92; no body citation sites in S1). No AC or test count change."
  - "1.13 (2026-07-18): Fix round 30, adversary pass 30 — P30-001 (MEDIUM): File Structure src/cli/issue/attachments.rs Purpose cell had 'AttachmentSubcommand enum' — struck; enum's sole definition site is src/cli/mod.rs (line 412); attachments.rs holds handlers + helpers only; Purpose cell reworded to 'handlers + helpers only — AttachmentSubcommand enum is defined in src/cli/mod.rs, NOT here'; P24-001 residue. No AC or test count change; input-hash unchanged (spec v1.3.89, 118ba52)."
  - "1.12 (2026-07-18): Fix round 25, adversary pass 25 — P25-001 (MEDIUM): Architecture Mapping + Purity Classification rows renamed serialize_attachment_object → serialize_attachment_curated (canonical name used by Task 5 and all 11 downstream references; S3 AC-015 VP-576-004 test calls serialize_attachment_curated directly — stale name was a compile trap). No test-count or AC changes; input-hash unchanged (spec v1.3.89, edea7b8)."
  - "1.11 (2026-07-18): Fix round 24, adversary pass 24 — P24-001 (MEDIUM): three coupled corrections to module-scaffolding rows. (1) IssueSubcommand does not exist in src/ — the real top-level enum is IssueCommand (src/cli/mod.rs:295); replaced at all three sites. (2) src/cli/mod.rs File Structure row rewritten to make nested structure explicit: 'Add Attachment { #[command(subcommand)] command: AttachmentSubcommand } variant to the IssueCommand enum (src/cli/mod.rs:295) AND define the AttachmentSubcommand enum with the List { key, filter, output, ... } variant — <KEY>/--filter args live on List, NOT on Attachment'; S-576-2/3/4/5 all depend on S1 creating AttachmentSubcommand. (3) Task 2 dispatch wording rewritten to mirror comment-CRUD pattern: IssueCommand::Attachment { command: sub } => match sub { AttachmentSubcommand::List { .. } => handle_attachment_list(..) } (src/cli/issue/mod.rs:75 pattern). S-576-2/3/4 mod.rs rows verified coherent (they were correct this pass — not changed). No count changes; input-hash unchanged (spec v1.3.89, edea7b8)."
  - "1.10 (2026-07-18): Fix round 23, adversary pass 23 — P23-002 (LOW): S-576-1 AC-009 same-class gap: BC-2.7.006 defines 5xx and Network rows (bc-2 ~714-715) but AC-009 covered only 404/401/403. Extended AC-009 body with 'When 5xx → exit 1, stderr contains API error (<N>)' and 'When network error → exit 1, stderr contains Could not reach'. Added 2 named tests: test_bc_2_7_006_key_5xx_exit_1 (loose-substring API error (); exit 1) and test_bc_2_7_006_key_network_exit_1 (loose-substring Could not reach; exit 1). Test count 12→14 (acceptance_criteria_count stays 11; AC-009 now has 5 test functions); 4-surface sweep: Task 1 '12 tests; AC-009 has 3'→'14 tests; AC-009 has 5', Task 8 '12'→'14', Token Budget ~4,400→~4,800/Total ~34,200→~34,600, File Structure '12 wiremock; AC-009 has 3'→'14 wiremock; AC-009 has 5'; input-hash unchanged (spec v1.3.89, edea7b8)."
  - "1.9 (2026-07-18): Fix round 22, adversary pass 22 — P22-001 (LOW): spec moved v1.3.88→v1.3.89; input-hash refreshed (fb4b623→edea7b8). Task 2 (~308) mutants.toml note flipped from 'DEFERRED to S-576-2 / Do NOT add' to new add-at-creation obligation (prd-delta-576.md S1 obligation (f)): ADD examine_globs entries for attachments.rs files IN THIS STORY; CWE-116 rationale + S-577-1 precedent cited; mutants_glob_existence passes trivially (files exist at S1); S2/S3/S4 confirm-present at their delivery points. File Structure gains .cargo/mutants.toml MODIFY row (ADD examine_globs entries per S1 obligation (f))."
  - "1.8 (2026-07-18): Fix round 21, adversary pass 21 — P21-001 (LOW): AC-009 (~197-201) extended with two named sub-tests to cover 401 and 403 taxonomy rows (bc-2 ~711-713): test_bc_2_7_006_key_401_exit_2 (assert exit 2 + stderr contains 'Not authenticated' AND 'jr auth login' per v1.3.88 loose-substring form) and test_bc_2_7_006_key_403_exit_1 (assert exit 1 + stderr contains 'Permission denied: cannot access issue <KEY>.' canonical string); AC-009 body updated with explicit 401/403 output specs; test count 10→12 (acceptance_criteria_count stays 11; AC-009 now has 3 test functions); 4-surface sweep: Task 1 '10 tests'→'12 tests (AC-009 has 3)', Task 8 '10'→'12', Token Budget ~4,000→~4,400/Total ~33,800→~34,200, File Structure '10 wiremock'→'12 wiremock (AC-009 has 3)'; input-hash refreshed to spec v1.3.88 (fb4b623)."
  - "1.7 (2026-07-18): Fix round 17, adversary pass 17 — bundle-wide test-fn reconciliation sweep (S1 sweep): S1 had 11 ACs but only 10 test functions in attachment_list.rs (AC-003 has 2; AC-010 uses e2e_cli_surface_guard.rs — 0 new; AC-011 uses claude_md_citations.rs — 0 new); corrected Task 1 body '11 AC tests'→'10 tests (11 ACs; AC-003 has 2; AC-010 uses e2e_cli_surface_guard.rs — 0 new; AC-011 uses claude_md_citations.rs — 0 new)', Task 8 header '11 tests GREEN'→'10 tests GREEN', File Structure '11 wiremock integration tests (all ACs)'→'10 wiremock integration tests (11 ACs; AC-003 has 2; AC-010 uses e2e_cli_surface_guard.rs — 0 new; AC-011 uses claude_md_citations.rs — 0 new)'; acceptance_criteria_count stays 11; input-hash unchanged (spec v1.3.87, 8aa6154)."
  - "1.6 (2026-07-18): Fix round 16, adversary pass 16 — P16-003 (LOW): PRE-F4-UNICODE-DISPLAY-SANITIZATION banner (~89) and Out-of-Scope entry (~423) reworded — removed 'before implementation begins/starts' blocking-gate framing; now: 'accepted residual for this bundle per v1.3.81 NEW-576-V3-002; display_sanitize_filename is FROZEN for this bundle; any bidi-control work targets a future spec increment; PRE-F4 obligation exists and is tracked at the pipeline level — NOT an S1 pre-implementation gate'; obligation's existence preserved (human ruling: extend display-sanitization before F4 delivery); only the blocking-gate framing is removed; no AC count change; input-hash unchanged (spec v1.3.87, 8aa6154)."
  - "1.5 (2026-07-17): Fix round 5, adversary pass 5 — P5-008: AC-008 missing-= branch tail corrected: 'Invalid filter ...: expected key=value form.' now appends 'Accepted keys: mime=, name=, size-max=.' (P4-009 only verified the unknown-key branch; missing-= branch was missing the tail per EC-2.7.003-2 full canonical string); P5-009: Task 9 Blocks line corrected from 'S-576-3' to 'S-576-2, S-576-3, S-576-4' (all three downstream stories depend on S1); input-hash refreshed to spec v1.3.83 (285193a)."
  - "1.4 (2026-07-17): Fix round 4, adversary pass 4 — P4-002: AC-011 item 2 CHANGELOG entry corrected from umbrella 'attachment list/download/upload/delete subcommand tree' to S1-scoped 'attachment list subcommand + JSON output + filters' (each story owns its own entry); File Structure CHANGELOG row updated to match; P4-009: AC-008 unknown-key branch 'Accepted keys: mime=, name=, size-max=.' verified present (missing-= branch tail was NOT checked — fixed in 1.5); P4-011: blocks expanded from ['S-576-3'] to ['S-576-2','S-576-3','S-576-4'] (S-576-2 and S-576-4 also depend on S-576-1 per dependency graph)."
  - "1.3 (2026-07-17): Fix round 3, adversary pass 3 — P3-009: Task 2 mutants.toml deferral note corrected from 'S-576-3' to 'S-576-2' (prd-delta-576.md S2 obligation (e): sanitize_attachment_filename ships at S2, making S2 the correct delivery slot); P3-018: Task 7 step 2 CHANGELOG entry scoped to 'attachment list' only (each downstream story appends its own entry); 4-surface sweep: no AC count change."
  - "1.2 (2026-07-17): Fix round 2 P2-016 — Task 0(c) corrected: display_sanitize_filename is CWE-116 display-only (0x00–0x1F and 0x7F → '?'); the 5-step CWE-22 disk-path algorithm with containment check/214-byte cap/step-5.5 belongs to sanitize_attachment_filename (S-576-2), not display_sanitize_filename (S-576-1)."
  - "1.1 (2026-07-17): Fix round 1, adversary pass 1 P1-005/P1-025 — display_sanitize_filename rewritten as CWE-116 ONLY (replace 0x00–0x1F and 0x7F with '?'; no path-separator strip, no 214-byte truncation, no device-name filtering — those belong to sanitize_attachment_filename in S2); mutants.toml deferred-to-S3 note added to Task 2."
  - "1.0 (2026-07-17): Initial F3 story authored per F2 CONVERGED spec v1.3.81."
# F3 gate approved DEC-185 2026-07-19 — status flipped to ready
---

> **tdd_mode:** strict — full TDD Iron Law enforced (todo!() + Red Gate ≥0.5 required before Step 4 dispatch).

> **DISPLAY-SANITIZATION OBLIGATION (SEC-576-011, CWE-116, DEC-184 R3.13):** S1 is the EARLIEST consumer of the `display_sanitize_filename` helper. The helper MUST be introduced in this story (in `src/cli/issue/attachments.rs` or a shared helpers module within that file). Apply it to all Filename cells in the attachment list table (BC-2.7.001). Stories S3 (BC-3.9.017 step-2 prompts) and S4 (BC-3.9.015 delete confirmation) reuse the same helper — do NOT duplicate it.

> **DISPLAY-SANITIZATION SCOPE (BC-2.7.011 v1.3.94 — PRE-F4-UNICODE DISCHARGED):** The PRE-F4-UNICODE obligation is discharged @ v1.3.94. `display_sanitize_filename` covers the full extended character set: ASCII 0x00–0x1F/0x7F + Unicode bidi controls (U+202A..U+202E, U+2066..U+2069) + line separators (U+2028/U+2029) + NEL (U+0085); char-based matching, each character → one `?`. This extended set IS the frozen spec for this bundle — implement it from the start in S1.

# S-576-1: `jr issue attachment list` — table + JSON output + client-side filters

**Bundle**: SOH-ATTACHMENTS-1
**GitHub issues**: #576, #585
**BC anchors**: BC-2.7.001 (table), BC-2.7.002 (JSON + contentUrl + closes #585), BC-2.7.003 (mime filter), BC-2.7.004 (name filter), BC-2.7.005 (size-max filter), BC-2.7.006 (error taxonomy)
**VPs**: VP-576-004 (list half — BC-2.7.002 authority; upload half + full cross-path test lands at S3)

---

## Narrative

- **As a** developer or team lead using `jr` to inspect Jira issues
- **I want** `jr issue attachment list <KEY>` to display all attachments as a table and optionally as JSON, with client-side filtering by MIME type, filename, and size
- **So that** I can discover, audit, and select specific attachments on a Jira issue without opening the web UI, and downstream scripts can parse attachment metadata reliably (including the `contentUrl` field from issue #585)

---

## Behavioral Contracts

| BC | Title | This Story's Scope |
|----|-------|--------------------|
| BC-2.7.001 | `attachment list <KEY>` renders table: id, filename, mimeType, size (human-readable), created, author; output channel profile 2 (stdout data, stderr hints) | Full |
| BC-2.7.002 | `attachment list --output json` returns array of attachment objects including `contentUrl` field; JSON render invariant #526 (`output::render_json`); closes #585 contentUrl surface; **BTreeMap-alphabetical key order at all depths: author < contentUrl < created < filename < id < mimeType < size** (P19-001) | Full (list half of VP-576-004) |
| BC-2.7.003 | `--filter mime=<glob>` client-side mimeType filter; glob case-insensitive; AND-combined with other filters; **EC-2.7.003-2: "clap-or-application" → "application" pre-flight check (P18-003)** | Full |
| BC-2.7.004 | `--filter name=<glob>` client-side filename filter; JRACLOUD-96384 match-by-id note (display name unreliable for matching) | Full |
| BC-2.7.005 | `--filter size-max=<bytes>` client-side size filter; no hard-coded cap; non-numeric input → exit 64 | Full |
| BC-2.7.006 | Unknown/inaccessible KEY → exit 64; full error taxonomy: 404 issue-not-found, 401 not-authenticated, 5xx API error, network failure; **403 override row added to error-taxonomy.md (P18-002)** | Full |

---

## Acceptance Criteria

### AC-001 (traces to BC-2.7.001 postcondition: table columns in display order)

`jr issue attachment list <KEY>` against a wiremock-served issue with one attachment renders a comfy-table on stdout with exactly six columns in order: **ID | Filename | Type | Size | Created | Author**. The `Size` column shows human-readable formatted bytes (e.g., `42.0 KB`). The `Author` column shows `displayName`; falls back to `accountId` when `displayName` is absent/null; falls back to `"(anonymous)"` when both are absent/null (full chain per EC-2.7.001-3). Thumbnail field is NOT present in the table.

**Display-sanitization mandate (SEC-576-011, CWE-116)**: the `display_sanitize_filename` helper (see Architecture Compliance Rules) is called on EVERY `filename` value before rendering in the Filename column. `display_sanitize_filename` replaces ALL characters in the extended sanitization set per BC-2.7.011 v1.3.94 with `?` (CWE-116 terminal injection prevention — BC-2.7.011 display-sanitization primary clause): ASCII 0x00–0x1F/0x7F + Unicode bidi controls (U+202A..U+202E, U+2066..U+2069) + line separators (U+2028/U+2029) + NEL (U+0085); char-based matching, each → one `?`. This is the ONLY operation it performs: it does NOT strip path separators, does NOT truncate to 214 bytes, and does NOT filter Windows device names (those belong to `sanitize_attachment_filename` in S-576-2, which is the disk-path variant). A filename containing null bytes or control characters MUST render as the sanitized form, never the raw form, in the table. The helper lives in `src/cli/issue/attachments.rs` and is the canonical first instantiation.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_001_table_six_columns_order` (wiremock)

---

### AC-002 (traces to BC-2.7.001 postcondition: zero-attachment empty table)

When the issue exists but has zero attachments, `jr issue attachment list <KEY>` exits 0 with empty stdout (pipe-friendly) and emits `"No attachments on <KEY>."` to stderr. In JSON mode (`--output json`), exit 0, stdout `[]`, no stderr.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_001_zero_attachments_empty_stdout_stderr_hint`

---

### AC-003 (traces to BC-2.7.002 postcondition: JSON array shape; contentUrl rename; #526 invariant)

`jr issue attachment list <KEY> --output json` returns a pretty-printed JSON array routed through `output::render_json` (#526 invariant — NOT `serde_json::to_string_pretty`). Each element has exactly the keys `{author, contentUrl, created, filename, id, mimeType, size}` in BTreeMap-alphabetical order. Key invariants:
- `"self"` MUST be OMITTED from every element (VP-576-004 assertion 1).
- `"content"` MUST be RENAMED to `"contentUrl"` in every element (VP-576-004 assertion 2).
- `size` is a raw `u64` integer (bytes), not a human-formatted string.
- `author` is `null` when the author field is absent or null in the Jira API response. When the author field is present, it is ALWAYS emitted as the curated two-field object `{"accountId": <value>, "displayName": <value>}` only — values as-received including null; no other Jira author fields (`self`, `avatarUrls`, `accountType`, `timeZone`, `emailAddress`, `active`) may appear (BC-2.7.002 v1.3.95 P1-002 ruling). Partial-author case (author present, both subfields absent or null) → `{"accountId": null, "displayName": null}` — the curated two-field form with null values, NOT a null top-level author. BTreeMap ordering applies within the author object: `accountId` < `displayName`.

**VP-576-004 list half**: assert no element contains `"self"` key AND every element contains `"contentUrl"` and no element contains `"content"` key.

- **Tests:** `tests/attachment_list.rs::test_bc_2_7_002_json_shape_curated_form` (VP-576-004 list half; gains sub-assertions per v1.3.95 P1-002: (a) full-author fixture — wiremock Jira response includes `self`/`avatarUrls`/`accountType` in the author sub-object → emitted `"author"` has exactly `{"accountId": ..., "displayName": ...}` with no extra fields; (b) partial-author fixture — Jira response has `author` present but `accountId` and `displayName` both absent/null → emitted `"author": {"accountId": null, "displayName": null}`, not `null`; no new test function — extend the existing fn); `tests/attachment_list.rs::test_bc_2_7_002_json_uses_render_json_not_string_pretty`

---

### AC-004 (traces to BC-2.7.001 postcondition: filter-count hint; BC-2.7.002: JSON hint behavior)

When a `--filter` flag is active and reduces the displayed row count (N < M), a hint is emitted to stderr: `"Showing N of M attachments."` This hint fires in BOTH human and JSON modes (BC-2.7.001 EC-2.7.001-2). When a filter is active but excludes nothing (N == M), the hint is NOT emitted. When no filter is active, no hint is emitted.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_001_filter_count_hint_fires_when_reduced` — extend the existing function with explicit sub-assertions covering the DELIBERATE ASYMMETRY (EC-2.7.001-2): (a) `--output json` + reducing filter (N < M) → stdout = filtered JSON array AND stderr contains `"Showing N of M attachments."` (hint fires in JSON mode — opposite of bundle-wide hint suppression rule); (b) N == M (filter active but no exclusions) → NO hint on stderr in either mode; (c) human mode N < M → hint on stderr (existing coverage). No new test function; all three cases as sub-assertions within the single test.

---

### AC-005 (traces to BC-2.7.003 postcondition: mime glob filter)

`--filter mime=<glob>` retains only rows whose `mimeType` matches the glob (case-insensitive; `*` matches any sequence including `/`; `?` matches any single character). Full attachment list is fetched before filtering. Combined with `--output json`, returns only matching elements.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_003_mime_filter_image_wildcard` — extend the existing function with sub-assertions per BC-2.7.003 glob semantics: (a) **star-crosses-slash**: glob `image*` MUST match `image/png` (the `*` crosses the `/` separator — verifies `*` is not restricted to intra-segment); `image*` must NOT match `application/pdf`; (b) **single-char wildcard**: glob `image/pn?` matches `image/png` but NOT `image/jpeg`. No new test functions; no test count change.

---

### AC-006 (traces to BC-2.7.004 postcondition: name glob filter; AND composition)

`--filter name=<glob>` retains only rows whose `filename` matches the glob (case-insensitive; `*` matches any sequence; `?` matches any single character). Multiple `--filter` flags combine with AND semantics. JRACLOUD-96384 note: multiple attachments sharing the same `filename` are all returned; no dedup.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_004_name_filter_glob_and_composition` — extend the existing function with a `?`-glob sub-assertion per BC-2.7.004 glob semantics: glob `report-?.pdf` matches `report-1.pdf` and `report-A.pdf` but NOT `report-10.pdf` (single-character constraint). No new test function; no test count change.

---

### AC-007 (traces to BC-2.7.005 postcondition: size-max filter)

`--filter size-max=<bytes>` retains only rows whose `size` (bytes, u64) ≤ the given limit. A non-integer argument → exit 64 before any HTTP call. `--filter size-max=0` retains zero-byte attachments only.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_005_size_max_filter_and_parse_error`

---

### AC-008 (traces to BC-2.7.003 EC-2.7.003-2: invalid filter key or missing =)

If a `--filter` value does not contain `=` → exit 64, message includes `"Invalid filter '<VALUE>': expected key=value form. Accepted keys: mime=, name=, size-max=."`. If the key before `=` is unknown → exit 64, message includes `"Unknown filter key '<KEY>'. Accepted keys: mime=, name=, size-max=."`. Both cases fire before any HTTP call.

- **Test:** `tests/attachment_list.rs::test_bc_2_7_003_invalid_filter_key_exits_64`

---

### AC-009 (traces to BC-2.7.006 postcondition: unknown/inaccessible KEY exit 64)

When the issue KEY does not exist (404 from `GET /rest/api/3/issue/{key}?fields=attachment`) → exit 64, stderr `"Issue <KEY> not found or not accessible."`. When 401 → exit 2, stderr contains `"Not authenticated"` AND `"jr auth login"` (full literal: `Not authenticated. Run "jr auth login" to connect.` — `src/error.rs::JrError` + `client.rs`). When 403 → exit 1, stderr contains `"Permission denied: cannot access issue <KEY>."`. When 5xx → exit 1, stderr contains `"API error (<N>)"`. When network error → exit 1, stderr contains `"Could not reach"` (full literal: `Could not reach <host> — check your connection` — `src/error.rs::JrError::NetworkError`).

- **Tests:** `tests/attachment_list.rs::test_bc_2_7_006_unknown_key_exits_64`; `tests/attachment_list.rs::test_bc_2_7_006_key_401_exit_2` (assert exit 2 + stderr contains "Not authenticated" AND "jr auth login" per v1.3.88 loose-substring form); `tests/attachment_list.rs::test_bc_2_7_006_key_403_exit_1` (assert exit 1 + stderr contains `"Permission denied: cannot access issue <KEY>."` canonical string); `tests/attachment_list.rs::test_bc_2_7_006_key_5xx_exit_1` (assert exit 1 + stderr contains `"API error ("`); `tests/attachment_list.rs::test_bc_2_7_006_key_network_exit_1` (assert exit 1 + stderr contains `"Could not reach"` per v1.3.89/v1.3.88 loose-substring)

---

### AC-010 (traces to BC-2.7.001 precondition: CLI flags surface — e2e surface guard)

`tests/e2e_cli_surface_guard.rs` SURFACE table gains entries for `attachment list` paths and flags: `<KEY>` (required positional), `--filter <FILTER>` (repeatable), `--output json`, `--no-input`, `--profile <NAME>`, `--no-color`. The `test_parser_paths_are_subset_of_surface_table` guard passes with these entries present.

- **Test:** `tests/e2e_cli_surface_guard.rs` (SURFACE table entries; always-run guard)

---

### AC-011 (traces to BC-2.7.001 postcondition: README/CHANGELOG/CLAUDE.md/json-output-shapes.md obligations — FIRST surface shipped)

All GAP-AUDIT-576-001 §§3.1/3.4 first-surface obligations are met in this story:

1. `README.md` gains a command-table row for `jr issue attachment list` (§3.1 — first surface shipped).
2. `CHANGELOG.md` gains entry: `feat(issue): attachment list subcommand + JSON output + filters (#576)` in the Unreleased section (§3.1 — first surface shipped; S-576-2 through S-576-4 each append their own scoped entries).
3. `docs/specs/json-output-shapes.md` gains list shapes rows — table output columns: `ID | Filename | Type | Size | Created | Author`; JSON array shape: `[{author,contentUrl,created,filename,id,mimeType,size}]` (§3.1 — list surface).
4. `CLAUDE.md` §Architecture `src/cli/issue/` listing gains `attachments.rs`; `src/api/jira/` listing gains `attachments.rs` (§3.4 — first story to create these files).
5. **PRE-DELIVERY GATE — BC-2.7.001 completeness-probe (blocks S1 PR merge)**: BC-2.7.001 ~554 carries a mandatory S1 delivery obligation: the implementer MUST verify that `fields.attachment` is returned COMPLETE (non-paginated) before the S1 PR is merged. The correctness of `--all` / `--newest N` (S-576-2 / BC-3.9.019) and `--older-than` (S-576-4 / BC-3.9.019) depends entirely on this completeness assumption — a partial list would silently miss attachments. **Discharge paths (either satisfies the gate):** (a) live-verify against a real Jira Cloud instance with >100 attachments on one issue and confirm the full list is returned — wiremock tests do NOT satisfy this; OR (b) cite Atlassian documentation that explicitly bounds the maximum attachment count per issue. **PR merge gate:** PR description MUST include the line `"BC-2.7.001 completeness-probe: [discharge path chosen + result]."` A PR without this line is BLOCKED from merge.

- **Test:** `tests/claude_md_citations.rs::test_claude_md_citations_resolve_to_real_files` passes after CLAUDE.md update.

---

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| `handle_attachment_list` | `src/cli/issue/attachments.rs` | Effectful (HTTP + stdout) |
| `list_attachments` | `src/api/jira/attachments.rs` | Effectful (HTTP) |
| `display_sanitize_filename` | `src/cli/issue/attachments.rs` | Pure (string transform; CWE-116 guard) |
| `pub fn serialize_attachment_curated` | `src/cli/issue/attachments.rs` | Pure (serde/BTreeMap); `pub` required — S3 integration test calls directly (VP-576-004 cross-path, P74-001) |
| `output::render_json` | `src/output.rs` (existing) | Pure (#526 invariant) |

---

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-1 | Zero attachments on valid issue | Exit 0, empty stdout, stderr hint `"No attachments on <KEY>."` |
| EC-2 | `attachment list <KEY>` with no filters | No filter-count hint on stderr; full list rendered |
| EC-3 | Filter active, all rows excluded (N=0) | Exit 0, empty table/`[]` JSON; hint `"Showing 0 of M attachments."` |
| EC-4 | Filter active, N==M (no rows excluded) | No hint emitted (P39-I2 N==M clause) |
| EC-5 | `author` absent/null on attachment object | Table column shows `"(anonymous)"`; JSON emits `"author": null` |
| EC-6 | `displayName` absent but `accountId` present | Table column shows `accountId`; JSON emits curated `{"accountId": <value>, "displayName": null}` (v1.3.95) |
| EC-7 | Filename contains path separator or control char | `display_sanitize_filename` output rendered in table; raw value in JSON (JSON is pass-through) |
| EC-8 | `--filter name=` with multiple same-filename attachments | All matching rows returned; no dedup (JRACLOUD-96384) |
| EC-9 | Invalid filter key (e.g., `--filter type=foo`) | Exit 64 before HTTP; "Unknown filter key" message |

---

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `handle_attachment_list` | Effectful-shell | HTTP call + stdout writes |
| `list_attachments` | Effectful-shell | HTTP (reqwest) — reads API response |
| `display_sanitize_filename` | Pure-core | String → String; no I/O; CWE-116 defense |
| `pub fn serialize_attachment_curated` | Pure-core | Struct → BTreeMap serialization; no I/O; `pub` required (P74-001) |
| `tests/attachment_list.rs` | Effectful-shell (test boundary) | wiremock HTTP interception |

---

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~9,000 |
| `src/cli/issue/attachments.rs` (new file, ~350 LOC estimated) | ~5,500 |
| `src/api/jira/attachments.rs` (new file, ~200 LOC) | ~3,500 |
| `src/output.rs` (existing — render_json reference) | ~2,000 |
| BC files (6 BCs × ~800 tokens avg) | ~4,800 |
| `tests/attachment_list.rs` (new test file, 14 tests) | ~4,800 |
| `tests/e2e_cli_surface_guard.rs` (modification) | ~2,000 |
| Tool outputs overhead | ~3,000 |
| **Total** | **~34,600** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~17%** |

---

## Tasks (MANDATORY)

### Task 0: Read BC-2.7.001..006 in full before writing any tests or production code

Read `.factory/specs/prd/bc-2-issue-read.md` §2.7 fully. Extract: (a) exact table column order from BC-2.7.001; (b) exact JSON key order from BC-2.7.002; (c) `display_sanitize_filename` CWE-116 display-only clause from BC-2.7.011 v1.3.94 (extended set: ASCII 0x00–0x1F/0x7F + bidi controls + line separators + NEL — see BC-2.7.011 display-sanitization primary clause; no path-separator stripping, no 214-byte cap, no device-name filtering — the 5-step CWE-22 disk-path algorithm with containment check, 214-byte cap, step 5.5 trailing whitespace/dot strip, and Windows device-name caller note belongs to `sanitize_attachment_filename`, which is extracted in S-576-2); (d) error strings verbatim from BC-2.7.006. Do NOT rely on summary — the BC bodies are the ground truth.

> **PRE-DELIVERY GATE (blocks S1 PR merge) — BC-2.7.001 ASSUMPTION ~554:** Before creating the PR, discharge the BC-2.7.001 completeness assumption: live-verify `fields.attachment` is returned COMPLETE (non-paginated) against a real Jira Cloud instance with >100 attachments on one issue, OR cite Atlassian documentation bounding the maximum attachment count per issue. Wiremock tests do NOT satisfy discharge path (a). The correctness of S-576-2 `--all`/`--newest N` and S-576-4 `--older-than` (BC-3.9.019) depends on this. PR description MUST include: `"BC-2.7.001 completeness-probe: [discharge path and result]."` Without this line the PR is BLOCKED. See AC-011 item 5.

### Task 1: Author RED test anchors in `tests/attachment_list.rs` (new file)

Write all 14 tests (11 ACs; AC-003 has 2, AC-009 has 5; AC-010 uses existing e2e_cli_surface_guard.rs — 0 new; AC-011 uses existing claude_md_citations.rs — 0 new) as wiremock subprocess tests. The file follows the `tests/comment_delete.rs` / `tests/attachment_list.rs` pattern. Use `jr_cmd_with_xdg` + per-test `TempDir` for cache/config isolation. Confirm all tests FAIL (RED) against the unimplemented `todo!()` stubs.

### Task 2: Create module skeleton — `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs`

Create both files with `todo!()` stubs for:
- `handle_attachment_list(key: &str, filters: ..., output_format: &OutputFormat, client: &JiraClient) -> Result<()>`
- `display_sanitize_filename(name: &str) -> String` (pure helper; CWE-116)
- `list_attachments(client: &JiraClient, key: &str) -> Result<Vec<AttachmentObject>>`

Wire `handle_attachment_list` into `src/cli/issue/mod.rs` dispatch: `IssueCommand::Attachment { command: sub } => match sub { AttachmentSubcommand::List { .. } => handle_attachment_list(..) }` (pattern mirrors comment-CRUD at `src/cli/issue/mod.rs:75`).

Add `attachments.rs` to `src/cli/issue/mod.rs` and `src/api/jira/mod.rs` re-exports.

Verify compile gate: `cargo build` MUST succeed with `todo!()` stubs (all RED tests are compile-passing).

**mutants.toml obligation (prd-delta-576.md S1 obligation (f) — ADD AT CREATION, P22-001)**: Add `examine_globs` entries for `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` to `.cargo/mutants.toml` IN THIS STORY. These files are CREATED here; per prd-delta-576.md S1 obligation (f) the entries are added at file-creation time (CWE-116 rationale: `display_sanitize_filename` is a security-relevant display-sanitization function that ships in S1; S-577-1 precedent: "new CLI handler file → add to mutants.toml at creation" — see PSI table above). `tests/mutants_glob_existence.rs` MUST remain GREEN (the files exist at S1 delivery — passes trivially). S-576-2 Task 2 (S2 obligation (e)) and S-576-3 AC-010 (S3 obligation (a)) confirm-present / ADD IF ABSENT at their later delivery points (primary delivery moved from S2 to S1 per P22-001).

### Task 3: Implement `display_sanitize_filename` (pure helper — CWE-116; SEC-576-011)

**CRITICAL — CWE-116 display-sanitization ONLY (P1-005, updated BC-2.7.011 v1.3.94)**: `display_sanitize_filename(name: &str) -> String` replaces ALL characters in the extended sanitization set with `?`. The set (char-based matching, each → one `?`):
- ASCII control characters: 0x00–0x1F and 0x7F
- Unicode bidi controls: U+202A..U+202E (LEFT-TO-RIGHT EMBEDDING..RIGHT-TO-LEFT OVERRIDE), U+2066..U+2069 (LEFT-TO-RIGHT ISOLATE..POP DIRECTIONAL ISOLATE)
- Line separators: U+2028 (LINE SEPARATOR), U+2029 (PARAGRAPH SEPARATOR)
- NEL: U+0085 (NEXT LINE)

That is the COMPLETE algorithm. It does NOT:
- Strip path separators (`/`, `\`, `:`) — those are disk-path concerns for `sanitize_attachment_filename` in S2
- Truncate to 214 bytes — no truncation
- Filter Windows device names (CON, NUL, COM1, etc.) — disk-path concern
- Strip leading dots — disk-path concern
- Apply step 5.5 trailing whitespace/dot strip — disk-path concern

The function always returns a `String` (never `None`/`Option`). If the input is all control chars, the result is a string of `?` characters — still returned.

Write unit tests in `src/cli/issue/attachments.rs #[cfg(test)]` covering: null byte (`\0`), DEL (`\x7F`), tab (`\t`), newline (`\n`), `\x1F`, and a normal filename (unchanged). **Mandated by BC-2.7.011 v1.3.94:** also cover U+202E (RIGHT-TO-LEFT OVERRIDE → `?`), U+2028 (LINE SEPARATOR → `?`), U+0085 (NEL → `?`). All pure-core, no wiremock needed. These three cases are sub-cases within the existing `#[cfg(test)]` suite; no new integration test functions; no 4-surface count change.

**Distinction**: `display_sanitize_filename` (this helper, S1) is the display-channel variant — prevents terminal injection. `sanitize_attachment_filename` (S2) is the disk-path variant — implements the full BC-2.7.011 5-step CWE-22 algorithm with path-separator stripping, device-name filtering, 214-byte cap, and containment check. They are separate functions; neither calls the other. Do NOT conflate them.

### Task 4: Implement `list_attachments` in `src/api/jira/attachments.rs`

Issue `GET /rest/api/3/issue/{key}?fields=attachment`. Map 404 → exit 64 per BC-2.7.006 error table. Return `Vec<AttachmentObject>` deserialized from `fields.attachment[]`. The `AttachmentObject` struct uses `#[serde(rename = "content")]` on the field and renames it to `content_url` for the curated form.

### Task 5: Implement `handle_attachment_list` in `src/cli/issue/attachments.rs`

1. Call `list_attachments`. Handle 404/401/403/5xx per BC-2.7.006 error table.
2. Apply `--filter mime=`, `--filter name=`, `--filter size-max=` in order (AND semantics). Validate filter key names; exit 64 on unknown key.
3. Emit filter-count hint to stderr when N < M (suppress when N == M; suppress in JSON mode for zero-attachment hint only per EC-2.7.001-1/EC-2.7.001-2 asymmetry).
4. For table output: render via `comfy-table` with 6 columns in BC-2.7.001 order. Call `display_sanitize_filename` on every Filename cell.
5. For JSON output: serialize via `serialize_attachment_curated` (BTreeMap-ordered: `author` < `contentUrl` < `created` < `filename` < `id` < `mimeType` < `size`; omit `"self"`, rename `"content"` → `"contentUrl"`). Route through `output::render_json`.

**Visibility obligation (P74-001):** `serialize_attachment_curated` MUST be declared `pub fn` — S-576-3's integration test `tests/attachment_upload.rs::test_vp_576_004_curated_shape_upload_and_list_are_structurally_identical` calls it directly (VP-576-004 cross-path). Correspondingly, all `AttachmentObject` struct fields in `src/api/jira/attachments.rs` must be `pub` to allow direct fixture construction in that test — specifically: `pub self_url`, `pub id`, `pub filename`, `pub author`, `pub created`, `pub size`, `pub mime_type`, `pub content` (8 fields; `mime_type` uses `#[serde(rename = "mimeType")]`, `content` uses `#[serde(rename = "content")]`, `self_url` uses `#[serde(rename = "self")]`). The `pub mod attachments;` visibility in `src/cli/issue/mod.rs` (see File Structure) is also required for the same reason.

### Task 6: AC-010 — `tests/e2e_cli_surface_guard.rs` SURFACE entries

Add SURFACE table rows for `attachment list` paths and flags. Run `cargo test --test e2e_cli_surface_guard` to confirm `test_parser_paths_are_subset_of_surface_table` passes.

### Task 7: AC-011 — README, CHANGELOG, docs/specs, CLAUDE.md

1. `README.md`: add row for `jr issue attachment list` to the command table.
2. `CHANGELOG.md`: add `feat(issue): attachment list subcommand + JSON output + filters (#576)` to the Unreleased section. Scoped to this story's shipped surface (list only). Downstream stories S-576-2, S-576-3, S-576-4, S-576-5 each append their own CHANGELOG entries for their respective surfaces.
3. `docs/specs/json-output-shapes.md`: add list shapes rows.
4. `CLAUDE.md`: add `attachments.rs` to `src/cli/issue/` and `src/api/jira/` architecture listings.

### Task 8: Confirm all 14 tests GREEN + full suite

```
cargo test --test attachment_list
cargo test --lib -- display_sanitize_filename
cargo test --test e2e_cli_surface_guard
cargo test
```

All must pass. `cargo clippy -- -D warnings` must be clean.

### Task 9: Create PR

PR title: `feat(issue): attachment list + JSON output + filters (S-576-1, #576 part 1)`.
Reference this story. Blocks: S-576-2, S-576-3, S-576-4.
PR body notes: "First story of SOH-ATTACHMENTS-1; creates `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs`; introduces `display_sanitize_filename` helper (earliest consumer for SEC-576-011/CWE-116). Closes #585 (contentUrl surface in BC-2.7.002)."

---

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-577-1 (comment CRUD subgroup) | ADR-0012 Seam-B extraction; `.cargo/mutants.toml` examine_globs entry added at extraction time | New CLI handler file → add to mutants.toml at creation; wire stub + dispatch in same Task 2 compile-gate | `output::render_json` required for all `--output json` paths (#526 invariant); direct `serde_json::to_string_pretty` forbidden |
| S-577-5 (comment edit visibility) | DEC-174 prompt mechanism: `eprint!+read_line`, NOT `dialoguer::Confirm`; interact_on forbidden | `--output json` must not break on stderr hints; stdout stays clean | `dialoguer::Confirm` returns `Err(NotConnected)` on piped stderr — confirmed broken for this use case |

---

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| `display_sanitize_filename` is the SOLE display-sanitization emit site introduced in S1; S3 and S4 REUSE it, never duplicate | DEC-184 R3.13 (earliest-consumer gate) | Grep: `grep -n "display_sanitize_filename" src/cli/issue/attachments.rs` — exactly 1 definition; callers from S3/S4 must import, not redefine |
| All `--output json` paths MUST route through `output::render_json` or `output::print_output` (never direct `to_string_pretty`) | JSON render invariant #526; CLAUDE.md §Conventions | `grep -rn "to_string_pretty\|compact" src/cli/issue/attachments.rs` — must return empty |
| No unsafe code without justification comment | CLAUDE.md §Conventions | `cargo clippy -- -D warnings`; zero warnings policy |
| Curated attachment-object shape: `"self"` OMITTED, `"content"` → `"contentUrl"` (VP-576-004) | BC-2.7.002; BC-3.9.009 | VP-576-004 list-half wiremock assertions in test suite confirm both invariants |
| `list_attachments` issues `GET /rest/api/3/issue/{key}?fields=attachment` — no dedicated attachment-list endpoint exists | BC-2.7.001; research §1a VERIFIED | Implementation must not assume a separate `/attachments` endpoint |
| No lint suppression (`#[allow]`) without refactoring and user approval | CLAUDE.md §Conventions | `cargo clippy -- -D warnings` must pass clean |

---

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| reqwest | as in `Cargo.lock` (0.13.x) | HTTP client — `GET /rest/api/3/issue/{key}?fields=attachment`; no new features required for S1 (no multipart/stream needed for list) |
| serde / serde_json | as in `Cargo.lock` | JSON deserialization of `fields.attachment[]`; BTreeMap serialization for curated form |
| comfy-table | as in `Cargo.lock` | Attachment list table rendering (6 columns) |
| wiremock | as in `Cargo.lock` | Integration test HTTP mocking |

---

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `src/cli/issue/attachments.rs` | CREATE | `handle_attachment_list`, `display_sanitize_filename`, curated serialization helpers; new module (handlers + helpers only — `AttachmentSubcommand` enum is defined in `src/cli/mod.rs`, NOT here) |
| `src/api/jira/attachments.rs` | CREATE | `list_attachments` API function; `AttachmentObject` type |
| `src/cli/issue/mod.rs` | MODIFY | Add `pub mod attachments;` re-export + `AttachmentSubcommand::List` dispatch arm under `IssueCommand::Attachment { command: sub }` (pattern: `IssueCommand::Attachment { command: sub } => match sub { AttachmentSubcommand::List { .. } => handle_attachment_list(..) }`); `pub` required because S-576-3's integration test calls `serialize_attachment_curated` directly (VP-576-004 cross-path; P74-001) — mirrors `pub mod interactions;`/`pub mod workflow;` precedent |
| `src/api/jira/mod.rs` | MODIFY | Add `pub mod attachments;` re-export |
| `src/cli/mod.rs` | MODIFY | Add `Attachment { #[command(subcommand)] command: AttachmentSubcommand }` variant to the `IssueCommand` enum (`src/cli/mod.rs:295`) AND define the `AttachmentSubcommand` enum with the `List { key, filter, output, … }` variant — the `<KEY>`/`--filter` args live on `List`, NOT on `Attachment` (S-576-2/3/4/5 all depend on S1 creating `AttachmentSubcommand`) |
| `tests/attachment_list.rs` | CREATE | 14 wiremock integration tests (11 ACs; AC-003 has 2, AC-009 has 5; AC-010/AC-011 use existing test files — 0 new in attachment_list.rs) |
| `tests/e2e_cli_surface_guard.rs` | MODIFY | SURFACE entries for `attachment list` paths and flags (AC-010) |
| `README.md` | MODIFY | Command-table row for `jr issue attachment list` (AC-011) |
| `CHANGELOG.md` | MODIFY | `feat(issue): attachment list subcommand + JSON output + filters (#576)` (AC-011; S1-scoped entry only) |
| `docs/specs/json-output-shapes.md` | MODIFY | List table columns + JSON array shape rows (AC-011) |
| `CLAUDE.md` | MODIFY | Add `attachments.rs` to architecture src-tree listings (AC-011; §3.4 obligation) |
| `.cargo/mutants.toml` | MODIFY | ADD `examine_globs` entries for `src/cli/issue/attachments.rs` and `src/api/jira/attachments.rs` (prd-delta-576.md S1 obligation (f) — add at file-creation time; CWE-116 rationale + S-577-1 precedent; P22-001) |

**MUST NOT change**: `src/cli/issue/interactions.rs`, `src/cli/issue/workflow.rs`, `src/cli/issue/create.rs`, `src/cli/issue/edit.rs` — these are finalized by S-577-1 dispatch.

---

## Out-of-Scope

- `jr issue attachment download` (S-576-2): `sanitize_attachment_filename` for disk writes (CWE-22) is a separate helper with a containment check — NOT the same as `display_sanitize_filename` (CWE-116 display only).
- `jr issue attachment upload` (S-576-3): starts in a separate story once S1's curated-serialization plumbing is available.
- `jr issue attachment delete` (S-576-4): delete handler is independent.
- JSM `--public`/`--internal` upload (S-576-5): separate story.
- Unicode bidi-control and line-separator sanitization: now IN SCOPE — discharged @ v1.3.94 per BC-2.7.011 display-sanitization primary clause. `display_sanitize_filename` covers the extended set from the start (S1 implements it; see Task 3).

---

## Spec Reference Links

- BC-2.7.001..006: `.factory/specs/prd/bc-2-issue-read.md §2.7`
- VP-576-004: `.factory/specs/prd/bc-2-issue-read.md §BC-2.7.002`
- DEC-184: authorized story decomposition at spec v1.3.81
- ADR-0017: `.factory/specs/architecture/decisions/ADR-0017-first-multipart-streaming-http-surface.md`
- prd-delta-576.md Scope S1 row: `.factory/phase-f2-spec-evolution/prd-delta-576.md`
