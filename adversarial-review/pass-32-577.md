# Adversary Pass 32 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (1 HIGH + 3 LOW)
Received at session wrap 2026-07-10; fix round 38 NOT yet dispatched. Resume = dispatch fix round 38 to spec author, then CV verify, then adversary pass 33.
## F1 (HIGH) bc-3-issue-write.md ~2319 BC-3.5.006 delivery item (b): TWO mutually exclusive cleanup mandates in one paragraph — stale sentence-1 clause "self-cleaning via jsm_self_close convention" (pre-pass-29) contradicts the authoritative later per-comment-DELETE rule ("NOT via jsm_self_close"). Fix: delete the stale sentence-1 clause or replace with forward-reference "self-cleaning per the per-comment DELETE rule stated below." One policy, not two.
## F2 (LOW) BC-3.5.005 ~2260 + EC-3.5.008-2 ~2387: human echo markers "(marked internal)"/"(marked public)" unpinned by any VP. Fix: add VP-577-025 — edit --internal → stderr contains "(marked internal)"; variant --public --yes → "(marked public)".
## F3 (LOW) BC-3.5.006 item (b): teardown uses `jr issue comment delete` which ships in the SAME story — sequencing constraint unstated. Fix: add note — Delete subcommand must ship first in-story for CLI teardown; raw-API-DELETE fallback permitted but drops the incidental CLI regression signal; story PR must declare which pattern is used.
## F4 (LOW) EC-3.5.012-5 ~2547: BC-3.4.011 wrongly listed among try_parse regression obligations (it is post-clap HTTP-400 handling, orthogonal to the parse-time intercept). Fix: remove BC-3.4.011 from item (a) or justify the coupling explicitly; items (b)-(f) are defensibly coupled.
