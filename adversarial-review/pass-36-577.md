## Adversary Pass 36 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (0 HIGH + 1 MEDIUM + 3 LOW)

Lens: newest-text scrutiny (rounds 39-40 additions) + cross-EC interaction matrix + URL-encoding coherence + changelog arithmetic. All 4 fixed in round 41 (spec v1.3.32).

## F-1 (MEDIUM) VP-577-028 second variant tautological: `--help` text assertion relies on clap `requires()` rendering into help output, which is not guaranteed across clap versions and is absent in practice. Fix: replaced with runtime probe — invoke handler with empty positional + `--yes` → confirm exit 64 with body-error proving handler entry before the guard.

## F-2 (LOW) field-6 rung (b) empty-string-value gap: `{"type":"role","value":"","identifier":"X"}` fell through to rung (c) because rung (b) tested `value.is_some()` rather than non-empty. Fix: broadened rung (b) to "not a non-empty string" (`value.as_deref().filter(|s| !s.is_empty())`).

## F-3 (LOW) VP-577-027 dual-mount wiremock hit-count relied on undocumented raw-vs-decoded path matching: `MockServer::received_requests()[0].url` returns the raw percent-encoded URL, but the VP assertion used a decoded path comparison, which wiremock may silently pass or fail depending on version. Fix: reformulated to `received_requests()[0].url` raw-byte assertion + explicit 204 status template.

## F-4 (LOW) EC-3.5.002-2 site-ordering: named `client.rs` first (generic HTTP layer, no path templates) before per-endpoint helpers — generic layer does not have path-template context. Fix: per-endpoint-helper-first ordering (`api/jira/issues.rs::update_comment` then `api/client.rs`).

Informational (non-findings): cross-EC interaction matrices for edit and delete show single-reading-per-cell (no ambiguity); changelog arithmetic verified (v1.3.29→v1.3.30→v1.3.31 entries internally consistent); %20 URL-encoding semantics verified (RFC 3986 §2.1, space→%20 in path segments, consistent with EC-3.5.012-5 KEY encoding); body-trim asymmetry (add trims, edit trims, delete has no body) documented as deliberate product choice.
