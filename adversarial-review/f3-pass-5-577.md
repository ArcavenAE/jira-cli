# F3 Adversary Pass 5 — SOH-COMMENT-CRUD-1 story package (issue #577)

**VERDICT: FINDINGS — 0 HIGH + 2 MEDIUM + 2 LOW. All 4 fixed in F3 fix round 6 (+5 stale-count stragglers).**

Lens: story package review — VP variant completeness, IO error scoping, filename accuracy.

---

## Findings

### HIGH

None.

### MEDIUM

**F-1 MEDIUM — VP-577-021 variant-2 (body-absent fallback + byte pin) unowned**
VP-577-021 has 7 variants; variant-2 covers the body-absent fallback path (comment with no body field returns a specific placeholder string). No AC in `S-577-6` owned this variant — it would not be tested. **Fix:** `S-577-6` `AC-011` added, asserting the body-absent fallback string and byte pin for variant-2. S-577-6 now has 11 ACs.

**F-2 MEDIUM — VP-577-028 variant-2 (runtime clap-requires probe) dropped**
VP-577-028 has 2 variants; variant-2 covers the runtime `clap`-level required-flag probe (verifying `--yes` is not silently accepted without `--public`). The variant was present in the VP spec but dropped from `S-577-5` AC-010 during a prior fix round. **Fix:** AC-010 extended with variant-2 assertion covering the runtime clap-requires behavior.

### LOW

**F-3 LOW — Blanket `IO map_err` vs EC-3.5.009-1 NotFound-only scoping**
`S-577-2` PSI contained a blanket `map_err(|e| JrError::Io(e))` pattern for file-read errors. EC-3.5.009-1 scopes the exit-1 path to `NotFound` only — other IO errors (permission denied, is-directory, etc.) should propagate differently. The blanket remap would incorrectly exit-1 on all IO errors. **Fix:** PSI updated to kind-matched remap: `ErrorKind::NotFound` → exit 1; other IO errors → exit 64 with message.

**F-4 LOW — Purity-table filename stale**
`S-577-4` Purity table referenced `comment_api.rs` as the file for the comment CRUD helper module. The canonical filename established in S-577-1 is `comment_crud_api.rs`. **Fix:** filename corrected to `comment_crud_api.rs` in the Purity table.

---

## Informational

Full variant→AC coverage map verified complete (19/19 variants assigned):
- VP-577-021: 7 variants — all AC-mapped across S-577-6
- VP-577-028: 2 variants — both AC-mapped in S-577-5
- VP-577-030: 2 variants — both AC-mapped in S-577-5
- VP-577-025: 2 variants — both AC-mapped in S-577-1
- VP-577-026: 3 variants — all AC-mapped in S-577-6
- VP-577-022: 3 variants — all AC-mapped in S-577-4

---

*Pass 5 complete. F3 fix round 6 closed all 4 findings + 5 stale-count stragglers pre-emptively fixed. F3 adversary pass 6 next (STRICT; streak 0/3; trajectory →8→10→9→5→4).*
