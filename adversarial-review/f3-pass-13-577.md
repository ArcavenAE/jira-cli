# F3 Adversary Pass 13 — SOH-COMMENT-CRUD-1 story package (issue #577) — verdict FINDINGS (0 HIGH + 0 MEDIUM + 1 LOW). Fixed in F3 fix round 14 (+1 cosmetic pre-empted).

**Date:** 2026-07-11
**Pass:** F3-13
**Scope:** Stories S-577-1..6 (issue #577 comment CRUD)
**Verdict:** FINDINGS — 1 LOW. Cosmetic pre-empted intra-round.
**STRICT streak:** 0/3 (reset on LOW)

---

## F-1 LOW: AC-009 item-h verification grep tautological

**Story:** S-577-1
**Severity:** LOW
**Status:** FIXED in fix round 14

**Finding:** AC-009 item-h verification grep searched the registry file for its own filename — a tautology that cannot fail on the missing-rows regression it was meant to catch. This grep was late-added at the DEC-170 gate closure and was never validated against the actual missing-row scenario. A search for `json-output-shapes.md` inside `json-output-shapes.md` will always match, providing zero coverage assurance.

**Fix applied (round 14):** Verification rebuilt with:
1. `changed_fields` row-marker grep — searches the registry for the new `changed_fields` column entries introduced by comment CRUD operations (verified absent pre-implementation, so sound)
2. `"deleted"` row-marker grep — searches for the delete-operation row (verified absent pre-implementation)

Both greps are validated against the pre-implementation state to confirm they would fail correctly before the implementation lands.

---

## Cosmetic (pre-empted): client.rs::from_slice phantom-symbol cite collapsed

**Story:** S-577-1
**Severity:** Cosmetic (pre-empted before pass-13 report filed)
**Status:** CLOSED intra-round

**Observation:** A nested phrasing cited `client.rs::from_slice` as if it were a symbol in `client.rs`. The actual call is `serde_json::from_slice` called inside `client.rs::get` (~line 265). Collapsed to the accurate `serde_json::from_slice` cite per #408 symbol-form convention.

---

## Informational: Sound-coverage inventory documented

Third consecutive single-LOW pass. Adversary documented a sound-coverage inventory to accelerate future passes:
- Code-reality cites (file::fn symbols, not phantom cites)
- Exhaustive inventories (flat-form sites, stub params, VP/holdout maps)
- Wire shapes (JSON output fields, HTTP verb/path tables)
- Doc obligations (CLAUDE.md, CHANGELOG, spec entries)

No action required. Documented for F3 gate reference.

---

## Summary

| # | Severity | Title | Status |
|---|----------|-------|--------|
| F-1 | LOW | AC-009 item-h grep tautological (changed_fields + "deleted" row-marker greps) | FIXED round 14 |
| COS-1 | COSMETIC | client.rs::from_slice phantom-symbol cite collapsed | CLOSED intra-round |
