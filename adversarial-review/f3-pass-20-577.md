# F3 Adversary Pass 20 — SOH-COMMENT-CRUD-1 story package (issue #577) — verdict FINDINGS (0 HIGH + 2 MEDIUM + 0 LOW). Both fixed in F3 fix round 20 (+2 CV residuals closed intra-round).

**Date:** 2026-07-11
**Pass:** F3-20
**Scope:** Stories S-577-1..6 (issue #577 comment CRUD)
**Verdict:** FINDINGS — 2 MEDIUM.
**STRICT streak:** 0/3 (reset on MEDIUM)

---

## F-1 MEDIUM: Red-Gate VP-008 bullet analyzed the wrong invocation

**Story:** S-577-1
**Severity:** MEDIUM
**Status:** FIXED in fix round 20

**Finding:** The round-19 residual VP-008 fix analyzed the `add` subcommand form (`jr issue comment add KEY text`) rather than the spec-mandated flat form (`jr issue comment KEY text` — the pre-migration invocation that must be intercepted). This was introduced by an orchestrator false premise in the round-19 residual dispatch — the second orchestrator false premise this session, both caught by the adversary loop.

The correct flat-form derivation: `jr issue comment KEY text` — the pre-change CLI parses this correctly (KEY is the issue key, `text` is the comment body, `comment` routes to the existing flat handler). After the breaking change, the same invocation hits the new subcommand dispatch and `text` is not a valid subcommand → exit 2 with the migration hint. The discriminator is the specific migration-hint text, not exit code alone (exit 2 also fires on unrelated parse errors). Pre-change exit is non-2 (HTTP/auth path).

**Fix applied (round 20):** VP-008 bullet rewritten with the correct flat-form pre-change derivation (parses-OK → HTTP/auth non-2 exit) and post-change derivation (text-not-a-subcommand → exit 2 + migration hint).

---

## F-2 MEDIUM: EC-011 context()-vs-argv discriminator untested

**Story:** S-577-1
**Severity:** MEDIUM
**Status:** FIXED in fix round 20

**Finding:** The spec mandates a `context()` walk to discriminate the `jr issue comment KEY text` flat form from `jr issue comment add KEY text` (EC-011). The story's 12 ACs verified the external behavior but contained no AC testing the `context()` mechanism specifically. A naive argv-scanner implementation would pass all 12 ACs while silently using a different (less reliable) discrimination mechanism — the spec mandate was unverified.

**Fix applied (round 20):** AC-013 added — tests EC-011 (positive: `context()` walk correctly identifies flat-form caller) + EC-010 (negative: `context()` does not misfire on the new subcommand form). S-577-1 AC count increases from 12 to 13. 8-location count sweep performed to propagate the new count.

---

## CV Residuals (closed intra-round)

1. **AC-013 cite split:** EC-3.5.012-1 hint-scope vs Implementation-note `context()`-mandate citations separated to avoid conflating the two distinct obligations
2. **File-Structure 8-test count:** updated to reflect AC-013 addition

---

## Note

Two orchestrator false premises caught this session (pass-19 VP-008 wrong-invocation; pass-20 confirms the correction). Both resolved by adversary loop — loop is functioning as designed.

---

## Summary

| # | Severity | Title | Status |
|---|----------|-------|--------|
| F-1 | MEDIUM | VP-008 analyzed wrong invocation (add form not flat form); corrected derivation | FIXED round 20 |
| F-2 | MEDIUM | EC-011 context() discriminator untested; AC-013 added (S-577-1 now 13 ACs) | FIXED round 20 |
| CV-1 | RESIDUAL | AC-013 cite split (hint-scope vs context()-mandate) | CLOSED intra-round |
| CV-2 | RESIDUAL | File-Structure 8-test count updated | CLOSED intra-round |
