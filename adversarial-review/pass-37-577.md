## Adversary Pass 37 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (0 HIGH + 1 MEDIUM + 3 LOW)

Lens: wide-spectrum. All 4 fixed in round 42 (spec v1.3.33).

## F-01 (MEDIUM) BC-3.5.012 Edit range included BC-3.5.010 (View's BC, not an Edit BC): the range "BC-3.5.005..011" was listed as the Edit-operation cross-reference map, but BC-3.5.010 belongs to View (field-by-field rendering), not Edit. Fix: corrected to non-contiguous list "BC-3.5.005..009, 011".

## F-02 (LOW) VP-577-009 lacked BTreeSet exact-set assertion: sibling wire-shape VPs (VP-577-005, VP-577-012) assert exact key-set membership via BTreeSet comparison; VP-577-009 (add_comment response shape) used a weaker subset check. Fix: reformulated to BTreeSet exact key-set assertion, symmetric with sibling VPs.

## F-03 (LOW) Label-value separator format underivable from BC prose and N/A render unpinned: no normative sentence in BC-3.5.010 specified the separator character between label and value (single space) or the record delimiter (LF). "N/A" render for unavailable fields also had no VP assertion. Fix: added normative separator sentence (fields 1-6, single space, LF delimiter) + VP-577-021 third variant pinning "JSM internal: N/A" display.

## F-04 (LOW) EC-3.5.010-2 catch-all "any adf_to_text error → exit 64" justified only by snapshot claim: the exception clause covered all adf_to_text failure modes with a single catch-all, but only the recursion-depth case (CWE-674) was substantiated by existing spec evidence. Fix: split into (a) recursion-depth guard exit-64 (BC-7.2.012 cited) and (b) future/unknown adf_to_text error kinds not yet covered by explicit ECs (forward-looking note, not an obligation).

Informational (non-findings): 8-surface counts hold (BC 624, VP 28, Stories 105 consistent in STATE.md + CANONICAL-COUNTS + BC-INDEX); add_comment/handle_comment citations resolve to `src/cli/issue/comments.rs` + `src/api/jira/issues.rs` (verified via grep); Comment-struct serde hazard confirmed documented in BC-3.5.001 EC-3.5.001-3; exit-code taxonomy reconciles (64 on validation, 0 on idempotent, 130 on Ctrl-C); JR_STDIN_IS_TTY seam absent is expected (F4 obligation, recorded in Follow-up Obligations).
