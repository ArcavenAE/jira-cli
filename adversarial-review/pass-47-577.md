# Adversary Pass 47 — SOH-COMMENT-CRUD-1 F2 (issue #577)

**VERDICT: CLEAN — 0 findings. STRICT streak 2/3 (second window).**

Lens: free-choice anti-convergence refutation attempt — self-validation loop applied to every candidate defect.

---

## Candidate Defects Examined

All candidates resolved on re-read, confirmed settled, or uncorroborated after full inspection:

- **Body-source chain** (--stdin > --file > positional): code-reality checked; priority order correct and matches spec EC-3.5.007 clauses. No defect.
- **trim-to-ADF path**: confirmed trim applied before ADF conversion on all three source paths. No defect.
- **urlencoding site**: `urlencoding::encode` applied to issue key in comment-path construction — verified matches spec requirement. No defect.
- **`print_success` → stderr**: confirmed `print_success` routes to stderr per house convention; spec correctly documents this. No defect.
- **Exit-code map**: BC-3.5.009/010/011 exit codes (64 / 1 / 0) confirmed consistent with `error.rs` enum arms and spec EC tables. No defect.
- **`add` path `bail!→1` divergence rationale** (vs edit exit-64): intentional product distinction per spec — add-path network failure exits 1 (unrecoverable IO), edit-path validation failure exits 64 (user-correctable). Rationale present in spec notes. No defect.
- **8-surface count agreement** (BC 624 / VP 30 / Stories 105 / Holdouts 88): all surfaces checked; STATE.md, BC-INDEX, CANONICAL-COUNTS, and prd frontmatter agree. No defect.
- **README cross-refs precision**: correctly excludes line 213 (plural context); line references in spec point to singular usage sites. No defect.
- **Holdout-BC coherence ×4**: H-NEW-COMMENT-001 through H-NEW-COMMENT-004 each map to a live BC clause; mapping confirmed accurate. No defect.
- **VP reachability spot-checks** (VP-577-027/028/006/013/029/030): each VP traces to a testable condition reachable by the specified test path. No defect.
- **Semantic anchors**: all resolve — `sd.public.comment`, `write:jira-work`, `DELETE /rest/api/3/issue/{key}/comment/{id}` paths, `--internal`/`--public` enum variants. No defect.
- **Edge-case-catalog non-intersection**: structurally confirmed — EC groups do not overlap or double-count boundary conditions. No defect.
- **Error-taxonomy sync**: Section-3 override rows (403 → 64, 404 → 64) from pass-44 fix round 47 present and consistent with BC-3.5.010 error table. No defect.

---

## Informational Non-Findings (no action required)

1. **Security-review point-in-time snapshots** (SEC-577-001..004 traces_to v1.3.28): these are expected point-in-time artifacts. Subsequent spec changes (v1.3.29–v1.3.39) did not alter the security-relevant claims those properties cover. Settled; no re-review warranted.

2. **EC-3.5.010-2(a) UserError-on-server-caused-render-failure semantics**: the spec maps a server-side 500 during comment render to a `UserError` exit (64) rather than a system error (1). The rationale is that the user can retry with a simpler body. Semantics are intentional and documented. Awareness noted; no change needed.

3. **edit vs add empty-body message divergence**: `issue comment edit` with an empty body after trim emits a different error message than `issue comment add` with an empty body. The add path emits a byte-identical message to the existing `issue comment` empty-body behavior (EC-3.5.012-2). Divergence is intentional product behavior. No defect.

---

*Pass 47 complete. Next: adversary pass 48 — if CLEAN, F2 STRICT CONVERGES (window 46+47+48).*
