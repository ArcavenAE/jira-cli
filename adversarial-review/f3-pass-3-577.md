# F3 Adversary Pass 3 — SOH-COMMENT-CRUD-1 story package (issue #577)

**VERDICT: FINDINGS — 1 HIGH + 4 MEDIUM + 4 LOW. All 9 fixed in F3 fix round 4.**

Lens: story package review — hint verbatim fidelity, dependency resolution, human-channel coverage, VP anchoring.

---

## Findings

### HIGH

**F1 HIGH — Invented `--public` hint string failed its own AC-007/VP-577-006 pin + stdin hint requirement line wrong**
`S-577-5` AC-007 pinned a byte-exact assertion on the `--public` confirmation hint, but the hint string in the story was invented (paraphrased from spec context, not the verbatim BC-3.5.008 item-1 message). The AC would have passed a stub producing any text containing a few key words, not the spec-exact message. Additionally the stdin hint requirement line pointed to the wrong BC clause. **Fix:** hint text replaced with spec-verbatim BC-3.5.008 item-1 message; requirement lines split and re-anchored to correct BC sub-clauses.

### MEDIUM

**M1 — Phantom `regex`/`once_cell` dependencies claimed current in Cargo.toml**
`S-577-1` PSI section listed `regex` and `once_cell` as dependencies to add to `Cargo.toml`, implying these are not already present. Both are already transitive dependencies; pulling them in as direct dependencies is unnecessary and would add maintenance burden. **Fix:** replaced with dependency-free char scan pattern; no new Cargo.toml entries needed.

**M2 — Human-channel echo assertions orphaned (VP-577-025 markers + VP-577-023 human variant)**
VP-577-025 (human output format for comment-add success) had no AC asserting the marker text; VP-577-023 had a JSON-only AC with no human-channel variant. **Fix:** `S-577-1` AC-005/006 updated to include marker assertions; `S-577-4` AC-001 gains human sub-assertion for the non-JSON path.

**M3 — `S-577-1` AC-012 traced BC-3.5.009 absent from story frontmatter**
AC-012 (added in pass-2 to cover EC-3.5.009-2/3/4 error paths) cited BC-3.5.009 in its body, but BC-3.5.009 was not listed in `S-577-1`'s frontmatter `bcs:` field, making the traceability matrix incomplete. **Fix:** BC-3.5.009 added to `S-577-1` frontmatter as scoped entry.

**M4 — `S-577-6` Rule 6 VP-016 mis-anchor → VP-021 v1**
`S-577-6` Rule 6 cited VP-016 (which anchors to a delete-path property) as the authority for the view-format correctness requirement. VP-021 variant 1 is the correct anchor for the single-comment view surface. **Fix:** Rule 6 VP reference changed to VP-021 v1.

### LOW

**F6 LOW — Fabricated `try_parse` precedent in PSI**
`S-577-3` PSI section stated "following the `try_parse` precedent established in the edit handler" — but no such precedent exists in the current codebase; `try_parse` is not used in the edit handler. This fabricated precedent would mislead implementers. **Fix:** replaced with INTRODUCES-wording ("this story INTRODUCES the `try_parse` pattern for comment ID validation").

**F7 LOW — Delete-confirm sketch message drift**
`S-577-3` delete-confirmation prompt sketch text drifted from the spec-verbatim wording in BC-3.5.008. AC-003 had only a single-pin assertion. **Fix:** sketch text replaced with spec verbatim; AC-003 updated to dual-pin (confirm text + cancel text).

**F8 LOW — EC-3.5.006 VP-021 cite dropped**
`S-577-4` EC-3.5.006 sub-clause had its VP-021 citation removed during a prior fix round. The VP is still reachable by this AC and should be cited. **Fix:** VP-021 citation restored to EC-006.

**F9 LOW — Shared `--id` guard re-anchored to owning-BC restatements**
Several stories referenced the `--id` required-field guard via a generic "required flag check" note rather than citing the owning BC clause. EC-3.5.005-2 (verified to exist in bc-3-issue-write.md) and BC-3.5.010 both have explicit clauses. **Fix:** guard notes updated to cite EC-3.5.005-2 and BC-3.5.010 with EC-3.5.002-1 cross-refs.

---

*Pass 3 complete. F3 fix round 4 closed all 9 findings. F3 adversary pass 4 next (STRICT; streak 0/3; trajectory →8→10→9).*
