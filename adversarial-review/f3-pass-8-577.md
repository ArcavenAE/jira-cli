# F3 Adversary Pass 8 — SOH-COMMENT-CRUD-1 story package (issue #577)

**VERDICT: FINDINGS — 0 HIGH + 2 MEDIUM + 1 LOW. All 3 fixed in F3 fix round 9.**

Lens: story package review — count propagation correctness, release-gate sketch fidelity, index accuracy.

---

## Findings

### HIGH

None.

### MEDIUM

**F-1 MEDIUM — Stale cross-story test arithmetic (22:10+12 vs actual 23:11+12; third recurrence of count-propagation class)**
`S-577-5` stated a total cross-story test count of 22 (10 from S-577-3 + 12 from S-577-5). The actual count is 23 (11 from S-577-3 + 12 from S-577-5) — S-577-3 gained an AC during fix round 7 but the derived sum in S-577-5 was not updated. This is the third recurrence of the count-propagation class in this story package. **Fix:** exhaustive sweep applied to all 3 sites containing cross-story test arithmetic; all corrected to 23:11+12.

**F-2 MEDIUM — Release-gate sketch was presence-only (functionally inert vs the regression it guards)**
`S-577-3` release-gate sketch tested that the `JR_NO_INPUT_SEAM` env var resolves correctly — a presence-only check that does not guard against the actual regression (the gate being compiled into release builds). The correct pattern is an adjacency-window idiom: assert that debug builds accept the seam and release builds ignore it, mirroring `base_url_release_gate.rs:44-72` exactly. A presence-only test passes even if the `#[cfg(debug_assertions)]` guard is removed. **Fix:** sketch replaced with adjacency-window idiom (two-assertion form: debug accepts / release ignores).

### LOW

**F-3 LOW — STORY-INDEX S-577-6 row stale "7-field" phrasing**
The STORY-INDEX row for S-577-6 described the comment view output as "7-field format." Following the pass-6 fix (removal of the invented "Body:" label), the canonical format is 6-labeled-fields + unlabeled body block. **Fix:** STORY-INDEX S-577-6 row updated to "6-labeled-fields" canonical phrasing.

---

## Informational Non-Findings

- **`try_parse` help/version pass-through**: adequately specced in S-577-3 PSI; the `--help`/`--version` short-circuit occurs before `try_parse` is called, so no special handling is needed. Non-finding.
- **`dialoguer` stdin assumption**: spec-delegated to F4 proof — the implementation story (F4) will verify `dialoguer` reads from the correct stdin handle under `--no-input`. Not a story-level gap.
- **BC-3.5.001 correctly unanchored**: BC-3.5.001 (the `jr issue comment` subcommand exists) is a pre-existing behavioral contract not introduced by this feature; unanchored status is correct. Non-finding.
- **Unused `Edit` fields clippy hygiene**: `CommentEditArgs` struct may have fields unused after dispatch. This is a clippy hygiene note for F4 implementation, not a story correctness gap.

---

*Pass 8 complete. F3 fix round 9 closed all 3 findings. F3 adversary pass 9 next (STRICT; streak 0/3; trajectory →8→10→9→5→4→4→2→3).*
