## Adversary Pass 39 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (0 HIGH + 1 MEDIUM + 4 LOW)

Lens: EXHAUSTIVE promise→pin coverage matrix + v1.3.34 newest-text + final-gate read. All 5 + 1 marginal fixed in round 44 (spec v1.3.35). Full uncovered-promise inventory produced: 22 items — 5 findings, 1 marginal (fixed), 17 adjudicated acceptable (recorded below for future-pass reference).

## F1 (LOW) edit human success line unpinned: VP-577-023 asserted exact JSON key-set for EDIT 200 but the corresponding human-mode stdout success line ("<KEY> comment <id> updated") had no VP variant — asymmetric with the DELETE human line added in v1.3.33. Fix: VP-577-023 extended with human-mode variant.

## F2 (LOW) edit-cancel JSON envelope unpinned: when the user cancels the edit prompt (empty input accepted as no-change), the spec described returning a 0-exit JSON envelope but no VP asserted the exact shape of that envelope. Fix: NEW VP-577-029 pinning the edit-cancel 200 JSON envelope (`{"key":..., "comment_id":..., "changed":false}`).

## F3 (LOW) dialoguer-Err exit-130 unpinned: EC-3.5.003-3/008-5 specified dialoguer-Err → exit 130, but no VP exercised this path end-to-end (stdin-closed / TTY-removed mid-prompt). Fix: NEW VP-577-030 with two variants (stdin-closed on add prompt; stdin-closed on confirm-delete prompt).

## F4 (LOW) JSDCLOUD-6050 hint unpinned (3 paths): EC-3.5.008-1 specified that the JSDCLOUD-6050 hint must NOT be suppressed for internal/public explicit writes on JSM issues, but VP-577-025 only pinned the add path. The edit-explicit and delete-explicit paths were unasserted. Fix: VP-577-025 extended with ×2 additional variants for edit-explicit and delete-explicit, proving the hint-not-suppressed guarantee across all three paths.

## F5 (MEDIUM) field-6 ladder rungs a/b/c unpinned: BC-3.5.010 field-6 (visibility) is the highest-churn spec text across rounds 38-44, yet VP-577-021 only had 3 variants (none, "role"+"id", "role"+"value") without pinning the individual rung-a/rung-b/rung-c discriminants. Fix: VP-577-021 variants 4/5/6 added (rung a: present+non-empty identifier; rung b: present but empty/null identifier; rung c: unknown type key).

## M1 (marginal) field-5 "No" rung: when the issue is non-JSM, field-6 line renders as "No" (non-JSM, no visibility concept); no VP variant exercised this exact "No" output. Fix: VP-577-021 variant 7 added ("JSM internal: No").

---

## Accepted Inventory (17 items — adjudicated non-finding for future-pass reference)

| # | Promise / Path | Rationale |
|---|----------------|-----------|
| 1 | Prompt text wording (add confirmation) | Byte-pinning = over-specification; user-visible UX text, not a behavioral contract |
| 2 | Prompt text wording (delete confirmation) | Same: byte-pinning = over-specification |
| 3 | Non-interactive `--public` message text | H-NEW-COMMENT-002 pins load-bearing substrings; full byte-pin = over-specification |
| 4 | `clap conflicts_with` enforcement (--public vs --internal) | Library invariant; clap enforces, no product logic to test |
| 5 | `clap conflicts_with` enforcement (--yes vs --no-input) | Library invariant |
| 6 | `clap conflicts_with` enforcement (--file vs positional) | Library invariant |
| 7 | `--dry-run` unknown-flag rejection | Library invariant; clap rejects unknown flags |
| 8 | LIST-alias permutations (`comments`, `comment list`) | Library/alias invariant; not a behavioral contract |
| 9 | EC-3.5.009-6 visibility-only edit path | Deliberate out-of-scope (F1 ruling); not in current story |
| 10 | EC-3.5.012-2 byte-identical add body re-submit | `cli_smoke.rs` covers; also overlaps EC-3.5.012-5 (wire-level) |
| 11 | `--markdown` path rendering | adf.rs-owned BCs; VP coverage lives in ADF test suite |
| 12 | `--file` / `--stdin` echo in changed_fields | Source-agnostic invariant; positional representative covers the field |
| 13 | `updated: true` value in EDIT response | No realistic regression path; value is Jira-constant |
| 14 | 401 / other-4xx clauses | Framework-level error handling; VP-577-024 covers 403 path |
| 15 | 403 body-surfacing (add path) | Mechanism shared with VP-577-024 (edit path proven) |
| 16 | 403 body-surfacing (delete path) | Mechanism shared with VP-577-024 |
| 17 | EC-3.5.006-2 / EC-3.5.007-2 non-JSM paths | Not wire-observable (no sd.public.comment property on non-JSM issues); VP-577-002/003 cover the wire shape |
