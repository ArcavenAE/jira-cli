## Adversary Pass 41 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (0 HIGH + 0 MEDIUM + 1 LOW)

Lens: wide-spectrum. Fixed in round 46 (spec v1.3.37).

## F-01 (LOW) VP-577-002/003 never pinned properties[0].key == "sd.public.comment" or array cardinality: both VPs asserted that the request body includes a non-empty properties array but never verified the key name or that exactly one entry is present. A key-name typo (e.g. `sd_public_comment`) or a stray second entry would pass all assertions while the JSM visibility change silently no-ops server-side — Jira ignores unknown property keys, producing a silent correctness failure with no HTTP error. Fix: assertion (d) added to both VP-577-002 and VP-577-003: `properties[0].key == "sd.public.comment"` AND `len(properties) == 1`.

Informational (non-findings): SEC-577-001..004 mitigations all verified applied in current spec (sanitization, rate-limit, error-surface, auth-scope); Comment-struct field-absence claim code-verified (fields not present in GET response are skipped rather than nulled); security-review SEC-577-005 label description references "Public/Internal" boolean — a point-in-time snapshot; spec now uses Yes/No/N-A rendering — noted for future security-review refresh; VP-577-027 `url::Url::path()` raw-encoding achievability confirmed (percent-encoded chars preserved by the url crate's path() accessor); delivery-obligation duplication (BC-3.5.003 item (c) and BC-3.5.006 item (c) both specify the same JSDCLOUD-6050 hint obligation) noted as future consolidation candidate.
