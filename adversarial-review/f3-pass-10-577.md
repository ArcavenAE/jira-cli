# F3 Adversary Pass 10 — SOH-COMMENT-CRUD-1 story package (issue #577)

**VERDICT: FINDINGS — 1 HIGH + 1 LOW. Both fixed in F3 fix round 11.**

Lens: story package review — flat-form migration inventory completeness, count precision.

---

## Findings

### HIGH

**F-1 HIGH — Flat-form migration inventory incomplete (AC-006 undercounted + `tests/cli_handler.rs` entirely unscoped)**
`S-577-1` AC-006 claimed the flat-form migration task covers 4 `cli_smoke` tests. The actual count is 6 — two struct-destructure patterns in `cli_smoke` would become compile errors after the CLI refactor (old `IssueArgs` flat structure replaced by `CommentArgs` subgroup). The 2 uncounted tests would cause Red Gate failures with no implementation fix possible for S-577-1 alone.

Additionally, `tests/cli_handler.rs` was entirely unscoped — 2 subprocess tests in that file assert on the old flat `jr issue comment <key> <body>` invocation and would fail with exit 2 (unknown argument) plus the migration hint after refactor. These tests were absent from AC-006's audit table.

**Fix:** AC-006 completely rewritten as two-part:
- **Part A**: six `cli_smoke` struct-destructure sites (full enumeration)
- **Part B**: `tests/cli_handler.rs` ×2 subprocess tests (exit-2 migration hint path)

Task 0 (pre-implementation scope survey), Arch-Map, and File-Structure sections updated to include the `tests/cli_handler.rs` scope.

Exhaustive 17-hit audit table added with all hits dispositioned:
- 6 hits → Part A (struct destructures in cli_smoke)
- 2 hits → Part B (subprocess tests in cli_handler.rs)
- 1 hit → item-d (CHANGELOG entry)
- 9 hits → item-e (incl. doc comment; 4 `plural-Comments` string literals confirmed out-of-scope — these reference the word "comments" in human-readable text, not code identifiers)

### LOW

**F-2 LOW — 23-count conflated ACs with test functions (AC-010 = 2 functions)**
Three sites in `S-577-5` used "23 tests" phrasing where the intended meaning was "23 ACs, 24 test functions." AC-010 contains two test functions (one per VP variant), making the function count 24 while the AC count is 23. The conflation would cause confusion when the Red Gate test-count verification step checks function counts vs AC counts. **Fix:** phrasing corrected to "23 ACs / 24 fns" at all 3 sites.

---

## Informational Non-Findings

- **EC-3.5.012-4 `--dry-run` clap-automatic**: the `--dry-run` flag is handled automatically by clap's `allow_hyphen_values` configuration for the subgroup. No VP needed, no test needed for the flag itself — covered by clap's built-in behavior. Non-finding.
- **STORY-INDEX wave-row BC range**: the BC range in the STORY-INDEX wave-row header is defensible as a span (first BC to last BC of the feature). Non-finding.

---

*Pass 10 complete. F3 fix round 11 closed both findings. F3 adversary pass 11 next (STRICT; streak 0/3; trajectory →8→10→9→5→4→4→2→3→2→2).*
