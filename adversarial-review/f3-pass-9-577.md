# F3 Adversary Pass 9 — SOH-COMMENT-CRUD-1 story package (issue #577)

**VERDICT: FINDINGS — 1 HIGH + 1 LOW. Both fixed in F3 fix round 10.**

Lens: story package review — holdout anchor correctness, fixture literal fidelity.

---

## Findings

### HIGH

**F-1 HIGH — `holdout_anchors` cyclically rotated across S-577-4/5/6 (structurally unsatisfiable assignments)**
The holdout anchor assignments across the story package were cyclically rotated:
- `S-577-4` anchored H-NEW-COMMENT-004 (should be H-NEW-COMMENT-001)
- `S-577-5` anchored H-NEW-COMMENT-001 (should be H-NEW-COMMENT-002)
- `S-577-6` anchored H-NEW-COMMENT-002 (should be H-NEW-COMMENT-004)

The most critical consequence: S-577-6 (the view story) was assigned H-NEW-COMMENT-002, which is a PUT-wire holdout testing the edit path. A view-only story has no PUT implementation — the holdout is structurally unsatisfiable for S-577-6. Any Red Gate run would require implementing edit logic inside a view story to pass the holdout.

**BONUS discovery:** H-NEW-COMMENT-005 (BC-3.5.003 delete-confirm gate) was found to be previously UNANCHORED — no story in the package owned it. This is a coverage gap that would have left BC-3.5.003 untested through the full holdout evaluation.

**Fix:** Anchor assignments un-rotated to correct mapping:
- S-577-3: `holdout_anchors: [H-NEW-COMMENT-003, H-NEW-COMMENT-005]` (delete story; adds the previously-unanchored 005)
- S-577-4: `holdout_anchors: [H-NEW-COMMENT-001]` (edit story; correct anchor)
- S-577-5: `holdout_anchors: [H-NEW-COMMENT-002]` (public-gate story; correct anchor)
- S-577-6: `holdout_anchors: [H-NEW-COMMENT-004]` (view story; correct anchor)

Full holdout map is now **5/5 exactly-once** — every holdout anchored to exactly one story, no story assigned an architecturally-mismatched holdout.

### LOW

**F-2 LOW — VP-577-022 fixture literals deviated from spec byte pins**
Two VP-577-022 fixture literals in the story package deviated from spec-exact values:
- Edit path fixture: used `"not valid!"` instead of spec-pinned `"10001;x"`
- View path fixture: used `"abc/def"` instead of spec-pinned `"../x"`

Byte-exact pins matter because VP-577-022 validates the exact error message shape returned for invalid comment IDs. **Fix:** both fixture literals aligned to spec-verbatim values (`"10001;x"` and `"../x"`).

---

## Informational Non-Findings

- **Points/index arithmetic**: story point totals and wave-index counts verified clean across all 6 stories. Non-finding.
- **All codebase reality-checks pass**: function signatures, module paths, and trait implementations cross-checked against current `src/`; no invented symbols. Non-finding.
- **Spec-vs-decomposition sequencing note**: "delete-in-S-577-1" phrasing in the master dispatch table is satisfied via the dependency chain (S-577-1 wires the dispatch; S-577-3 implements the handler). Correct decomposition — not a gap.
- **String fidelity across load-bearing pins**: all AC byte-exact assertions verified against corresponding BC/VP spec text. Non-finding.

---

*Pass 9 complete. F3 fix round 10 closed both findings. Full holdout map now 5/5 exactly-once. F3 adversary pass 10 next (STRICT; streak 0/3; trajectory →8→10→9→5→4→4→2→3→2).*
