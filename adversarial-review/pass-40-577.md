## Adversary Pass 40 — SOH-COMMENT-CRUD-1 F2 (issue #577) — verdict FINDINGS (0 HIGH + 0 MEDIUM + 3 LOW)

Lens: wide-spectrum with settled coverage adjudications honored. All 3 fixed in round 45 (spec v1.3.36). Note: first pass with zero MEDIUM+ findings; hygiene-class only.

## F-01 (LOW) BC-3.5.010 routing sentence mis-anchored view handler to ::handle_comment (add path): the prose named `handle_comment` as the implementation site for the comment-view routing table, but `handle_comment` is the add-path handler. The view path is dispatched via a distinct function. Fix: routing sentence rewritten naming `handle_comment_view` (matches the delta-analysis PF-017 naming).

## F-02 (LOW) SEC-577-001 cited 3× without definitional pointer — PREMISE CORRECTED by orchestrator: adversary flagged SEC-577-001 as undefined (searched only `.factory/specs/`). Orchestrator corrected: SEC-577-001 IS defined at `phase-f2-spec-evolution/security-review-577.md:51`. Fix: first-cite definitional pointer added at BC-3.5.007 (~line 2376) so future readers can locate the definition without a filesystem search.

## F-03 (LOW) VP-577-013 JSON-literal notation ambiguous vs sibling VP-577-029 BTreeSet: VP-577-013 used a JSON-literal `{"key":...}` notation for the view-response key-set while VP-577-029 used an exact BTreeSet clause — inconsistent within the VP family. Orchestrator adjudicated: harmonize to BTreeSet. Fix: VP-577-013 exact key-set clause added, consistent with sibling VPs (VP-577-023, VP-577-029).
