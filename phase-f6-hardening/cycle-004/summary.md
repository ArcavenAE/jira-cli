# Phase F6 Targeted Hardening — cycle-004 (windows-correctness) — VERDICT: PASS/COMPLETE

Scope: cycle-004 delta (baseline 42e92b46 → develop 024de4d8). Info-asymmetry wall honored (formal-verifier + security-reviewer did not read F5 findings).

- Kani (formal): JUSTIFIED SKIP — not provisioned in-repo (cycle-002/003 precedent); proptest/unit substitution. VP coverage: 0 GAP across all 14 new VPs (VP-AUTHDX-010..023). See kani-results.md.
- Fuzz (cargo-fuzz): JUSTIFIED SKIP — not provisioned (cycle-002/003 precedent); proptest arbitrary-input substitution, 0 uncovered input surface (DPAPI envelope parse, tenant_info JSON+64KiB body-cap, profile-name guard, cloud_id plausibility). See fuzz-results.md.
- Mutation: PROCESS GAP FOUND + PARTIALLY CLOSED. None of the cycle-004 files were in .cargo/mutants.toml examine_globs → CI mutation had ZERO signal on the delta (cargo mutants --in-diff returned zero mutants). Via explicit --file override, the pure/decision logic killed 97-100% on the default-CI-testable, host-compilable surface (meets >=95% credential-module bar): DPAPI envelope encode/decode/wrap/unwrap, reject_unsafe_profile_component (CWE-22 guard), is_reserved_windows_device_name, should_fallback_to_dpapi, is_plausible_cloud_id, validate_and_trim_site_url, fetch_cloud_id soft-fail, legacy_none_orphan_clear_target, should_mark_auth_method_before_attempt, classify_dpapi_removal_result. Sub-90% raw rates = spec-declared boundaries only: keyring-gated (VP-AUTHDX-005/006/007; killable under JR_RUN_KEYRING_TESTS=1) + Windows-#[cfg] DPAPI (VP-AUTHDX-010; killable on windows-latest). The 5 genuine tenant.rs body-cap-boundary survivors were KILLED via FIX-F6-1 (PR #775 @ 024de4d8) — tenant.rs now 21/21 (100%) and added to examine_globs. See mutation-results.md + code-delivery/FIX-F6-1-pr-review.md.
- Security (Touchpoint #3): CLEAN — no CRIT/HIGH. cargo audit + cargo deny clean (only the pre-existing accepted chacha20-yanked skip, DEC-185); new dep windows-sys 0.60 is cfg(windows)-scoped, zero new graph nodes (transitive via keyring). Manual CWE pass clean (CWE-532 no leakage; DPAPI unsafe FFI USER-scope-only+null-blob-guarded; CWE-22 path traversal closed by layered guards; tenant_info SSRF surface closed — https-only/no-redirect/size-capped/seam-release-gated; no credential over-deletion). DEC-334 confirmed. See security-scan-results.md.
- Regression: full suite GREEN (CI 3-OS matrix + local 4900+/0).
- DTU adversarial: SKIPPED (dtu_required:false; tenant_info is a real endpoint, not a cloned DTU). Accessibility: SKIPPED (backend, no UI).

Verdict: F6 COMPLETE. All hardening checks pass or justified-skip; the one genuine test-quality gap (tenant.rs body-cap) fixed in-cycle; the mutation examine_globs coverage gap partially closed (tenant.rs) with the remainder deferred (see follow-up).
