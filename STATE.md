---
document_type: pipeline-state
level: ops
version: "3.58"
status: active
producer: state-manager
timestamp: 2026-09-04T05:17:21Z
phase: F2
pipeline: ACTIVE
inputs: []
input-hash: "[live-state]"
traces_to: ""
project: jira-cli
mode: brownfield
current_step: "D-chain cite D-453 latest brownfield (unchanged). trajectory-tail →1→3→0→2 (unchanged this burst). F2 SCOPED ADVERSARIAL CONVERGENCE INTERMEDIATE CHECKPOINT #3: Passes 7-8 ran since Burst 5 — Pass 7 (3 findings: 0 CRIT/0 HIGH/1 MED/2 LOW, novelty MED-LOW, close-to-converged) and Pass 8 (3 findings: 0 CRIT/1 HIGH/2 MED/0 LOW, novelty MED — the HIGH + 2 MED landed on the CLEAR path, auth logout/remove, a surface untouched by Passes 1-7) — both resolved via architect->product-owner->formal-verifier fix chains. Full trajectory 17->9->5->4->4->4->[sweep 3]->3->3. Clean-streak 0/3 (no clean pass yet). A human convergence decision (continue toward 3 consecutive clean vs. gate now with documented residuals) is being put to the operator at this checkpoint. develop unchanged @ 42e92b46."
trajectory_tail: "→1→3→0→2"
maintenance_run:
  status: COMPLETE
  date: 2026-08-25
  findings_count: 10
  fixes_applied: 6
  fixes_pending: 4
  pr: 737
current_cycle: "cycle-004"
feature_mode_bundle: windows-correctness
dtu_required: false
phase_2_status: APPROVED
phase_2_approved_at: 2026-05-07
phase_3_status: SOH-ATTACHMENTS-1 F5 SCOPED ADVERSARIAL CONVERGED 2026-07-24 STRICT (14 rounds; window pass-12/pass-13/pass-14 CLEANx3; spec v1.3.99 to v1.3.106; BC-INDEX v6.38 to v6.44; develop @ db207b81) + F7 DELTA CONVERGENCE APPROVED 2026-07-25 (DEC-186; 5-dim PASS; MAXIMUM_VIABLE_REFINEMENT_REACHED) + CYCLE FULLY CLOSED 2026-07-25
cycle_001_status: "list-read-ergonomics -- CLOSED (DEC-309), historical; see cycles/cycle-001/"
cycle_002_status: "field-dx -- CLOSED + RELEASED 2026-09-01 (DEC-311 close; v0.7.0-dev.3 tagged @ 87f17aff, release.yml run 33459579699 triggered). All 5 stories delivered/merged (F4); F5 CONVERGED (FIX-F5-001, PR #747); F6 COMPLETE (FIX-F6-001, PR #749); F7 COMPLETE (5-dim convergence PASS + full regression PASS 4660/0/106, FIX-F7-001, PR #750 @ 2000c455); RELEASED (version bump PR #751 @ 87f17aff, tag v0.7.0-dev.3 pushed, release.yml triggered). Pipeline SHIPPED."
cycle_003_status: "auth-profile-dx -- CLOSED + RELEASED 2026-09-03 (v0.7.0-dev.4 @ 42e92b46, PR #767; release.yml run 33769389700 SUCCESS; GitHub prerelease published, 10 assets/5 targets). F1-F7 complete + released. Pipeline shipped."
cycle_004_status: "windows-correctness -- F1 delta-analysis APPROVED at human gate 2026-09-03 (DEC-335); Phase F2 spec-evolution IN PROGRESS -- architect + product-owner + formal-verifier F2 deliverables ALL COMPLETE (742 BCs, 55 VPs). F2 SCOPED ADVERSARIAL CONVERGENCE IN PROGRESS -- 8 fresh-context passes run (17->9->5->4->4->4->3->3 findings, all fixed) plus one post-Pass-6 fresh-context consistency-validator sweep (3 findings, all fixed); clean-streak 0/3. AWAITING HUMAN CONVERGENCE DECISION: continue toward 3-consecutive-clean (dispatch Pass 9) OR proceed to F2 human gate now with residuals documented. 4-story scope: dpapi-storage-fix + honest-fail-message (bundled into one release), windows-docs, cloud_id-correctness (incl. A-PA-LOW-001). windows-vs-windows-sys dependency decision RESOLVED by ADR-0021 (windows-sys 0.60.2, already-present transitive dep, promoted to direct)."
activation_head: "42e92b46"
activation_version: "v0.7.0-dev.4"
---

<!-- STATE.md SIZE BUDGET (2026-09-03, cycle-004 F2 ADVERSARIAL-CONVERGENCE burst / Burst 6):
     318 lines (wc-l) -- this burst makes Pass-7 and Pass-8 fix work durable in one
     atomic commit and brings STATE.md current -- it had drifted 2 passes behind reality
     (still read "Pass 7 NEXT"). soft-target 200; hard cap 500.
     margin from soft-target = 118 -- 118 lines OVER the soft target of 200 (documented,
     ongoing known deviation across cycles-002/003/004, not a blocker). margin from actual
     (hard cap) = 182 lines of headroom remain before the hard cap of 500. Phase Progress/
     Current Phase Steps/Convergence Status/Concurrent Cycles/Constraints Carried Forward/
     Drift-Standing/Session Resume Checkpoint all updated in place. RECOVERY CONTEXT: this
     session resumed cleanly after the Burst-5 checkpoint (`f59f29e2`) -- no crash or
     agent-stall this burst. Since Burst 5: (1) adversarial Pass 7 (3 findings: 0 CRIT/0
     HIGH/1 MED/2 LOW, novelty MED-LOW -- close to converged) resolved via architect->
     product-owner->formal-verifier fix chain; (2) adversarial Pass 8 (3 findings: 0 CRIT/1
     HIGH/2 MED/0 LOW, novelty MED -- the HIGH + 2 MED landed on the CLEAR path, auth
     logout/remove, a surface untouched by Passes 1-7) resolved via the same fix-chain
     pattern -- Pass 8's fix reworks `clear_profile_oauth_pair`/`clear_profile_creds` to
     route DPAPI-file removal through a new `clear_dpapi_file_tolerating_path_escape`
     adapter that maps `ProfilePathEscape` to `Ok(())`, so a pre-existing Unix profile
     named e.g. `con` still clears cleanly on every OS (BC-1.4.038 new Invariant 3 +
     EC-1.4.038-5/6, ADR-0021 §7), pinned by a new VP-018 clear-path swallow oracle. Pass 8
     also corrected an overstated VP CI-classification tally (Finding #1, an honesty fix --
     see Convergence Status below) and flagged `--cloud-id` as not durable across `auth
     refresh` (EC-1.2.052-5). Full adversarial trajectory:
     17->9->5->4->4->4->[post-Pass-6 sweep 3]->3->3. Clean-streak 0/3 -- no clean pass yet.
     This burst: (1) committed the accumulated Pass-7/Pass-8 fix-round artifacts
     (`vp-delta.md`, `architecture-delta.md`, `ADR-0021`, `bc-1-auth-identity.md` -- no new
     files this burst) to factory-artifacts in one atomic commit alongside this STATE.md
     update and a new cycle-004 burst-log Burst 6 entry; (2) updated STATE.md's Phase
     Progress F2-SPEC-EVOLUTION row, Current Phase Steps table (Passes 7-8 marked DONE),
     Convergence Status / Concurrent Cycles / Constraints Carried Forward / Drift-Standing
     prose (adversarial trajectory extended, corrected VP CI-classification tally,
     clean-streak 0/3), and Session Resume Checkpoint (recording NEXT-on-resume as
     AWAITING HUMAN CONVERGENCE DECISION -- continue toward 3-consecutive-clean [dispatch
     Pass 9] OR gate now with documented residuals [re-run consistency-validator, then
     present the F2 gate]). No new DEC recorded -- this is convergence bookkeeping, and
     the convergence-strategy choice itself is explicitly deferred to the human at this
     checkpoint rather than decided unilaterally; DEC-335 (the F1 human gate) remains the
     latest Decisions Log entry. One full-content Write, no Edit chain (DEC-247). Hygiene:
     the three pre-existing-dirty files unrelated to any cycle (`regression-state.json`,
     `sidecar-learning.md`, the modified `S-cycle3-env-tag` demo gif) remain explicitly NOT
     staged this burst, consistent with every prior burst. -->

# Pipeline State: jira-cli

## Project Metadata

| Field | Value |
|-------|-------|
| **Product** | jr (Jira CLI) |
| **Mode** | BROWNFIELD / Rust |
| **Target Workspace** | develop to main |
| **trajectory-tail** | →1→3→0→2 (unchanged this burst) |
| **Last Updated** | F2 SCOPED ADVERSARIAL CONVERGENCE checkpoint #3 (2026-09-03): adversarial Passes 7-8 (3 vs 3 findings — Pass 7 close-to-converged; Pass 8's HIGH+2 MED landed on the previously-untouched CLEAR path, auth logout/remove) resolved via architect / product-owner / formal-verifier fix chains — previously uncommitted since Burst 5 — now durable in one atomic commit. Clean-streak 0/3; **AWAITING HUMAN CONVERGENCE DECISION** (continue to Pass 9 vs. gate now with documented residuals). `develop` unchanged at `42e92b46`. cycle-001/002/003 remain CLOSED. trajectory-tail →1→3→0→2 (unchanged this burst). |
| **Current Phase** | Feature Mode cycle-004 (`windows-correctness`) -- **Phase F2 (spec evolution), IN PROGRESS** -- architect + product-owner + formal-verifier steps ALL DONE; F2 scoped adversarial convergence IN PROGRESS (Passes 1-8 done+fixed + one consistency sweep done+fixed); **AWAITING HUMAN CONVERGENCE DECISION**. cycle-001, cycle-002, and cycle-003 remain CLOSED, historical. |
| **Activation HEAD** | `42e92b46` (`develop` tip; unchanged this burst -- no develop-side commit, cycle-004 has not started implementation) |

## Phase Progress (recent; full history in cycles/cycle-001/burst-log.md, cycles/cycle-002/burst-log.md, cycles/cycle-003/burst-log.md, cycles/cycle-004/burst-log.md, and factory-artifacts@43f4a5e3)

| Phase | Status | Completed | Gate | Notes | Finding Progression |
|-------|--------|-----------|------|-------|---------------------|
| F7-DELTA-CONVERGENCE (cycle-003) | APPROVED — CONVERGED | 2026-09-03 | human-approved F7 gate; 3-clean-passes bar confirmed satisfied at F5 | 5/5 dimensions PASS: spec-consistency, tests (regression 4763 passed/0 failed/157 ignored), implementation (mutation 100%, security 0 CRIT/0 HIGH), holdout evaluation (0.895), documentation. HIGH-3 (stale `docs/specs/multi-profile-auth.md`) + LOW-2 + LOW-3 resolved via docs PR #766 (squash commit `c9bb74f4`, CI `ci-gate` 15/15 green) per human directive, BEFORE release. Full detail: `cycles/cycle-003/phase-f7-convergence/delta-convergence-report.md`, `traceability-chain-delta.md`, `holdout-eval-delta.md`. | 733 BCs unchanged; 41 VPs unchanged |
| RELEASE v0.7.0-dev.4 (cycle-003) | RELEASED — SHIPPED | 2026-09-03 | human-authorized dev release; `release.yml` run `33769389700` SUCCESS | Human authorized "Proceed with the Dev release." Version-bump PR #767 squash-merged to `develop` (`c9bb74f4` → `42e92b46`); annotated tag `v0.7.0-dev.4` pushed at `42e92b46`; `release.yml` run `33769389700` concluded SUCCESS; GitHub prerelease published 2026-09-03T14:58:38Z with 10 assets (5 targets × archive+sha256: x86_64/aarch64 linux-gnu, x86_64/aarch64 apple-darwin, x86_64 windows-msvc). Cargo.toml/lock @ 0.7.0-dev.4; CHANGELOG [Unreleased] → [0.7.0-dev.4] - 2026-09-03. cycle-003 `auth-profile-dx` is now CLOSED. | PR #767 @ `42e92b46`; tag `v0.7.0-dev.4`; GitHub prerelease, 10 assets/5 targets — counts unchanged |
| F1-DELTA-ANALYSIS (cycle-004) | APPROVED | 2026-09-03 | human-approved F1 gate (DEC-335) | #759+#760 bundle EXPANDED to a 4-story scope: `dpapi-storage-fix` + `honest-fail-message` (bundled into one release) + `windows-docs` + `cloud_id-correctness` (human-added, also closes `A-PA-LOW-001`). Windows-validation plan ACCEPTED: F4 CI spike + REQUIRED F7 manual Windows smoke gate. `windows`-vs-`windows-sys` dependency choice DEFERRED to a F2 ADR; `clear_all_credentials` confirmed untouched (test-only). Full detail: `cycles/cycle-004/phase-f1-delta-analysis/delta-analysis.md`, `affected-files.txt`. | counts unchanged (733/41/106/168) |
| **F2-SPEC-EVOLUTION (cycle-004)** | **IN PROGRESS** | 2026-09-03 (partial — this burst) | (feature-mode spec evolution, no human gate at entry) | architect (ADR-0021, ADR-0022, architecture-delta.md), product-owner (PRD delta: +9 BCs/1 amended, total_bcs 733→742), and formal-verifier (VP delta: +14 VPs, 41→55) F2 deliverables ALL COMPLETE. **F2 scoped adversarial convergence IN PROGRESS:** 8 fresh-context passes run against the spec delta, each followed by a full architect→product-owner (and, for VP-level closures, formal-verifier) fix chain — Pass 1 (17 findings), Pass 2 (9, novelty MED), Pass 3 (5, novelty MOD-HIGH), Pass 4 (4, novelty MOD-LOW), Pass 5 (4, novelty substantive), Pass 6 (4, novelty HIGH), Pass 7 (3: 0 CRIT/0 HIGH/1 MED/2 LOW, novelty MED-LOW — close to converged), Pass 8 (3: 0 CRIT/1 HIGH/2 MED/0 LOW, novelty MED — the HIGH + 2 MED landed on the CLEAR path, `auth logout`/`auth remove`, a surface untouched by Passes 1-7). All findings from all 8 passes resolved. A fresh-context comprehensive cross-file consistency-validator sweep ran post-Pass-6 (3 findings, all fixed). Pass 8's fix reworks the CLEAR path's DPAPI-file removal to tolerate a path-escaping legacy profile name (BC-1.4.038 new Invariant 3), and separately corrects an earlier-round overstatement of the new VPs' CI-coverage classification (see Convergence Status). Clean-streak 0/3 — 3 CONSECUTIVE clean passes required to converge; none clean yet. **A human convergence decision (continue toward 3-clean vs. gate now with documented residuals) is being put to the operator at this checkpoint.** Reserved Windows device-name set finalized at 30 (ADR-0021 §9). | counts: **742 BCs** unchanged this burst; **55 VPs** unchanged since Pass 6 (14 new, VP-AUTHDX-010..023); 106 holdouts unchanged; 168 stories unchanged. Adversarial trajectory: **17→9→5→4→4→4→[sweep 3]→3→3** (clean 0/3, AWAITING HUMAN CONVERGENCE DECISION) |

## Current Phase Steps (cycle-004, Phase F2 spec evolution)

| Step | Status | Notes |
|------|--------|-------|
| F1 human gate | APPROVED | DEC-335: standard F1→F7 route; 4-story scope confirmed; Windows-validation plan accepted; `windows`-vs-`windows-sys` deferred to F2 |
| F2 architect / product-owner / formal-verifier deliverables | DONE | ADR-0021, ADR-0022, architecture-delta.md; PRD delta (+9 BCs/1 amended, 733→742); VP delta (+14 VPs, 41→55). Full detail archived to `cycles/cycle-004/burst-log.md` Bursts 3-4. |
| F2 scoped adversarial convergence — Passes 1-4 | DONE — FIXED | 17→9→5→4 findings, all resolved. Full detail archived to `cycles/cycle-004/burst-log.md` Burst 4. |
| F2 scoped adversarial convergence — Passes 5-6 | DONE — FIXED | 4→4 findings, both HIGHs per pass second-order/propagation defects, all resolved via architect→product-owner→formal-verifier chains. Full detail archived to `cycles/cycle-004/burst-log.md` Burst 5. |
| F2 fresh-context consistency-validator sweep (post-Pass-6) | DONE — FIXED | 3 findings (0 CRIT/0 HIGH/2 MED/1 LOW), all fixed; assessed corpus as unusually clean after 6 rounds. |
| F2 scoped adversarial convergence — Pass 7 | DONE — FIXED | 3 findings (0 CRIT/0 HIGH/1 MED/2 LOW), novelty MED-LOW — assessed as close to converged; resolved via architect→product-owner→formal-verifier fix chain |
| F2 scoped adversarial convergence — Pass 8 | DONE — FIXED | 3 findings (0 CRIT/1 HIGH/2 MED/0 LOW), novelty MED — HIGH + 2 MED all landed on the CLEAR path (`auth logout`/`auth remove`), a surface Passes 1-7 had not touched; fix adds BC-1.4.038 Invariant 3 (`clear_dpapi_file_tolerating_path_escape` adapter) + VP-018 clear-path swallow oracle, and corrects an overstated VP CI-classification tally (Finding #1) |
| cycle-004 STATE.md adversarial-convergence checkpoint #3 | DONE | This burst — one full-content Write; Passes 7-8 fix work committed; STATE.md's 2-pass staleness corrected; no new DEC (convergence-strategy choice deferred to the human, not decided unilaterally) |
| F2 human convergence decision | PENDING | Operator to choose: (a) continue adversarial convergence — dispatch Pass 9, keep going toward 3 CONSECUTIVE clean passes (clean-streak currently 0/3); or (b) proceed to the F2 human gate now, with the current residual-free-but-not-yet-3x-clean state documented as the convergence rationale |
| F2 scoped adversarial convergence — Pass 9 | CONDITIONAL — NEXT IF CONTINUING | Dispatch only if the operator selects option (a) above |
| F2 consistency-validator final audit | PENDING | Re-run only if further fixes land after Pass 7-8 (the post-Pass-6 sweep already covers corpus state through Pass 6; Pass 7-8 fixes are small and BC/VP-body-scoped); runs before the F2 human gate regardless of which convergence option is chosen |
| F2 human gate | PENDING | Final human approval of the F2 spec delta before F3 incremental stories |

(Prior cycle-004 Burst-1 through Burst-5 steps — human triage, pre-cycle research pass, DEC-334, scaffolding bootstrap, F1 dispatch, F1 human gate/DEC-335, crash recovery of architect+product-owner deliverables, formal-verifier VP delta, adversarial Passes 1-6, post-Pass-6 consistency sweep — archived to `cycles/cycle-004/burst-log.md` Bursts 1-5.)

## Decisions Log

| ID | Decision | Rationale | Phase | Date | Made By |
|----|----------|-----------|-------|------|---------|
| DEC-335 | cycle-004 F1 delta-analysis APPROVED at human gate — standard F1→F7 route, 4 stories (`dpapi-storage-fix` + `honest-fail-message` bundled into one release, `windows-docs`, `cloud_id-correctness` incl. `A-PA-LOW-001`). Windows-validation plan: F4 CI spike + required F7 manual Windows smoke gate. `windows`-vs-`windows-sys` dependency choice deferred to F2 ADR; `clear_all_credentials` untouched (test-only). #759 severity HIGH (bug-fix), not expedited | Human reviewed the architect's F1 report (§13 open questions: scope, `cloud_id` fix inclusion, Windows-validation acceptability, `windows`-crate decision, `clear_all_credentials` exclusion, sequencing) and answered all six: scope EXPANDED (not merely the #759+#760 bundle) by folding in the `cloud_id` auto-discovery FIX itself (not just #760's doc caveat) as a 4th story, since it directly closes a pre-existing carried-forward standing item (`A-PA-LOW-001`) rather than leaving it open alongside a new cycle touching the same auth surface; `honest-fail-message` bundled with `dpapi-storage-fix` into one release (vs. the report's suggested independent fast-follow sequencing) to avoid shipping a user-facing message change ahead of the underlying fix it describes; Windows-validation plan accepted without modification; the `windows`-vs-`windows-sys` dependency tradeoff needs the architect's F2 recommendation against a real `cargo add` dry-run, not a same-gate decision; `clear_all_credentials` has no production call sites and stays untouched per its own do-not-reintroduce warning | F1 | 2026-09-03 | human |
| DEC-334 | Human authorized a new Feature-Mode cycle (cycle-004 `windows-correctness`) bundling GitHub issues #759 + #760. #759 fix strategy = keyring-first + user-scope DPAPI-encrypted file fallback (%LOCALAPPDATA%) for oversized OAuth access+refresh tokens (atomic) + honest-fail backstop (match `keyring::Error::TooLong`, accurate message, explicit grant-revoke); chunking and scope-trim rejected after research validation (`.factory/research/win-oauth-keychain-blob-limit-2026-09-03.md`). #760 (README Windows install + config-path + cloud_id caveat) bundled in | #759 is a live, high-impact defect on `v0.7.0-dev.4` — cycle-003 (DEC-313) just made OAuth the default at profile creation, so every fresh Windows OAuth login is affected; the research pass ruled out chunking (no peer precedent) and scope-trimming (not durable) before recommending the DPAPI-file peer-standard. #760 is a low-risk docs fix naturally bundled with the same Windows-correctness theme, and its cloud_id caveat overlaps a cycle-003 carried-forward standing item (`A-PA-LOW-001`) | F1 | 2026-09-03 | human |
| DEC-333 | Human authorized and executed the cycle-003 dev release **v0.7.0-dev.4** (version-bump PR #767 squash-merged to `develop` @ `42e92b46`, annotated tag `v0.7.0-dev.4` pushed, `release.yml` run `33769389700` concluded SUCCESS, GitHub prerelease published with 10 assets/5 targets). cycle-003 (`auth-profile-dx`) is now **CLOSED** | F7 (delta convergence) reached human-approved CONVERGENCE at DEC-332 with the one outstanding HIGH finding already resolved via a pre-release docs PR; the human then explicitly triggered the held release action, completing the cycle's final gate and closing it | RELEASE | 2026-09-03 | human |
| (330 older cycle-003/002/001 decisions) | DEC-332 through DEC-309 and earlier — unchanged this burst | — | F1-F7/historical | 2026-08-24…2026-09-03 | various — see `cycles/cycle-003/burst-log.md` Bursts 13-22 for the full listing |

**cycle-004 note (Burst 6, this burst — F2 ADVERSARIAL-CONVERGENCE CHECKPOINT #3):** No new DEC this burst. DEC-335 (F1 human gate) remains the latest decision. Adversarial Passes 7-8 and their fix chains are codified spec-refinement content (VP delta extension, BC-body amendments, ADR-0021 correction), not a human decision requiring its own DEC entry — they were produced under the standard F2 spec-evolution mandate DEC-335 already authorized. The convergence-strategy choice (continue vs. gate now) IS being explicitly routed to the human at this checkpoint, but as a pending decision, not yet a recorded one — it will receive its own DEC once made.

## Skip Log

| Step | Skipped? | Justification |
|------|----------|----------------|
| UX Spec (cycle-002) | yes | `jr` is a CLI-only product; field-dx bundle adds no UI surfaces. |
| DTU creation (cycle-002) | yes | `dtu_required: false` -- no external service behavior is being cloned by this bundle. |
| F5 secondary review-tier (Step 7, cycle-002) | yes | Every story already individually adversarially converged in F4; primary pass found only 1 low-likelihood MED + 4 LOW. |
| F6 Kani formal verification (cycle-002) | yes | Not set up in repo; proptest substitution justified (32/32 VPs covered, 0 GAP). |
| F6 cargo-fuzz (cycle-002) | yes | Not set up in repo; proptest arbitrary-input substitution justified (0 uncovered input surface). |
| F6 DTU adversarial testing / accessibility re-check (cycle-002) | yes | `dtu_required: false`; `feature_type: backend-cli`, no UI surface. |
| UX Spec (cycle-003) | yes | `jr` is CLI-only; auth-profile-dx confirmed no-UI-surface at F1/F2, same as cycle-002. |
| DTU creation (cycle-003) | yes | `dtu_required: false` -- no external service behavior is being cloned; auth flows target the real Atlassian OAuth/token endpoints already covered by existing DTU-not-required precedent. |
| Demo recording (cycle-003, Waves 4-5) | yes | Human decision (standing since post-PR#757): demos skipped for Wave 4's two stories and Wave 5's final story. |
| F6 Kani formal verification (cycle-003) | yes | Not set up in repo; proptest substitution justified — VP-AUTHDX-001..009 all covered, 0 GAP. |
| F6 cargo-fuzz (cycle-003) | yes | Not set up in repo; proptest arbitrary-input substitution justified, same precedent as cycle-002. |
| UX Spec (cycle-004) | yes | `jr` is CLI-only; F1 delta-analysis (architect, §8) explicitly confirmed `feature_type: backend (infrastructure; no UI)` across all 4 stories — no UX Spec needed, same precedent as cycle-002/003. |
| DTU creation (cycle-004, tentative) | tbd | Likely `dtu_required: false` -- #759's DPAPI-file fallback targets the OS keychain/filesystem, not a third-party service being cloned. F1 delta-analysis did not explicitly re-confirm this -- settle explicitly at F2. |

## Blocking Issues

<!-- Open issues only. Move resolved issues to cycles/<cycle>/blocking-issues-resolved.md. -->

| ID | Issue | Severity | Blocking Phase | Owner | Resolution |
|----|-------|----------|-----------------|-------|------------|
| (none currently open) | -- the DEC-namespace disambiguation question is tracked debt, not a hard blocker | -- | -- | -- | -- |

## Convergence Status

`cycle-001` (`list-read-ergonomics`) CLOSED at F7 (DEC-309): historical, unchanged this burst.

`cycle-002` (`field-dx`) F2-F7 COMPLETE, human-authorized at the F7 gate (DEC-311). **RELEASED 2026-09-01 as `v0.7.0-dev.3`.** Historical, unchanged this burst.

`cycle-003` (`auth-profile-dx`) F1-F7 all COMPLETE, human-approved at every gate. **RELEASED 2026-09-03 as `v0.7.0-dev.4`** (DEC-333). **cycle-003 is CLOSED** — SHIPPED, historical, unchanged this burst. Outstanding, non-blocking, carried to a future maintenance/self-improvement cycle: MED-1 (VP count, 41, unverified line-by-line), LOW-4/LOW-6 (doc nits), the 4-story template-compliance gap, the cycle-003 bookkeeping input-hash cascade (6 files), and the two S-7.02 process-gap deferrals (STORY-INDEX index-currency; story-template-compliance) — all recorded in `cycles/cycle-003/lessons.md` and Drift/Standing below.

`cycle-004` (`windows-correctness`) **F1 APPROVED (DEC-335) — Phase F2 (spec evolution) IN PROGRESS, scoped adversarial convergence underway; a human convergence decision is now pending.** architect (ADR-0021 — Windows OAuth keyring-first + user-scope DPAPI-encrypted-file fallback, also resolving the F1-deferred `windows`-vs-`windows-sys` question in favor of `windows-sys` 0.60.2; ADR-0022 — API-token `cloud_id` via `GET /_edge/tenant_info`, closing `A-PA-LOW-001`), product-owner (PRD delta — 9 NEW BCs [BC-1.4.035..040, BC-1.2.052..054] + 1 AMENDED [BC-1.4.028], plus a new Invariant 3 on BC-1.4.038 added this burst), and formal-verifier (VP delta — 14 NEW VPs, VP-AUTHDX-010..023) F2 deliverables are ALL COMPLETE. Since then, 8 fresh-context scoped adversarial passes have run against the spec delta, each followed by a full fix chain: Pass 1 (17 findings), Pass 2 (9, novelty MED), Pass 3 (5, novelty MOD-HIGH), Pass 4 (4, novelty MOD-LOW), Pass 5 (4, novelty substantive), Pass 6 (4, novelty HIGH), Pass 7 (3: 0 CRIT/0 HIGH/1 MED/2 LOW, novelty MED-LOW — assessed as close to converged), Pass 8 (3: 0 CRIT/1 HIGH/2 MED/0 LOW, novelty MED — the HIGH + 2 MED all landed on the CLEAR path, `auth logout`/`auth remove`, a surface Passes 1-7 had never touched). All findings resolved. A fresh-context comprehensive cross-file consistency-validator sweep ran post-Pass-6 (3 findings, 0 CRIT/0 HIGH/2 MED/1 LOW, all fixed). **Pass 8 design outcomes (this burst):** the seam-wording contract was tightened — `should_fallback_to_dpapi(err)` is now documented as returning `true` **iff** the error is `keyring::Error::TooLong`, not unconditionally on any keyring failure; `--cloud-id` was confirmed NOT durable across `auth refresh` (EC-1.2.052-5 — a documented, non-blocking residual, not fixed this burst); and — the substantive fix — the CLEAR path (`auth logout`/`auth remove`) was found to have a real backward-compatibility gap: `clear_profile_oauth_pair`/`clear_profile_creds` called the DPAPI-file removal helper directly, which could return `ProfilePathEscape` for a pre-existing profile name that predates the path-traversal guard (e.g. a Unix profile literally named `con`), causing the clear operation to fail where every other backend clears successfully. Fixed via a new `clear_dpapi_file_tolerating_path_escape` adapter (BC-1.4.038 new Invariant 3 + EC-1.4.038-5/6, ADR-0021 §7) that maps `ProfilePathEscape` to `Ok(())` at this one call site only — clearing must be permissive even where storing/reading is deliberately strict. A new VP-018 pins this clear-path swallow behavior. **Pass 8 also issued an honesty correction (Finding #1) to the VP CI-classification tally that earlier passes had left overstated:** of the 14 new VPs, **10 are fully default-CI** (VP-AUTHDX-013, 014, 015, 016, 017, 018, 019, 020, 021, 023), **2 have a default-CI portion plus a keyring-gated state core** (VP-AUTHDX-011, 012), **1 has a keyring-gated core plus a Windows-only real-DPAPI tail** (VP-AUTHDX-022), and **1 is Windows-only** (VP-AUTHDX-010) — this supersedes the earlier, overstated claim of "13 of 14 default CI." **Clean-streak 0/3 — three CONSECUTIVE clean passes are required to converge; none has yet been clean.** **A human convergence decision is being put to the operator at this checkpoint:** continue adversarial convergence (dispatch Pass 9, keep going toward 3 consecutive clean) OR proceed to the F2 human gate now with the current state — 8 passes run, all findings resolved, no CRIT ever found, finding severity trending down (novelty MED-LOW at Pass 7) except for the one genuinely new CLEAR-path surface Pass 8 caught — documented as the convergence rationale. **Counts: total_bcs unchanged at 742 this burst (Passes 7-8 were BC-body refinements, not new BCs); VPs unchanged at 55 since Pass 6** (14 new total, VP-AUTHDX-010..023); holdout scenarios unchanged at 106; stories unchanged at 168. Reserved Windows device-name set finalized at 30 (ADR-0021 §9). **Next:** await the human's convergence-strategy decision; if continuing, dispatch adversarial Pass 9 (fresh context); if gating, re-run the consistency-validator to confirm the Pass 7-8 fixes introduced no new cross-file drift, then present the F2 human gate.

**cycle-004 is the sole cycle with open work.** cycle-001, cycle-002, and cycle-003 are all CLOSED. **Next:** human convergence decision → (Pass 9 and repeat until 3 consecutive clean) OR (final consistency-validator confirmation → F2 human gate) → F3 incremental stories.

## Concurrent Cycles

Four tracked cycles, **cycle-004 is the sole OPEN cycle — cycle-001, cycle-002, and cycle-003 are all CLOSED, no open work.** `cycle-001` is CLOSED, historical. `cycle-002` (`field-dx`) is **CLOSED + RELEASED** (2026-09-01), historical. `cycle-003` (`auth-profile-dx`) is **CLOSED + RELEASED** (2026-09-03, DEC-333) as **`v0.7.0-dev.4`** @ `42e92b46`, historical: F1-F7 all complete, all 7 stories merged, F5 CONVERGED, F6 PASS, F7 APPROVED — CONVERGED, RELEASED. `cycle-004` (`windows-correctness`) is **OPEN, Phase F2 (spec evolution) IN PROGRESS** (F1 APPROVED at human gate, DEC-335, 2026-09-03): architect + product-owner + formal-verifier F2 steps ALL DONE; F2 scoped adversarial convergence IN PROGRESS — 8 passes run (17→9→5→4→4→4→3→3 findings, all fixed) plus a post-Pass-6 consistency-validator sweep (3 findings, all fixed), clean-streak 0/3, **AWAITING HUMAN CONVERGENCE DECISION**. 4-story scope — `dpapi-storage-fix` + `honest-fail-message` (bundled, #759) + `windows-docs` (#760) + `cloud_id-correctness` (incl. `A-PA-LOW-001`, human-added); Windows-validation = F4 CI spike + REQUIRED F7 manual gate; `windows`-vs-`windows-sys` dependency question RESOLVED by ADR-0021 (`windows-sys`, no new dependency-graph node); `develop` unchanged at `42e92b46` (no implementation started). The standing auto-merge policy (DEC-330/DEC-331, fully autonomous when CI green + reviewer merge-recommendation + all HIGH/MED findings addressed) and the `gh pr merge`/push MAIN-session-only constraint both remain in effect for cycle-004's future story/fix PRs. **Pipeline is ACTIVE**; `phase` frontmatter is **F2**. **Next:** human convergence decision → Pass 9 (if continuing) or F2 human gate (if gating now) → F3 incremental stories.

## Constraints Carried Forward

**cycle-004 (windows-correctness, OPEN, F2 IN PROGRESS — scoped adversarial convergence, Burst 6):** ADR-0016 (Windows build target: x86_64-pc-windows-msvc, AppData config/cache paths, Windows Credential Manager keyring via the `keyring` crate's `windows-native` feature, `.zip` packaging, CI) is the direct architectural predecessor this cycle extends. Windows Credential Manager isolation posture (SEC-WCM-DOC: OS-level user-session isolation is the trust boundary, `CRED_TYPE_GENERIC` entries) is unchanged by the DPAPI-file fallback design — the fallback file itself inherits an equivalent or stronger isolation guarantee (user-scope `%LOCALAPPDATA%`, DPAPI-encrypted, i.e. OS-user-keyed), per **ADR-0021** (keyring-first, DPAPI-file fallback ONLY on `keyring::Error::TooLong`; the access/refresh pair is stored entirely in one backend, never split; honest-fail backstop reachable only when both keyring AND the DPAPI store fail; delete/clear paths clean up the DPAPI file too). Convergence hardened this design further through Pass 8: **delete-keyring-first ordering** (Pass 3 HIGH fix, VP-AUTHDX-022) remains in force; a typed **`CorruptSecretFile`** marker error distinguishes a corrupt/undecryptable DPAPI file from a genuine backend/IO error; the honest-fail backstop message is **split per call site**; a debug-only test seam, **`JR_FORCE_DPAPI_FALLBACK`**, lets the DPAPI-fallback branch of `engage_dpapi_fallback` be exercised deterministically off real Windows (VP-AUTHDX-023); and **`ProfilePathEscape`** renders discriminated-first at both the read AND store sites. **New this burst (Pass 7/8 hardening):** the seam-wording contract for `should_fallback_to_dpapi(err)` is now documented precisely as returning `true` **iff** `err` is `keyring::Error::TooLong` (not unconditionally on any keyring error) — a Pass 7 wording-precision fix, not a behavior change (the implementation already matched only `TooLong`; only the spec prose was loose). `--cloud-id` is confirmed **not durable across `auth refresh`** (EC-1.2.052-5, Pass 7 finding) — documented as a non-blocking residual, not fixed this burst (the refresh path never re-derives or re-persists `cloud_id`, so a manually-overridden value survives refresh only because refresh doesn't touch that field at all; this is currently benign but is flagged for the human gate's attention). **The substantive Pass 8 fix:** the CLEAR path (`auth logout`/`auth remove`) is now protected against a real backward-compatibility gap on `clear_profile_oauth_pair`/`clear_profile_creds` — a pre-existing profile name that predates BC-1.4.040's path-traversal guard (e.g. a legacy Unix profile literally named `con`) could cause the DPAPI-file removal step to return `ProfilePathEscape`, blocking an otherwise-successful clear. BC-1.4.038 gains a new **Invariant 3**: the CLEAR path routes DPAPI-file removal through `clear_dpapi_file_tolerating_path_escape`, an adapter that maps `ProfilePathEscape` → `Ok(())` at this call site only (never at the store/read sites, where the guard must remain strict) — closing EC-1.4.038-5/6 and pinned by a new **VP-018** clear-path swallow oracle. **Pass 8 Finding #1 (honesty correction, no design change):** the VP CI-classification tally cited in earlier convergence bookkeeping was overstated ("13 of 14 default CI"); the corrected tally is **10 fully default-CI** (VP-AUTHDX-013/014/015/016/017/018/019/020/021/023), **2 default-CI-portion + keyring-gated state core** (VP-AUTHDX-011/012), **1 keyring-gated core + Windows-only real-DPAPI tail** (VP-AUTHDX-022), **1 Windows-only** (VP-AUTHDX-010) — see Convergence Status above for the full breakdown. The **F1-deferred `windows`-vs-`windows-sys` dependency question remains RESOLVED** by ADR-0021 §5. `clear_all_credentials` confirmed out of scope, left untouched. **cloud_id acquisition (ADR-0022)** and its `https://`-only precondition, honest classic-vs-scoped-token rescoping, and third-fetch-trigger-site confirmation (`jr auth refresh`) are all unchanged this burst. `S-MAINT-532` and the 10-story SELF-IMPROVEMENT `S-PG-*` backlog remain explicitly OUT of cycle-004 scope. **PRD delta (Burst 6 update):** `specs/prd/bc-1-auth-identity.md` — BC-1.4.038 gains Invariant 3 (the clear-path escape-tolerance adapter) + EC-1.4.038-5/6; all other BC-body content from Passes 1-6 stands unchanged. `total_bcs` unchanged at 742. **VP delta (Burst 6 update):** `vp_count` unchanged at 55 (Pass 7-8 added no new VP — VP-018 is a pre-existing VP number reused for the clear-path oracle, not a new count; confirm this citation against `vp-delta.md` before F2 human gate if a discrepancy is suspected). **Reserved Windows device-name set unchanged at 30** (ADR-0021 §9).

**cycle-004 (F1 APPROVED at human gate, Burst 2, historical):** Human approved the architect's F1 delta-analysis at the human gate (DEC-335): standard F1→F7 route; 4-story scope confirmed — `dpapi-storage-fix` + `honest-fail-message` (#759, bundled into one release) + `windows-docs` (#760) + `cloud_id-correctness` (human-added, fetch+persist `cloud_id` via `GET /_edge/tenant_info` on API-token login). Windows-validation plan ACCEPTED: F4 spikes whether `windows-latest` CI can exercise DPAPI; a manual smoke test on real Windows is a REQUIRED F7 gate, not optional. **Hygiene note RESOLVED at Burst 2:** the previously-uncommitted `research/win-oauth-keychain-blob-limit-2026-09-03.md` and modified `research/RESEARCH-INDEX.md` were committed to factory-artifacts at Burst 2 alongside STATE.md and the F1 artifacts.

**cycle-003 (RELEASE + CLOSE, historical):** Human authorized and executed the cycle-003 dev release (DEC-333): version-bump PR #767 squash-merged to `develop` (`c9bb74f4` → `42e92b46`), annotated tag `v0.7.0-dev.4` pushed, `release.yml` run `33769389700` concluded SUCCESS, GitHub prerelease published 2026-09-03T14:58:38Z with 10 assets across 5 targets. **cycle-003 (`auth-profile-dx`) is CLOSED.** Per the S-7.02 cycle-closing checklist, both process-gap deferral candidates are confirmed present as justified deferrals (target: a future SELF-IMPROVEMENT/maintenance cycle) and are codified in `cycles/cycle-003/lessons.md`: (1) `STORY-INDEX.md` was frozen at F3 and never updated through F4/F5/F6 execution — an index-currency process gap; (2) the story-template-compliance gap (4 of 7 story files missing the `level` frontmatter key plus the Architecture Mapping / Purity / Library sections). **All prior outstanding, non-blocking items carried forward verbatim:** MED-1 (VP count, 41, still not independently re-verified line-by-line), LOW-4 (BC-INDEX title paraphrase), LOW-6 (env-flag scope-boundary note), the 4-story template-compliance gap, and the 6-file cycle-003 bookkeeping input-hash cascade — none of these block cycle-004; all deferred to a future maintenance/self-improvement cycle.

**cycle-003 (F7 CLOSE, HUMAN-APPROVED/CONVERGED, historical):** Human approved Phase F7 delta convergence as CONVERGED (5/5 dimensions PASS: regression 4763/0/157, mutation 100%, holdout evaluation 0.895, security 0 CRITICAL/0 HIGH; zero regressions). HIGH-3 + LOW-2 + LOW-3 RESOLVED via docs PR #766 (squash commit `c9bb74f4`): `docs/specs/multi-profile-auth.md` reconciled, `logout.rs` stale comment fixed, CLAUDE.md DEC-322 notice documented; CI `ci-gate` PASS 15/15. `develop` HEAD advanced `202414f2` → `c9bb74f4` → `42e92b46` (release). **Outstanding, unchanged:** MED-1, LOW-4, LOW-6, and the 4-story template-compliance gap.

**cycle-003 (bookkeeping input-hash cascade, non-blocking, historical):** Three targeted `compute-input-hash --update` passes over files under `.factory/cycles/cycle-003/` reduced cycle-003 STALE files from 17 to 6, but did not reach 0 due to a genuine circular-dependency cascade among `phase-f7-convergence/consistency-audit-delta.md` and, under `phase-f3-stories/`, `wave-holdout-scenarios/wave-{1,3,4}-holdout-scenarios.md`, `dependency-graph-extended.md`, and `wave-schedule.md`. Documented per the orchestrator's explicit 3-pass stop condition; non-blocking, distinct from `F7-GATE-SYSTEMIC-INPUT-HASH-DRIFT-BOOKKEEPING` below.

**cycle-003 (F7 PRE-GATE CONSISTENCY AUDIT, historical):** A fresh-context consistency audit ahead of the F7 human gate found 12 findings (1 CRITICAL / 3 HIGH / 2 MEDIUM / 6 LOW), all documentation/index-layer. All CRITICAL/HIGH/most-MED findings FIXED (CRIT-1 STORY-INDEX staleness, HIGH-1/HIGH-2 AC-trace staleness, MED-2 wave-label drift, LOW-1 overclaim correction, LOW-5 stale story-count headline, plus HIGH-3/LOW-2/LOW-3 via docs PR #766). **Still OUTSTANDING, non-blocking, carried to a future maintenance cycle:** MED-1 (VP count 41, unverifiable-not-wrong); LOW-4/LOW-6 (documentation nits, full detail `cycles/cycle-003/phase-f7-convergence/consistency-audit-delta.md`). **Non-blocking standing item:** 4 of 7 cycle-003 story files missing the story-template `level` frontmatter key and Architecture Mapping/Purity/Library sections — pre-existing template-compliance gap, does not block any shipped story. Full detail: `cycles/cycle-003/burst-log.md` Burst 20.

**cycle-003 (Bursts 16-19, historical, resolved within cycle-003):** F5 findings (login-switch relogin-then-replace MED, refresh locked-keychain error-swallow MED, logout unset-`auth_method` handling, comment-accuracy nits, `clear_profile_cache` empty-guard, BC-1.1.016↔DEC-321 spec reconciliation) all RESOLVED via PR #763/#764. MED-2 (VP-AUTHDX-005/006/008 keyring-gated coverage-boundary) ADDRESSED (spec now states the CI-coverage boundary explicitly) but the underlying gap (no in-CI real-keychain coverage) remains open, carried to a future maintenance cycle — **directly relevant to cycle-004's #759 fix**, since the DPAPI-file fallback path will need its own keychain-injection-seam consideration for CI coverage (partially addressed by `JR_FORCE_DPAPI_FALLBACK`, which covers the fallback-engagement branch, not real-keychain behavior itself). F6 (targeted hardening) reached GATE VERDICT PASS: mutation 100% (28/28), security 0 CRIT/0 HIGH/0 MED (1 pre-existing LOW `chacha20` advisory), regression GREEN (4763/0/157), Kani-substitution 0 GAP, fuzz justified-skip. **Two LOW follow-ups from PR #764's AI review, non-blocking, carried forward:** (a) broaden the `clear_profile_cache` guard to reject `.`/`..`/traversal components; (b) add an explicit regression test for FIX-1 call-site-2. **Kept OPEN, unchanged, carried forward:** `JR_OAUTH_CODE` debug-gating (LOW/MED seam hygiene); the `{target:?}` Debug-quoting NIT (LOW); the `auth list`/`auth status` STATUS divergence (deferred cosmetic, pre-existing); F2-03 (auth_header resolution efficiency, LOW); F2-04 (non-atomic config write, LOW/MED); the `remove.rs` step-enumeration doc-comment nit (LOW). **Still uncommitted, unrelated to any cycle, noted at every burst since before this session:** `regression-state.json`, `sidecar-learning.md`, and the modified `S-cycle3-env-tag` demo gif remain uncommitted — left untouched again this burst per explicit instruction.

**cycle-003 (earlier F4/F3/F2 resolutions, historical, not listed here as open):** F1 (BYO-OAuth-cred over-delete on `auth refresh`) and the adr0011 doc-drift LOW item are CLOSED (Burst 15). The ADR-0011-staged-not-applied item is CLOSED (Burst 14) — `S-cycle3-adr0011-newtype` (PR #758) applied the staged amendment. DEC-NAMESPACE-COLLISION-RISK remains clean (max allocated ID DEC-335, no collision this burst). `S-cycle3-remove-logout-semantics`'s SEC-1 HIGH finding and DEC-331's recording are historical (Burst 13). Wave 1/2 integration gates PASSED (Bursts 11/12); all F2/F3-gate residuals FIXED/RATIFIED (Bursts 7/8/9); `S-cycle3-env-tag`, `S-cycle3-percred-storage`, `S-cycle3-credential-absence-guard`, `S-cycle3-remove-logout-semantics` all squash-merged (Bursts 10-13). `STORY-INDEX.md`'s pre-existing grep-count discrepancy note (a naive unique-`S-*`-ID scan returning ~165 distinct IDs against `total_stories: 168`) remains still-open, very likely counting-methodology noise, not root-caused — flagged for a future maintenance pass.

**Still open (2026-09-02, cycle-003 Wave 1 adversary, standing drift, not a cycle blocker):** `auth status` (a documented read-only probe) can transitively trigger the OAuth `"default"`-profile lazy-migration WRITE via `load_oauth_tokens` — pre-existing OAuth behavior, unrelated to cycle-003's per-credential redesign. Tracked here for future maintenance-cycle attention.

**Still open (2026-09-01, cycle-002 F7 human gate + S-7.02 cycle-closing checklist -- justified deferral, carried forward unchanged):**
- `CYCLE-002-PROCESS-GAP-DEFERRAL-1/2/3` (all LOW, justified deferral — no follow-up story exists in STORY-INDEX; target: a future SELF-IMPROVEMENT maintenance cycle). Full detail: `cycles/cycle-002/lessons.md` Process-Level items 3/4/5.

**Still open (2026-08-31, cycle-002 F5/F6, unchanged):**
- `F5-EDIT-GATEB-SHARE`, `F5-ISSUETYPE-CASEFOLD-SPLIT`, `F5-VP578021-WEAK-NEGPIN` (all LOW).
- `SEC-F6-1`, `SEC-F6-2` (cross-refs `SEC-001-EDITMETA-RECURSION-GUARD`), `SEC-F6-3` (all LOW).
- `S-578-3-SHARED-ASSET-VALIDATOR`, `S-578-3-FIELDVALUESPEC-RELOCATION`, `S-578-3-PR742-RESIDUAL-NITS` (all LOW).
- `S-578-2-PR741-RESIDUAL-NITS` (LOW, 7 items; `code-delivery/S-578-2/pr-review.md`).

**Still open (unchanged, LOW doc-hygiene / process, non-blocking):**
- `S-580-1-PR740-S1/S2/S3/N1/N2`, `BC-3.3.010-CITATION-UPGRADE-ELIGIBLE`.
- `PRD-DELTA-ROUND2-STEP2A-STALE-NARRATION`, `PLATFORM-ASSET-WIRE-SHAPE-UNVERIFIED`, `M1-EDITMETA-STATUS-PERMISSION-CAVEAT`, `PRD-DELTA-SUMMARY-AMENDED-BC-COUNT-STALE`.
- `BC-INDEX-GUARD-GAP`, `GUARD-SCOPE-COPY-PASTE-PATTERN`, `COUNT-RECONCILIATION-FORCED-CONSISTENCY-PATTERN` -- logged in `cycles/cycle-002/lessons.md`.
- `HOLDOUT-COVERAGE-GAP-LIST-READ-ERGONOMICS-FLAGS`, `STORY-INDEX-DECLARED-VS-FILE-COUNT-MISMATCH` (both LOW, pre-existing).
- **A-PA-LOW-001** (stale `cloud_id` survives oauth→api_token switch → `jr assets` sends Basic auth to the OAuth gateway → 401 instead of a clean error) — **now IN-SCOPE for cycle-004, closed directly by ADR-0022's `cloud_id-correctness` design (F1 human gate, DEC-335; F2 architecture + convergence hardening complete); see the cycle-004 note above. No longer a standalone future-maintenance item once cycle-004 ships.**
- **OBS-PB-1** (`auth status` credential probe `.is_ok()` swallows a genuine keyring backend error into "not found").
- `auth.rs:~1160` stale doc comment; `remove.rs` step-enumeration doc nit; `chacha20` 0.10.0 yanked-crate advisory (routine `cargo update -p chacha20` at next maintenance sweep).

**Standing (unchanged):**
- 5 cargo Dependabot PRs held open pending `syn 2.0`-vs-`3.0` convergence.
- `ADOPT-MERGE-METHOD-RULESETS`, `S-TRAIL-DERIVATION-GUARD-1`, `AX23-001` ratification.
- `F7-GATE-SYSTEMIC-INPUT-HASH-DRIFT-BOOKKEEPING` -- ~142 historical stale `input-hash` artifacts factory-wide; standing debt, **not** a cycle blocker.
- 10-story SELF-IMPROVEMENT `S-PG-*` backlog (all `draft`, need PO BC-authorship before `ready`) -- the 3 `CYCLE-002-PROCESS-GAP-DEFERRAL` items, plus cycle-003's 2 S-7.02 process-gap deferral candidates (codified in `cycles/cycle-003/lessons.md`), are candidates for future stories in this same epic.

## Session Resume Checkpoint

**Date:** 2026-09-03. **Position:** cycle-004 (`windows-correctness`), Phase F2 spec evolution IN PROGRESS — **F2 scoped adversarial convergence, intermediate checkpoint #3**; `develop` @ `42e92b46`; pipeline **ACTIVE**. cycle-001, cycle-002, and cycle-003 remain CLOSED, historical, unaltered by this burst.

**Convergence counter:** cycle-004 F2 scoped adversarial convergence — **8 passes run, trajectory 17→9→5→4→4→4→[sweep 3]→3→3, clean-streak 0/3.** Minimum 3 CONSECUTIVE clean passes required to converge under the standard rule — but a human convergence decision is now pending on whether to continue toward that bar or gate now with the current, well-hardened state.

**What's new this burst:** since the Burst-5 checkpoint (`f59f29e2`), adversarial Pass 7 (3 findings: 0 CRIT/0 HIGH/1 MED/2 LOW, novelty MED-LOW — assessed as close to converged) and Pass 8 (3 findings: 0 CRIT/1 HIGH/2 MED/0 LOW, novelty MED — the HIGH + 2 MED all on the CLEAR path, `auth logout`/`auth remove`, a surface untouched by Passes 1-7) both ran and were resolved via full architect→product-owner→formal-verifier fix chains. Pass 7 was a wording-precision fix (the `should_fallback_to_dpapi(err)` seam contract now states explicitly that it returns `true` **iff** `TooLong`, not unconditionally) plus a documented non-blocking residual (`--cloud-id` not durable across `auth refresh`, EC-1.2.052-5). Pass 8's HIGH was a genuine backward-compatibility defect: `clear_profile_oauth_pair`/`clear_profile_creds` could fail with `ProfilePathEscape` for a pre-existing legacy profile name (e.g. `con`) that predates the path-traversal guard, blocking the CLEAR path where every other backend clears successfully — fixed via a new `clear_dpapi_file_tolerating_path_escape` adapter (BC-1.4.038 Invariant 3 + EC-1.4.038-5/6, ADR-0021 §7), pinned by a new VP-018 swallow oracle. Pass 8 also corrected an overstated VP CI-classification tally (Finding #1, honesty fix — see Convergence Status for the corrected 10/2/1/1 breakdown). This work — spanning `vp-delta.md`, `architecture-delta.md`, `ADR-0021`, and `bc-1-auth-identity.md` — sat uncommitted across both passes. This burst: (1) verified `scripts/check-spec-counts.sh` (exit 0) and `scripts/check-bc-cumulative-counts.sh` (742 total/9 files, exit 0), plus `vp-delta.md`'s `input-hash` currency (`95218fd`); (2) committed the Pass-7/Pass-8 accumulated fix artifacts to factory-artifacts in one atomic commit; (3) did NOT stage the three pre-existing unrelated dirty files (`regression-state.json`, `sidecar-learning.md`, the `S-cycle3-env-tag` demo gif); (4) updated STATE.md via one full-content Write (v3.57 → v3.58): Phase Progress F2-SPEC-EVOLUTION row, Current Phase Steps table (Passes 7-8 DONE, plus a new pending "F2 human convergence decision" row), Convergence Status/Concurrent Cycles/Constraints Carried Forward/Drift-Standing prose (trajectory extended to 17→9→5→4→4→4→[sweep 3]→3→3, clean-streak 0/3, corrected VP CI tally), and this Session Resume Checkpoint. No new DEC recorded — DEC-335 remains the latest F1-gate decision; the convergence-strategy choice is deferred to the human, not decided unilaterally by this burst.

**Substantive design outcomes hardened this convergence run (brief, cumulative through Pass 8):** stale-keyring-shadows-DPAPI closure via delete-keyring-first ordering (Pass 3); the host-independent `reject_unsafe_profile_component` guard is default-CI-testable including entry-point wiring; a typed `CorruptSecretFile` read error; the honest-fail message split per-site with a Site-3 proactive stale-pair clear; the classic-vs-scoped-token Assets claim in ADR-0022 honestly rescoped; the `/_edge/tenant_info` fetch requires `https://`; `jr auth refresh` confirmed as a third `cloud_id` fetch-trigger site; the debug-only `JR_FORCE_DPAPI_FALLBACK` test seam plus its VP-AUTHDX-023 release-gate pin; `ProfilePathEscape` discriminated-first rendering at read and store sites; the 30-name reserved Windows device-name set; **(new this burst)** the `should_fallback_to_dpapi` seam contract precision fix (`TooLong` iff, not unconditional); `--cloud-id`'s non-durability across `auth refresh` documented as a residual; the CLEAR-path `ProfilePathEscape`-tolerance adapter (BC-1.4.038 Invariant 3, VP-018); and the corrected, honest VP CI-classification tally (10 fully default-CI / 2 default-CI-portion+keyring-gated-core / 1 keyring-gated-core+Windows-only-tail / 1 Windows-only).

**In-flight now:** F2 scoped adversarial convergence is paused at a human decision point after Pass 8. No adversarial pass is currently dispatched.

**Pending human decisions / policy in effect:** **AWAITING HUMAN CONVERGENCE DECISION** — the operator must choose between (a) continuing adversarial convergence toward 3 CONSECUTIVE clean passes (dispatch Pass 9; clean-streak resets to 0/3 on any finding, currently 0/3 already since no pass has yet been clean), or (b) proceeding to the F2 human gate now, accepting the current state (8 passes run, all findings resolved, finding trend improving except for the one genuinely new CLEAR-path surface Pass 8 caught, zero CRIT across all 8 passes) as sufficient convergence evidence with residuals (the `--cloud-id` refresh-durability note, EC-1.2.052-5) documented rather than fixed. Either path requires re-running the consistency-validator only if further fixes land after Pass 7-8 (a light re-run to confirm no new cross-file drift is recommended regardless, given the BC-1.4.038 Invariant 3 addition touches a shared clear-path helper). The standing auto-merge policy (DEC-330/DEC-331) and the `gh pr merge`/push MAIN-session-only constraint both carry forward into cycle-004's future story/fix PRs.

**Tracked non-blocking follow-ups carried forward verbatim (full detail `cycles/cycle-003/lessons.md` and Drift/Standing below):** all cycle-003 items (MED-1 VP count unverified; LOW-4/LOW-6 doc nits; 4-story template-compliance gap [cycle-003's, an unrelated numbering coincidence with cycle-004's 4 stories]; the 6-file bookkeeping input-hash cascade; `JR_OAUTH_CODE` debug-gating; `{target:?}` Debug-quoting; `auth list`/`auth status` STATUS divergence; F2-03/F2-04; keychain-injection-seam for VP-AUTHDX coverage [partially addressed by `JR_FORCE_DPAPI_FALLBACK`, not fully]; **A-PA-LOW-001** — closed directly by ADR-0022's design, spec-level work complete, implementation lands at F4; OBS-PB-1; `auth.rs:~1160` stale doc comment; `remove.rs` step-enumeration doc nit; `chacha20` 0.10.0 yanked-crate advisory; the two S-7.02 process-gap deferrals), plus all cycle-002 and cycle-001 standing items, PLUS this burst's new residual (`--cloud-id` non-durable across `auth refresh`, EC-1.2.052-5, Pass 7) — carry ALL forward verbatim.

**NEXT on resume (exact):** Present the pending human convergence decision to the operator: (a) continue — dispatch F2 scoped adversarial Pass 9 (fresh context) over the current spec delta (`bc-1-auth-identity.md`, `ADR-0021`, `ADR-0022`, `architecture-delta.md`, `vp-delta.md`); if CLEAN that's clean-streak 1/3, dispatch Pass 10, and so on until 3 CONSECUTIVE clean passes are reached; if findings surface, run the fix chain and reset the clean-streak; or (b) gate now — re-run the consistency-validator (fresh context) to confirm the Pass 7-8 fixes introduced no new cross-file drift, then bring the F2 spec delta to the F2 human gate with the 8-pass convergence history and the `--cloud-id` residual documented as the rationale.

**Resume command:** `/vsdd-factory:next-step`.

**Superseded checkpoints:** the prior cycle-004 checkpoint (v3.57, 2026-09-03 — recorded adversarial Passes 5-6 and the post-Pass-6 consistency sweep, advancing the position to "Pass 7 NEXT") is superseded in place by this checkpoint and queued for archival to `cycles/cycle-004/session-checkpoints.md` (not yet created), alongside the earlier v3.56 (Passes 1-4 checkpoint), v3.55 (crash-recovery), v3.54 (F1-APPROVED/F2-dispatch-pending), and v3.53 (F1-IN-PROGRESS) checkpoints and the still-pending cycle-003 checkpoint archival (v3.52 CLOSED+RELEASED, plus the earlier-queued v3.49/v3.50/v3.51) to `cycles/cycle-003/session-checkpoints.md`, and v3.48 (F6-IN-PROGRESS), v3.47 (F5-CONVERGED/PAUSED), v3.46 (F5-FINDINGS-FIXED), v3.45 (F4-PHASE-COMPLETE), v3.44 (Wave-4-COMPLETE), v3.43 (Wave-3-COMPLETE), v3.42 (Wave-2-COMPLETE), v3.41 (Wave-1-COMPLETE), v3.40 (Wave-1-story-1-merged), v3.39 (F3-GATE-APPROVED/F4-ACTIVE), v3.38 (F3 authored/integrated, gate pending), v3.37 (F2-gate-approval/residual-sweep), v3.36 (F2 CONVERGED/PAUSED), v3.35 (SESSION-WRAP/PAUSED), v3.34 (F2-GATE-FIX-ROUND-COMPLETE), v3.33 (F2 authoring complete), v3.32 (F2 in progress), and v3.31 (F1-pending). Earlier archives (RELEASED/SHIPPED v3.29, F7-PASS/AWAITING-GATE v3.27, F6-COMPLETE v3.26, F5-COMPLETE v3.25, F4-COMPLETE v3.24, `WRAP-F4-WAVE2-COMPLETE-PAUSE` v3.23, and the SESSION-WRAP checkpoint) remain at `cycles/cycle-002/session-checkpoints.md`. The `list-read-ergonomics` cycle-001 CLOSED-position checkpoint (v3.05) remains archived at `cycles/cycle-001/session-checkpoints.md`.

## Historical Content

| Content | Location |
|---------|----------|
| cycle-001 burst history | `cycles/cycle-001/burst-log.md` |
| cycle-002 burst history | `cycles/cycle-002/burst-log.md` (Bursts 1-14 = F2/F3/F4; 15 = F5 CONVERGED + FIX-F5-001; 16 = F6 COMPLETE + FIX-F6-001; 17 = F7 delta-convergence analyses PASS + FIX-F7-001; 18 = F7 human gate APPROVED + cycle-002 CLOSED; 19 = release v0.7.0-dev.3 SHIPPED; 20 = SESSION-WRAP) |
| cycle-003 burst history | `cycles/cycle-003/burst-log.md` (Burst 1 = cycle OPENED; Bursts 2-9 = F1 through F3 gates; Bursts 10-15 = F4 Waves 1-5, all 7 stories merged; Burst 16 = F5 findings fixed; Burst 17 = SESSION WRAP, F5 CONVERGED; Burst 18 = SESSION RESUMED, F6 DISPATCHED; Burst 19 = F6 COMPLETE — PASS; Burst 20 = F7 pre-gate consistency audit; Burst 21 = F7 human gate APPROVED — CONVERGED (DEC-332); Burst 22 = RELEASE v0.7.0-dev.4 SHIPPED, cycle-003 CLOSED (DEC-333)) |
| cycle-004 burst history | `cycles/cycle-004/burst-log.md` (Burst 1 = cycle OPENED — DEC-334 recorded, #759+#760 bundle confirmed, F1 delta-analysis dispatched; Burst 2 = F1 human gate APPROVED — DEC-335 recorded, 4-story scope confirmed [`cloud_id-correctness` added], advanced to F2 spec evolution; Burst 3 = CRASH RECOVERY — recovered + checkpoint-committed the architect's [ADR-0021, ADR-0022] and product-owner's [+9 BCs/1 amended] F2 deliverables left uncommitted by a crashed session; Burst 4 = F2 SCOPED ADVERSARIAL CONVERGENCE INTERMEDIATE CHECKPOINT — formal-verifier's VP delta [+13 VPs] + 4 adversarial passes [17→9→5→4 findings, all fixed] committed, clean-streak 0/3; Burst 5 = F2 SCOPED ADVERSARIAL CONVERGENCE INTERMEDIATE CHECKPOINT #2 — Passes 5-6 [4→4 findings, all fixed] + a post-Pass-6 consistency-validator sweep [3 findings, all fixed] committed, VP count 54→55, clean-streak 0/3; Burst 6 = F2 SCOPED ADVERSARIAL CONVERGENCE INTERMEDIATE CHECKPOINT #3 — Passes 7-8 [3→3 findings, all fixed, incl. BC-1.4.038 Invariant 3 CLEAR-path fix + VP CI-classification honesty correction] committed, clean-streak 0/3, AWAITING HUMAN CONVERGENCE DECISION) |
| cycle-004 F1 delta-analysis artifacts | `cycles/cycle-004/phase-f1-delta-analysis/delta-analysis.md` (impact boundary, 3-story [+1 contingent] decomposition, Windows-validation plan, 6 open questions; APPROVED at human gate, DEC-335) + `affected-files.txt` (NEW/MODIFIED/DEPENDENT/DOCS file manifest) |
| cycle-004 F2 spec-evolution artifacts | `cycles/cycle-004/phase-f2-spec-evolution/architecture-delta.md` (architect delta, recovered Burst 3, refined Passes 1-8 + sweep); `cycles/cycle-004/phase-f2-spec-evolution/vp-delta.md` (formal-verifier's VP delta, 14 VPs total, extended Pass 6, refined Pass 8); `specs/architecture/decisions/ADR-0021-windows-oauth-secret-storage-dpapi-fallback.md`; `specs/architecture/decisions/ADR-0022-api-token-cloud-id-acquisition-tenant-info.md` (both recovered Burst 3, refined Passes 1-6 + sweep; ADR-0021 further refined Pass 8) |
| cycle-004 cloud_id research | `research/edge-tenant-info-cloudid-2026-09-03.md` (validates `/_edge/tenant_info` as the `cloud_id` source underlying ADR-0022; recovered + committed Burst 3) |
| cycle-003 grounding artifacts | `cycles/cycle-003/investigation/auth-profile-current-state.md` (current-state map); `cycles/cycle-003/investigation/modern-cli-auth-profile-research.md` (modern-CLI research, 39 sources, 4 ranked recommendations) |
| cycle-003 F1 delta-analysis report | `cycles/cycle-003/phase-f1-delta-analysis/delta-analysis.md` (impact boundary, affected specs/stories/tests, regression risk; APPROVED at human gate) |
| cycle-003 F2 spec-evolution artifacts | `cycles/cycle-003/phase-f2-spec-evolution/architecture-delta.md`; `cycles/cycle-003/phase-f2-spec-evolution/adr-0011-amendment-staged.md` (ADR-0011 amendment — APPLIED via `S-cycle3-adr0011-newtype`) |
| cycle-003 F3 story-decomposition artifacts | `cycles/cycle-003/phase-f3-stories/` — `decomposition-manifest.md`, `S-cycle3-*.md` ×7, `dependency-graph-extended.md`, `wave-schedule.md`, `conflict-report.md`, `wave-holdout-scenarios/wave-{1..5}-holdout-scenarios.md` |
| cycle-003 F4 implementation artifacts | `cycles/cycle-003/phase-f4-implementation/regression-baseline.md`; `wave-1-integration-gate.md`; `wave-2-integration-gate.md` (Waves 3-5 had no standalone report, treated PASSED-implied) |
| cycle-003 F6 targeted-hardening artifacts | `cycles/cycle-003/phase-f6-hardening/summary.md`; `kani-results.md`, `fuzz-results.md`, `mutation-results.md`, `security-scan-results.md` |
| cycle-003 F7 delta-convergence artifacts | `cycles/cycle-003/phase-f7-convergence/consistency-audit-delta.md`; `delta-convergence-report.md`; `traceability-chain-delta.md`; `holdout-eval-delta.md` (score 0.895) |
| cycle-003 F7 close delivery evidence | `code-delivery/FIX-F7-DOCS-1/pr-review.md` (PR #766) — top-level `code-delivery/` path (project-wide accumulating convention, DF-030) |
| cycle-003 release delivery evidence | version-bump PR #767 (`develop` @ `42e92b46`); tag `v0.7.0-dev.4`; `release.yml` run `33769389700` (external to `.factory/`); GitHub prerelease (10 assets/5 targets, external to `.factory/`) |
| cycle-003 F4 story delivery evidence (all 7 stories) | `cycles/cycle-003/code-delivery/S-cycle3-{env-tag,percred-storage,credential-absence-guard,remove-logout-semantics,adr0011-newtype,oauth-default-creation,chosen-flow-reconcile}/` (demos + `pr-review.md` per story; Waves 4-5 skip demos per human decision) |
| cycle-003 F5 fix-PR delivery evidence | `code-delivery/FIX-F5-login-switch/pr-review.md` (PR #763); `code-delivery/FIX-F5-refinement/pr-review.md` (PR #764) |
| cycle-003 lessons learned | `cycles/cycle-003/lessons.md` (codifies the two S-7.02 process-gap deferrals as justified deferrals targeting a future SELF-IMPROVEMENT/maintenance cycle) |
| cycle-004 research grounding | `research/win-oauth-keychain-blob-limit-2026-09-03.md` (pre-cycle research validating the #759 fix strategy; committed to factory-artifacts Burst 2) |
| F5 scoped-adversarial review report (cycle-002) | `phase-f5-adversarial/adversarial-delta-review.md`; `phase-f5-adversarial/convergence-summary.md` |
| F6 targeted-hardening report (cycle-002) | `phase-f6-hardening/summary.md`; `kani-results.md`, `fuzz-results.md`, `mutation-results.md`, `security-scan-results.md` |
| F7 delta convergence report + traceability (cycle-002) | `phase-f7-convergence/delta-convergence-report.md`; `phase-f7-convergence/traceability-chain-delta.md`; `phase-f7-convergence/consistency-audit-delta.md`; `phase-f7-convergence/holdout-eval-delta.md`; `cycles/cycle-002/convergence/traceability-chain.md` (master) |
| cycle-001/cycle-002 convergence trajectory + session checkpoints + lessons + resolved blockers | `cycles/cycle-001/` and `cycles/cycle-002/` (see per-cycle files; cycle-002 session-checkpoints.md holds all prior F2-F7 + RELEASED + SESSION-WRAP checkpoints) |
| S-578-2/S-578-3/S-578-4 delivery artifacts | `cycles/cycle-002/S-578-2/`, `S-578-3/`, `S-578-4/` (red-gate-log.md, adversary-convergence-state.json); `code-delivery/S-578-2/`, `S-578-3/`, `S-578-4/` (pr-review.md, demos) |
| FIX-F5-001/FIX-F6-001/FIX-F7-001 delivery artifacts | `code-delivery/FIX-F5-001/`, `FIX-F6-001/`, `FIX-F7-001/` (pr-description.md, pr-review.md) |
| Release v0.7.0-dev.3 delivery artifacts | `code-delivery/release-v0.7.0-dev.3/` (pr-description.md, pr-review.md) |
| Pre-2026-08-25 compaction history | factory-artifacts commit `43f4a5e3` |

## Drift / Standing Items

**cycle-004 (F2 IN PROGRESS, scoped adversarial convergence, Burst 6, this burst):** adversarial Pass 7 (3 findings: 0 CRIT/0 HIGH/1 MED/2 LOW, novelty MED-LOW — close to converged) and Pass 8 (3 findings: 0 CRIT/1 HIGH/2 MED/0 LOW, novelty MED — HIGH + 2 MED all on the previously-untouched CLEAR path, `auth logout`/`auth remove`) ran since the Burst-5 checkpoint, resolved via architect→product-owner→formal-verifier fix chains. **Pass 8's HIGH is a real backward-compatibility defect, now closed:** `clear_profile_oauth_pair`/`clear_profile_creds` could fail with `ProfilePathEscape` for a legacy profile name predating the path-traversal guard, blocking the clear operation — fixed via a `clear_dpapi_file_tolerating_path_escape` adapter mapping `ProfilePathEscape`→`Ok(())` at the clear-path call site only (BC-1.4.038 new Invariant 3 + EC-1.4.038-5/6, ADR-0021 §7), pinned by new VP-018. **Pass 8 also issued an honesty correction to the VP CI-classification tally** (Finding #1): the corrected breakdown of the 14 new VPs is 10 fully default-CI, 2 default-CI-portion+keyring-gated-core, 1 keyring-gated-core+Windows-only-tail, 1 Windows-only — superseding the earlier overstated "13 of 14 default CI" claim. **Pass 7's fix was a wording-precision correction** (the `should_fallback_to_dpapi(err)` contract now states `true` iff `TooLong`, not unconditional) plus a documented non-blocking residual: `--cloud-id` is not durable across `auth refresh` (EC-1.2.052-5). **Clean-streak 0/3 — not yet converged under the standard 3-consecutive-clean rule.** **A human convergence decision is now pending** (see Session Resume Checkpoint): continue toward 3 consecutive clean passes (dispatch Pass 9), or proceed to the F2 human gate now with the 8-pass convergence history and the `--cloud-id` residual documented as rationale. `A-PA-LOW-001` remains directly closed by ADR-0022's design — implementation lands when story 4 (`cloud_id-correctness`) ships in F4. The F1-deferred `windows`-vs-`windows-sys` dependency question remains RESOLVED by ADR-0021 §5. `clear_all_credentials` confirmed out of scope, left untouched. **Hygiene note:** the accumulated Pass-7/Pass-8 fix artifacts (`vp-delta.md`, `architecture-delta.md`, `ADR-0021`, `bc-1-auth-identity.md`, all modified — no new files this burst) are committed to factory-artifacts this burst alongside STATE.md and the burst-log entry; the three pre-existing unrelated dirty files (`regression-state.json`, `sidecar-learning.md`, `S-cycle3-env-tag` demo gif) remain explicitly NOT staged, per standing instruction. **Remaining F2 work:** the human convergence decision itself, then either adversarial Pass 9+ (if continuing) or a final consistency-validator confirmation + the F2 human gate (if gating now).

**cycle-004 (F2 IN PROGRESS, scoped adversarial convergence, Burst 5, historical):** adversarial Pass 5 (4 findings: 1 HIGH/1 MED/2 LOW, novelty substantive) and Pass 6 (4 findings: 2 HIGH/2 MED, novelty HIGH) ran since the Burst-4 checkpoint, both HIGHs in both passes being second-order/propagation defects from earlier fixes interacting, all resolved via architect→product-owner→formal-verifier fix chains. A post-Pass-6 fresh-context comprehensive cross-file consistency-validator sweep found and fixed 3 further findings (0 CRIT/0 HIGH/2 MED/1 LOW). New design hardening that burst: the debug-only `JR_FORCE_DPAPI_FALLBACK` test seam with its VP-AUTHDX-023 release-gate pin; `ProfilePathEscape` discriminated-first rendering at both read and store sites; BC-1.4.028's four-way partial-state discrimination fully aligned. Superseded in currency by the Burst-6 entry above; recorded here for the audit trail.

**cycle-004 (F2 IN PROGRESS, scoped adversarial convergence, Burst 4, historical):** formal-verifier authored the F2 VP delta (13 new VPs, `vp_count` 41→54) and 4 fresh-context scoped adversarial passes ran against the F2 spec delta since the Burst-3 crash recovery, trajectory 17→9→5→4, all findings resolved. The most significant fix that round: delete-keyring-first ordering (Pass 3 HIGH, `STALE-KEYRING-SHADOWS-DPAPI`) — closed a real defect where a pre-existing keyring pair could permanently shadow a fresh DPAPI fallback pair; VP-AUTHDX-022 pins the closure. Superseded in currency by the Burst-5 entry above; recorded here for the audit trail.

**cycle-004 (F2 IN PROGRESS, CRASH RECOVERY, Burst 3, historical):** the prior session crashed mid-F2; the architect's ADR-0021/ADR-0022/architecture-delta.md and the product-owner's 9-BC PRD delta were produced before the crash and recovered + checkpoint-committed at Burst 3 (`6cf5778a`). Recorded here for the audit trail.

**cycle-004 (F1 APPROVED at human gate, Burst 2, historical):** Human approved the architect's F1 delta-analysis at the human gate (DEC-335): standard F1→F7 route; 4-story scope confirmed — `dpapi-storage-fix` + `honest-fail-message` (#759, bundled into one release) + `windows-docs` (#760) + `cloud_id-correctness` (human-added, fetch+persist `cloud_id` via `GET /_edge/tenant_info` on API-token login). Windows-validation plan ACCEPTED: F4 spikes whether `windows-latest` CI can exercise DPAPI; a manual smoke test on real Windows is a REQUIRED F7 gate, not optional. **Hygiene note RESOLVED at Burst 2:** the previously-uncommitted `research/win-oauth-keychain-blob-limit-2026-09-03.md` and modified `research/RESEARCH-INDEX.md` were committed to factory-artifacts at Burst 2 alongside STATE.md and the F1 artifacts.

**cycle-003 (RELEASE + CLOSE, historical):** Human authorized and executed the cycle-003 dev release (DEC-333): version-bump PR #767 squash-merged to `develop` (`c9bb74f4` → `42e92b46`), annotated tag `v0.7.0-dev.4` pushed, `release.yml` run `33769389700` concluded SUCCESS, GitHub prerelease published 2026-09-03T14:58:38Z with 10 assets across 5 targets. **cycle-003 (`auth-profile-dx`) is CLOSED.** Per the S-7.02 cycle-closing checklist, both process-gap deferral candidates are confirmed present as justified deferrals (target: a future SELF-IMPROVEMENT/maintenance cycle) and are codified in `cycles/cycle-003/lessons.md`: (1) `STORY-INDEX.md` was frozen at F3 and never updated through F4/F5/F6 execution — an index-currency process gap; (2) the story-template-compliance gap (4 of 7 story files missing the `level` frontmatter key plus the Architecture Mapping / Purity / Library sections). **All prior outstanding, non-blocking items carried forward verbatim:** MED-1 (VP count, 41, still not independently re-verified line-by-line), LOW-4 (BC-INDEX title paraphrase), LOW-6 (env-flag scope-boundary note), the 4-story template-compliance gap, and the 6-file cycle-003 bookkeeping input-hash cascade — none of these block cycle-004; all deferred to a future maintenance/self-improvement cycle.

**cycle-003 (F7 PRE-GATE CONSISTENCY AUDIT, historical):** A fresh-context consistency audit ahead of the F7 human gate found 12 findings (1 CRITICAL / 3 HIGH / 2 MEDIUM / 6 LOW), all documentation/index-layer. All CRITICAL/HIGH/most-MED findings FIXED (CRIT-1 STORY-INDEX staleness, HIGH-1/HIGH-2 AC-trace staleness, MED-2 wave-label drift, LOW-1 overclaim correction, LOW-5 stale story-count headline, plus HIGH-3/LOW-2/LOW-3 via docs PR #766). **Still OUTSTANDING, non-blocking, carried to a future maintenance cycle:** MED-1 (VP count 41, unverifiable-not-wrong); LOW-4/LOW-6 (documentation nits, full detail `cycles/cycle-003/phase-f7-convergence/consistency-audit-delta.md`). **Non-blocking standing item:** 4 of 7 cycle-003 story files missing the story-template `level` frontmatter key and Architecture Mapping/Purity/Library sections — pre-existing template-compliance gap, does not block any shipped story. Full detail: `cycles/cycle-003/burst-log.md` Burst 20.

**cycle-003 (Bursts 16-19, historical, resolved within cycle-003):** F5 findings (login-switch relogin-then-replace MED, refresh locked-keychain error-swallow MED, logout unset-`auth_method` handling, comment-accuracy nits, `clear_profile_cache` empty-guard, BC-1.1.016↔DEC-321 spec reconciliation) all RESOLVED via PR #763/#764. MED-2 (VP-AUTHDX-005/006/008 keyring-gated coverage-boundary) ADDRESSED (spec now states the CI-coverage boundary explicitly) but the underlying gap (no in-CI real-keychain coverage) remains open, carried to a future maintenance cycle — **directly relevant to cycle-004's #759 fix**, since the DPAPI-file fallback path will need its own keychain-injection-seam consideration for CI coverage. F6 (targeted hardening) reached GATE VERDICT PASS: mutation 100% (28/28), security 0 CRIT/0 HIGH/0 MED (1 pre-existing LOW `chacha20` advisory), regression GREEN (4763/0/157), Kani-substitution 0 GAP, fuzz justified-skip. **Two LOW follow-ups from PR #764's AI review, non-blocking, carried forward:** (a) broaden the `clear_profile_cache` guard to reject `.`/`..`/traversal components; (b) add an explicit regression test for FIX-1 call-site-2. **Kept OPEN, unchanged, carried forward:** `JR_OAUTH_CODE` debug-gating (LOW/MED seam hygiene); the `{target:?}` Debug-quoting NIT (LOW); the `auth list`/`auth status` STATUS divergence (deferred cosmetic, pre-existing); F2-03 (auth_header resolution efficiency, LOW); F2-04 (non-atomic config write, LOW/MED); the `remove.rs` step-enumeration doc-comment nit (LOW). **Still uncommitted, unrelated to any cycle, noted at every burst since before this session:** `regression-state.json`, `sidecar-learning.md`, and the modified `S-cycle3-env-tag` demo gif remain uncommitted — left untouched again this burst per explicit instruction.

**cycle-003 (earlier F4/F3/F2 resolutions, historical, not listed here as open):** F1 (BYO-OAuth-cred over-delete on `auth refresh`) and the adr0011 doc-drift LOW item are CLOSED (Burst 15). The ADR-0011-staged-not-applied item is CLOSED (Burst 14) — `S-cycle3-adr0011-newtype` (PR #758) applied the staged amendment. DEC-NAMESPACE-COLLISION-RISK remains clean (max allocated ID DEC-335, no collision this burst). `S-cycle3-remove-logout-semantics`'s SEC-1 HIGH finding and DEC-331's recording are historical (Burst 13). Wave 1/2 integration gates PASSED (Bursts 11/12); all F2/F3-gate residuals FIXED/RATIFIED (Bursts 7/8/9); `S-cycle3-env-tag`, `S-cycle3-percred-storage`, `S-cycle3-credential-absence-guard`, `S-cycle3-remove-logout-semantics` all squash-merged (Bursts 10-13). `STORY-INDEX.md`'s pre-existing grep-count discrepancy note (a naive unique-`S-*`-ID scan returning ~165 distinct IDs against `total_stories: 168`) remains still-open, very likely counting-methodology noise, not root-caused — flagged for a future maintenance pass.

**Still open (2026-09-02, cycle-003 Wave 1 adversary, standing drift, not a cycle blocker):** `auth status` (a documented read-only probe) can transitively trigger the OAuth `"default"`-profile lazy-migration WRITE via `load_oauth_tokens` — pre-existing OAuth behavior, unrelated to cycle-003's per-credential redesign. Tracked here for future maintenance-cycle attention.

**Still open (2026-09-01, cycle-002 F7 human gate + S-7.02 cycle-closing checklist -- justified deferral, carried forward unchanged):**
- `CYCLE-002-PROCESS-GAP-DEFERRAL-1/2/3` (all LOW, justified deferral — no follow-up story exists in STORY-INDEX; target: a future SELF-IMPROVEMENT maintenance cycle). Full detail: `cycles/cycle-002/lessons.md` Process-Level items 3/4/5.

**Still open (2026-08-31, cycle-002 F5/F6, unchanged):**
- `F5-EDIT-GATEB-SHARE`, `F5-ISSUETYPE-CASEFOLD-SPLIT`, `F5-VP578021-WEAK-NEGPIN` (all LOW).
- `SEC-F6-1`, `SEC-F6-2` (cross-refs `SEC-001-EDITMETA-RECURSION-GUARD`), `SEC-F6-3` (all LOW).
- `S-578-3-SHARED-ASSET-VALIDATOR`, `S-578-3-FIELDVALUESPEC-RELOCATION`, `S-578-3-PR742-RESIDUAL-NITS` (all LOW).
- `S-578-2-PR741-RESIDUAL-NITS` (LOW, 7 items; `code-delivery/S-578-2/pr-review.md`).

**Still open (unchanged, LOW doc-hygiene / process, non-blocking):**
- `S-580-1-PR740-S1/S2/S3/N1/N2`, `BC-3.3.010-CITATION-UPGRADE-ELIGIBLE`.
- `PRD-DELTA-ROUND2-STEP2A-STALE-NARRATION`, `PLATFORM-ASSET-WIRE-SHAPE-UNVERIFIED`, `M1-EDITMETA-STATUS-PERMISSION-CAVEAT`, `PRD-DELTA-SUMMARY-AMENDED-BC-COUNT-STALE`.
- `BC-INDEX-GUARD-GAP`, `GUARD-SCOPE-COPY-PASTE-PATTERN`, `COUNT-RECONCILIATION-FORCED-CONSISTENCY-PATTERN` -- logged in `cycles/cycle-002/lessons.md`.
- `HOLDOUT-COVERAGE-GAP-LIST-READ-ERGONOMICS-FLAGS`, `STORY-INDEX-DECLARED-VS-FILE-COUNT-MISMATCH` (both LOW, pre-existing).
- **A-PA-LOW-001** (stale `cloud_id` survives oauth→api_token switch → `jr assets` sends Basic auth to the OAuth gateway → 401 instead of a clean error) — **now IN-SCOPE for cycle-004, closed directly by ADR-0022's `cloud_id-correctness` design (F1 human gate, DEC-335; F2 architecture + convergence hardening complete); see the cycle-004 note above. No longer a standalone future-maintenance item once cycle-004 ships.**
- **OBS-PB-1** (`auth status` credential probe `.is_ok()` swallows a genuine keyring backend error into "not found").
- `auth.rs:~1160` stale doc comment; `remove.rs` step-enumeration doc nit; `chacha20` 0.10.0 yanked-crate advisory (routine `cargo update -p chacha20` at next maintenance sweep).

**Standing (unchanged):**
- 5 cargo Dependabot PRs held open pending `syn 2.0`-vs-`3.0` convergence.
- `ADOPT-MERGE-METHOD-RULESETS`, `S-TRAIL-DERIVATION-GUARD-1`, `AX23-001` ratification.
- `F7-GATE-SYSTEMIC-INPUT-HASH-DRIFT-BOOKKEEPING` -- ~142 historical stale `input-hash` artifacts factory-wide; standing debt, **not** a cycle blocker.
- 10-story SELF-IMPROVEMENT `S-PG-*` backlog (all `draft`, need PO BC-authorship before `ready`) -- the 3 `CYCLE-002-PROCESS-GAP-DEFERRAL` items, plus cycle-003's 2 S-7.02 process-gap deferral candidates (codified in `cycles/cycle-003/lessons.md`), are candidates for future stories in this same epic.
