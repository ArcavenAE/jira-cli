---
document_type: pipeline-state
level: ops
version: "2.42"
status: active
producer: state-manager
timestamp: 2026-08-12T14:13:25Z
phase: 3
pipeline: PAUSED
inputs: []
input-hash: "[live-state]"
traces_to: ""
project: jira-cli
mode: brownfield
current_step: "S-CIGATE-3-WIP-VERIFIED (2026-08-12): session resumed; `factory-worktree-health` ran BLOCKING and PASSED -- `.factory/` in-sync with `origin/factory-artifacts`, `.worktrees/S-CIGATE-3` healthy. RECONCILIATION: the worktree was found 4 commits ahead of the last-recorded tip `aeeebe01` (`99f53383`, `dfc69662`, `bc86d4ce`, `d32f9f67` -- ADV pass-2/5/6 fixes the S-CIGATE-3-PUSHED burst's record predated) plus uncommitted WIP (a new `test_ac_008_guards_are_key_spelling_and_indent_agnostic`, ~165 lines, AC-008 two-axis spelling x indent guard-agnosticism proof; a small `CLAUDE.md` doc edit). This is a reconciliation of the prior burst's completeness (accurate as of its own verification, which predated this further local work), not a contradiction of it -- the S-CIGATE-3 adversarial window's 0/3 disposition is unchanged and not reopened by this burst. Human ruled (DEC-267): verify the WIP fully, then commit -- do NOT push, do NOT open a PR, hold for human review. Verification PASSED: `cargo test --test ci_gate_completeness` 58/58 (incl. new AC-008 test); full `cargo test` no regressions; `cargo clippy --all-targets -- -D warnings` PASS after an in-scope fix of 5 `clippy::doc_lazy_continuation` errors in the new test's doc comment; `cargo fmt --all -- --check` clean. Round-15's `#[cfg(unix)]` dead-code class checked and does not apply (diff adds only doc comments + one always-run test fn). A real Windows build was NOT run (not locally reproducible). WIP committed as `73a117cb` -- `test(ci-gate): add AC-008 two-axis (spelling x indent) guard-agnosticism proof (S-CIGATE-3)`. Working tree clean. Branch `test/ci-gate-real-yaml-parser` HEAD = `73a117cb`, **22 commits ahead of `origin/develop`**, of which **5 are unpushed** to `origin/test/ci-gate-real-yaml-parser` (still `aeeebe01`) -- re-verified via `git fetch` + `git rev-parse origin/...` + `git rev-list --count`. NOT pushed, NO PR opened. Drift discovered mid-burst (pre-burst hygiene, `git -C .factory status`): `.factory/stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` has an uncommitted local edit (v1.3->v1.6, three CORRECTION entries) that appears to already fix `AC-006-FALSE-RATIONALE-UNCORRECTED` -- left untouched this burst per the state-burst skill's pre-burst-hygiene rule (unrelated modifications must not contaminate the burst commit); recorded as new drift item `AC-006-CORRECTION-DRAFTED-UNCOMMITTED` (MEDIUM). Drift `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH) strengthened: 22 total commits now unvalidated by any CI run (17 pushed-no-PR + 5 unpushed), `origin` still at `aeeebe01`. No product BCs touched, no STORY-INDEX change. Files updated this burst: `STATE.md` (v2.41->v2.42), `cycles/cycle-001/{burst-log,session-checkpoints,decisions-archive,drift-items-open-detail}.md`. Next: human ruling on push/PR/merge for `test/ci-gate-real-yaml-parser`, plus review of the uncommitted AC-006 correction draft. trajectory-tail →1→3→0→2 (unchanged -- not a Step-4.5 or S-CIGATE-3-window pass). D-chain cite D-453 latest brownfield (unchanged -- no new D-numbers introduced)."
trajectory_tail: "→1→3→0→2"
maintenance_run: CLOSED
current_cycle: "cycle-001"
feature_mode_bundle: SOH-DX-1-F4-DELIVERY
dtu_required: false
phase_2_status: APPROVED
phase_2_approved_at: 2026-05-07
phase_3_status: SOH-ATTACHMENTS-1 F5 SCOPED ADVERSARIAL CONVERGED 2026-07-24 STRICT (14 rounds; window pass-12/pass-13/pass-14 CLEANx3; spec v1.3.99 to v1.3.106; BC-INDEX v6.38 to v6.44; develop @ db207b81) + F7 DELTA CONVERGENCE APPROVED 2026-07-25 (DEC-186; 5-dim PASS; MAXIMUM_VIABLE_REFINEMENT_REACHED) + CYCLE FULLY CLOSED 2026-07-25
activation_head: "7b3ba371"
activation_version: "v0.6.0-dev.11"
---

<!-- STATE.md SIZE BUDGET (2026-08-12, S-CIGATE-3-WIP-VERIFIED burst): 185 lines (wc-l) --
     soft-target 200. margin from soft-target ~= 15. margin from actual to hard cap 500 ~= 315.
     Pre-burst figure was 183 lines (measured via `wc -l` immediately before this burst);
     this burst is a small, additive verify-and-reconcile burst -- one Phase-Progress row
     swapped, one Current-Phase-Steps row swapped, the Session Resume Checkpoint replaced
     (prior archived verbatim to session-checkpoints.md), two drift-item rows touched (one
     updated, one new) -- net line delta near zero because equivalent prose volume was moved
     out to cycles/cycle-001/burst-log.md in the same burst. See cycles/cycle-001/
     {decisions-archive, drift-items-open-detail, burst-log, session-checkpoints, lessons,
     convergence-trajectory}.md for everything relocated out of this file historically. -->

# Pipeline State: jira-cli

## Project Metadata

| Field | Value |
|-------|-------|
| **Product** | jr (Jira CLI) |
| **Mode** | BROWNFIELD / Rust |
| **Target Workspace** | develop to main |
| **trajectory-tail** | →1→3→0→2 (unchanged this burst -- S-CIGATE-3's own adversarial window is story-scoped, not a Step-4.5 window; full window detail: cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md § S-CIGATE-3) |
| **Last Updated** | S-CIGATE-3-WIP-VERIFIED 2026-08-12 (trajectory-tail →1→3→0→2, unchanged): session resumed; found the story worktree 4 commits + uncommitted WIP ahead of the last-recorded tip (reconciliation, not a discrepancy). Human ruled (DEC-267) verify-then-commit, hold before push/PR. WIP verified (58/58 ci_gate_completeness tests, full test suite, clippy -D warnings, fmt clean) and committed as `73a117cb`. Branch now 22 commits ahead of `origin/develop`, 5 unpushed beyond the previously-pushed `aeeebe01` tip. Still NO PR opened, NO CI ever run on this branch. `develop` still @ `d55bedf7`, unchanged. |
| **Current Phase** | Feature Mode SOH-DX-1 **F4 DELIVERY** -- DEC-204 fully ADJUDICATED (DEC-245). F2 APPROVED (DEC-196), F3 APPROVED (DEC-197). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671, `df203233`). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged to `develop` as `a5e1d087`, closes #626 -- DEC-262); trust-layer correction **PR #675 also MERGED** (`d55bedf7` -- DEC-264). `develop` CI SUCCESS on `d55bedf7` (run 31450052302). **S-CIGATE-3 (durable YAML-parser fix) IMPLEMENTED, WIP-VERIFIED, still UNMERGED, NO PR, NEVER CI-VALIDATED** -- 22 commits on `test/ci-gate-real-yaml-parser` (5 unpushed beyond the pushed `aeeebe01` tip), story status `in-progress`; 6-pass story-scoped window ended PERMANENTLY at 0/3 (DEC-265), unchanged/not reopened this burst. Human ruling pending, in order: push the 5 unpushed commits; open a PR (first-ever CI trigger); merge. Adversary (SOH-DX-1 Step 4.5, unaffected by S-CIGATE-3's separate window): 61 total Step-4.5-eligible passes (56 STRICT + pass-60/pass-61 targeted delta review) plus ADV-P675; 6 VOID; 2 NOT RUN (DEC-209); pass-20 SUPERSEDED (DEC-216); window 57/58/59 CLOSED 0/3 -- TENTH consecutive window without 3/3. Step 4.5 ends PERMANENTLY at **0/3** -- DEC-262 authorized merge on code grounds. STORY-INDEX v1.5.81 (127 stories; denominator unreconciled -- see Drift Items). AX23-001 PENDING RATIFICATION. **Factory PAUSED -- no phase transition this burst, verify-and-reconcile bookkeeping only.** |
| **Next Phase** | Pending human decisions, in order: (1) **Whether/when to push the 5 unpushed commits** on `test/ci-gate-real-yaml-parser` to `origin` (remote stale at `aeeebe01`). (2) **Whether/when to open a PR** -- the concrete next action that would trigger the branch's first CI run (`S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED`, now covering 22 commits). (3) **Merge ruling on S-CIGATE-3** once CI has run (DEC-262-shape ruling available as precedent, not yet exercised). (4) **Review the uncommitted AC-006 correction draft** already sitting in `.factory/stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` (drift `AC-006-CORRECTION-DRAFTED-UNCOMMITTED`) -- if sound, commit it explicitly; this would likely close `AC-006-FALSE-RATIONALE-UNCORRECTED`. (5) Whether to instantiate `.factory/policies.yaml` (absent; all six S-CIGATE-3 passes ran on baseline rubric only). (6) Second independent required CI check for GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE. (7) Gitleaks blocking / enforce_admins / `strict: false` config half. (8) Perimeter extension -- `docs/demo-evidence/` and/or `.factory/cycles/`. (9) Two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`). (10) `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT` / `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` / `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` follow-up experiments (E1/E2, ~10 min each). (11) Restore the telemetry-file convention deliberately (`TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`) -- not urgent. Then: S-TRAIL-DERIVATION-GUARD-1, AX23-001, S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1. |
| **Activation HEAD** | 7b3ba371 (PR #654 squash-merged 2026-07-25; SOH-ATTACHMENTS-1 CYCLE FULLY CLOSED; activation_version v0.6.0-dev.11) |

## Phase Progress

<!-- Full per-burst history through S-CIGATE-3-WIP-VERIFIED (2026-08-12): cycles/cycle-001/burst-log.md.
     The two rows below (pass-60/61, S-626-1-MERGE fix burst) are retained live per the
     structural adversary-pass/fix-burst requirement (D-435(b)/D-447(d)) and are ALSO archived
     verbatim in burst-log.md. The S-CIGATE-3-PUSHED row was folded into burst-log.md's prose
     this burst; every other prior burst is archived there too. -->
| Phase | Status | Completed | Gate | Notes | Finding Progression |
|-------|--------|-----------|------|-------|---------------------|
| pass-60 adversary (ADV-P60, Rust delta) / pass-61 adversary (ADV-P61, shell delta) | COMPLETE | 2026-08-10 | -- | Targeted delta reviews (not a Step-4.5 window) of `1381af17..5ca51bc2`; 9 actionable findings; 8 CLOSED pre-merge (3 HIGH + 1 MEDIUM + 4 LOW, via `736fea28`/`23ace476`/`f656f873`), 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN. | →1→3→0→2 (unchanged -- not a window pass) |
| **S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE fix burst (2026-08-10): PR #667 squash-merged to develop as `a5e1d087` (DEC-128/DEC-262), closing #626, via fix burst (`736fea28`+`23ace476`+`f656f873`). `develop` CI SUCCESS (run 31432422878). Story delivered and MERGED.** | COMPLETE | 2026-08-10 | -- | Factory paused pending next-priority dispatch. Four drift items recorded (2 process-gap, S-7.02-dispositioned inline; 2 external-research). DEC-262 recorded. Worktree cleanup complete. Next: S-CIGATE-3. | →1→3→0→2 |
| **S-CIGATE-3-WIP-VERIFIED (2026-08-12): resumed session found the worktree 4 commits + uncommitted WIP ahead of the last-recorded tip (reconciliation). Human ruled verify-then-commit, hold before push/PR (DEC-267). WIP verified (58/58 tests + full suite + clippy + fmt) and committed as `73a117cb`. Branch now 22 commits ahead of `origin/develop`, 5 unpushed.** | COMPLETE (bookkeeping) / AWAITING PUSH+PR+CI+MERGE RULING | 2026-08-12 | -- | Drift `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH) strengthened to cover all 22 commits. New drift `AC-006-CORRECTION-DRAFTED-UNCOMMITTED` (MEDIUM) -- an uncommitted story-file edit already appears to fix the AC-006 rationale, left uncommitted per pre-burst hygiene (out of this burst's instructed scope). | (S-CIGATE-3 window, not Step 4.5 -- trajectory-tail unchanged →1→3→0→2) |

## Current Phase Steps

<!-- Full step-by-step burst history: cycles/cycle-001/burst-log.md. This burst's own row is the
     only one retained live; the S-CIGATE-3-PUSHED row was archived there this burst under the
     same header convention as its Phase Progress counterpart. -->
| Step | Agent | Status | Output |
|------|-------|--------|--------|
| **S-CIGATE-3-WIP-VERIFIED (2026-08-12): state-manager ran the Single-Commit Burst Protocol (TD-VSDD-053, `vsdd-factory:state-burst` skill) to record: (1) session resume + BLOCKING `factory-worktree-health` PASS; (2) reconciliation -- worktree found 4 commits + uncommitted WIP ahead of the last-recorded tip; (3) human decision (DEC-267) to verify then commit, holding before push/PR; (4) verification results (58/58 `ci_gate_completeness` tests, full suite, clippy -D warnings, fmt clean) all re-stated as reported, not independently re-run by state-manager; (5) the WIP commit `73a117cb` and its position (22 commits ahead of `origin/develop`, 5 unpushed) re-verified via `git fetch`+`git rev-parse`+`git rev-list --count` in the read-only worktree; (6) a newly-discovered drift item (`AC-006-CORRECTION-DRAFTED-UNCOMMITTED`) from an uncommitted story-file edit found during pre-burst hygiene, left untouched per protocol. | state-manager | COMPLETED | `STATE.md` v2.41->v2.42, `cycles/cycle-001/{burst-log,session-checkpoints,decisions-archive,drift-items-open-detail}.md` appended (DEC-267; drift `AC-006-CORRECTION-DRAFTED-UNCOMMITTED` new, `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` strengthened), all committed to factory-artifacts in ONE atomic commit (Single-Commit Burst Protocol, no Stage-2 backfill, no SHA placeholder), pushed via CAS. No product code touched by this factory-artifacts burst itself -- the `73a117cb` commit and its verification were performed in `.worktrees/S-CIGATE-3` as separately-instructed work. `.factory/stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` left dirty/uncommitted (drift, not this burst's to resolve). `regression-state.json`/`sidecar-learning.md` left dirty per standing convention. |

## Decisions Log

<!-- Full Decisions Log (DEC-001 through DEC-267, minus the rows below) extracted to
     cycles/cycle-001/decisions-archive.md across the 2026-08-09 COMPACTION burst and every
     subsequent compaction, most recently this burst's addition of DEC-267 (human ruling to
     verify-then-commit the S-CIGATE-3 WIP, holding before push/PR) -- a historical record of a
     completed session action, not a standing constraint on future work, so DEC-267 is archived
     directly rather than added live here. Retained here, in full, are only the decisions that
     still GOVERN behavior today. -->
| ID | Decision | Rationale | Phase | Date |
|----|----------|-----------|-------|------|
| DEC-128 | **MERGE AUTHORITY IS THE HUMAN'S (CRITICAL).** Delivery sub-agents must NOT self-authorize merges, spawn fix agents, push, or enter unbounded loops. CI green / mergeStateStatus CLEAN is not merge authorization. *(Original ruling is inside the collapsed DEC-001..DEC-155 range in decisions-archive.md, which has no separate DEC-128 row of its own; this entry restates the standing citation form used verbatim throughout this project's history.)* | Foundational merge-safety constraint; cited at every subsequent merge-adjacent decision (DEC-234, DEC-235, DEC-238, DEC-243, DEC-258, DEC-262, DEC-264, DEC-265, DEC-266, DEC-267...). | Phase 0-3 (original); standing | archived origin; standing |
| DEC-206 | **VOID PROTOCOL FOR ISOLATION BREACHES.** Adversary passes where orchestrator dispatch defects leak banned-path content are VOID for step-4.5 window eligibility; findings remain valid. | Human ruling on isolation protocol. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-03 |
| DEC-224 | **ISOLATION ELIGIBILITY PRINCIPLE.** A pass is ELIGIBLE (not VOID) when a letter-of-rule isolation deviation occurred but zero banned content actually surfaced; VOID applies only when banned content actually became visible. Self-disclosure without surfacing is a POSITIVE signal. Applied and held across every subsequent window through 57/58/59. | Principled distinction: the rule prevents contamination, not path syntax deviation. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-04 |
| DEC-245 | **DEC-204 ADJUDICATED -- CONSERVATIVE READING RULED.** A pass is CLEAN only with zero HIGH, zero MEDIUM and zero LOW findings; INFO-only findings still count as CLEAN; LOW findings reset the window regardless of GAP-vs-refinement classification. Step 4.5 remains 0/3, confirmed by ruling. Applied by DEC-265b to S-CIGATE-3's own story-scoped window for cross-story consistency. | Lenient reading would have stopped the cycle before discovering ADV-P50-HIGH-001, pass-51's three HIGH findings, pass-53's two HIGH findings, pass-55's HIGH finding, pass-57's HIGH finding, this burst's ADV-P60/P61 HIGHs, and S-CIGATE-3's pass-3 HIGH -- the conservative criterion has been expensive but a productive defect finder to the very end. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-08 |
| DEC-259 | **NEXT PRIORITY REMAINS S-CIGATE-3, NOT AN ELEVENTH WINDOW.** Human ruled the durable YAML-parser fix (S-CIGATE-3) remains next, rather than dispatching an eleventh STRICT adversarial window. **STATUS (2026-08-12): S-CIGATE-3 now IMPLEMENTED, WIP-VERIFIED (see DEC-265, DEC-266, DEC-267), unmerged, no PR, human ruling pending.** | Ten consecutive windows without 3/3, and this window proved the class generates NEW AXES (positional-assumption, alongside value-reparse) rather than only new instances of one already-known axis -- now evidence-backed argument, not inference, for replacing hand-rolled line-based extraction with a real YAML parser. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-260 | **S-CIGATE-3 PARSER DECISION: `saphyr-parser` PINNED `=0.0.11`, DEV-DEPENDENCY, EVENT-STREAM API ONLY.** Rejected a PyYAML shell-out (no workflow in this Rust project uses Python) and `serde_yaml_ng` (cannot see anchors, tags, styles, or duplicate keys). The crate's own high-level `saphyr::Yaml` API is FORBIDDEN for this story's guards -- verified to silently collapse duplicate keys, erase quoting styles, and resolve aliases, reintroducing the exact "checker sees less than the runner" defect class this story exists to close. `yaml-rust2 0.11.0` recorded as the pre-vetted fallback. **STATUS (2026-08-11): implemented as specified -- `saphyr-parser =0.0.11`, event-stream API only, confirmed via `Cargo.toml` and the story's own AC-002.** | Established by direct execution against a real build in an isolated scratch worktree (`yaml-crate-probe`) this session, not from crate documentation alone; human-approved this session. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-262 | **MERGE AUTHORIZED ON CODE GROUNDS WITH STEP 4.5 PERMANENTLY AT 0/3 AFTER TEN WINDOWS AND 61 PASSES.** Human ruled PR #667 could merge without ever reaching the DEC-199/DEC-245 3/3-CLEAN convergence bar. PR #667 squash-merged to `develop` as `a5e1d087`, closing #626. Two pre-merge targeted delta reviews (pass-60/pass-61, ADV-P60/ADV-P61, not a Step-4.5 window) covered the previously-unreviewed `1381af17..5ca51bc2` delta and found 9 actionable findings, 8 closed (3 HIGH + 1 MEDIUM + 4 LOW) via fix burst before merge; 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN, not fixed. | `src/` 0-defect across 32+ passes; all findings across ten windows plus the two delta reviews confined to test/guard infrastructure, never shipped product code; the guard apparatus is materially stronger than what `develop` carried before this story, and its documented claims now match its actual behavior after this burst's fixes; the residual failure class (line-based extraction generating genuinely new inspection axes each window) is routed to S-CIGATE-3 rather than an eleventh window, which the ten-window trend suggested would likely find a new axis rather than converge. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |

## Skip Log

All S-WIN-1..6 + #475 + S-FORK-OPS-BACKFILL-1 + S-FORK-OPS-GITLEAKS-DOC-1 + S-MAINT-DEAD-CITATION-CI + S-626-1 per-AC demos: Yes -- adapted (S-626-1: 11 artifacts at `.factory/demos/S-626-1/`). See cycles/cycle-001/burst-log.md.

## Blocking Issues

None open.

## Drift Items (open / tracked)

<!-- COMPACTED 2026-08-09, RE-COMPACTED 2026-08-11 (COMPACT-STATE burst); 1 row updated + 1 new
     row added by this burst (S-CIGATE-3-WIP-VERIFIED). This table is a compact one-line-per-item
     index. Full narrative bodies for ALL rows below are in
     cycles/cycle-001/drift-items-open-detail.md -- update the row there and this index together
     going forward; do not let a Summary cell regrow past one line. Per-burst new/updated/closed
     drift-item ledger through this burst: cycles/cycle-001/burst-log.md. -->
| ID | Severity | Summary |
|----|----------|---------|
| GUARD-MODE-UNREACHABLE-LOCALLY | HIGH | Environment-gated guard branches have no local test coverage by construction. CLOSED for this instance (`f2bea32e`); general rule DEFERRED to review checklist. Full detail: `cycles/cycle-001/drift-items-open-detail.md`. |
| POSITIONAL-ASSUMPTION-AXIS | HIGH | `extract_key_name_at_indent`'s hard-coded 4-space indent is unchecked -- second axis of the line-based-extraction defect class. **CLOSED BY CONSTRUCTION (2026-08-11, S-CIGATE-3-IMPLEMENTED)** -- event-stream model has no indent arithmetic. Full detail: `drift-items-open-detail.md`. |
| MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM | MEDIUM | FOURTH+FIFTH INSTANCE (2026-08-11, S-CIGATE-3-IMPLEMENTED): a dispatch instruction's own prose total (16 commits, 29 findings) each mismatched its own supporting data (17 commits, 27 findings); both re-derived and corrected. Standing rule reaffirmed. Full detail: `drift-items-open-detail.md`. |
| RED-PROOF-NEEDS-SPELLING-VARIANTS | HIGH | Confirmed TWO-AXIS: a RED proof must cover both the spelling axis and the indent/position axis. **CLOSED (2026-08-11, S-CIGATE-3-IMPLEMENTED)** -- two-axis RED proofs (AC-008) across every rewritten guard; the AC-008 test itself was verified and committed (`73a117cb`) this burst. Full detail: `drift-items-open-detail.md`. |
| MATRIX-FAIL-FAST-MASKS-SCOPE | LOW | Matrix fail-fast made cancelled legs on run 31406705091 read as genuinely failed. Full detail: `drift-items-open-detail.md`. |
| ORCHESTRATOR-STALE-AGENT-NAME-COLLISION | MEDIUM | A stopped, re-dispatched agent stays addressable under a canonical-looking name. Corrective: never resolve inputs by contacting agents by name. Full detail: `drift-items-open-detail.md`. |
| RESEARCH-ARTIFACTS-NOT-PERSISTED | MEDIUM | DEC-246 drove a code change with no research artifact file. DEFERRED, no story opened. Full detail: `drift-items-open-detail.md`. |
| DEC-246-OVERCLAIMED-CONFIRMS | MEDIUM | DEC-246's original 8/8 CONFIRM was overclaimed; two re-validation passes (DEC-249, DEC-261) found a mixed disposition. Full detail: `drift-items-open-detail.md`. |
| BURST-LOG-DEFEATS-PLAIN-GREP | LOW | `burst-log.md` contains bytes plain `grep` treats as binary; use `grep -a`. Full detail: `drift-items-open-detail.md`. |
| ADVERSARY-PASSES-27-61-HAVE-NO-DETAIL-FILE | LOW | Per-pass detail artifacts exist only for passes 1-19/21-26; `ADV-P1-INDEX.md` is the sole record for the rest (now also true of S-CIGATE-3's 6 passes). Full detail: `drift-items-open-detail.md`. |
| STORY-INDEX-DENOMINATOR-UNRECONCILED | MEDIUM | `STORY-INDEX.md` declares 127 stories; actual file/row counts don't reconcile. No audit run yet. Full detail: `drift-items-open-detail.md`. |
| STORY-STATUS-DRIFT-INDEX-UNRELIABLE | MEDIUM | 37 stories marked `ready` spot-check as already shipped. Recommend a reconciliation sweep. Full detail: `drift-items-open-detail.md`. |
| RUNNER-SOURCE-NOT-AN-ORACLE-FOR-WORKFLOW-PARSING | MEDIUM | `actions/runner` public source lags shipped server-side parsing (e.g. rejects anchors GitHub shipped 2025-09-18); not authoritative for parsing questions. Full detail: `drift-items-open-detail.md`. |
| DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST | HIGH | An agent reported a mechanism complete having written only documentation for it; verification checked the reported metric, not the mechanism. CLOSED for this instance (`f656f873`); general rule DEFERRED. Full detail: `drift-items-open-detail.md`. |
| ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION | MEDIUM | A requested "small consistency fix" converted a fail-closed extractor panic into a fail-open silent miss. CLOSED for this instance (`f656f873`); general rule DEFERRED. Full detail: `drift-items-open-detail.md`. |
| JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT | MEDIUM | Passwordless sudo on GitHub-hosted runners makes `ADV-P59-LOW-001` not closable by any in-script check; real control is not running untrusted steps before the gate step. Reconfirmed 2026-08-11 (DEC-263). Full detail: `drift-items-open-detail.md`. |
| GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE | MEDIUM | `GITHUB_ACTIONS` env-var overwrite UNVERIFIED; `RUNNER_OS` CONFIRMED protected by `ScriptHandler.cs`'s write-ordering (DEC-263). Minimal experiment specified, not run. Full detail: `drift-items-open-detail.md`. |
| JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION | MEDIUM | `resolve_trusted_jq`'s decision-path trust reduces to `/usr/bin` being root-only (INFERRED, not CONFIRM); a ~10-min experiment (E1) would settle it. DEFERRED. Full detail: `drift-items-open-detail.md`. |
| MACOS-ALLOWLIST-TRUSTS-WRITABLE-DIR | LOW | `/opt/homebrew/bin` is runner-writable without sudo, `test` job's macOS leg only (not the decision path). Entry must NOT be removed -- reproduces CI-BREAK-1. Full detail: `drift-items-open-detail.md`. |
| CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS | MEDIUM | PR #675, whose purpose was correcting prior inaccuracies, introduced two new MEDIUM false claims of its own (amended `d2430a8a`). Corrective: apply the same claim-accuracy review to correction PRs. Full detail: `drift-items-open-detail.md`. |
| SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN | LOW | A source's scoped claim lost its scope at each citation hop, gaining an unwarranted CONFIRM label at the last hop (amended `d2430a8a`). Corrective: re-fetch the original at each hop. Full detail: `drift-items-open-detail.md`. |
| AC-006-FALSE-RATIONALE-UNCORRECTED | MEDIUM | Story file's AC-006 rationale is FALSE (msrv job cannot be affected by a dev-dependency pin). **A candidate fix already exists (2026-08-12) as an uncommitted local edit -- see `AC-006-CORRECTION-DRAFTED-UNCOMMITTED` below -- but is not yet landed, so this item remains OPEN.** Full detail: `cycles/cycle-001/drift-items-open-detail.md`. |
| AC-006-CORRECTION-DRAFTED-UNCOMMITTED | MEDIUM | **NEW (2026-08-12, S-CIGATE-3-WIP-VERIFIED).** An uncommitted, unstaged local edit to the story file (v1.3->v1.6) that appears to substantively fix `AC-006-FALSE-RATIONALE-UNCORRECTED` was discovered mid-burst; left untouched (pre-burst-hygiene rule) pending a human/orchestrator review-and-commit decision. Full detail: `drift-items-open-detail.md`. |
| VALUE-SIDE-ANCHOR-GAP-UNCLOSED | LOW | `resolve_value` discards `anchor_id` on a scalar's VALUE side (`run: &x cmd`); documented not closed, no exploit constructible against any pinned guard. Full detail: `drift-items-open-detail.md`. |
| POLICIES-YAML-NOT-INSTANTIATED | LOW | `.factory/policies.yaml` absent from this project; all six S-CIGATE-3 adversarial passes ran on baseline rubric only. Full detail: `drift-items-open-detail.md`. |
| RECURRING-DEFECT-RELOCATES-NOT-CLOSES | MEDIUM | Same defect shape relocated 4x across S-CIGATE-3 passes 3-6 (by NAME, then `run:` KEY, then `run:` VALUE, then "actually RUNS") before a class sweep closed it. Full detail: `drift-items-open-detail.md`. |
| S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED | HIGH | **STRENGTHENED (2026-08-12, S-CIGATE-3-WIP-VERIFIED).** `test/ci-gate-real-yaml-parser` now carries **22 commits** never validated by CI (17 pushed-no-PR + 5 further unpushed local commits, incl. this burst's verified `73a117cb`); `origin` remains at `aeeebe01`. Full detail: `cycles/cycle-001/drift-items-open-detail.md`. |
| TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT | LOW | Burst `86ddb331` committed `regression-state.json`/`sidecar-learning.md` to factory-artifacts, breaking the "leave dirty, never commit" convention. Human ruled leave-as-is (DEC-266b) rather than force-push a revert. Full detail: `drift-items-open-detail.md`. |

## Convergence Status

**COMPACT-STATE (2026-08-11):** proactive compaction burst (human-approved); no adversary pass, no findings added or closed, does not move the trajectory-tail (→1→3→0→2, unchanged) or Step 4.5's 0/3. Extracted the three most recent closed-burst Convergence Status paragraphs (S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE, SHELL-TRUST-ASSUMPTIONS, PR675-MERGE+ADV-P675-CLOSE) to `cycles/cycle-001/convergence-trajectory.md`.

**S-CIGATE-3-WIP-VERIFIED (2026-08-12):** verify-and-reconcile bookkeeping burst, NOT a Step-4.5 window and NOT a new S-CIGATE-3 adversarial pass -- does not move SOH-DX-1's trajectory-tail (→1→3→0→2, unchanged) or Step 4.5's 0/3 (permanently closed per DEC-262), and does not reopen or re-score S-CIGATE-3's own 6-pass window (still ended PERMANENTLY at 0/3, DEC-265, 27 findings, all fixed). This burst's only new facts: 4 previously-unrecorded local commits plus one new, verified, committed WIP commit (`73a117cb`) are now part of the record (22 commits total, 5 unpushed), and an uncommitted candidate fix for `AC-006-FALSE-RATIONALE-UNCORRECTED` was discovered and left for human review. `ADV-P1-INDEX.md` combined total unchanged at 488. Full trajectory detail: cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md § S-CIGATE-3 + cycles/cycle-001/convergence-trajectory.md.

BC-INDEX v6.75 / STORY-INDEX v1.5.81 (127 stories) / ARCH-INDEX v0.16. S-626-1 adversary: 61 Step-4.5-eligible passes plus ADV-P675; 461 findings (unchanged, SOH-DX-1 scope). S-CIGATE-3 adversary: 6 story-scoped passes, 27 findings (unchanged this burst). `ADV-P1-INDEX.md` combined total: 488. **PR #667 MERGED** (`a5e1d087`, closes #626 -- DEC-262); **PR #675 MERGED** (`d55bedf7` -- DEC-264). `develop` CI SUCCESS on `d55bedf7` (run 31450052302), unchanged by this burst. src/ 0-defect THIRTY-THIRD-plus consecutive (SOH-DX-1 scope). **S-CIGATE-3 implemented, WIP-verified (22 commits, 5 unpushed), unmerged, no PR, never CI-validated -- human ruling pending.** AX23-001 PENDING RATIFICATION. **Factory PAUSED.** Full trajectory detail: cycles/cycle-001/convergence-trajectory.md + cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md.

SOH-ATTACHMENTS-1 + prior cycles FULLY CLOSED. See cycles/cycle-001/convergence-trajectory.md.

## Concurrent Cycles

<!-- Prior completed cycles archived to burst-log: ADF-CODE-MARK-EXCLUSIVITY (DEC-163, v0.6.0-dev.8), SOH-BUGS-1 (DEC-167, v0.6.0-dev.9), SOH-COMMENT-CRUD-1 (DEC-176, v0.6.0-dev.10), SOH-ATTACHMENTS-1 (DEC-186, v0.6.0-dev.11). See cycles/cycle-001/convergence-trajectory.md. -->
| Cycle | Status | Notes |
|-------|--------|-------|
| SOH-DX-1 (issues #639+#627+#626) | F4 DELIVERY -- DEC-204 fully ADJUDICATED (DEC-245). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged as `a5e1d087`, closes #626 -- DEC-262; trust-layer record correction PR #675 also MERGED as `d55bedf7` -- DEC-264); Step 4.5 ends PERMANENTLY at 0/3 -- 61 Step-4.5-eligible passes, never reached 3/3, plus ADV-P675 (not Step-4.5-eligible). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671 squash-merged `df203233`); **S-CIGATE-3 IMPLEMENTED, WIP-VERIFIED, UNMERGED** (v1.3, status `in-progress`; 22 commits on `test/ci-gate-real-yaml-parser`, 5 unpushed beyond the pushed `aeeebe01` tip, no PR opened, never CI-validated; 6-pass story-scoped window ended PERMANENTLY at 0/3 -- DEC-265; human ruling pending on push/PR-opening/merge); **S-CIGATE-4** (done) unchanged; **S-TRAIL-DERIVATION-GUARD-1** (P2/draft, status unresolved) unchanged. FACTORY PAUSED (SESSION-WRAP-PAUSE, COMPACT-STATE, S-CIGATE-3-IMPLEMENTED, S-CIGATE-3-PUSHED, S-CIGATE-3-WIP-VERIFIED -- all 2026-08-11/12). | 3 stories: S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1, S-626-1 (DELIVERED AND MERGED). Plus S-CIGATE-2 (DELIVERED/MERGED), S-CIGATE-3 (implemented, WIP-verified, unmerged, no PR, never CI-validated, 22 commits/5 unpushed), S-CIGATE-4 (P1, done), S-TRAIL-DERIVATION-GUARD-1 (P2, draft, status unresolved). S-626-1 adversary: 61 Step-4.5-eligible passes + ADV-P675; 461 findings; THIRTY-THIRD-plus zero-src/-defect consecutive. S-CIGATE-3 adversary: 6 story-scoped passes, 27 findings, all fixed. AX23-001 PENDING. |

## Session Resume Checkpoint
| Field | Value |
|-------|-------|
| Position | Feature Mode SOH-DX-1, phase F4. **S-CIGATE-3-WIP-VERIFIED burst (2026-08-12): session resumed; `factory-worktree-health` ran BLOCKING and PASSED.** Factory remains PAUSED (unchanged). trajectory-tail →1→3→0→2 (unchanged). Headline: S-626-1 DELIVERED (PR #667 merged as `a5e1d087`, closes #626); PR #675 MERGED (`d55bedf7`). `develop` @ `d55bedf7`, unchanged this burst, CI SUCCESS (run 31450052302). **RECONCILIATION this burst's record: the `.worktrees/S-CIGATE-3` worktree was found 4 commits ahead of the last-recorded tip `aeeebe01`** (`99f53383`, `dfc69662`, `bc86d4ce`, `d32f9f67` -- ADV pass-2/5/6 fixes the prior burst's record predated) **plus uncommitted WIP** (a new `test_ac_008_guards_are_key_spelling_and_indent_agnostic`, ~165 lines; a small `CLAUDE.md` edit). **Human ruled (DEC-267): verify the WIP fully, then commit -- do NOT push, do NOT open a PR, hold for human review.** Verification PASSED: `cargo test --test ci_gate_completeness` 58/58 (incl. new AC-008 test); full `cargo test` no regressions; `cargo clippy --all-targets -- -D warnings` PASS (after fixing 5 `clippy::doc_lazy_continuation` errors in the new test's doc comment); `cargo fmt --all -- --check` clean. WIP committed as `73a117cb`. Working tree of `.worktrees/S-CIGATE-3` clean (re-verified). Branch `test/ci-gate-real-yaml-parser` HEAD = `73a117cb`, **22 commits ahead of `origin/develop`**, of which **5 are unpushed** (re-verified via `git fetch`+`git rev-parse origin/...`+`git rev-list --count`; `origin` still at `aeeebe01`). **NO PR opened, NO CI ran** (unchanged from prior burst; not re-confirmed via a fresh `gh pr list` call this burst since no PR-affecting action occurred). Drift discovered mid-burst: `.factory/stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` has an uncommitted local edit (v1.3→v1.6) that appears to already fix `AC-006-FALSE-RATIONALE-UNCORRECTED` -- left untouched (new drift `AC-006-CORRECTION-DRAFTED-UNCOMMITTED`), out of this burst's instructed scope. |
| Convergence | Unchanged this burst. SOH-DX-1's Step 4.5 remains PERMANENTLY at 0/3 (trajectory-tail →1→3→0→2, unchanged). S-CIGATE-3's OWN 6-pass story-scoped adversarial window remains ended PERMANENTLY at 0/3 (27 findings, 1 HIGH + 10 MEDIUM + 16 LOW, all fixed, no pass ever CLEAN, DEC-265) -- not reopened or re-scored by this burst; no new adversarial pass ran. `ADV-P1-INDEX.md` combined total: 488, unchanged. `src/` 0-defect streak intact throughout SOH-DX-1 scope (33+ consecutive). This burst added no findings and closed none — it reconciled 4 pre-existing commits and one new, verified commit into the record. |
| Pending human decisions / next steps, in priority order | (1) **Whether/when to push the 5 unpushed commits** to `origin` (remote stale at `aeeebe01`). (2) **Whether/when to open a PR for `test/ci-gate-real-yaml-parser`** -- the concrete next action that would trigger the branch's first-ever CI run. (3) **Merge ruling on S-CIGATE-3** once CI has run -- the adversarial window ended 0/3, never converged; the DEC-262 shape is available as precedent but not yet exercised. (4) **Review the uncommitted AC-006 correction draft** in the story file (`AC-006-CORRECTION-DRAFTED-UNCOMMITTED`) -- if sound, commit explicitly; would likely close `AC-006-FALSE-RATIONALE-UNCORRECTED`. (5) Value-side anchor gap (`run: &x cmd`, `resolve_value` discards `anchor_id`) -- documented, not closed. (6) Whether the `ScalarStyle::Plain` fidelity mandate should become a formal decision record. (7) Whether to instantiate `.factory/policies.yaml` (absent; all six S-CIGATE-3 passes ran on baseline rubric only). (8) 643 lines merged unreviewed via PR #667 remain a residual (SHELL-TRUST-ASSUMPTIONS validated the shell portion, DEC-263; the Rust portion is largely superseded by S-CIGATE-3's own rewrite). (9) Two ~10-minute throwaway-workflow experiments remain unrun: E1 (`/usr/bin` write-access probe) and E2 (`/opt/homebrew/bin` writability). (10) STORY-INDEX has two unresolved integrity problems (37 stories marked `ready` that spot-check as shipped; `total_stories: 127` unreconciled against ~91 files / ~210 rows) -- recommend an audit/reconciliation sweep. (11) `.factory/hooks/` was never instantiated in this project. (12) Restore the telemetry-file convention deliberately when convenient -- not urgent. (13) 16+ local branches with deleted remotes (`[gone]`), pre-existing, never actioned. Carried forward unchanged: second required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); gitleaks/enforce_admins/`strict: false` config ruling; perimeter extension (`docs/demo-evidence/`, `.factory/cycles/`); `S-MAINT-CR-008`/`S-TRAIL-DERIVATION-GUARD-1` unresolved statuses; AX23-001 ratification; MIXED-SET-DASH-ARM-UNPINNED scheduling; ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling; SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade; BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS follow-up BC/VP-minting authorization. |
| Not lost work | `.worktrees/S-CIGATE-3` and its branch `test/ci-gate-real-yaml-parser` are **NOT lost or abandoned work** -- they hold the full, tested, adversarially-reviewed, now further-verified S-CIGATE-3 implementation (22 commits), deliberately left with 5 commits unpushed beyond the previously-pushed 17-commit tip pending the human's next-step rulings (DEC-128, DEC-266a, DEC-267). The two dirty `.factory/` telemetry files (`regression-state.json`, `sidecar-learning.md`), the uncommitted `.factory/stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` edit, and the untracked `.claude/` product-repo files are all **standing conventions / pre-existing or discovered-but-unresolved drift** -- explicitly NOT work-in-progress that got interrupted by this pause. A fresh session should not attempt to push, open a PR, merge, or "clean up" any of this without a separate, explicit instruction. |
| Closing note | This burst performed NO product code changes itself -- the `73a117cb` commit and its verification were performed in `.worktrees/S-CIGATE-3` per explicit, separate instruction; this factory-artifacts burst only records that outcome. Every SHA/count cited was re-derived against a live command this burst (`git log --oneline`, `git rev-parse`, `git fetch origin ...` + `git rev-parse origin/...`, `git rev-list --count`, `git -C .factory status`) rather than assumed from the dispatch instruction. Every write to this file used the `Write` tool with a single full-content write, per the standing DEC-247 convention. |
| Resume command | Open a fresh session in this project and run `/vsdd-factory:next-step`. It reads `STATE.md` and picks up at the push/PR/merge-ruling decision for `test/ci-gate-real-yaml-parser` as the next priority. |

## RESUME PLAN (cold-start)
Step 1 (BLOCKING): Run vsdd-factory:factory-worktree-health. Read this file.
Step 2 -- **S-CIGATE-3 (durable YAML-parser fix) is IMPLEMENTED and WIP-VERIFIED on `test/ci-gate-real-yaml-parser`, HEAD `73a117cb`, 22 commits ahead of `origin/develop`, of which 5 (`99f53383`..`73a117cb`) are UNPUSHED beyond the previously-pushed tip `aeeebe0147e2cb616c34c6f05b54f135d62dd229`.** NO PR opened (merge authority is the human's, DEC-128). **NO CI has ever run on this branch** -- `ci.yml` triggers push-only on `develop`/`main`; a `test/`-prefixed branch fires nothing without an open PR -- drift item `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH, strengthened this burst): all 22 commits are validated only locally on macOS, and this project has a recorded incident (CI run 31128902318) of a Windows-only dead-code failure structurally unreachable by local verification. Its own 6-pass story-scoped adversarial window ended PERMANENTLY at 0/3 (27 findings, all fixed, no CLEAN pass; DEC-265) -- not reopened this burst. A candidate fix for `AC-006-FALSE-RATIONALE-UNCORRECTED` was discovered this burst as an **uncommitted local edit** to the story file (v1.3→v1.6) -- review and, if sound, commit it explicitly (drift `AC-006-CORRECTION-DRAFTED-UNCOMMITTED`). Separately, `86ddb331`'s telemetry-file convention break remains left in place per human ruling (DEC-266b; drift item `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`, LOW) -- not lost or accidental, do not revert without explicit instruction. **Factory is PAUSED.** trajectory-tail →1→3→0→2 (unchanged this burst). SOH-DX-1's own state is unchanged from the prior burst: S-626-1 DELIVERED AND MERGED (`a5e1d087`, closes #626); PR #675 also MERGED (`d55bedf7`); Step 4.5 ends permanently at 0/3 (DEC-262). `develop` CI SUCCESS on `d55bedf7` (run 31450052302), unaffected by S-CIGATE-3. Seek human confirmation/dispatch, in order: (1) whether/when to push the 5 unpushed commits; (2) whether/when to open a PR (triggers first-ever CI run on this branch); (3) S-CIGATE-3 merge ruling once CI has run; (4) review/commit decision on the uncommitted AC-006 correction draft; (5) whether to instantiate `.factory/policies.yaml`; (6) second independent required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); (7) gitleaks blocking / enforce_admins / `strict: false` config half; (8) whether to extend the perimeter to `docs/demo-evidence/` and/or `.factory/cycles/`; (9) the two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`); (10) follow-up experiments for `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT`/`GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE`/`JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` (E1/E2); (11) restore the telemetry-file convention deliberately when convenient.
Step 3 -- Once S-CIGATE-3's push/PR/merge ruling is resolved: also pending: S-640-1 handoff, S-MAINT-576-HYG-1, S-639-1 (BREAKING/v0.6.0-dev.12). MIXED-SET-DASH-ARM-UNPINNED test story to schedule. ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling needed; SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade ruling needed; BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS needs a follow-up-story ruling; STORY-INDEX denominator audit and STORY-STATUS-DRIFT reconciliation sweep recommended. **Dispatch discipline reminder (standing):** state-manager runs LAST in a burst; never dispatch two agents writing to a shared artifact concurrently; NEVER solicit input by messaging an agent by name -- the orchestrator is the single source of completed results (ORCHESTRATOR-STALE-AGENT-NAME-COLLISION); verify count/trail claims against the derivation command, not a recorded number (MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM, five-times-recurred); frontier variety, not pass count, is what makes a CLEAN verdict meaningful; a RED proof must cover every semantically-equivalent spelling AND every legal indent/position of its target (RED-PROOF-NEEDS-SPELLING-VARIANTS, closed for S-CIGATE-3's rewritten guards); an environment-gated guard branch must carry a test that forces the gate variable (GUARD-MODE-UNREACHABLE-LOCALLY); verify the mechanism a change claims to add, not only the metric it reports on (DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST); a change to an extractor's failure direction needs its own bidirectional RED proof, regardless of how the requesting instruction is framed (ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION); a correction is itself an operation that can corrupt a record -- apply the same claim-accuracy review to a record-correction PR as to the record it corrects, and to a story file's own AC text as to the code comment already fixed (PR675-MERGE+ADV-P675-CLOSE, AC-006-FALSE-RATIONALE-UNCORRECTED); pushing a branch, opening a PR, and committing further verified WIP are three separable human authorizations -- do not conflate any one of them with another (DEC-266a, DEC-267); a convention break discovered after the fact is not automatically worth a force-push to fix -- weigh the revert's own risk against leaving it as documented drift (DEC-266b); an unrelated uncommitted modification discovered mid-burst must not contaminate the burst commit -- record it as drift and leave it for explicit review instead (AC-006-CORRECTION-DRAFTED-UNCOMMITTED, this burst); when STATE.md needs an update, use the Write tool with content that advances `timestamp:` -- that is the sanctioned path (DEC-247); when `.factory/cycles/cycle-001/burst-log.md` needs grepping, use `grep -a` (BURST-LOG-DEFEATS-PLAIN-GREP); STATE.md's Phase Progress section must always retain at least one row mentioning 'pass-N' and one row mentioning 'fix burst', every prescribed trajectory-tail-cell site must carry the arrow-sequence literally, and the frontmatter D-chain cite must track the MAX D-number cited anywhere in the body -- `validate-state-structure`/`validate-trajectory-tail-cell-completeness`/`validate-dispatch-advance` each block a write that misses one of these (D-435(b)/D-443(a)/D-447(d)/D-453(d)).

## Open Issues Tracker

| Issue | Title | Status | Priority | Notes |
|-------|-------|--------|----------|-------|
| #429 | jr_isolated() crypto-random suffix | OPEN | LOW | DEC-029 deferred to human. Do NOT close autonomously. |
| #400 | Test-hardening + process-gap follow-ups | OPEN -- Story A MERGED PR #431. Story B + engine items remain. | LOW | |
| #372 | cargo-mutants partial baseline | OPEN | LOW | Follow-up from #346 |
| #387/#368 | git history rewrite / open PR | OPEN | LOW | Force-push needed. Deferred. |

## Historical Content

See `cycles/cycle-001/`: burst-log.md, convergence-trajectory.md, session-checkpoints.md, lessons.md, blocking-issues-resolved.md, decisions-archive.md, drift-items-closed.md, drift-items-open-detail.md, adversarial-reviews/ADV-P1-INDEX.md.
