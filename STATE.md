---
document_type: pipeline-state
level: ops
version: "2.37"
status: active
producer: state-manager
timestamp: 2026-08-11T02:50:00Z
phase: 3
pipeline: ACTIVE
inputs: []
input-hash: "[live-state]"
traces_to: ""
project: jira-cli
mode: brownfield
current_step: "POST-CLOSE ARITHMETIC CORRECTION (2026-08-10, second same-day correction on this burst, distinct from the WINDOW-57-58-59 process-gap note immediately below): the S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE burst's own ADV-P1-INDEX.md arithmetic was wrong at commit time. Team-lead-supplied aggregates (+10 new findings, 452 total, 6 actionable) were recorded as authoritative without independently summing the per-pass severity tallies already present in that same commit's own table (P60 2H+0M+3L+3I=8, P61 0H+2M+2L+2I=6, sum=14, not 10). A downstream agent dispatched to add itemized finding detail caught the mismatch by arithmetic, correctly declined to silently override the recorded (wrong) number, and left a reconciliation note flagging the discrepancy rather than fabricating detail -- exactly right. This correction burst supplies the corrected arithmetic the team-lead re-derived by hand: total_findings 442->456 (not 452), +14 new (not +10), 9 actionable non-INFO findings (not 6: 3 HIGH + 1 MEDIUM + 5 LOW). 8 of the 9 actionable findings are CLOSED; the 9th, `ADV-P61-LOW-004` (check 13 of `run_jq_trust_self_test` hard-fails `--self-test` on any host whose ambient jq sits outside the hardcoded `/usr/bin`/`/bin`/`/usr/local/bin`/`/opt/homebrew/bin` allowlist -- nix, mise/asdf, `~/.local/bin`, Linux Homebrew), was independently VERIFIED OPEN against the committed code at this HEAD (`4ee308fb`) -- both by direct reading of `resolve_trusted_jq`/`run_jq_trust_self_test` in `scripts/check-ci-gate.sh` and by an empirical `--self-test` reproduction (a shimmed untrusted-directory jq produces a real exit code 1). `ADV-P1-INDEX.md` v2.15->v2.16 corrected in full: header (`total_findings`, `severity_distribution` now `{CRIT:0,HIGH:35,MED:131,LOW:157,INFO:133}`), all Pass-60/Pass-61 catalog rows (six generic "detail not itemized" placeholders replaced with real content cross-checked against `scripts/check-ci-gate.sh`'s own in-code ID citations -- e.g. `ADV-P61-LOW-003`/`ADV-P61-INFO-006` are literal comment citations in that file, confirming they are real dispatch IDs rather than invented by this recording pass), the Reconciliation note, and the Pass 60/61 Summary. Recorded as the THIRD instance of drift item `MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM` -- see Drift Items table; corrective sharpened this instance: an aggregate is a measurement, derive it, do not estimate it; a downstream agent flagging an arithmetic mismatch is a correction to act on, not noise to override. D-chain cite D-254..D-262 (exhaustive) latest brownfield. trajectory-tail →1→3→0→2. POST-CLOSE CORRECTION (2026-08-10): the closed WINDOW-57-58-59+SWEEP-2+CI-BREAK burst below is unchanged and stands; this note records a same-day process-gap discovered immediately after that commit landed. The state-manager (this agent) messaged agents by name (`pass-57`/`pass-58`/`pass-59`) seeking itemized finding detail; those were STALE, STOPPED dispatches at superseded head `910b8ab0` (two commits stale), not the real `pass-57b`/`pass-58b`/`pass-59b` agents that produced the 23 findings against `1381af17`. Messaging a stopped agent WOKE it -- `pass-57` and `pass-58` correctly refused and flagged the head mismatch; `pass-59` also refused; a separately-solicited `class-sweep` agent (already completed hours earlier) was woken and died on an API error. No fabricated data reached the permanent record: the finding catalog already committed in `ADV-P1-INDEX.md`/`STATE.md` was built from the team-lead's dispatch brief and independently re-verified commit content (`a17939e2`, `f2bea32e`), not from the stale agents' replies, which arrived after the burst commit had already landed and were correctly refused/empty. Recorded as new drift item `ORCHESTRATOR-STALE-AGENT-NAME-COLLISION` (MEDIUM) per team-lead instruction -- see Drift Items table. WINDOW-57-58-59+SWEEP-2+CI-BREAK (2026-08-10): Reviewed frozen head `1381af17` -- discovered a prior working session had already landed it ("test(ci): close pass-54/55/56 LOW residuals + Guard B docstring"), previously unrecorded on any `.factory/` surface; caught it up, closing three deferred drift items (`EXTRACT-JOB-BLOCK-RAW-ANCHOR-WIDENED`, `DENOMINATOR-GUARD-USES-EXACT-LINE-MATCH`, `SIBLING-WORKFLOW-FRONTIER-UNRETIRED`). Dispatched pass-57 adversary, pass-58 adversary, pass-59 adversary review against three human-approved frontiers (DEC-254: C1-lexer differential conformance -- Python ports cross-checked against PyYAML 6.0.3 + Ruby Psych + actionlint, standalone rustc reproductions, an 11-job re-indent sweep; C5-falsifiability -- scratch-tree per-mutation rebuilds; C3-side-channels -- inter-step side channels inside ci-gate, never probed in 56 prior passes, real extractors replayed against 7 mutated ci.yml copies). Window CLOSED 0/3 -- TENTH consecutive window without 3/3 since window 30/31/32; 23 findings (1H/4M/6L/12I), zero rediscoveries, converging on a NEW failure axis distinct from window 54/55/56's value-reparse-only diagnosis: `extract_key_name_at_indent`'s hard-coded 4-space job-child indent is assumed, never checked (`ADV-P57-HIGH-001` -- a legal 6-space sibling job body bypasses Guard A, the sole automated control for the DEC-246 duplicate-check-name vector, full suite green) -- plus a jq trust-boundary gap (`ADV-P58-LOW-001`). Closed as a fix burst (class sweep `a17939e2`, DEC-255; 3 files, 588 insertions/67 deletions) rather than 23 point fixes; the implementer REFUSED to change `ci.yml`'s POL-11 canary to an exact-count equality (documented reason: the `test` job runs a 2-OS matrix, 4 of 27 tests are `#[cfg(unix)]`-gated, a literal `27` would red every Windows leg on a green tree) -- Rust-side-only fix instead. `a17939e2` broke CI for real: live run 31406705091 -- `Test (macos-latest)` genuinely FAILED (two `#[cfg(unix)]` subprocess tests panicked; root cause `resolve_trusted_jq()` pinned `/usr/bin/jq` only, wrong for macOS Homebrew jq); `ci-gate` correctly FAILED downstream -- the first end-to-end evidence this cycle of the gate behaving correctly on a genuine failure. Fixed by `f2bea32e` (DEC-257, CI-BREAK-1): per-`RUNNER_OS` trusted-jq directory allowlist, fail-closed on unset `RUNNER_OS`, new `run_jq_trust_self_test` (13 checks) closing the local-test-coverage gap that let the wrong pin ship unnoticed. PR #667 remains OPEN, HELD (DEC-202, reaffirmed DEC-258) at new head `f2bea32e`; CI FINAL 15/15 PASS, mergeStateStatus CLEAN, re-verified live via `gh pr view 667` (not re-read from a commit message). Human ruled next priority remains S-CIGATE-3, not an eleventh window (DEC-259) -- now evidence-backed by two independent hand-rolled-extraction failure axes found across two consecutive windows. Four drift items dispositioned per S-7.02: three new (`GUARD-MODE-UNREACHABLE-LOCALLY` HIGH, `POSITIONAL-ASSUMPTION-AXIS` HIGH, `MATRIX-FAIL-FAST-MASKS-SCOPE` LOW) plus a second instance of `MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM` (an in-flight jq fix was mischaracterized as "worse than the break" from a run missing `RUNNER_OS`, hours after the first instance's corrective was logged) and an update to `RED-PROOF-NEEDS-SPELLING-VARIANTS` (now confirmed two-axis: spelling AND indent) -- all closed via explicit inline S-7.02 deferrals, no new story opened this burst. D-chain cite D-254..D-259 latest brownfield. S-CIGATE-3 SPEC COMMIT (2026-08-10, team-lead-dispatched burst, spec-only, no code changed): committed `stories/S-CIGATE-3-ci-yml-real-yaml-parser.md` (v1.1->v1.2) and `stories/STORY-INDEX.md` (v1.5.78->v1.5.79) to factory-artifacts, recording DEC-260 (`saphyr-parser` `=0.0.11`, event-stream API only -- human-approved this session, established by execution in isolated scratch worktree `yaml-crate-probe`) and folding in drift items `POSITIONAL-ASSUMPTION-AXIS`/`RED-PROOF-NEEDS-SPELLING-VARIANTS` as resolved-by-design via new AC-008. Recorded two new drift items (`STORY-INDEX-DENOMINATOR-UNRECONCILED` MEDIUM, `STORY-STATUS-DRIFT-INDEX-UNRELIABLE` MEDIUM -- see Drift Items table) plus two corrections to prior beliefs: `deny.toml` carries ~40 `bans.skip` entries (verified via grep), not five as earlier stated from reading only the file head; the `rand` exemption's real mechanism is cargo-deny's `bans.multiple-versions-include-dev` defaulting to `false`, not "never enters the actual build". Also reconfirmed `EXTRACT-JOB-BLOCK-RAW-ANCHOR-WIDENED` was already CLOSED via `1381af17` prior to this burst, not open -- a prior orchestrator brief had mischaracterized it as open; the story-writer caught this and folded it in as corroborating evidence instead, not a new AC. S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE (2026-08-10, team-lead-dispatched fix burst): human exercised merge authority (DEC-128) and released the DEC-202 hold -- PR #667 squash-merged to develop as `a5e1d087` at 2026-08-10T21:08:10Z (48 commits, closes #626); remote branch `ci/fix-toolchain-sha-msrv` auto-deleted. `develop` CI on `a5e1d087` (live run 31432422878): SUCCESS -- 12 success, 2 skipped (`Mutation testing` + `Secret Scan (gitleaks)`, both `pull_request`-only); `CI Gate` correctly reported success on a push event with `mutants` skipped -- the first end-to-end production confirmation of the `ALLOWED_SKIPS` mechanism (S-CIGATE-2, `df203233`) this cycle has recorded. `develop` also advanced through `e5c43da1` (#666, unrelated `taiki-e/install-action` repin) in the same window. Before merge, two TARGETED DELTA REVIEWS (not STRICT Step-4.5 window passes -- dispatched because the human asked whether #667 was fully reviewed, not whether CI was green) covered `1381af17..5ca51bc2`, the 1,024 insertions no prior pass had seen: pass-60 adversary / ADV-P60 (Rust delta, 2H/0M/3L/3I, NOT CLEAN) and pass-61 adversary / ADV-P61 (shell delta, 0H/2M/2L/2I, NOT CLEAN; the orchestrator re-rated MEDIUM-001 to HIGH after independently reproducing a verified false green with the guard engaged, meeting DEC-245's HIGH definition -- a call the reviewer flagged as one it would not make unilaterally). Three HIGHs verified end-to-end against code CI had called green four times: `ADV-P61-HIGH-001` (`resolve_trusted_jq()` validated jq's directory via `dirname`, itself PATH-resolved -- a second shim defeats it, reproduced `OK fmt/clippy/test = success` exit 0 on a payload where all three genuinely FAILED), `ADV-P60-HIGH-001` (the `#[ignore]`/`#[cfg]` enforcement `a17939e2` documented as written was never actually written -- the target test body had exactly one assertion), `ADV-P60-HIGH-002` (the `- `-marker strip added at the orchestrator's own request as `ADV-P57-INFO-004` regressed `extract_job_display_name` from fail-closed to fail-open, legal per PyYAML/Ruby Psych/actionlint). Fixed as a fix burst, all CI-green: `736fea28`+`23ace476` (shell) and `f656f873` (Rust, plus three LOWs). Nine actionable findings identified (3 HIGH + 1 MEDIUM + 5 LOW across P60/P61); 8 CLOSED (3 HIGH + 1 MEDIUM + 4 LOW) in `ADV-P1-INDEX.md` v2.14->v2.16 (442->456 total findings, +14 new); 1 LOW (`ADV-P61-LOW-004`) verified OPEN -- see current_step correction note and MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM, third instance. Two NEW process-gap drift items S-7.02-dispositioned inline, no new story: `DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST` (HIGH -- a dispatched item was reported complete having written only documentation, test body byte-unchanged; the burst-close verification checked the metric the mechanism reports on rather than the mechanism itself) and `ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION` (MEDIUM -- an orchestrator-requested consistency fix converted a fail-closed extractor into a fail-open one). Two further drift items recorded from external research: `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT` (MEDIUM -- GitHub-hosted runners have passwordless sudo, confirmed; `sudo cp` over the trusted jq binary is undetectable by any in-script allowlist) and `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` (MEDIUM, UNVERIFIED -- `actions/runner`'s `_setEnvBlockList` has no `GITHUB_*`/`RUNNER_*` prefix filter and the 44-key regeneration allowlist excludes `actions`, so an earlier step's `GITHUB_ACTIONS=false` write to `$GITHUB_ENV` likely persists; `RUNNER_OS` IS protected). **DEC-262** recorded: merge authorized on code grounds with Step 4.5 permanently at 0/3 after ten windows and 61 total adversary passes -- the 3/3-CLEAN convergence bar was never met, and merging accepted that given `src/` 0-defect across 32+ passes, all findings confined to test infrastructure, guards materially stronger than develop's prior state with claims now matching behavior, and the residual class (line-based extraction generating new axes, not new instances of a known axis) routed to S-CIGATE-3 rather than an eleventh window. Post-merge cleanup (verified against PR merge records, not `git branch --merged`, since squash merges are not ancestors): `.worktrees/S-626-1` and `.worktrees/S-CIGATE-2` removed; local branches deleted; `git remote prune origin` run; main repo fast-forwarded `df203233`->`a5e1d087`. Three worktrees remain: main, `.factory`, `.reference`. 15 local branches with deleted remotes remain, pre-existing, not touched. Story status `S-626-1`: `in-progress` -> `done`. `S-626-1.md` v1.35->v1.36 (FIX ROUND 31); `STORY-INDEX.md` v1.5.79->v1.5.80. Three lessons logged to `lessons.md`, tagged `[codified]`: verify the mechanism not the metric it reports on; a failure-direction change needs a bidirectional RED proof; CI green and mergeable is not fully reviewed. Next priority unchanged: S-CIGATE-3 (v1.2, 9 ACs, `saphyr-parser` `=0.0.11`, DEC-260). SHELL-TRUST-ASSUMPTIONS RESEARCH PASS (2026-08-11, team-lead-dispatched, doc-only, no code changed): committed `research/ci-gate-shell-trust-assumptions-2026-08-10.md` (896 lines) + `research/RESEARCH-INDEX.md`, externally validating all six primary assumptions behind the 401 lines of security-relevant shell that merged unreviewed via PR #667 (`736fea28`/`23ace476`). 8 CONFIRM, 2 INCONCLUSIVE-on-primary (Q1b/Q3b, labelled INFERRED not promoted), 0 REFUTE. Recorded as **DEC-263** (decisions-archive.md; research-pass pattern, not added to this file's active Decisions Log, matching DEC-261's precedent). Three new drift items plus one update -- see Drift Items table: `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` (MEDIUM), `MACOS-ALLOWLIST-TRUSTS-WRITABLE-DIR` (LOW, test-leg only), `SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD` (LOW, follow-up PR being raised), `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` updated with Q4's stronger ordering-based mechanism for why `RUNNER_OS` cannot be unset. Two throwaway-workflow experiments (E1 `/usr/bin` writability, E2 `/opt/homebrew/bin` writability) specified but explicitly deferred, not run this pass. `regression-state.json`/`sidecar-learning.md` intentionally left dirty per standing instruction. PR675-MERGE+ADV-P675-CLOSE (2026-08-11, team-lead-dispatched close-out burst): PR #675 (`docs(ci-gate): correct trust-layer record to match code (S-626-1)`, branch `docs/ci-gate-trust-scope`) squash-merged to `develop` as `d55bedf7` at 2026-08-11T01:40:55Z; remote branch auto-deleted (confirmed via `git fetch --prune` after an initial stale `ls-remote` read showed it still present -- refetch, not assumption, settled it). `develop` CI on `d55bedf7` (run 31450052302): SUCCESS -- 12 success + 2 legitimately-skipped (`Mutation testing` + `Secret Scan (gitleaks)`, both `pull_request`-only); `CI Gate` correctly reported success, all 12 required jobs `success`, `mutants` the sole allowlisted skip. `Scorecard` and `E2E (Live Jira)` also passed on the merge commit. PR #675 was documentation-only -- `CLAUDE.md` + `scripts/check-ci-gate.sh`, comment/prose text only, zero non-comment lines changed (independently re-verified via `git show --stat`) -- correcting the project record to match what the shipped code already knew about the jq-trust layer from the SHELL-TRUST-ASSUMPTIONS research pass (DEC-263) immediately above. Before merge, the PR was subjected to a TARGETED CLAIM-ACCURACY REVIEW (ADV-P675, not a Step-4.5 window pass -- in this repository a doc comment IS the operative control for several recorded residuals, so a false comment is a real defect in its own right). Result: 0 HIGH / 2 MEDIUM / 2 LOW / 1 INFO, NOT CLEAN, recommendation merge-with-amendments; all four actionable findings amended in `d2430a8a` before merge and independently re-verified in the committed text. `ADV-P675-MEDIUM-001`: the comment claimed Ubuntu 24.04 is "the first LTS with usrmerge" and CONFIRM-labelled it -- false: Ubuntu has shipped merged-`/usr` for new installs since 18.10 Cosmic, so 20.04/22.04 LTS were already usrmerged; the cited Rockcraft source is correct but scoped to Ubuntu as a rock/container base image, not installations generally -- the error propagated source-to-artifact-to-comment, gaining a CONFIRM label at the last hop. The operative conclusion (one physical directory, no canonicalization) was true and unaffected; the false premise misled toward MORE apparent risk, not less. Amended by explicit retraction, not silent deletion, so the error cannot be re-derived from the same source; CONFIRM re-anchored to the merged-`/usr`-since-18.10 property. `ADV-P675-MEDIUM-002`: the PR asserted as settled fact that `/usr/bin`/`/bin` "require root" unlike the paths above -- this is Q1b, INCONCLUSIVE-on-primary-sources / INFERRED per `research/ci-gate-shell-trust-assumptions-2026-08-10.md`, the single open question bearing on the decision path; aggravated by being unrequested drift (the dispatch asked only for the `/opt/homebrew/bin` sentence) and internally inconsistent (~70 lines later the same PR correctly labelled lower-stakes claims INFERRED). Amended by labelling only. `ADV-P675-LOW-001`: a C# snippet quoting `ScriptHandler.cs` elided the line that BINDS `runtimeContext`, presenting the elision as a full quotation -- as printed, the inner loop referenced an undeclared identifier, directly under the word "UNCONDITIONALLY"; amended by restoring the binding line. `ADV-P675-LOW-002`: two untouched comment blocks still claimed strict mode engages on `GITHUB_ACTIONS=true`, contradicting the DEC-263 re-key to `RUNNER_OS` -- pre-existing, not a regression, but left the file worse than uniformly-stale; amended. The 5th finding (INFO, 1) is counted in the severity tally supplied by the dispatch (0H/2M/2L/1I = 5) but was not itemized with a title or detail in that dispatch brief -- recorded here as a tally entry only, not fabricated with invented content, consistent with this project's standing rule against estimating what should be derived (`MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM`). Verified CLEAN with no finding: comment detachment (checked against the assembled file, not the diff); the `CLAUDE.md` sudo sentence (verbatim quote, correct placement, correctly refuses the overclaim); the macOS compatibility-not-security framing and its scope claim (independently re-verified against `ci.yml`: `ci-gate` and `spec-guard` both `ubuntu-latest`); the "second, independent channel" wording; the `ScriptHandler.cs` write-ordering citation (confirmed across four primary-source fetches); the "don't cite the docs' non-overwrite sentence" note. `ADV-P1-INDEX.md` v2.16->v2.17 updated: header (`total_findings` 456->461, `severity_distribution` `{CRIT:0,HIGH:35,MED:131,LOW:157,INFO:133}` -> `{CRIT:0,HIGH:35,MED:133,LOW:159,INFO:134}`, re-derived by summation, not assumed) plus a new `## Pass ADV-P675` section (finding catalog, reconciliation note on the uncounted 5th finding, isolation/verified-clean note, summary). Recorded **DEC-264**: PR #675 merged after adversarial claim-accuracy review and four amendments; reviewing a documentation-only PR found two MEDIUMs, justifying the practice of applying the same review discipline to record-correction PRs as to code PRs (decisions-archive.md; research/closeout-pass pattern, matching DEC-261/DEC-263's precedent -- not added to this file's active Decisions Log, since this PR did not itself change enforcement behavior). Closed drift item `SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD` -- the follow-up PR it named is `d55bedf7`, merged; the sudo bound now lives in `CLAUDE.md` beside the unpinned-`uses:` residual (see drift-items-closed.md). Two new drift items recorded (see Drift Items table): `CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS` (MEDIUM, process-gap -- a PR whose sole purpose was correcting inaccurate claims introduced two new ones, including the same overclaim class it existed to fix) and `SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN` (LOW, process-gap -- MEDIUM-001's mechanism: a source's scoped claim lost its scope at each citation hop, gaining a confidence label only at the last one). Post-merge state re-derived, not assumed: `develop` @ `d55bedf7`; main repo working tree fast-forwarded and clean; three worktrees unchanged (main, `.factory`, `.reference`); local branch `docs/ci-gate-trust-scope` now shows `[gone]` after `git fetch --prune` (joining, not exempted from, the pre-existing 15-branch stale-`[gone]` set -- now 16, still not actioned, per the standing carry-forward deferral). This is the session-closing burst -- no open PR or review window remains. Next priority unchanged: S-CIGATE-3 (v1.2, 9 ACs, `saphyr-parser` `=0.0.11`, DEC-260)."
trajectory_tail: "→1→3→0→2"
maintenance_run: CLOSED
current_cycle: "cycle-001"
feature_mode_bundle: SOH-DX-1-F4-DELIVERY
dtu_required: false
phase_2_status: APPROVED
phase_2_approved_at: 2026-05-07
phase_3_status: SOH-ATTACHMENTS-1 F5 SCOPED ADVERSARIAL CONVERGED 2026-07-24 STRICT (14 rounds; window pass-12/pass-13/pass-14 CLEANx3; spec v1.3.99 to v1.3.106; BC-INDEX v6.38 to v6.44; develop @ db207b81) + F7 DELTA CONVERGENCE APPROVED 2026-07-25 (DEC-186; 5-dim PASS; MAXIMUM_VIABLE_REFINEMENT_REACHED) + CYCLE FULLY CLOSED 2026-07-25
activation_head: "7b3ba371"
activation_version: "v0.6.0-dev.11"
---

<!-- STATE.md SIZE BUDGET (2026-08-11, updated by the PR675-MERGE+ADV-P675-CLOSE burst on top of
     SHELL-TRUST-ASSUMPTIONS RESEARCH PASS): 189 lines (wc-l, verified) -- soft-target 200.
     margin from soft-target ~= -11 (under soft-target). margin from actual to hard cap 500 ~= 311.
     This burst appended one PR675-MERGE+ADV-P675-CLOSE paragraph to current_step, added
     one Phase Progress row and one Current Phase Steps row, closed one drift item
     (SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD, moved to drift-items-closed.md), added two new
     drift-item rows (CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS,
     SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN), and appended one Convergence Status
     paragraph -- net +9 lines from the prior 180. DEC-264 recorded directly in
     cycles/cycle-001/decisions-archive.md ONLY (research/closeout-pass pattern, matching
     DEC-261/DEC-263 -- not added to this file's active Decisions Log table). See
     cycles/cycle-001/{decisions-archive, drift-items-closed, drift-items-open-detail, burst-log,
     session-checkpoints, lessons}.md for everything relocated out of this file historically. -->

# Pipeline State: jira-cli

## Project Metadata

| Field | Value |
|-------|-------|
| **Product** | jr (Jira CLI) |
| **Mode** | BROWNFIELD / Rust |
| **Target Workspace** | develop to main |
| **trajectory-tail** | →1→3→0→2 (unchanged this burst -- ADV-P60/P61 and ADV-P675 were targeted delta/claim-accuracy reviews, not Step-4.5 STRICT windows, and are not counted toward the trajectory-tail's window arithmetic; full window detail: cycles/cycle-001/burst-log.md) |
| **Last Updated** | PR675-MERGE+ADV-P675-CLOSE 2026-08-11: trajectory-tail →1→3→0→2 (unchanged -- not a window pass). **PR #675 MERGED** to `develop` as `d55bedf7` (2026-08-11T01:40:55Z), a documentation-only correction of the S-626-1/DEC-263 trust-layer record (`CLAUDE.md` + `scripts/check-ci-gate.sh`, zero non-comment lines changed). `develop` CI on `d55bedf7` SUCCESS (run 31450052302), `CI Gate` green, `Scorecard`/`E2E (Live Jira)` also passed. Pre-merge targeted claim-accuracy review ADV-P675 found 0H/2M/2L/1I; all 4 actionable findings CLOSED (`d2430a8a`). **DEC-264** recorded: reviewing a documentation-only PR found two MEDIUMs, justifying the practice. Drift item `SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD` CLOSED; two new process-gap items recorded (`CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS`, `SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN`). `ADV-P1-INDEX.md` v2.16→v2.17 (456→461 total findings). Session-closing burst -- no open PR or review window remains. |
| **Current Phase** | Feature Mode SOH-DX-1 **F4 DELIVERY** -- DEC-204 fully ADJUDICATED (DEC-245). F2 APPROVED (DEC-196), F3 APPROVED (DEC-197). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671, `df203233`). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged to `develop` as `a5e1d087`, 2026-08-10T21:08:10Z, closes #626 -- DEC-262); its trust-layer record correction **PR #675 also MERGED** (`d55bedf7`, 2026-08-11T01:40:55Z, DEC-264). `develop` CI SUCCESS on `d55bedf7` (run 31450052302). Adversary: 61 total Step-4.5-eligible passes (56 STRICT + pass-60/pass-61 targeted delta review) plus ADV-P675 (targeted claim-accuracy review, also not Step-4.5-eligible); 6 VOID; 2 NOT RUN (DEC-209); pass-20 SUPERSEDED (DEC-216); window 57/58/59 CLOSED 0/3 -- TENTH consecutive window without 3/3 since window 30/31/32. Step 4.5 ends PERMANENTLY at **0/3** -- DEC-262 authorized merge on code grounds without ever reaching 3/3 CLEAN. STORY-INDEX v1.5.80 (127 stories; denominator unreconciled -- see Drift Items). AX23-001 PENDING RATIFICATION. |
| **Next Phase** | Pending human decisions, in order: (1) **S-CIGATE-3 implementation (durable YAML-parser fix) -- NEXT PRIORITY per DEC-259**, evidence-backed by two independent hand-rolled-extraction failure axes plus prior-burst HIGH findings in the same extraction class. (2) Second independent required CI check for GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE. (3) Gitleaks blocking / enforce_admins / `strict: false` config half. (4) Perimeter extension -- `docs/demo-evidence/` and/or `.factory/cycles/`. (5) Two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`). (6) `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT` / `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` / `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` follow-up experiments (E1/E2, ~10 min each). Then: S-TRAIL-DERIVATION-GUARD-1, AX23-001, S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1. |
| **Activation HEAD** | 7b3ba371 (PR #654 squash-merged 2026-07-25; SOH-ATTACHMENTS-1 CYCLE FULLY CLOSED; activation_version v0.6.0-dev.11) |

## Phase Progress

<!-- Full per-burst history through WINDOW-57-58-59+SWEEP-2+CI-BREAK (2026-08-10):
     cycles/cycle-001/burst-log.md (that burst's displaced Phase-Progress/Current-Phase-Steps rows
     archived there this burst under "### Archived Phase Progress Row (from
     WINDOW-57-58-59+SWEEP-2+CI-BREAK)" / "### Archived Current Phase Steps Row (from
     WINDOW-57-58-59+SWEEP-2+CI-BREAK)"). -->
| Phase | Status | Completed | Gate | Notes | Finding Progression |
|-------|--------|-----------|------|-------|---------------------|
| pass-60 adversary (ADV-P60, Rust delta) / pass-61 adversary (ADV-P61, shell delta) | COMPLETE | 2026-08-10 | -- | Targeted delta reviews (not a Step-4.5 window) of `1381af17..5ca51bc2`; 9 actionable findings; 8 CLOSED pre-merge (3 HIGH + 1 MEDIUM + 4 LOW, via `736fea28`/`23ace476`/`f656f873`), 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN. | →1→3→0→2 (unchanged -- not a window pass) |
| **S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE fix burst (2026-08-10): PR #667 squash-merged to develop as `a5e1d087` (DEC-128/DEC-262), closing #626, via fix burst (`736fea28`+`23ace476`+`f656f873`). `develop` CI SUCCESS (run 31432422878). Story delivered and MERGED.** | COMPLETE | 2026-08-10 | -- | Factory paused pending next-priority dispatch. Four drift items recorded (2 process-gap, S-7.02-dispositioned inline; 2 external-research). DEC-262 recorded. Worktree cleanup complete. Next: S-CIGATE-3. | →1→3→0→2 |
| **SHELL-TRUST-ASSUMPTIONS research pass (2026-08-11): external validation of the 401-line shell-trust layer merged via PR #667. 0 REFUTE.** | COMPLETE | 2026-08-11 | -- | DEC-263 recorded (decisions-archive.md). Three new drift items + one updated. E1/E2 experiments deferred. Doc-only, no code changed. | →1→3→0→2 (unchanged -- not a window pass) |
| **PR675-MERGE+ADV-P675-CLOSE (2026-08-11): PR #675 squash-merged to develop as `d55bedf7` (doc-only S-626-1 trust-layer correction). `develop` CI SUCCESS (run 31450052302). ADV-P675 targeted claim-accuracy review pre-merge, 0H/2M/2L/1I, all 4 actionable CLOSED.** | COMPLETE | 2026-08-11 | -- | DEC-264 recorded. 1 drift item CLOSED (`SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD`), 2 NEW (`CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS`, `SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN`). `ADV-P1-INDEX.md` v2.16→v2.17 (456→461). Session-closing burst; next: S-CIGATE-3. | →1→3→0→2 (unchanged -- not a window pass) |

## Current Phase Steps

<!-- Full step-by-step burst history: cycles/cycle-001/burst-log.md. Prior burst's row archived
     there this burst under the same headers as its Phase Progress counterpart. -->
| Step | Agent | Status | Output |
|------|-------|--------|--------|
| **S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE (2026-08-10): state-manager closed the burst per the Single-Commit Burst Protocol. Recorded PR #667 merge to `develop` (`a5e1d087`), `develop` CI validation (run 31432422878, first production `ALLOWED_SKIPS` confirmation), pass-60/pass-61 (ADV-P60/ADV-P61) targeted delta reviews (9 actionable findings, 8 closed via fix burst `736fea28`/`23ace476`/`f656f873`: 3 HIGH + 1 MEDIUM + 4 LOW; 1 LOW `ADV-P61-LOW-004` confirmed OPEN), DEC-262, four drift items (2 process-gap S-7.02-dispositioned inline, 2 external-research), and worktree cleanup. Updated `ADV-P1-INDEX.md` v2.14->v2.16 (pass 61, 456 total findings, 0C/35H/131M/157L/133I; `status`->`closed-merged`; `pr`->MERGED). Bumped `S-626-1.md` v1.35->v1.36 (FIX ROUND 31, `status: in-progress`->`status: done`) and `STORY-INDEX.md` v1.5.79->v1.5.80 (row status marker updated in place). Appended `burst-log.md` + `session-checkpoints.md` entries; logged 3 lessons to `lessons.md` tagged `[codified]`; archived the prior burst's Phase-Progress/Current-Phase-Steps rows. Re-derived (not assumed) `develop` HEAD, worktree list, and gone-branch count via direct `git` commands before writing any narrative claim. | state-manager | COMPLETED | `STATE.md` v2.33->v2.34 + `ADV-P1-INDEX.md` + `S-626-1.md` + `STORY-INDEX.md` + `burst-log.md` + `session-checkpoints.md` + `lessons.md`, committed to factory-artifacts (one atomic commit), pushed via CAS. Next: S-CIGATE-3 (durable YAML-parser fix) per DEC-259. **POST-CLOSE ARITHMETIC CORRECTION (2026-08-10, same day):** `STATE.md` v2.34->v2.35 + `ADV-P1-INDEX.md` v2.15->v2.16, correcting the 442->452/+10/6-actionable arithmetic error above to the verified 442->456/+14/9-actionable (8 closed, 1 OPEN); see current_step correction note. |
| **SHELL-TRUST-ASSUMPTIONS research pass (2026-08-11): committed `research/ci-gate-shell-trust-assumptions-2026-08-10.md` + `research/RESEARCH-INDEX.md`; recorded DEC-263 (decisions-archive.md) and three new drift items + one update to `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` (Drift Items table). Doc-only, no code changed; `regression-state.json`/`sidecar-learning.md` left dirty per standing instruction. | state-manager | COMPLETED | `STATE.md` v2.35->v2.36 + `cycles/cycle-001/decisions-archive.md` (DEC-263) + `research/ci-gate-shell-trust-assumptions-2026-08-10.md` + `research/RESEARCH-INDEX.md`, committed to factory-artifacts (one atomic commit), pushed. Next: S-CIGATE-3 (durable YAML-parser fix) per DEC-259, unchanged. |
| **PR675-MERGE+ADV-P675-CLOSE (2026-08-11): state-manager closed the session per the Single-Commit Burst Protocol. Recorded PR #675 merge to `develop` (`d55bedf7`), `develop` CI validation (run 31450052302), ADV-P675 targeted claim-accuracy review (5 findings: 0H/2M/2L/1I, 4 actionable CLOSED pre-merge via `d2430a8a`), DEC-264, drift-item disposition (1 closed, 2 new), and post-merge state re-derivation. Updated `ADV-P1-INDEX.md` v2.16->v2.17 (pass 61 unchanged, +5 findings via a new `## Pass ADV-P675` section, 456->461 total, severity distribution re-derived by summation). Appended `burst-log.md` + `session-checkpoints.md` entries (session-closing checkpoint); logged 2 lessons to `lessons.md` tagged `[codified]`. Moved `SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD` to `drift-items-closed.md`. Re-derived (not assumed) `develop` HEAD, remote branch deletion, worktree list, and gone-branch count via direct `git`/`gh` commands before writing any narrative claim. | state-manager | COMPLETED | `STATE.md` v2.36->v2.37 + `ADV-P1-INDEX.md` + `cycles/cycle-001/decisions-archive.md` (DEC-264) + `cycles/cycle-001/drift-items-closed.md` + `burst-log.md` + `session-checkpoints.md` + `lessons.md`, committed to factory-artifacts (one atomic commit), pushed. Next: S-CIGATE-3 (durable YAML-parser fix) per DEC-259, unchanged. |

## Decisions Log

<!-- Full Decisions Log (DEC-001 through DEC-253, plus DEC-261, DEC-263, and DEC-264) extracted to
     cycles/cycle-001/decisions-archive.md across the 2026-08-09 COMPACTION burst, this burst's
     own archival of DEC-246+248..253, and three same-day research/closeout-pass bursts that
     recorded DEC-261/DEC-263/DEC-264 directly to the archive. Retained here, in full, are only
     the decisions that still GOVERN behavior today. -->
| ID | Decision | Rationale | Phase | Date |
|----|----------|-----------|-------|------|
| DEC-128 | **MERGE AUTHORITY IS THE HUMAN'S (CRITICAL).** Delivery sub-agents must NOT self-authorize merges, spawn fix agents, push, or enter unbounded loops. CI green / mergeStateStatus CLEAN is not merge authorization. *(Original ruling is inside the collapsed DEC-001..DEC-155 range in decisions-archive.md, which has no separate DEC-128 row of its own; this entry restates the standing citation form used verbatim throughout this project's history.)* | Foundational merge-safety constraint; cited at every subsequent merge-adjacent decision (DEC-234, DEC-235, DEC-238, DEC-243, DEC-258, DEC-262, DEC-264...). | Phase 0-3 (original); standing | archived origin; standing |
| DEC-202 | **PR #667 HELD until fixes land and a fresh window opens.** *(Extracted clause from the combined DEC-199..DEC-203 ruling; full combined row in decisions-archive.md.)* SUPERSEDED 2026-08-10 by DEC-262 -- hold released, PR merged. | Human ruling 2026-07-31, reaffirmed at every subsequent window close through DEC-258; released by DEC-262. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-07-31 |
| DEC-206 | **VOID PROTOCOL FOR ISOLATION BREACHES.** Adversary passes where orchestrator dispatch defects leak banned-path content are VOID for step-4.5 window eligibility; findings remain valid. | Human ruling on isolation protocol. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-03 |
| DEC-224 | **ISOLATION ELIGIBILITY PRINCIPLE.** A pass is ELIGIBLE (not VOID) when a letter-of-rule isolation deviation occurred but zero banned content actually surfaced; VOID applies only when banned content actually became visible. Self-disclosure without surfacing is a POSITIVE signal. Applied and held across every subsequent window through 57/58/59. | Principled distinction: the rule prevents contamination, not path syntax deviation. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-04 |
| DEC-245 | **DEC-204 ADJUDICATED -- CONSERVATIVE READING RULED.** A pass is CLEAN only with zero HIGH, zero MEDIUM and zero LOW findings; INFO-only findings still count as CLEAN; LOW findings reset the window regardless of GAP-vs-refinement classification. Step 4.5 remains 0/3, confirmed by ruling. | Lenient reading would have stopped the cycle before discovering ADV-P50-HIGH-001, pass-51's three HIGH findings, pass-53's two HIGH findings, pass-55's HIGH finding, pass-57's HIGH finding, and this burst's ADV-P60/P61 HIGHs -- the conservative criterion has been expensive but a productive defect finder to the very end. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-08 |
| DEC-254 | **INSPECTION FRONTIERS C1-LEXER/C5-FALSIFIABILITY/C3-SIDE-CHANNELS APPROVED FOR WINDOW 57/58/59.** Chosen against the documented exhaustion survey: C1-lexer (differential conformance against the code `910b8ab0`/`1381af17` added), C5-falsifiability (census of the new pins), C3-side-channels (inter-step side channels inside `ci-gate`, never probed in 56 prior passes). | Frontier variety, not pass count, is what makes a CLEAN verdict meaningful (DECAY-CURVE-MEASURES-QUESTION-EXHAUSTION-NOT-CONVERGENCE). | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-255 | **WINDOW 57/58/59 FINDINGS FIXED AS A CLASS SWEEP (`a17939e2`), NOT POINT FIXES; POL-11 EXACT-COUNT CHANGE REFUSED.** All 23 findings (12 scoped items) closed via one class sweep spanning two new root-cause axes (positional-assumption, jq trust-boundary). The implementer refused to change `ci.yml`'s POL-11 canary to a literal exact-count equality (`test` job runs a 2-OS matrix; 4 of 27 tests are `#[cfg(unix)]`-gated) -- fixed Rust-side only. Extends DEC-243/DEC-244 class-sweep precedent. | Point-fixing would leave the same defect shape reachable at the next construction; the refused change would have traded a real defect for a real regression on Windows. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-256 | **SIBLING-WORKFLOW EXPOSURE + Q4/Q8 TREATED AS HANDLED VIA GUARDS, NOT RE-SPENT AS FRONTIERS.** Extends DEC-250: `1381af17`'s docstring correction + new `exclude:`-key assertion is treated as closing drift item `SIBLING-WORKFLOW-FRONTIER-UNRETIRED`; window 57/58/59 did not re-probe this surface as its own frontier. | Avoids re-spending a frontier on a question already closed by a source-level assertion. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-257 | **CI-BREAK-1 RECORDED (real, not synthesized).** `a17939e2`'s own fix for `ADV-P58-LOW-001` (jq trust-boundary gap) shipped `resolve_trusted_jq()` pinned strictly to `/usr/bin/jq`, wrong for `macos-latest`'s Homebrew jq. Live CI run 31406705091: `Test (macos-latest)` genuinely FAILED; `ci-gate` correctly FAILED downstream. Fixed by `f2bea32e`: per-`RUNNER_OS` trusted-jq allowlist + `run_jq_trust_self_test` (13 checks), closing the local-test-coverage gap that let the wrong pin ship. | First end-to-end evidence this cycle of the gate behaving correctly on a genuine failure; the underlying gap (`GUARD-MODE-UNREACHABLE-LOCALLY`) is now closed for this instance and recorded as a general rule. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-258 | **PR #667 HOLD STANDS (DEC-202 REAFFIRMED).** Window 57/58/59 found a verified live HIGH bypass of Guard A (`ADV-P57-HIGH-001`, positional-assumption axis) in the apparatus this PR ships, independent of the CI-BREAK-1 episode. Hold continues under DEC-202/DEC-128 -- CI green / mergeStateStatus CLEAN is not merge authorization. SUPERSEDED 2026-08-10 by DEC-262. | A live HIGH bypass in the exact mechanism under review is precisely the risk DEC-202 exists to hold against. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-259 | **NEXT PRIORITY REMAINS S-CIGATE-3, NOT AN ELEVENTH WINDOW.** Human ruled the durable YAML-parser fix (S-CIGATE-3) remains next, rather than dispatching an eleventh STRICT adversarial window. | Ten consecutive windows without 3/3, and this window proved the class generates NEW AXES (positional-assumption, alongside value-reparse) rather than only new instances of one already-known axis -- now evidence-backed argument, not inference, for replacing hand-rolled line-based extraction with a real YAML parser. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-260 | **S-CIGATE-3 PARSER DECISION: `saphyr-parser` PINNED `=0.0.11`, DEV-DEPENDENCY, EVENT-STREAM API ONLY.** Rejected a PyYAML shell-out (no workflow in this Rust project uses Python) and `serde_yaml_ng` (cannot see anchors, tags, styles, or duplicate keys). The crate's own high-level `saphyr::Yaml` API is FORBIDDEN for this story's guards -- verified to silently collapse duplicate keys, erase quoting styles, and resolve aliases, reintroducing the exact "checker sees less than the runner" defect class this story exists to close. `yaml-rust2 0.11.0` recorded as the pre-vetted fallback. | Established by direct execution against a real build in an isolated scratch worktree (`yaml-crate-probe`) this session, not from crate documentation alone; human-approved this session. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-262 | **MERGE AUTHORIZED ON CODE GROUNDS WITH STEP 4.5 PERMANENTLY AT 0/3 AFTER TEN WINDOWS AND 61 PASSES.** Human ruled PR #667 could merge without ever reaching the DEC-199/DEC-245 3/3-CLEAN convergence bar. PR #667 squash-merged to `develop` as `a5e1d087`, closing #626. Two pre-merge targeted delta reviews (pass-60/pass-61, ADV-P60/ADV-P61, not a Step-4.5 window) covered the previously-unreviewed `1381af17..5ca51bc2` delta and found 9 actionable findings, 8 closed (3 HIGH + 1 MEDIUM + 4 LOW) via fix burst before merge; 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN, not fixed. | `src/` 0-defect across 32+ passes; all findings across ten windows plus the two delta reviews confined to test/guard infrastructure, never shipped product code; the guard apparatus is materially stronger than what `develop` carried before this story, and its documented claims now match its actual behavior after this burst's fixes; the residual failure class (line-based extraction generating genuinely new inspection axes each window) is routed to S-CIGATE-3 rather than an eleventh window, which the ten-window trend suggested would likely find a new axis rather than converge. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |

## Skip Log

All S-WIN-1..6 + #475 + S-FORK-OPS-BACKFILL-1 + S-FORK-OPS-GITLEAKS-DOC-1 + S-MAINT-DEAD-CITATION-CI + S-626-1 per-AC demos: Yes -- adapted (S-626-1: 11 artifacts at `.factory/demos/S-626-1/`). See cycles/cycle-001/burst-log.md.

## Blocking Issues

None open.

## Drift Items (open / tracked)

<!-- COMPACTED 2026-08-09: this table is a compact one-line-per-item index. Full narrative
     bodies for the pre-existing OPEN items are in cycles/cycle-001/drift-items-open-detail.md
     -- update the row there and this index together for those. Items added this burst are
     new and are documented here in full (one-line, with deferral targets inline for process-gap
     items per S-7.02); their full narrative is also in burst-log.md/lessons.md. Per-burst
     new/updated/closed drift-item ledger through this burst: cycles/cycle-001/burst-log.md. -->
| ID | Severity | Summary |
|----|----------|---------|
| GUARD-MODE-UNREACHABLE-LOCALLY | HIGH | An environment-gated guard branch (e.g. `GITHUB_ACTIONS=true`-only) has, by construction, no local test coverage until a test forces the gate variable. **CLOSED for this instance** by `f2bea32e`'s `run_jq_trust_self_test`; **general rule DEFERRED** with immediate effect as a standing review-checklist item: any environment-gated branch must carry a test that forces the gate variable. |
| POSITIONAL-ASSUMPTION-AXIS | HIGH | `extract_key_name_at_indent`'s hard-coded 4-space indent is assumed, never checked -- second distinct axis of the line-based-extraction defect class (axis 1 = value re-parse, closed window 54/55/56). **DEFERRED to S-CIGATE-3** -- both axes are eliminated by construction once a real YAML parser replaces hand-rolled extraction. |
| MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM | MEDIUM | **THIRD INSTANCE (2026-08-10).** Instance 1: an in-flight jq fix was reported "worse than the break" after running `GITHUB_ACTIONS=true` without `RUNNER_OS`, mistaking `resolve_trusted_jq()`'s correct fail-closed behavior for breakage. Instance 2: recorded in the WINDOW-57-58-59 note above (same root cause, different measurement). Instance 3: the orchestrator supplied aggregate ADV-P1-INDEX.md finding counts (+10/452/6-actionable) it had not actually summed, in the closeout of the very burst that logged instance 2 -- the receiving agent caught it by arithmetic and correctly preserved the authoritative-but-wrong numbers rather than silently overriding them, flagging the mismatch instead of fabricating detail; corrected same-day to +14/456/9-actionable (see current_step correction note). **Standing rule reaffirmed with immediate effect, sharpened this instance:** an aggregate is a measurement -- derive it, do not estimate it; verify a measurement's method (which numbers were actually summed, which env vars were actually set) before reporting its result, every time; and a downstream agent flagging an arithmetic or measurement mismatch is a correction to act on, not noise to override. |
| RED-PROOF-NEEDS-SPELLING-VARIANTS | HIGH | **Confirmed TWO-AXIS.** `ADV-P57-HIGH-001` showed a RED proof covering every key spelling still misses a hard-coded indent assumption; a RED proof must cover BOTH the spelling axis AND the indent/position axis. **DEFERRED to S-CIGATE-3.** |
| MATRIX-FAIL-FAST-MASKS-SCOPE | LOW | `Test (ubuntu-latest)`/`Test (windows-latest)` reported `failure` on run 31406705091 but were CANCELLED by matrix fail-fast, not genuinely failing -- a reader of the check list would conclude three platforms broke when only one did. |
| ORCHESTRATOR-STALE-AGENT-NAME-COLLISION | MEDIUM | When a dispatch is stopped and re-dispatched under a suffixed name, the stopped agent stays addressable under a name that reads as canonical. **Corrective (team-lead ruling):** downstream agents must never resolve inputs by contacting agents by name -- the orchestrator is the single source of completed results; re-dispatches must retire the prior name rather than suffix it. |
| RESEARCH-ARTIFACTS-NOT-PERSISTED | MEDIUM | DEC-246 drove a real code change and retired an inspection frontier with no artifact file. **DEFERRED, no story this burst** -- mechanical-enforcement idea routed to the next STORY-INDEX grooming pass. |
| DEC-246-OVERCLAIMED-CONFIRMS | MEDIUM | DEC-246 recorded as 8/8 CONFIRM; re-validation (DEC-249) finds 5 CONFIRM, 2 INCONCLUSIVE (Q4/Q8), 1 split. A second, independent research pass (DEC-261) further dispositioned the remaining open questions: 3 CONFIRM, 3 INCONCLUSIVE, 2 split, 1 REFUTE (the "duplicate check names -> false green" evidence characterization). |
| BURST-LOG-DEFEATS-PLAIN-GREP | LOW | `cycles/cycle-001/burst-log.md` contains bytes making plain `grep` treat it as binary and return silent false negatives; requires `grep -a`. Live foot-gun for future agents -- confirmed again this burst. |
| ADVERSARY-PASSES-27-61-HAVE-NO-DETAIL-FILE | LOW | Per-pass detail artifacts exist for passes 1-19 and 21-26 only; `ADV-P1-INDEX.md` is the sole record for the rest, now including passes 54-61 plus ADV-P675 (widened this burst to include the targeted claim-accuracy review). |
| STORY-INDEX-DENOMINATOR-UNRECONCILED | MEDIUM | `STORY-INDEX.md` declares `total_stories: 127`, but `.factory/stories/` contains 91 `.md` files and the index carries ~210 table rows across multiple tables -- the three numbers do not reconcile. No burst has audited the denominator; the standing convention ("no new stories, leave 127 unchanged") preserves the discrepancy indefinitely. **No story opened this burst; recommend an audit as a follow-up.** |
| STORY-STATUS-DRIFT-INDEX-UNRELIABLE | MEDIUM | 37 stories marked `status: ready` spot-check as already SHIPPED on `develop`. Consequence: STORY-INDEX cannot currently answer "what is open" for anyone not present in this session. `S-626-1`'s own status was corrected this burst (`in-progress`->`done`), reducing the drift set by one instance but not closing the general item. **Recommend a status-reconciliation sweep as a follow-up story, or an explicit deferral; not actioned this burst.** |
| RUNNER-SOURCE-NOT-AN-ORACLE-FOR-WORKFLOW-PARSING | MEDIUM | Caught up this burst (previously recorded only in `burst-log.md`/DEC-261, not yet reflected here). `actions/runner`'s public source rejects YAML anchors GitHub shipped in production 2025-09-18, so it cannot be trusted as an oracle for server-side workflow parsing -- though it remains authoritative for step-execution-stage questions (`StepsRunner.cs`). **No action required beyond this citation; standing caveat for future research passes.** |
| DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST | HIGH | An agent reported a dispatched item complete having written only documentation for it, including "see this test's assertions below," with the test body byte-unchanged. The verification performed at burst-close checked the METRIC the mechanism reports on (`#[test]`=27, denominator=27) rather than the mechanism itself -- a doc-only change passes identically. **CLOSED for this instance** by `f656f873`; **general rule DEFERRED** with immediate effect: verify the mechanism a change claims to add, not only the metric it reports on. |
| ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION | MEDIUM | The orchestrator requested `ADV-P57-INFO-004` (the `- ` marker strip) as a small consistency fix; it converted a fail-closed extractor panic into a fail-open silent miss on a legal, common YAML layout. **CLOSED for this instance** by `f656f873`; **general rule DEFERRED** with immediate effect: a change to an extractor's failure direction needs its own bidirectional RED proof, regardless of how the requesting instruction is framed. |
| JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT | MEDIUM | External research CONFIRMED GitHub-hosted runners (`ubuntu-latest` AND `macos-latest`) have passwordless sudo; `sudo cp` over the trusted `jq` binary is undetectable by any in-script directory allowlist. `ADV-P59-LOW-001` is therefore not closable by any in-script check -- the landed fixes still close the PATH-shim class and raise attacker cost. **Real control is not running untrusted steps before the gate step (deliberate, out-of-scope `uses:`-pinning decision, round 13).** **Reconfirmed 2026-08-11 (DEC-263):** the same sudo bound was independently re-derived against primary sources and is the operative fact behind `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` below. |
| GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE | MEDIUM | **UNVERIFIED for `GITHUB_ACTIONS`; `RUNNER_OS` now CONFIRMED protected by a stronger, more general mechanism (updated 2026-08-11, DEC-263, `research/ci-gate-shell-trust-assumptions-2026-08-10.md` Q4).** `actions/runner`'s `_setEnvBlockList` has no `GITHUB_*`/`RUNNER_*` prefix filter, and the 44-key runtime-env regeneration allowlist excludes `actions` -- so an earlier step's `GITHUB_ACTIONS=false` write to `$GITHUB_ENV` likely persists (still unverified). `RUNNER_OS` cannot be unset or emptied: `ScriptHandler.cs` applies runtime contexts LAST via an unconditional `Environment[env.Key] = env.Value` (not `TryAdd`, not null-guarded), after global env and step `env:` -- this defeats every in-job mechanism (`$GITHUB_ENV`, workflow/job/step `env:`) at once, not just the one prior research cited. **Caution:** do not cite the docs' "`GITHUB_*`/`RUNNER_*` can't be overwritten" sentence as the reason -- the `$GITHUB_ENV` write blocklist is verbatim `{ "NODE_OPTIONS" }`; the docs' claim is true in effect but enforced by write-ordering, not a write-time blocklist. **Minimal experiment for the `GITHUB_ACTIONS` half specified (2-step probe workflow), not run this burst.** |
| JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION | MEDIUM | **NEW (2026-08-11, DEC-263, `research/ci-gate-shell-trust-assumptions-2026-08-10.md`).** `resolve_trusted_jq`'s decision-path trust property reduces to `/usr/bin` being root-only on `ubuntu-latest` -- a fact absent from `runner-images`' readme, its build scripts, and GitHub's hosted-runner docs (Q1b, INFERRED not CONFIRM). Passwordless `sudo` on GitHub-hosted runners is documented and CONFIRMED (Q1c), so the modeled attacker gets root for free; the guard's real effect is raising the cost of forging the gate's decision by one word (`sudo cp shim /usr/bin/jq`), not preventing it. **A ~10-minute experiment (E1: `ubuntu-latest` job running `stat`/`touch /usr/bin/__probe`) settles it -- a `WRITABLE-NO-SUDO` result would REFUTE Q1b and void the guard on the decision path entirely.** DEFERRED, not run this pass. |
| MACOS-ALLOWLIST-TRUSTS-WRITABLE-DIR | LOW | **NEW (2026-08-11, DEC-263), scope: `test` job's macOS leg only -- NOT the decision path (`ci-gate`/`spec-guard` are both `ubuntu-latest`).** `/opt/homebrew/bin` is runner-writable without sudo (Homebrew chowns its prefix to the installing user; `runner-images`' `install-homebrew.sh` installs `jq` with no `sudo` anywhere) -- so an earlier step on `macos-latest` could unprivileged-overwrite the trusted `jq` there. Containment confirmed by the self-test's `linux-rejects-macos-only-homebrew-dir` check (the two OS allowlists do not leak into each other). **The macOS allowlist entry must NOT be removed -- doing so reproduces CI-BREAK-1.** Correct fix is a comment recording it as a compatibility assertion, not a security one; not actioned this pass. |
| CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS | MEDIUM | **NEW (2026-08-11, ADV-P675).** PR #675 -- whose sole purpose was correcting the S-626-1 trust-layer record's inaccuracies -- introduced two new MEDIUM-severity false claims of its own: an overclaimed "first LTS with usrmerge" premise that lost its citation scope (`ADV-P675-MEDIUM-001`), and an INCONCLUSIVE/INFERRED research finding restated as settled fact, inconsistent with the same PR's own labelling elsewhere (`ADV-P675-MEDIUM-002`). Both amended in `d2430a8a` before merge. **Corrective:** a record-correction change needs the same claim-accuracy review as the record it corrects; any claim touching an INCONCLUSIVE/INFERRED item must carry its label explicitly, since unlabelled statements adjacent to labelled ones read as confirmed. |
| SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN | LOW | **NEW (2026-08-11, ADV-P675).** `ADV-P675-MEDIUM-001`'s mechanism: a source's scoped claim (Rockcraft's usrmerge note, scoped to Ubuntu-as-rock-base-image) lost its scope at each citation hop (source -> research artifact -> code comment), gaining a CONFIRM confidence label only at the last hop. Amended in `d2430a8a`. **Corrective:** when a citation chain crosses artifacts, re-fetch the original rather than inheriting the quote -- each hop was locally reasonable and the compounded result was false. |

## Convergence Status

**S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE (2026-08-10):** PR #667 squash-merged to `develop` as `a5e1d087` (48 commits, closes #626) -- human exercised merge authority (DEC-128), released the DEC-202 hold. `develop` CI on `a5e1d087` (run 31432422878): SUCCESS, 12 success + 2 legitimately-skipped (`Mutation testing` + `Secret Scan`, both `pull_request`-only) -- first production confirmation of the `ALLOWED_SKIPS` mechanism (S-CIGATE-2). Two targeted delta reviews (pass-60 / ADV-P60 Rust delta, pass-61 / ADV-P61 shell delta -- NOT counted toward Step 4.5's window arithmetic) covered `1381af17..5ca51bc2`, the 1,024 insertions no prior pass had seen, and found 9 actionable findings; 8 fixed via fix burst pre-merge and CI-green (3 HIGH + 1 MEDIUM + 4 LOW, `736fea28`/`23ace476`/`f656f873`); 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN, not fixed. `ADV-P1-INDEX.md` v2.14→v2.16 (442→456 total findings, +14 new (corrected)). **DEC-262**: merge authorized on code grounds with Step 4.5 permanently at 0/3 after ten windows and 61 total adversary passes -- the 3/3-CLEAN convergence bar defined by DEC-199/DEC-245 was never met, and merging accepted that outcome. Full trajectory detail: cycles/cycle-001/convergence-trajectory.md + cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md.

**SHELL-TRUST-ASSUMPTIONS research pass (2026-08-11):** external validation only, no adversary pass, does not move the trajectory-tail or Step 4.5's 0/3. DEC-263 recorded; zero REFUTE against the merged shell delta; two experiments (E1/E2) remain to fully close Q1b/Q3b.

**PR675-MERGE+ADV-P675-CLOSE (2026-08-11):** PR #675 (`docs(ci-gate): correct trust-layer record to match code (S-626-1)`) squash-merged to `develop` as `d55bedf7` (2026-08-11T01:40:55Z) -- documentation-only, `CLAUDE.md` + `scripts/check-ci-gate.sh`, zero non-comment lines changed. `develop` CI on `d55bedf7` (run 31450052302): SUCCESS, 12 success + 2 legitimately-skipped; `CI Gate` correctly green; `Scorecard`/`E2E (Live Jira)` also passed. A targeted claim-accuracy review (ADV-P675, not a Step-4.5 window; not counted toward Step 4.5's 0/3) found 0H/2M/2L/1I, NOT CLEAN; all 4 actionable findings CLOSED pre-merge (`d2430a8a`). **DEC-264**: reviewing a documentation-only PR found two MEDIUMs, justifying the practice of applying the same claim-accuracy review discipline to record-correction PRs as to code PRs. `ADV-P1-INDEX.md` v2.16→v2.17 (456→461 total findings). This is the session-closing burst -- no open PR or review window remains.

BC-INDEX v6.75 / STORY-INDEX v1.5.80 (127 stories) / ARCH-INDEX v0.16. SOH-DX-1 F2 APPROVED (DEC-196, 2026-07-29). F3 APPROVED (DEC-197, 2026-07-29): spec v1.3.169; BC 658 (unchanged); holdouts 106. S-626-1 adversary: 61 Step-4.5-eligible passes (56 STRICT + pass-60/pass-61 targeted delta review) plus ADV-P675 (targeted claim-accuracy review, not Step-4.5-eligible); 461 total findings (+5 this burst, corrected baseline 442). **PR #667 MERGED** to `develop` as `a5e1d087`, closes #626 -- DEC-262; its trust-layer record correction **PR #675 also MERGED** as `d55bedf7` -- DEC-264. `develop` CI SUCCESS on `d55bedf7` (run 31450052302). src/ 0-defect THIRTY-THIRD-plus consecutive (unchanged -- this burst touched only `.factory/` artifacts plus `CLAUDE.md`/`scripts/check-ci-gate.sh` comments). S-CIGATE-2 remains DELIVERED AND MERGED (PR #671, `df203233`); S-CIGATE-3 v1.2/P2/draft (parser decision DEC-260) -- **NEXT PRIORITY per DEC-259**; S-CIGATE-4 remains done; S-TRAIL-DERIVATION-GUARD-1 remains draft/P2. AX23-001 PENDING RATIFICATION.

SOH-ATTACHMENTS-1 + prior cycles FULLY CLOSED. See cycles/cycle-001/convergence-trajectory.md.

## Concurrent Cycles

<!-- Prior completed cycles archived to burst-log: ADF-CODE-MARK-EXCLUSIVITY (DEC-163, v0.6.0-dev.8), SOH-BUGS-1 (DEC-167, v0.6.0-dev.9), SOH-COMMENT-CRUD-1 (DEC-176, v0.6.0-dev.10), SOH-ATTACHMENTS-1 (DEC-186, v0.6.0-dev.11). See cycles/cycle-001/convergence-trajectory.md. -->
| Cycle | Status | Notes |
|-------|--------|-------|
| SOH-DX-1 (issues #639+#627+#626) | F4 DELIVERY -- DEC-204 fully ADJUDICATED (DEC-245). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged as `a5e1d087`, closes #626 -- DEC-262; trust-layer record correction PR #675 also MERGED as `d55bedf7` -- DEC-264); Step 4.5 ends PERMANENTLY at 0/3 -- 61 Step-4.5-eligible passes (56 STRICT + pass-60/pass-61 delta review), never reached 3/3, plus ADV-P675 (not Step-4.5-eligible). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671 squash-merged `df203233`); **S-CIGATE-3** (v1.2, P2/draft; parser decision DEC-260) -- **NEXT PRIORITY per DEC-259**; **S-CIGATE-4** (done) unchanged; **S-TRAIL-DERIVATION-GUARD-1** (P2/draft, status unresolved) unchanged. | 3 stories: S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1, S-626-1 (DELIVERED AND MERGED). Plus S-CIGATE-2 (DELIVERED/MERGED), S-CIGATE-3 (P2, draft, next priority), S-CIGATE-4 (P1, done), S-TRAIL-DERIVATION-GUARD-1 (P2, draft, status unresolved). S-626-1 adversary: 61 Step-4.5-eligible passes + ADV-P675; 461 findings; THIRTY-THIRD-plus zero-src/-defect consecutive. AX23-001 PENDING. |

## Session Resume Checkpoint
| Field | Value |
|-------|-------|
| Position | Feature Mode SOH-DX-1, phase F4. PR675-MERGE+ADV-P675-CLOSE burst (2026-08-11): PR #675 (S-626-1 trust-layer record correction, doc-only) squash-merged to `develop` as `d55bedf7` (2026-08-11T01:40:55Z). `develop` CI SUCCESS on `d55bedf7` (run 31450052302). Pre-merge targeted claim-accuracy review ADV-P675 found 0H/2M/2L/1I, all 4 actionable CLOSED (`d2430a8a`). DEC-264 recorded: reviewing a doc-only PR found two MEDIUMs, justifying the practice. Drift item `SUDO-BOUND-UNRECORDED-IN-PROJECT-RECORD` CLOSED; two new process-gap items recorded. **S-626-1, S-CIGATE-2, and PR #675's record correction are all DELIVERED AND MERGED -- no open PR, no open review window.** |
| Convergence | Step 4.5 ends PERMANENTLY at 0/3 -- 56 STRICT passes across ten windows never reached 3/3; pass-60/pass-61 (targeted delta review) and ADV-P675 (targeted claim-accuracy review) are both outside Step 4.5's window arithmetic. 461 total findings (+5 this burst). src/ 0-defect THIRTY-THIRD-plus consecutive (unchanged). |
| Not yet done | (1) S-CIGATE-3 implementation (durable YAML-parser fix, next priority per DEC-259, evidence-backed by two independent hand-rolled-extraction failure axes plus prior-burst HIGH findings in the same extraction class). (2) Second independent required CI check for GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE. (3) Gitleaks blocking / enforce_admins / `strict: false` config half. (4) Perimeter extension -- `docs/demo-evidence/` and/or `.factory/cycles/`. (5) Two unresolved story statuses: `S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`. (6) `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT` / `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` / `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` follow-up experiments (E1/E2 throwaway workflows, ~10 min each). Carried forward: S-TRAIL-DERIVATION-GUARD-1 tooling-approach evaluation, S-640-1 handoff, S-MAINT-576-HYG-1 scheduling, MIXED-SET-DASH-ARM-UNPINNED test story, ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling, SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade ruling, BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS follow-up story, STORY-INDEX denominator audit, STORY-STATUS-DRIFT reconciliation sweep. |
| In flight | `develop` @ `d55bedf7` (advanced from `a5e1d087` via PR #675 merge). PR #675 MERGED, closed. .factory @ factory-artifacts, this burst's commit is the head. Worktrees: main (`develop` @ `d55bedf7`), `.factory` (`factory-artifacts`), `.reference` (detached) -- unchanged, no worktree churn this burst. Local branch `docs/ci-gate-trust-scope` shows `[gone]` after `git fetch --prune` (joining the pre-existing 15-branch stale-`[gone]` set, now 16, not touched). No factory lock held. |
| Pending human decisions | Same six items as "Not yet done" above, in the same order, plus: trail-guard tooling for S-TRAIL-DERIVATION-GUARD-1, AX23-001 out-of-delta ratification, MIXED-SET-DASH-ARM-UNPINNED scheduling, ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling, SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade, BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS follow-up BC/VP-minting authorization. |
| Resume command | Open fresh session → `/vsdd-factory:next-step`. **S-626-1 and PR #675's record correction are both DELIVERED AND MERGED (`d55bedf7`); Step 4.5 ends permanently at 0/3 by DEC-262.** Recommended first step: dispatch S-CIGATE-3 (durable YAML-parser fix, DEC-259/DEC-260) as the next priority. Also pending: second required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); gitleaks/enforce_admins ruling; the three external-research drift items' follow-up experiments (E1/E2). `develop` @ `d55bedf7`. AX23-001 PENDING. **Standing notes carried forward: never solicit an agent by name for input; verify the mechanism a change claims to add, not the metric it reports on; a change to an extractor's failure direction needs a bidirectional RED proof, regardless of how the request is framed; "CI green and mergeStateStatus CLEAN" is not "fully reviewed"; knowing a guard's true strength is as valuable as finding it broken; a correction is itself an operation that can corrupt a record -- apply the same review discipline to a record-correction PR as to the record it corrects (this burst); a doc-only PR warrants review when doc comments are load-bearing controls -- "docs-only" is not a review-skip signal in this repository (this burst); when STATE.md needs compaction, use the Write tool with content that advances `timestamp:` -- that is the sanctioned path, not a blocked one (DEC-247).** |

## RESUME PLAN (cold-start)
Step 1 (BLOCKING): Run vsdd-factory:factory-worktree-health. Read this file.
Step 2 -- **S-626-1 is DELIVERED AND MERGED (`a5e1d087`, closes #626); its trust-layer record correction PR #675 is also DELIVERED AND MERGED (`d55bedf7`); Step 4.5 ends permanently at 0/3 (DEC-262).** `develop` CI SUCCESS on `d55bedf7` (run 31450052302). Human ruled (DEC-259) the next priority is S-CIGATE-3 (durable YAML-parser fix). Seek human confirmation/dispatch, in order: (1) S-CIGATE-3 implementation; (2) second independent required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); (3) gitleaks blocking / enforce_admins / `strict: false` config half; (4) whether to extend the perimeter to `docs/demo-evidence/` and/or `.factory/cycles/`; (5) the two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`); (6) follow-up experiments for `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT`/`GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE`/`JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` (E1/E2).
Step 3 -- Once S-CIGATE-3 is underway: also pending: S-640-1 handoff, S-MAINT-576-HYG-1, S-639-1 (BREAKING/v0.6.0-dev.12). MIXED-SET-DASH-ARM-UNPINNED test story to schedule. ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling needed; SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade ruling needed; BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS needs a follow-up-story ruling; STORY-INDEX denominator audit and STORY-STATUS-DRIFT reconciliation sweep recommended. **Dispatch discipline reminder (standing, expanded this burst):** state-manager runs LAST in a burst; never dispatch two agents writing to a shared artifact concurrently; NEVER solicit input by messaging an agent by name -- the orchestrator is the single source of completed results (ORCHESTRATOR-STALE-AGENT-NAME-COLLISION); verify count/trail claims against the derivation command, not a recorded number (MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM, twice-recurred); frontier variety, not pass count, is what makes a CLEAN verdict meaningful; a RED proof must cover every semantically-equivalent spelling AND every legal indent/position of its target (RED-PROOF-NEEDS-SPELLING-VARIANTS, two-axis); an environment-gated guard branch must carry a test that forces the gate variable (GUARD-MODE-UNREACHABLE-LOCALLY); verify the mechanism a change claims to add, not only the metric it reports on (DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST); a change to an extractor's failure direction needs its own bidirectional RED proof, regardless of how the requesting instruction is framed (ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION); "CI green and mergeStateStatus CLEAN" answers whether code compiles and passes its own tests, not whether every line of the diff has been reviewed -- explicitly confirm the reviewed range covers 100% of the diff before treating a PR as fully reviewed; knowing a guard's true strength is as valuable as finding it broken -- external validation with zero REFUTE is still worth recording, not just REFUTEs (SHELL-TRUST-ASSUMPTIONS); **a correction is itself an operation that can corrupt a record -- apply the same claim-accuracy review to a record-correction PR as to the record it corrects (PR675-MERGE+ADV-P675-CLOSE, this burst)**; **a doc-only PR warrants review when doc comments are load-bearing controls -- "docs-only" is not a review-skip signal in this repository (PR675-MERGE+ADV-P675-CLOSE, this burst)**; when STATE.md needs compaction, use the Write tool with content that advances `timestamp:` -- that is the sanctioned path, not a blocked one (DEC-247).

## Open Issues Tracker

| Issue | Title | Status | Priority | Notes |
|-------|-------|--------|----------|-------|
| #429 | jr_isolated() crypto-random suffix | OPEN | LOW | DEC-029 deferred to human. Do NOT close autonomously. |
| #400 | Test-hardening + process-gap follow-ups | OPEN -- Story A MERGED PR #431. Story B + engine items remain. | LOW | |
| #372 | cargo-mutants partial baseline | OPEN | LOW | Follow-up from #346 |
| #387/#368 | git history rewrite / open PR | OPEN | LOW | Force-push needed. Deferred. |

## Historical Content

See `cycles/cycle-001/`: burst-log.md, convergence-trajectory.md, session-checkpoints.md, lessons.md, blocking-issues-resolved.md, decisions-archive.md, drift-items-closed.md, drift-items-open-detail.md.
