---
document_type: pipeline-state
level: ops
version: "2.41"
status: active
producer: state-manager
timestamp: 2026-08-11T21:31:12Z
phase: 3
pipeline: PAUSED
inputs: []
input-hash: "[live-state]"
traces_to: ""
project: jira-cli
mode: brownfield
current_step: "S-CIGATE-3-PUSHED (2026-08-11): human ran `/wrap` to end the session; the pipeline was already PAUSED (COMPACT-STATE / S-CIGATE-3-IMPLEMENTED bursts), so this burst confirms rather than changes that status -- no product code, spec, or test file touched. Between the prior burst (`86ddb331`) and this one, the human explicitly authorized pushing (not merging) the S-CIGATE-3 story branch: `git push -u origin test/ci-gate-real-yaml-parser` succeeded; upstream now `origin/test/ci-gate-real-yaml-parser`; local HEAD == remote == `aeeebe0147e2cb616c34c6f05b54f135d62dd229` (re-verified via `git ls-remote origin refs/heads/test/ci-gate-real-yaml-parser` and `git rev-parse test/ci-gate-real-yaml-parser`, byte-identical). `git rev-list --count develop..test/ci-gate-real-yaml-parser` re-confirmed = 17 (unchanged from the prior burst's re-derivation; this time the dispatch instruction's own figure matched -- no discrepancy found or corrected this burst). NO PR opened -- verified via `gh pr list --head test/ci-gate-real-yaml-parser --json number,title,state` returning `[]`; the 'create a PR' link git prints on push is informational only. Merge authority remains the human's (DEC-128). NO CI ran on the branch -- `gh run list --branch test/ci-gate-real-yaml-parser` also returns zero rows, because this repo's `ci.yml` triggers on `push` only for `develop`/`main`; a `test/`-prefixed branch fires nothing without an open PR. Recorded as a live drift item (`S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED`, HIGH): the 17 commits have been validated only locally on macOS -- this project has a recorded incident (CI run 31128902318) where six `#[cfg(unix)]`-gated helpers became genuinely dead code and hard-errored under `-D warnings` on a Windows build, a defect class structurally unreachable by local verification alone; opening a PR is what would trigger validation, not done this burst. Second drift item recorded: burst `86ddb331` swept `regression-state.json`/`sidecar-learning.md` into the factory-artifacts commit via `git add -A`, breaking the standing 'leave dirty, never commit' telemetry-file convention; human ruled (DEC-266b) to leave it rather than revert (a revert would need a force-push on an already-pushed branch) -- recorded as `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT` (LOW); `sidecar-learning.md` confirmed dirty again as of this burst (`git -C .factory status`), which is normal for a live telemetry file. DEC-266 records both human rulings this session: (a) push without opening a PR; (b) leave the telemetry-file commit in place. No product code, spec, story file, or STORY-INDEX touched by this burst; `.worktrees/S-CIGATE-3` and the story branch untouched beyond the push itself (already completed before this burst began, not an action this burst performed). Files updated this burst: `STATE.md` (v2.40->v2.41), `cycles/cycle-001/{burst-log,session-checkpoints,decisions-archive,drift-items-open-detail}.md`. Next: human ruling on whether/when to open a PR for `test/ci-gate-real-yaml-parser` (which would trigger the CI validation this branch has never had), then the merge ruling. trajectory-tail →1→3→0→2 (unchanged -- not a Step-4.5 or S-CIGATE-3-window pass). D-chain cite D-453 latest brownfield (unchanged -- no new D-numbers introduced)."
trajectory_tail: "→1→3→0→2"
maintenance_run: CLOSED
current_cycle: "cycle-001"
feature_mode_bundle: SOH-DX-1-F4-DELIVERY
dtu_required: false
phase_2_status: APPROVED
phase_2_approved_at: 2026-05-07
phase_3_status: SOH-ATTACHMENTS-1 F5 SCOPED ADVERSARIAL CONVERGED 2026-07-24 STRICT (14 rounds; window pass-12/pass-13/pass-14 CLEANx3; spec v1.3.99 to v1.3.106; BC-INDEX v6.38 to v6.44; develop @ db207b81) + F7 DELTA CONVERGENCE APPROVED 2026-07-25 (DEC-186; 5-dim PASS; MAXIMUM_VIABLE_REFINEMENT_REACHED) + CYCLE FULLY CLOSED 2026-07-25
activation_head: "7b3ba371"
activation_version: "v0.6.0-dev.11"
---

<!-- STATE.md SIZE BUDGET (2026-08-11, S-CIGATE-3-PUSHED burst): 182 lines (wc-l) -- soft-target 200.
     margin from soft-target ~= 18. margin from actual to hard cap 500 ~= 318. Pre-burst figure was
     180 lines (measured via `wc -l` immediately before this burst); this burst is a small, additive
     push-confirmation + drift-recording burst -- one Phase-Progress row added, one Current-Phase-
     Steps row added, the Session Resume Checkpoint replaced (prior archived verbatim to
     session-checkpoints.md), two new Drift Item rows added. See cycles/cycle-001/{decisions-archive,
     drift-items-open-detail, burst-log, session-checkpoints, lessons, convergence-trajectory}.md
     for everything relocated out of this file historically. -->

# Pipeline State: jira-cli

## Project Metadata

| Field | Value |
|-------|-------|
| **Product** | jr (Jira CLI) |
| **Mode** | BROWNFIELD / Rust |
| **Target Workspace** | develop to main |
| **trajectory-tail** | →1→3→0→2 (unchanged this burst -- S-CIGATE-3's own adversarial window is story-scoped, not a Step-4.5 window; full window detail: cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md § S-CIGATE-3) |
| **Last Updated** | S-CIGATE-3-PUSHED 2026-08-11: human ran `/wrap`; pipeline confirmed PAUSED (unchanged). trajectory-tail →1→3→0→2 (unchanged). Story branch `test/ci-gate-real-yaml-parser` PUSHED to `origin` (human-authorized, DEC-266a) at `aeeebe0147e2cb616c34c6f05b54f135d62dd229`, 17 commits ahead of `develop` (re-verified). NO PR opened (`gh pr list` empty). NO CI ran (`gh run list` empty -- push-only trigger, no PR yet); recorded as drift `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED`. `86ddb331`'s telemetry-file commit left in place per human ruling (DEC-266b; drift `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`). `develop` still @ `d55bedf7`, unchanged. |
| **Current Phase** | Feature Mode SOH-DX-1 **F4 DELIVERY** -- DEC-204 fully ADJUDICATED (DEC-245). F2 APPROVED (DEC-196), F3 APPROVED (DEC-197). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671, `df203233`). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged to `develop` as `a5e1d087`, closes #626 -- DEC-262); trust-layer correction **PR #675 also MERGED** (`d55bedf7` -- DEC-264). `develop` CI SUCCESS on `d55bedf7` (run 31450052302). **S-CIGATE-3 (durable YAML-parser fix) IMPLEMENTED and now PUSHED but UNMERGED, NO PR, NEVER CI-VALIDATED** -- 17 commits on `test/ci-gate-real-yaml-parser`, pushed to `origin` this burst, story status `in-progress`; 6-pass story-scoped window ended PERMANENTLY at 0/3 (DEC-265), same shape as DEC-262; human ruling pending on opening a PR (which would trigger the branch's first-ever CI run) and, separately, on merge. Adversary (SOH-DX-1 Step 4.5, unaffected by S-CIGATE-3's separate window): 61 total Step-4.5-eligible passes (56 STRICT + pass-60/pass-61 targeted delta review) plus ADV-P675; 6 VOID; 2 NOT RUN (DEC-209); pass-20 SUPERSEDED (DEC-216); window 57/58/59 CLOSED 0/3 -- TENTH consecutive window without 3/3. Step 4.5 ends PERMANENTLY at **0/3** -- DEC-262 authorized merge on code grounds. STORY-INDEX v1.5.81 (127 stories; denominator unreconciled -- see Drift Items). AX23-001 PENDING RATIFICATION. **Factory PAUSED -- no phase transition this burst, push-confirmation + drift-recording only.** |
| **Next Phase** | Pending human decisions, in order: (1) **Whether/when to open a PR for `test/ci-gate-real-yaml-parser`** -- this is the concrete next action that would trigger the branch's first CI run (`S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED`); PR-opening and merge are now two separable decisions. (2) **Merge ruling on S-CIGATE-3** once CI has run (17 commits, adversarial window ended 0/3 never converged -- DEC-262-shape ruling available as precedent, not yet exercised; invoking it before a PR/CI cycle completes would additionally be a merge-with-zero-CI-runs decision). (3) Correct AC-006's false rationale in the story file (drift item `AC-006-FALSE-RATIONALE-UNCORRECTED`). (4) Whether to instantiate `.factory/policies.yaml` (absent; all six S-CIGATE-3 passes ran on baseline rubric only). (5) Second independent required CI check for GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE. (6) Gitleaks blocking / enforce_admins / `strict: false` config half. (7) Perimeter extension -- `docs/demo-evidence/` and/or `.factory/cycles/`. (8) Two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`). (9) `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT` / `GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE` / `JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` follow-up experiments (E1/E2, ~10 min each). (10) Restore the telemetry-file convention deliberately (`TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`) -- not urgent. Then: S-TRAIL-DERIVATION-GUARD-1, AX23-001, S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1. |
| **Activation HEAD** | 7b3ba371 (PR #654 squash-merged 2026-07-25; SOH-ATTACHMENTS-1 CYCLE FULLY CLOSED; activation_version v0.6.0-dev.11) |

## Phase Progress

<!-- Full per-burst history through S-CIGATE-3-PUSHED (2026-08-11): cycles/cycle-001/burst-log.md.
     The two rows below (pass-60/61, S-626-1-MERGE fix burst) are retained live per the
     structural adversary-pass/fix-burst requirement (D-435(b)/D-447(d)) and are ALSO archived
     verbatim in burst-log.md. The S-CIGATE-3-IMPLEMENTED row was archived this burst; every
     other prior burst is archived there under "### Archived Phase Progress Row (from <burst-name>)". -->
| Phase | Status | Completed | Gate | Notes | Finding Progression |
|-------|--------|-----------|------|-------|---------------------|
| pass-60 adversary (ADV-P60, Rust delta) / pass-61 adversary (ADV-P61, shell delta) | COMPLETE | 2026-08-10 | -- | Targeted delta reviews (not a Step-4.5 window) of `1381af17..5ca51bc2`; 9 actionable findings; 8 CLOSED pre-merge (3 HIGH + 1 MEDIUM + 4 LOW, via `736fea28`/`23ace476`/`f656f873`), 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN. | →1→3→0→2 (unchanged -- not a window pass) |
| **S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE fix burst (2026-08-10): PR #667 squash-merged to develop as `a5e1d087` (DEC-128/DEC-262), closing #626, via fix burst (`736fea28`+`23ace476`+`f656f873`). `develop` CI SUCCESS (run 31432422878). Story delivered and MERGED.** | COMPLETE | 2026-08-10 | -- | Factory paused pending next-priority dispatch. Four drift items recorded (2 process-gap, S-7.02-dispositioned inline; 2 external-research). DEC-262 recorded. Worktree cleanup complete. Next: S-CIGATE-3. | →1→3→0→2 |
| **S-CIGATE-3-PUSHED (2026-08-11): human `/wrap`; story branch `test/ci-gate-real-yaml-parser` PUSHED to `origin` (human-authorized) at `aeeebe0147e2cb616c34c6f05b54f135d62dd229`, 17 commits ahead of `develop` (re-verified). NO PR opened (`gh pr list` empty); NO CI ran (`gh run list` empty -- push-only trigger, no PR yet).** | COMPLETE (bookkeeping) / AWAITING PR+CI+MERGE RULING | 2026-08-11 | -- | Two human rulings recorded as DEC-266: (a) push without opening a PR; (b) leave the `86ddb331` telemetry-file commit in place as drift, do not revert. New drift item `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH) -- 17 commits validated only on macOS locally; this codebase has a recorded CI-only-catch incident (run 31128902318, six `#[cfg(unix)]` dead-code errors surfaced only on Windows) structurally unreachable by local verification. Second drift item `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT` (LOW). | (S-CIGATE-3 window, not Step 4.5 -- trajectory-tail unchanged →1→3→0→2) |

## Current Phase Steps

<!-- Full step-by-step burst history: cycles/cycle-001/burst-log.md. This burst's own row is the
     only one retained live; the S-CIGATE-3-IMPLEMENTED row was archived there this burst under
     the same header convention as its Phase Progress counterpart. -->
| Step | Agent | Status | Output |
|------|-------|--------|--------|
| **S-CIGATE-3-PUSHED (2026-08-11): state-manager ran the Single-Commit Burst Protocol (TD-VSDD-053, `vsdd-factory:state-burst` skill) to record: (1) human `/wrap` confirming the already-PAUSED pipeline; (2) `git push -u origin test/ci-gate-real-yaml-parser` (human-authorized, explicit) succeeded, upstream now `origin/test/ci-gate-real-yaml-parser`, local HEAD == remote == `aeeebe0147e2cb616c34c6f05b54f135d62dd229`; (3) re-verified 17 commits ahead of `develop` via `git rev-list --count`; (4) confirmed via `gh pr list --head test/ci-gate-real-yaml-parser` that NO PR is open; (5) confirmed via `gh run list --branch test/ci-gate-real-yaml-parser` that NO CI ran on this branch (push-only trigger scoped to develop/main); (6) reconfirmed via `git -C .factory status`/`ls-files` that `sidecar-learning.md` is dirty again (normal) and both telemetry files remain tracked from the `86ddb331` `git add -A` sweep, left as-is per human ruling. | state-manager | COMPLETED | `STATE.md` v2.40->v2.41, `cycles/cycle-001/{burst-log,session-checkpoints,decisions-archive,drift-items-open-detail}.md` appended (DEC-266; drift items `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` + `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`), all committed to factory-artifacts in ONE atomic commit (Single-Commit Burst Protocol, no Stage-2 backfill, no SHA placeholder), pushed via CAS. No product code, spec, story file, or STORY-INDEX touched. `.worktrees/S-CIGATE-3` and the story branch untouched by this burst beyond confirming the push already completed before this burst began. `regression-state.json`/`sidecar-learning.md` left dirty per the human ruling above (not reverted). |

## Decisions Log

<!-- Full Decisions Log (DEC-001 through DEC-266, minus the rows below) extracted to
     cycles/cycle-001/decisions-archive.md across the 2026-08-09 COMPACTION burst and every
     subsequent compaction, most recently this burst's addition of DEC-266 (human rulings on
     pushing S-CIGATE-3's branch without a PR, and leaving the telemetry-file commit as drift) --
     both rulings are historical record of a completed session action, not standing constraints
     on future work, so DEC-266 is archived directly rather than added live here. Retained here,
     in full, are only the decisions that still GOVERN behavior today. -->
| ID | Decision | Rationale | Phase | Date |
|----|----------|-----------|-------|------|
| DEC-128 | **MERGE AUTHORITY IS THE HUMAN'S (CRITICAL).** Delivery sub-agents must NOT self-authorize merges, spawn fix agents, push, or enter unbounded loops. CI green / mergeStateStatus CLEAN is not merge authorization. *(Original ruling is inside the collapsed DEC-001..DEC-155 range in decisions-archive.md, which has no separate DEC-128 row of its own; this entry restates the standing citation form used verbatim throughout this project's history.)* | Foundational merge-safety constraint; cited at every subsequent merge-adjacent decision (DEC-234, DEC-235, DEC-238, DEC-243, DEC-258, DEC-262, DEC-264, DEC-265, DEC-266...). | Phase 0-3 (original); standing | archived origin; standing |
| DEC-206 | **VOID PROTOCOL FOR ISOLATION BREACHES.** Adversary passes where orchestrator dispatch defects leak banned-path content are VOID for step-4.5 window eligibility; findings remain valid. | Human ruling on isolation protocol. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-03 |
| DEC-224 | **ISOLATION ELIGIBILITY PRINCIPLE.** A pass is ELIGIBLE (not VOID) when a letter-of-rule isolation deviation occurred but zero banned content actually surfaced; VOID applies only when banned content actually became visible. Self-disclosure without surfacing is a POSITIVE signal. Applied and held across every subsequent window through 57/58/59. | Principled distinction: the rule prevents contamination, not path syntax deviation. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-04 |
| DEC-245 | **DEC-204 ADJUDICATED -- CONSERVATIVE READING RULED.** A pass is CLEAN only with zero HIGH, zero MEDIUM and zero LOW findings; INFO-only findings still count as CLEAN; LOW findings reset the window regardless of GAP-vs-refinement classification. Step 4.5 remains 0/3, confirmed by ruling. Applied by DEC-265b to S-CIGATE-3's own story-scoped window for cross-story consistency. | Lenient reading would have stopped the cycle before discovering ADV-P50-HIGH-001, pass-51's three HIGH findings, pass-53's two HIGH findings, pass-55's HIGH finding, pass-57's HIGH finding, this burst's ADV-P60/P61 HIGHs, and S-CIGATE-3's pass-3 HIGH -- the conservative criterion has been expensive but a productive defect finder to the very end. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-08 |
| DEC-259 | **NEXT PRIORITY REMAINS S-CIGATE-3, NOT AN ELEVENTH WINDOW.** Human ruled the durable YAML-parser fix (S-CIGATE-3) remains next, rather than dispatching an eleventh STRICT adversarial window. **STATUS (2026-08-11): S-CIGATE-3 now IMPLEMENTED and PUSHED (see DEC-265, DEC-266), unmerged, no PR, human ruling pending.** | Ten consecutive windows without 3/3, and this window proved the class generates NEW AXES (positional-assumption, alongside value-reparse) rather than only new instances of one already-known axis -- now evidence-backed argument, not inference, for replacing hand-rolled line-based extraction with a real YAML parser. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-260 | **S-CIGATE-3 PARSER DECISION: `saphyr-parser` PINNED `=0.0.11`, DEV-DEPENDENCY, EVENT-STREAM API ONLY.** Rejected a PyYAML shell-out (no workflow in this Rust project uses Python) and `serde_yaml_ng` (cannot see anchors, tags, styles, or duplicate keys). The crate's own high-level `saphyr::Yaml` API is FORBIDDEN for this story's guards -- verified to silently collapse duplicate keys, erase quoting styles, and resolve aliases, reintroducing the exact "checker sees less than the runner" defect class this story exists to close. `yaml-rust2 0.11.0` recorded as the pre-vetted fallback. **STATUS (2026-08-11): implemented as specified -- `saphyr-parser =0.0.11`, event-stream API only, confirmed via `Cargo.toml` and the story's own AC-002.** | Established by direct execution against a real build in an isolated scratch worktree (`yaml-crate-probe`) this session, not from crate documentation alone; human-approved this session. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |
| DEC-262 | **MERGE AUTHORIZED ON CODE GROUNDS WITH STEP 4.5 PERMANENTLY AT 0/3 AFTER TEN WINDOWS AND 61 PASSES.** Human ruled PR #667 could merge without ever reaching the DEC-199/DEC-245 3/3-CLEAN convergence bar. PR #667 squash-merged to `develop` as `a5e1d087`, closing #626. Two pre-merge targeted delta reviews (pass-60/pass-61, ADV-P60/ADV-P61, not a Step-4.5 window) covered the previously-unreviewed `1381af17..5ca51bc2` delta and found 9 actionable findings, 8 closed (3 HIGH + 1 MEDIUM + 4 LOW) via fix burst before merge; 1 LOW (`ADV-P61-LOW-004`) confirmed OPEN, not fixed. | `src/` 0-defect across 32+ passes; all findings across ten windows plus the two delta reviews confined to test/guard infrastructure, never shipped product code; the guard apparatus is materially stronger than what `develop` carried before this story, and its documented claims now match its actual behavior after this burst's fixes; the residual failure class (line-based extraction generating genuinely new inspection axes each window) is routed to S-CIGATE-3 rather than an eleventh window, which the ten-window trend suggested would likely find a new axis rather than converge. | Feature Mode SOH-DX-1 F4 Step 4.5 | 2026-08-10 |

## Skip Log

All S-WIN-1..6 + #475 + S-FORK-OPS-BACKFILL-1 + S-FORK-OPS-GITLEAKS-DOC-1 + S-MAINT-DEAD-CITATION-CI + S-626-1 per-AC demos: Yes -- adapted (S-626-1: 11 artifacts at `.factory/demos/S-626-1/`). See cycles/cycle-001/burst-log.md.

## Blocking Issues

None open.

## Drift Items (open / tracked)

<!-- COMPACTED 2026-08-09, RE-COMPACTED 2026-08-11 (COMPACT-STATE burst); 2 new rows added by
     this burst (S-CIGATE-3-PUSHED). This table is a compact one-line-per-item index. Full
     narrative bodies for ALL rows below are in cycles/cycle-001/drift-items-open-detail.md --
     update the row there and this index together going forward; do not let a Summary cell
     regrow past one line. Per-burst new/updated/closed drift-item ledger through this burst:
     cycles/cycle-001/burst-log.md. -->
| ID | Severity | Summary |
|----|----------|---------|
| GUARD-MODE-UNREACHABLE-LOCALLY | HIGH | Environment-gated guard branches have no local test coverage by construction. CLOSED for this instance (`f2bea32e`); general rule DEFERRED to review checklist. Full detail: `cycles/cycle-001/drift-items-open-detail.md`. |
| POSITIONAL-ASSUMPTION-AXIS | HIGH | `extract_key_name_at_indent`'s hard-coded 4-space indent is unchecked -- second axis of the line-based-extraction defect class. **CLOSED BY CONSTRUCTION (2026-08-11, S-CIGATE-3-IMPLEMENTED)** -- event-stream model has no indent arithmetic. Full detail: `drift-items-open-detail.md`. |
| MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM | MEDIUM | FOURTH+FIFTH INSTANCE (2026-08-11, S-CIGATE-3-IMPLEMENTED): a dispatch instruction's own prose total (16 commits, 29 findings) each mismatched its own supporting data (17 commits, 27 findings); both re-derived and corrected. Standing rule reaffirmed. This burst's own re-derivation matched its dispatch instruction exactly (no new instance). Full detail: `drift-items-open-detail.md`. |
| RED-PROOF-NEEDS-SPELLING-VARIANTS | HIGH | Confirmed TWO-AXIS: a RED proof must cover both the spelling axis and the indent/position axis. **CLOSED (2026-08-11, S-CIGATE-3-IMPLEMENTED)** -- two-axis RED proofs (AC-008) across every rewritten guard. Full detail: `drift-items-open-detail.md`. |
| MATRIX-FAIL-FAST-MASKS-SCOPE | LOW | Matrix fail-fast made cancelled legs on run 31406705091 read as genuinely failed. Full detail: `drift-items-open-detail.md`. |
| ORCHESTRATOR-STALE-AGENT-NAME-COLLISION | MEDIUM | A stopped, re-dispatched agent stays addressable under a canonical-looking name. Corrective: never resolve inputs by contacting agents by name. Full detail: `drift-items-open-detail.md`. |
| RESEARCH-ARTIFACTS-NOT-PERSISTED | MEDIUM | DEC-246 drove a code change with no research artifact file. DEFERRED, no story opened. Full detail: `drift-items-open-detail.md`. |
| DEC-246-OVERCLAIMED-CONFIRMS | MEDIUM | DEC-246's original 8/8 CONFIRM was overclaimed; two re-validation passes (DEC-249, DEC-261) found a mixed disposition. Full detail: `drift-items-open-detail.md`. |
| BURST-LOG-DEFEATS-PLAIN-GREP | LOW | `burst-log.md` contains bytes plain `grep` treats as binary; use `grep -a`. Full detail: `drift-items-open-detail.md`. |
| ADVERSARY-PASSES-27-61-HAVE-NO-DETAIL-FILE | LOW | Per-pass detail artifacts exist only for passes 1-19/21-26; `ADV-P1-INDEX.md` is the sole record for the rest (now also true of S-CIGATE-3's 6 passes). Full detail: `drift-items-open-detail.md`. |
| STORY-INDEX-DENOMINATOR-UNRECONCILED | MEDIUM | `STORY-INDEX.md` declares 127 stories; actual file/row counts don't reconcile. No audit run yet. Full detail: `drift-items-open-detail.md`. |
| STORY-STATUS-DRIFT-INDEX-UNRELIABLE | MEDIUM | 37 stories marked `ready` spot-check as already shipped. Recommend a reconciliation sweep. Full detail: `drift-items-open-detail.md`. |
| RUNNER-SOURCE-NOT-AN-ORACLE-FOR-WORKFLOW-PARSING | MEDIUM | `actions/runner` public source lags shipped server-side parsing (e.g. rejects anchors GitHub shipped 2025-09-18); not authoritative for parsing questions. Full detail: `drift-items-open-detail.md`. |
| DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST | HIGH | An agent reported a mechanism complete having written only documentation for it; verification checked the reported metric, not the mechanism. CLOSED for this instance (`f656f873`); general rule DEFERRED. Full detail: `drift-items-open-detail.md`. |
| ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION | MEDIUM | A requested "small consistency fix" converted a fail-closed extractor panic into a fail-open silent miss. CLOSED for this instance (`f656f873`); general rule DEFERRED. Full detail: `drift-items-open-detail.md`. |
| JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT | MEDIUM | Passwordless sudo on GitHub-hosted runners makes `ADV-P59-LOW-001` not closable by any in-script check; real control is not running untrusted steps before the gate step. Reconfirmed 2026-08-11 (DEC-263). Full detail: `drift-items-open-detail.md`. |
| GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE | MEDIUM | `GITHUB_ACTIONS` env-var overwrite UNVERIFIED; `RUNNER_OS` CONFIRMED protected by `ScriptHandler.cs`'s write-ordering (DEC-263). Minimal experiment specified, not run. Full detail: `drift-items-open-detail.md`. |
| JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION | MEDIUM | `resolve_trusted_jq`'s decision-path trust reduces to `/usr/bin` being root-only (INFERRED, not CONFIRM); a ~10-min experiment (E1) would settle it. DEFERRED. Full detail: `drift-items-open-detail.md`. |
| MACOS-ALLOWLIST-TRUSTS-WRITABLE-DIR | LOW | `/opt/homebrew/bin` is runner-writable without sudo, `test` job's macOS leg only (not the decision path). Entry must NOT be removed -- reproduces CI-BREAK-1. Full detail: `drift-items-open-detail.md`. |
| CORRECTION-PR-INTRODUCED-NEW-FALSE-CLAIMS | MEDIUM | PR #675, whose purpose was correcting prior inaccuracies, introduced two new MEDIUM false claims of its own (amended `d2430a8a`). Corrective: apply the same claim-accuracy review to correction PRs. Full detail: `drift-items-open-detail.md`. |
| SCOPED-SOURCE-GENERALIZED-THROUGH-CITATION-CHAIN | LOW | A source's scoped claim lost its scope at each citation hop, gaining an unwarranted CONFIRM label at the last hop (amended `d2430a8a`). Corrective: re-fetch the original at each hop. Full detail: `drift-items-open-detail.md`. |
| AC-006-FALSE-RATIONALE-UNCORRECTED | MEDIUM | Story file's AC-006 rationale is FALSE (msrv job cannot be affected by a dev-dependency pin); `Cargo.toml`'s comment already corrected (`80a872e4`), story AC text intentionally not altered. Full detail: `drift-items-open-detail.md`. |
| VALUE-SIDE-ANCHOR-GAP-UNCLOSED | LOW | `resolve_value` discards `anchor_id` on a scalar's VALUE side (`run: &x cmd`); documented not closed, no exploit constructible against any pinned guard. Full detail: `drift-items-open-detail.md`. |
| POLICIES-YAML-NOT-INSTANTIATED | LOW | `.factory/policies.yaml` absent from this project; all six S-CIGATE-3 adversarial passes ran on baseline rubric only. Full detail: `drift-items-open-detail.md`. |
| RECURRING-DEFECT-RELOCATES-NOT-CLOSES | MEDIUM | Same defect shape relocated 4x across S-CIGATE-3 passes 3-6 (by NAME, then `run:` KEY, then `run:` VALUE, then "actually RUNS") before a class sweep closed it. Full detail: `drift-items-open-detail.md`. |
| S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED | HIGH | NEW (2026-08-11, S-CIGATE-3-PUSHED). `test/ci-gate-real-yaml-parser` pushed to `origin` but has never triggered a CI run (`ci.yml` is push-only on `develop`/`main`, no PR open yet) -- 17 commits validated only locally on macOS; this project has a recorded Windows-only CI catch (run 31128902318). Full detail: `drift-items-open-detail.md`. |
| TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT | LOW | NEW (2026-08-11, S-CIGATE-3-PUSHED). Burst `86ddb331` committed `regression-state.json`/`sidecar-learning.md` to factory-artifacts, breaking the "leave dirty, never commit" convention. Human ruled leave-as-is (DEC-266b) rather than force-push a revert. Full detail: `drift-items-open-detail.md`. |

## Convergence Status

**COMPACT-STATE (2026-08-11):** proactive compaction burst (human-approved); no adversary pass, no findings added or closed, does not move the trajectory-tail (→1→3→0→2, unchanged) or Step 4.5's 0/3. Extracted the three most recent closed-burst Convergence Status paragraphs (S-626-1-MERGE+ADV-P60-P61+BURST-CLOSE, SHELL-TRUST-ASSUMPTIONS, PR675-MERGE+ADV-P675-CLOSE) to `cycles/cycle-001/convergence-trajectory.md`.

**S-CIGATE-3-PUSHED (2026-08-11):** push-confirmation + drift-recording burst, NOT a Step-4.5 window and NOT a new S-CIGATE-3 adversarial pass -- does not move SOH-DX-1's trajectory-tail (→1→3→0→2, unchanged) or Step 4.5's 0/3 (permanently closed per DEC-262), and does not reopen S-CIGATE-3's own 6-pass window (still ended PERMANENTLY at 0/3, DEC-265, 27 findings, all fixed). This burst's only new facts: the story branch is now pushed to `origin` (DEC-266a) and confirmed to have triggered zero CI runs, and the prior burst's telemetry-file convention break is confirmed left in place (DEC-266b). `ADV-P1-INDEX.md` combined total unchanged at 488. Full trajectory detail: cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md § S-CIGATE-3 + cycles/cycle-001/convergence-trajectory.md.

BC-INDEX v6.75 / STORY-INDEX v1.5.81 (127 stories) / ARCH-INDEX v0.16. S-626-1 adversary: 61 Step-4.5-eligible passes plus ADV-P675; 461 findings (unchanged, SOH-DX-1 scope). S-CIGATE-3 adversary: 6 story-scoped passes, 27 findings (unchanged this burst). `ADV-P1-INDEX.md` combined total: 488. **PR #667 MERGED** (`a5e1d087`, closes #626 -- DEC-262); **PR #675 MERGED** (`d55bedf7` -- DEC-264). `develop` CI SUCCESS on `d55bedf7` (run 31450052302), unchanged by this burst. src/ 0-defect THIRTY-THIRD-plus consecutive (SOH-DX-1 scope). **S-CIGATE-3 implemented, pushed, unmerged, no PR, never CI-validated -- human ruling pending.** AX23-001 PENDING RATIFICATION. **Factory PAUSED.** Full trajectory detail: cycles/cycle-001/convergence-trajectory.md + cycles/cycle-001/adversarial-reviews/ADV-P1-INDEX.md.

SOH-ATTACHMENTS-1 + prior cycles FULLY CLOSED. See cycles/cycle-001/convergence-trajectory.md.

## Concurrent Cycles

<!-- Prior completed cycles archived to burst-log: ADF-CODE-MARK-EXCLUSIVITY (DEC-163, v0.6.0-dev.8), SOH-BUGS-1 (DEC-167, v0.6.0-dev.9), SOH-COMMENT-CRUD-1 (DEC-176, v0.6.0-dev.10), SOH-ATTACHMENTS-1 (DEC-186, v0.6.0-dev.11). See cycles/cycle-001/convergence-trajectory.md. -->
| Cycle | Status | Notes |
|-------|--------|-------|
| SOH-DX-1 (issues #639+#627+#626) | F4 DELIVERY -- DEC-204 fully ADJUDICATED (DEC-245). **S-626-1 DELIVERED AND MERGED** (PR #667 squash-merged as `a5e1d087`, closes #626 -- DEC-262; trust-layer record correction PR #675 also MERGED as `d55bedf7` -- DEC-264); Step 4.5 ends PERMANENTLY at 0/3 -- 61 Step-4.5-eligible passes, never reached 3/3, plus ADV-P675 (not Step-4.5-eligible). **S-CIGATE-2 DELIVERED AND MERGED** (PR #671 squash-merged `df203233`); **S-CIGATE-3 IMPLEMENTED, PUSHED, UNMERGED** (v1.3, status `in-progress`; 17 commits on `test/ci-gate-real-yaml-parser`, pushed to `origin` this burst, no PR opened, never CI-validated; 6-pass story-scoped window ended PERMANENTLY at 0/3 -- DEC-265; human ruling pending on PR-opening and merge); **S-CIGATE-4** (done) unchanged; **S-TRAIL-DERIVATION-GUARD-1** (P2/draft, status unresolved) unchanged. FACTORY PAUSED (SESSION-WRAP-PAUSE, COMPACT-STATE, S-CIGATE-3-IMPLEMENTED, S-CIGATE-3-PUSHED -- all 2026-08-11). | 3 stories: S-639-1 (BREAKING/v0.6.0-dev.12), S-627-1, S-626-1 (DELIVERED AND MERGED). Plus S-CIGATE-2 (DELIVERED/MERGED), S-CIGATE-3 (implemented, pushed, unmerged, no PR, never CI-validated), S-CIGATE-4 (P1, done), S-TRAIL-DERIVATION-GUARD-1 (P2, draft, status unresolved). S-626-1 adversary: 61 Step-4.5-eligible passes + ADV-P675; 461 findings; THIRTY-THIRD-plus zero-src/-defect consecutive. S-CIGATE-3 adversary: 6 story-scoped passes, 27 findings, all fixed. AX23-001 PENDING. |

## Session Resume Checkpoint
| Field | Value |
|-------|-------|
| Position | Feature Mode SOH-DX-1, phase F4. **S-CIGATE-3-PUSHED burst (2026-08-11): human ran `/wrap`.** Factory remains PAUSED (unchanged). trajectory-tail →1→3→0→2 (unchanged). Headline: S-626-1 DELIVERED (PR #667 merged as `a5e1d087`, closes #626); PR #675 MERGED (`d55bedf7`). `develop` @ `d55bedf7`, unchanged this burst, CI SUCCESS (run 31450052302). **NEW this burst's record: the story branch `test/ci-gate-real-yaml-parser` was PUSHED to `origin`, human-authorized in response to a direct question (DEC-266a).** `git push -u origin test/ci-gate-real-yaml-parser` succeeded; upstream now `origin/test/ci-gate-real-yaml-parser`; local HEAD == remote == `aeeebe0147e2cb616c34c6f05b54f135d62dd229` (re-verified via `git ls-remote` and `git rev-parse`, byte-identical). 17 commits ahead of `develop` (re-confirmed). Working tree of `.worktrees/S-CIGATE-3` clean. **NO PR opened** (`gh pr list --head test/ci-gate-real-yaml-parser` returns `[]`; merge authority remains the human's, DEC-128). **NO CI ran** (`gh run list --branch test/ci-gate-real-yaml-parser` returns zero rows -- `ci.yml` is push-only on `develop`/`main`; a `test/`-prefixed branch fires nothing without a PR) -- recorded as drift item `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH). Separately, `86ddb331`'s telemetry-file commit (`regression-state.json`/`sidecar-learning.md` swept into factory-artifacts, breaking the "leave dirty, never commit" convention) is confirmed left in place -- human ruled leave-as-is rather than force-push a revert (DEC-266b; drift item `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`, LOW); `sidecar-learning.md` confirmed dirty again as of this burst, which is normal. Nothing else is in flight: no open PR, no adversarial pass running, no uncommitted work anywhere except the standing-convention dirty `.factory/` telemetry files and pre-existing untracked `.claude/` product-repo files (both explicitly NOT abandoned work -- see "Not lost work" below). |
| Convergence | Unchanged this burst. SOH-DX-1's Step 4.5 remains PERMANENTLY at 0/3 (trajectory-tail →1→3→0→2, unchanged). S-CIGATE-3's OWN 6-pass story-scoped adversarial window remains ended PERMANENTLY at 0/3 (27 findings, 1 HIGH + 10 MEDIUM + 16 LOW, all fixed, no pass ever CLEAN, DEC-265). `ADV-P1-INDEX.md` combined total: 488, unchanged this burst -- no new adversarial pass ran (`{CRIT:0,HIGH:36,MED:143,LOW:175,INFO:134}`). `src/` 0-defect streak intact throughout SOH-DX-1 scope (33+ consecutive). This burst added no findings and closed none -- it is a push-confirmation and drift-recording burst only. |
| Pending human decisions / next steps, in priority order | (1) **Whether/when to open a PR for `test/ci-gate-real-yaml-parser`** -- the concrete next action that would trigger the branch's first-ever CI run; PR-opening and the merge ruling are now two separable decisions, not one. (2) **Merge ruling on S-CIGATE-3** once CI has run -- the adversarial window ended 0/3, never converged; the DEC-262 shape (merge on code grounds with convergence explicitly unmet) is available as precedent but not yet exercised, and invoking it before a PR/CI cycle completes would additionally be a merge-with-zero-prior-CI-runs decision, one step beyond any prior precedent in this project. (3) **Correct AC-006's false rationale in the story file** -- the exact-version-pin justification still claims protection of the `msrv` CI job, which is false (dev-dependency, `msrv` scope is lib+bins only); `Cargo.toml`'s own comment was already fixed in-branch (`80a872e4`), the story AC text was not. (4) Value-side anchor gap (`run: &x cmd`, `resolve_value` discards `anchor_id`) -- documented, not closed, no exploit constructible. (5) Whether the `ScalarStyle::Plain` fidelity mandate should become a formal decision record. (6) Whether to instantiate `.factory/policies.yaml` (absent; all six S-CIGATE-3 passes ran on baseline rubric only). (7) 643 lines merged unreviewed via PR #667 remain a residual (SHELL-TRUST-ASSUMPTIONS validated the shell portion, DEC-263; the Rust portion is largely superseded by S-CIGATE-3's own rewrite). (8) Two ~10-minute throwaway-workflow experiments remain unrun: E1 (`/usr/bin` write-access probe) and E2 (`/opt/homebrew/bin` writability). (9) STORY-INDEX has two unresolved integrity problems (37 stories marked `ready` that spot-check as shipped; `total_stories: 127` unreconciled against ~91 files / ~210 rows) -- recommend an audit/reconciliation sweep. (10) `.factory/hooks/` was never instantiated in this project. (11) Restore the telemetry-file convention deliberately when convenient -- not urgent. (12) 16+ local branches with deleted remotes (`[gone]`), pre-existing, never actioned. Carried forward unchanged: second required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); gitleaks/enforce_admins/`strict: false` config ruling; perimeter extension (`docs/demo-evidence/`, `.factory/cycles/`); `S-MAINT-CR-008`/`S-TRAIL-DERIVATION-GUARD-1` unresolved statuses; AX23-001 ratification; MIXED-SET-DASH-ARM-UNPINNED scheduling; ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling; SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade; BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS follow-up BC/VP-minting authorization. |
| Not lost work | `.worktrees/S-CIGATE-3` and its branch `test/ci-gate-real-yaml-parser` are **NOT lost or abandoned work** -- they hold the full, tested, adversarially-reviewed S-CIGATE-3 implementation, now durable on `origin` (pushed), deliberately left without an open PR pending the human's next-step ruling (DEC-128, DEC-266a). The two dirty `.factory/` telemetry files (`regression-state.json`, `sidecar-learning.md`) and the four untracked `.claude/` product-repo files (`.claude/hooks/`, `.claude/pr-reviews/`, `.claude/settings.local.json.bak`, `.claude/spec-config.json`) are both **standing conventions / pre-existing state from earlier in this session** -- explicitly NOT work-in-progress that got interrupted by this pause. A fresh session should not attempt to open a PR, merge, or "clean up" any of this without a separate, explicit instruction. |
| Closing note | This burst performed NO product code, spec, story, or STORY-INDEX changes -- it is a push-confirmation and drift-recording burst only. Every claim was re-derived against a live command this burst (`git ls-remote`, `git rev-parse`, `git rev-list --count`, `gh pr list`, `gh run list`, `git -C .factory status`/`ls-files`) rather than assumed from the dispatch instruction; all figures matched the dispatch instruction exactly this time (17 commits, zero PRs, zero CI runs) -- no discrepancy found or corrected, unlike the prior burst. Every write to this file used the `Write` tool with a single full-content write, per the standing DEC-247 convention. |
| Resume command | Open a fresh session in this project and run `/vsdd-factory:next-step`. It reads `STATE.md` and picks up at the PR/merge-ruling decision for `test/ci-gate-real-yaml-parser` as the next priority. |

## RESUME PLAN (cold-start)
Step 1 (BLOCKING): Run vsdd-factory:factory-worktree-health. Read this file.
Step 2 -- **S-CIGATE-3 (durable YAML-parser fix) is IMPLEMENTED and now PUSHED on `test/ci-gate-real-yaml-parser`, 17 commits `8af710f8`..`aeeebe01`, upstream `origin/test/ci-gate-real-yaml-parser` at `aeeebe0147e2cb616c34c6f05b54f135d62dd229`.** NO PR opened (`gh pr list` empty; merge authority is the human's, DEC-128). **NO CI has ever run on this branch** (`gh run list` empty -- `ci.yml` triggers push-only on `develop`/`main`; a `test/`-prefixed branch fires nothing without an open PR) -- drift item `S-CIGATE-3-BRANCH-NEVER-CI-VALIDATED` (HIGH): the 17 commits are validated only locally on macOS, and this project has a recorded incident (CI run 31128902318) of a Windows-only dead-code failure structurally unreachable by local verification. Its own 6-pass story-scoped adversarial window ended PERMANENTLY at 0/3 (27 findings, all fixed, no CLEAN pass; DEC-265). Two pre-existing bypasses (round-16 node-property residual; POSITIONAL-ASSUMPTION-AXIS/RED-PROOF-NEEDS-SPELLING-VARIANTS) were closed in-scope rather than deferred again (DEC-265a). AC-006's rationale in the story file remains FALSE and uncorrected (drift item `AC-006-FALSE-RATIONALE-UNCORRECTED`) -- `Cargo.toml`'s comment was already fixed in-branch. Separately, `86ddb331`'s telemetry-file convention break (`regression-state.json`/`sidecar-learning.md` committed via `git add -A`) is confirmed left in place per human ruling (DEC-266b; drift item `TELEMETRY-FILES-COMMIT-LEFT-AS-DRIFT`, LOW) -- not lost or accidental, do not revert without explicit instruction. **Factory is PAUSED.** trajectory-tail →1→3→0→2 (unchanged this burst). SOH-DX-1's own state is unchanged from the prior burst: S-626-1 DELIVERED AND MERGED (`a5e1d087`, closes #626); PR #675 also MERGED (`d55bedf7`); Step 4.5 ends permanently at 0/3 (DEC-262). `develop` CI SUCCESS on `d55bedf7` (run 31450052302), unaffected by S-CIGATE-3 (pushed, but no PR, unmerged). Seek human confirmation/dispatch, in order: (1) whether/when to open a PR for `test/ci-gate-real-yaml-parser` (triggers first-ever CI run on this branch); (2) S-CIGATE-3 merge ruling once CI has run (push branch already done; open PR? merge on code grounds per DEC-262-shape precedent, or require a fresh CLEAN pass first?); (3) correct AC-006's false rationale in the story file; (4) whether to instantiate `.factory/policies.yaml`; (5) second independent required CI check (GUARD-APPARATUS-HAS-SINGLE-POINT-OF-FAILURE); (6) gitleaks blocking / enforce_admins / `strict: false` config half; (7) whether to extend the perimeter to `docs/demo-evidence/` and/or `.factory/cycles/`; (8) the two unresolved story statuses (`S-MAINT-CR-008`, `S-TRAIL-DERIVATION-GUARD-1`); (9) follow-up experiments for `JQ-TRUST-NOT-CLOSABLE-IN-SCRIPT`/`GITHUB-ACTIONS-ENV-VAR-LIKELY-WRITABLE`/`JQ-TRUST-RESTS-ON-ONE-UNDOCUMENTED-PERMISSION` (E1/E2); (10) restore the telemetry-file convention deliberately when convenient.
Step 3 -- Once S-CIGATE-3's PR/merge ruling is resolved: also pending: S-640-1 handoff, S-MAINT-576-HYG-1, S-639-1 (BREAKING/v0.6.0-dev.12). MIXED-SET-DASH-ARM-UNPINNED test story to schedule. ISOLATION-WHITELIST-LEAKS-FINDING-IDS ruling needed; SHARED-WORKTREE-REVIEWER-CONTAMINATION downgrade ruling needed; BC-COVERAGE-GAP-FOR-CI-GATE-GUARDS needs a follow-up-story ruling; STORY-INDEX denominator audit and STORY-STATUS-DRIFT reconciliation sweep recommended. **Dispatch discipline reminder (standing):** state-manager runs LAST in a burst; never dispatch two agents writing to a shared artifact concurrently; NEVER solicit input by messaging an agent by name -- the orchestrator is the single source of completed results (ORCHESTRATOR-STALE-AGENT-NAME-COLLISION); verify count/trail claims against the derivation command, not a recorded number (MEASUREMENT-METHOD-PRODUCES-FALSE-CLAIM, now five-times-recurred; this burst's own re-derivations matched their dispatch instruction exactly, showing the discipline also confirms accuracy, not only catches error); frontier variety, not pass count, is what makes a CLEAN verdict meaningful; a RED proof must cover every semantically-equivalent spelling AND every legal indent/position of its target (RED-PROOF-NEEDS-SPELLING-VARIANTS, closed for S-CIGATE-3's rewritten guards); an environment-gated guard branch must carry a test that forces the gate variable (GUARD-MODE-UNREACHABLE-LOCALLY); verify the mechanism a change claims to add, not only the metric it reports on (DOC-CLAIMS-A-GUARD-THAT-DOES-NOT-EXIST); a change to an extractor's failure direction needs its own bidirectional RED proof, regardless of how the requesting instruction is framed (ORCHESTRATOR-FIX-INSTRUCTION-CAUSED-REGRESSION); a correction is itself an operation that can corrupt a record -- apply the same claim-accuracy review to a record-correction PR as to the record it corrects, and to a story file's own AC text as to the code comment already fixed (PR675-MERGE+ADV-P675-CLOSE, AC-006-FALSE-RATIONALE-UNCORRECTED); pushing a branch and opening a PR are separable human authorizations -- do not conflate "push authorized" with "PR/merge authorized" (DEC-266a, this burst); a convention break discovered after the fact is not automatically worth a force-push to fix -- weigh the revert's own risk against leaving it as documented drift (DEC-266b, this burst); when STATE.md needs an update, use the Write tool with content that advances `timestamp:` -- that is the sanctioned path (DEC-247); when `.factory/cycles/cycle-001/burst-log.md` needs grepping, use `grep -a` (BURST-LOG-DEFEATS-PLAIN-GREP); a downstream agent refusing to write a stale measurement is a correction to act on, not friction to route around; deleting a temporary equivalence/scaffolding test needs its own check for whether the replacement module has any tests of its own left; STATE.md's Phase Progress section must always retain at least one row mentioning 'pass-N' and one row mentioning 'fix burst', every prescribed trajectory-tail-cell site must carry the arrow-sequence literally, and the frontmatter D-chain cite must track the MAX D-number cited anywhere in the body -- `validate-state-structure`/`validate-trajectory-tail-cell-completeness`/`validate-dispatch-advance` each block a write that misses one of these (D-435(b)/D-443(a)/D-447(d)/D-453(d)).

## Open Issues Tracker

| Issue | Title | Status | Priority | Notes |
|-------|-------|--------|----------|-------|
| #429 | jr_isolated() crypto-random suffix | OPEN | LOW | DEC-029 deferred to human. Do NOT close autonomously. |
| #400 | Test-hardening + process-gap follow-ups | OPEN -- Story A MERGED PR #431. Story B + engine items remain. | LOW | |
| #372 | cargo-mutants partial baseline | OPEN | LOW | Follow-up from #346 |
| #387/#368 | git history rewrite / open PR | OPEN | LOW | Force-push needed. Deferred. |

## Historical Content

See `cycles/cycle-001/`: burst-log.md, convergence-trajectory.md, session-checkpoints.md, lessons.md, blocking-issues-resolved.md, decisions-archive.md, drift-items-closed.md, drift-items-open-detail.md, adversarial-reviews/ADV-P1-INDEX.md.
