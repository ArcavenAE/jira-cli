# Research Index

| Date | Type | Topic | File | Status |
|------|------|-------|------|--------|
| 2026-05-29 | general | Live-API E2E testing best practices (Jira Cloud REST v3 + Agile) | e2e-enhancement-best-practices.md | complete |
| 2026-05-31 | general | E2E portability: issue links, remote links, labels, bulk-edit tiers, assign/unassign | e2e-link-label-coverage.md | complete |
| 2026-06-09 | general | Bare-URL autolink scope for markdown_to_adf (issue #473): GFM rules, converter defaults, Jira ADF link-mark requirement | issue-473-bare-url-autolink-scope.md | complete |
| 2026-06-11 | general | ADF read-path E2E external API validation (issue #475): v3 ADF round-trip for description/comment, listItem-forbids-blockquote, server-side normalization risk, recency | issue-475-adf-e2e-external-validation.md | complete |
| 2026-06-28 | general | Fast & required cargo-mutants in GitHub Actions CI: build cost, caching, --jobs/--shard parallelism, timeout determinism, --in-diff scope, required-gate patterns (cargo-mutants 27.1.0) | mutation-ci-perf-2026-06-28.md | complete |
| 2026-07-24 | general | Disk-write error taxonomy for `jr issue attachment download` (F5-R5-001): ENOSPC/EACCES/generic message shape, peer-CLI hybrid pattern, ErrorKind stabilization at MSRV 1.85, cross-platform Windows mapping, synthetic-error testing | f5-r5-001-disk-error-taxonomy-2026-07-24.md | complete |
| 2026-08-09 | general | GitHub Actions gating semantics behind `ci-gate` (DEC-246, S-626-1): `needs` context contract + `result` domain, `always()` vs `!cancelled()`, skipped-required-check false-green, matrix jobs in `needs` / zero-leg case, required-check name matching, merge queue + Required Workflows availability, re-run semantics. Reconstruction of the 2026-08-08 pass (no artifact was written) plus independent primary-source re-validation; 5 CONFIRM, 2 INCONCLUSIVE, 1 split, 0 REFUTE | dec-246-github-actions-gating-semantics.md | complete (partially reconstructed — 2 of the original 8 questions unrecoverable) |
| 2026-08-10 | general | GitHub Actions open semantics behind `ci-gate` (DEC-246 follow-up, S-626-1): zero-leg matrix → `needs.result`, `matrix.exclude` reaching zero (ADV-P56-INFO-001), re-run/attempt-≥2 semantics, duplicate required-check-name resolution (Guard A premise), flow-style `jobs:` mapping acceptance (ADV-P55-MED-002), `$GITHUB_PATH` binary resolution + step-`env:` vs `$GITHUB_ENV` precedence (ADV-P59-LOW-001). Primary sources incl. `actions/runner` source at `main`; 3 CONFIRM, 3 INCONCLUSIVE (2 REQUIRES-EXECUTION), 2 split, **1 REFUTE** (zero-leg "community split" evidence characterization — no shipped guard invalidated); 5 minimal experiments specified | gh-actions-open-semantics-2026-08-10.md | complete (4 of 6 questions require an empirical run to close) |
| 2026-08-10 | general | `check-ci-gate.sh` shell-trust layer external assumptions (S-626-1, unreviewed 401-line delta merged via PR #667 / `a5e1d087`): `/usr/bin`+`/bin` writability on `ubuntu-latest`, usrmerge equivalence of the two Linux allowlist entries, `/opt/homebrew/bin` writability on `macos-latest`, whether `RUNNER_OS` can be UNSET (not merely overwritten), exact `RUNNER_OS` value domain + `*)` fail-closed arm, and `$(</dev/stdin)` fork-free builtin semantics over a pipe. Primary sources incl. `actions/runner` `ScriptHandler.cs`/`RunnerContext.cs`, `actions/runner-images`, Ubuntu/Canonical/Debian, Homebrew installer, GNU Bash manual; plus local `bash` execution (3.2.57 + 5.3.9) and production run 31432422878. **0 REFUTE**, 8 CONFIRM, 2 INCONCLUSIVE-on-primary (carried as labelled INFERRED, not promoted); 2 minimal experiments specified. Bottom line: the trust property holds on the decision path only because `/usr/bin` needs root, and passwordless `sudo` is documented — the guard closes exactly one vector (a no-sudo PATH shim); the macOS allowlist trusts a runner-writable directory (test leg only) | ci-gate-shell-trust-assumptions-2026-08-10.md | complete (Q1b `/usr/bin` writability requires a 10-min empirical run to close) |
| 2026-08-13 | general | Empty/unset date-field rendering convention for a new "Due Date" column (issue #668): how well-designed general-purpose CLIs render an empty cell in human tabular output — kubectl `<none>` (per-field, verified src), docker `<none>` (untagged images), gh (blank), git (blank/`--omit-empty`), aws (`None`), gcloud (configurable), terraform (semantic), jira-cli/glab (blank, unverified). Weighs dominant general-CLI convention vs. jr's existing `-` (all date fields: Created/Updated/table date column) and `(none)` (identity/list fields) usage. RECOMMENDATION: `-` for Due Date to match sibling date fields; `<none>` only if a full cross-codebase standardization pass is undertaken separately | 668-empty-date-rendering-convention.md | complete |
| 2026-08-17 | general | DISCOVERY-phase error posture for `jr component rename --all-projects` (S-608-1): fail-closed-abort vs skip-and-continue when a per-project components GET fails. Surveys fan-out mutation CLI enumeration-failure behavior (gh codespace delete --all = fail-closed on listing; AWS CLI auto-pagination = fail-closed at output assembly; kubectl ContinueOnError = continue-but-surface; git submodule/fetch --all); applies fail-safe defaults (Saltzer-Schroeder), least astonishment, deterministic-vs-transient error classification (GCP/AWS), and idempotent-retry (RFC 9110, Google SRE). Atlassian `GET /project/{key}/components` requires Browse Projects; search-vs-components permission mismatch is real, but exact 403/404/5xx semantics are UNDOCUMENTED on this endpoint (flagged INCONCLUSIVE). RECOMMENDATION: HYBRID — deterministic 403/404 on a known project → skip-and-warn (accounted in summary/exit); transient 5xx/network/429 → retry w/ backoff then FAIL-CLOSED abort (zero mutations); explicit `--best-effort` opt-in for partial. Key rule: "non-retryable ≠ safe to skip" | S-608-1-all-projects-discovery-error-posture.md | complete |
