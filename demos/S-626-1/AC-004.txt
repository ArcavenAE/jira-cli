# Head: c88374b4  Captured: 2026-08-03  Status: re-stamped (Round 5 — head-only; delta c88374b4 was test-only; ci.yml content unchanged)

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "1\.85\.0\|RUSTUP_TOOLCHAIN" .github/workflows/ci.yml
60:    name: MSRV (1.85.0)
70:      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88  # 1.85.0
72:          toolchain: "1.85.0"
76:      # 1.85.0. wiremock (0.6.x) uses let-chains and ships no rust-version field
82:      # require dev-dependencies that build at 1.85.0 — a wiremock downgrade-pin
86:          RUSTUP_TOOLCHAIN: "1.85.0"

---

AC-004 assertions verified:
  - The comment on the msrv dtolnay/rust-toolchain step reads "# 1.85.0" CONFIRMED (ci.yml :70)
  - The toolchain input reads "1.85.0" CONFIRMED (ci.yml :72)
  - Comment and explicit toolchain input agree: both say 1.85.0
  - The job name also reads "MSRV (1.85.0)" CONFIRMED (ci.yml :60), providing triple agreement
  - RUSTUP_TOOLCHAIN env on the cargo check step CONFIRMED (ci.yml :86)
  - Lines :76 and :82 are two comment lines within the 10-line wiremock-scope comment block
    (:74-83) that explains why --all-targets is excluded; they are not separate RUSTUP_TOOLCHAIN
    entries. They reference "1.85.0" as context in the explanation text.
  - Prior artifact recorded wrong comment text ("# the 1.85.0 MSRV toolchain.
    --all-features is intentional." at :77) and wrong env line (:80). The comment block was
    rewritten to the 10-line wiremock-scope version after that recording; RUSTUP_TOOLCHAIN
    is at :86 not :80.
