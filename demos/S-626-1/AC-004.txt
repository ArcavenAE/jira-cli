# Head: e49230a7  Captured: 2026-08-04  Status: re-stamped (Round 13 — head-only; e49230a7 was NO line-count change in ci.yml (one-for-one comment edit); all grep output line numbers confirmed unchanged; ci.yml grep content at :157/:167/:169/:173/:179/:183 unaffected)

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "1\.85\.0\|RUSTUP_TOOLCHAIN" .github/workflows/ci.yml
157:    name: MSRV (1.85.0)
167:      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88  # 1.85.0
169:          toolchain: "1.85.0"
173:      # 1.85.0. wiremock (0.6.x) uses let-chains and ships no rust-version field
179:      # require dev-dependencies that build at 1.85.0 — a wiremock downgrade-pin
183:          RUSTUP_TOOLCHAIN: "1.85.0"

---

AC-004 assertions verified:
  - The comment on the msrv dtolnay/rust-toolchain step reads "# 1.85.0" CONFIRMED (ci.yml :: msrv, :167)
  - The toolchain input reads "1.85.0" CONFIRMED (ci.yml :: msrv, :169)
  - Comment and explicit toolchain input agree: both say 1.85.0
  - The job name also reads "MSRV (1.85.0)" CONFIRMED (ci.yml :: msrv, :157), providing triple agreement
  - RUSTUP_TOOLCHAIN env on the cargo check step CONFIRMED (ci.yml :: msrv, :183)
  - Lines :173 and :179 are two comment lines within the 10-line wiremock-scope comment block
    (ci.yml :: msrv) that explains why --all-targets is excluded; they are not separate RUSTUP_TOOLCHAIN
    entries. They reference "1.85.0" as context in the explanation text.
  - Prior artifact recorded wrong comment text ("# the 1.85.0 MSRV toolchain.
    --all-features is intentional." at :77) and wrong env line (:80). The comment block was
    rewritten to the 10-line wiremock-scope version after that recording; RUSTUP_TOOLCHAIN
    is at :183 (was :182 before the +1 shift from commit 14416fd9; was :179 before the
    +3 shift from commit 84ab32ac; was :125 before the +54 shift from commit a247a343;
    was :86 before the +39 shift from commit 9312f11f).
