# Head: 9312f11f  Captured: 2026-08-03  Status: re-captured (Round 7 — ci.yml line numbers shifted +39 by commit 9312f11f; grep output line numbers updated)

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "1\.85\.0\|RUSTUP_TOOLCHAIN" .github/workflows/ci.yml
99:    name: MSRV (1.85.0)
109:      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88  # 1.85.0
111:          toolchain: "1.85.0"
115:      # 1.85.0. wiremock (0.6.x) uses let-chains and ships no rust-version field
121:      # require dev-dependencies that build at 1.85.0 — a wiremock downgrade-pin
125:          RUSTUP_TOOLCHAIN: "1.85.0"

---

AC-004 assertions verified:
  - The comment on the msrv dtolnay/rust-toolchain step reads "# 1.85.0" CONFIRMED (ci.yml :109)
  - The toolchain input reads "1.85.0" CONFIRMED (ci.yml :111)
  - Comment and explicit toolchain input agree: both say 1.85.0
  - The job name also reads "MSRV (1.85.0)" CONFIRMED (ci.yml :99), providing triple agreement
  - RUSTUP_TOOLCHAIN env on the cargo check step CONFIRMED (ci.yml :125)
  - Lines :115 and :121 are two comment lines within the 10-line wiremock-scope comment block
    (:113-122) that explains why --all-targets is excluded; they are not separate RUSTUP_TOOLCHAIN
    entries. They reference "1.85.0" as context in the explanation text.
  - Prior artifact recorded wrong comment text ("# the 1.85.0 MSRV toolchain.
    --all-features is intentional." at :77) and wrong env line (:80). The comment block was
    rewritten to the 10-line wiremock-scope version after that recording; RUSTUP_TOOLCHAIN
    is at :125 (was :86 before the +39 shift from commit 9312f11f).
