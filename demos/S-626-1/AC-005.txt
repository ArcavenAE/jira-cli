# Head: 84ab32ac  Captured: 2026-08-04  Status: re-stamped (Round 10 — head-only; sign-and-publish.yml, backfill-release.yml, release.yml unchanged by 84ab32ac (ci.yml + ci_gate_completeness.rs only); all three rustup target add sites unaffected)

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "E0463\|rustup target add\|Ensure target" .github/workflows/sign-and-publish.yml
61:      # the build fails with `error[E0463]: can't find crate for 'core'`.
63:      - name: Ensure target installed (defensive)
65:        run: rustup target add ${{ matrix.target }}

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && sed -n '57,68p' .github/workflows/sign-and-publish.yml

      # rust-toolchain.toml pins channel = "stable", which overrides the
      # toolchain dtolnay/rust-toolchain installs above. The target the
      # action installed sits on the wrong toolchain; without this step
      # the build fails with `error[E0463]: can't find crate for 'core'`.
      # Same defensive fix as release.yml.
      - name: Ensure target installed (defensive)
        shell: bash
        run: rustup target add ${{ matrix.target }}

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
        with:

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "E0463\|rustup target add\|Ensure target" .github/workflows/backfill-release.yml
75:      # native (non-cross) builds fail with `error[E0463]: can't find
77:      - name: Ensure target installed (defensive)
80:        run: rustup target add ${{ matrix.target }}

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && sed -n '72,83p' .github/workflows/backfill-release.yml

      # rust-toolchain.toml pins channel = "stable", which overrides the
      # toolchain dtolnay/rust-toolchain installs above. Without this step,
      # native (non-cross) builds fail with `error[E0463]: can't find
      # crate for 'core'`. Same defensive fix as release.yml.
      - name: Ensure target installed (defensive)
        if: ${{ !matrix.use_cross }}
        shell: bash
        run: rustup target add ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && grep -n "E0463\|rustup target add\|Ensure" .github/workflows/release.yml
43:      - name: Ensure cross-target installed (defensive)
46:        run: rustup target add ${{ matrix.target }}

$ cd /Users/zious/Documents/GITHUB/jira-cli/.worktrees/S-626-1 && sed -n '38,52p' .github/workflows/release.yml
        uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88  # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Ensure cross-target installed (defensive)
        if: ${{ !matrix.use_cross }}
        shell: bash
        run: rustup target add ${{ matrix.target }}

      - name: Install cross
        if: matrix.use_cross
        shell: bash
        run: cargo install cross --git https://github.com/cross-rs/cross


---

AC-005 assertions verified:
  - sign-and-publish.yml: "Ensure target installed (defensive)" step PRESENT at :63 CONFIRMED
  - sign-and-publish.yml: E0463 comment PRESENT at :61 CONFIRMED
  - sign-and-publish.yml: "rustup target add ${{ matrix.target }}" PRESENT at :65 CONFIRMED
  - backfill-release.yml: "Ensure target installed (defensive)" step PRESENT at :77 CONFIRMED
  - backfill-release.yml: E0463 comment PRESENT at :75 CONFIRMED
  - backfill-release.yml: "rustup target add ${{ matrix.target }}" PRESENT at :80 CONFIRMED
  - release.yml: "Ensure cross-target installed (defensive)" step PRESENT at :43 CONFIRMED
  - release.yml: "rustup target add ${{ matrix.target }}" PRESENT at :46 CONFIRMED
  - release.yml: NO E0463 comment (bare step name only, as documented in AC-5 story table) CONFIRMED
  - Both E0463 comments (sign-and-publish.yml:61, backfill-release.yml:75) and all three
    rustup target add steps are UNCHANGED from origin/develop
    (release.yml:46 has the step but no E0463 comment — asymmetry confirmed at line 73 above)
  - The do-not-remove constraint (P71-003) is satisfied for all three sites
