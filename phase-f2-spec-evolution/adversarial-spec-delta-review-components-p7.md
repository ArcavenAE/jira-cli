Adversarial Spec-Delta Review — Component Management (F2, pass 7). VERDICT: NOT CLEAN. 0 CRIT, 0 HIGH, 3 MEDIUM, 1 LOW, 5 INFO.

MEDIUM-1 [bc-8 BC-8.1.008 Behavior ~590-604 vs BC-8.4.002 ~1797-1799, BC-8.4.003 ~1813-1817; bc-2 BC-2.1.022 ~495-497]: P6 rewrite made BC-8.1.008 branch(1) enumerate "a NAME NAME|ID" with message "not found in project <key>. Run: jr component list" but BC-8.4.002 zero-match says "...Available: <alphabetical list>", BC-8.4.003 ambiguous says "Ambiguous component... Matches: <candidates>", BC-2.1.022 (same resolver) says "...Available:...". Two divergences: (1) zero-match drops candidate list; (2) ambiguous gets "not found" instead of "Ambiguous...Matches:". `jr component edit BadName --project ENG` → jr-component path and issue-list-component path emit different msgs for identical resolver failure. Fix: branch(1) → numeric-known-project 404 only; defer NAME sub-cases to BC-8.4.002/003.

MEDIUM-2 [prd-delta-components.md L53, L164] [process-gap]: L53 summary still "BC-8.1.004 guard covers list/edit/delete/rename --project [H2]" but P6 removed rename from BC-8.1.004 (now BC-8.3.005). L164 "No --project on ANY jr component subcommand → 64 BC-8.1.004" over-broad (create→exit 2 BC-8.1.005; rename→BC-8.3.005). prd-delta consumed by story-writer F3. process-gap: no sync mechanism for prd-delta tables when BC scope changes (verification-delta §3 drifted same way until P6). Fix: correct L53 (list/edit/delete) + L164 (per-subcommand exit codes/owners).

MEDIUM-3 [verification-delta §3 L544-566 vs bc-3 BC-3.4.017/020/021; prd-delta L123-125]: §3 completeness claim false — BC-3.4.017/020/021 in neither has-VP nor exception list. BC-3.4.020 = --label+--component silent-drop exit-64 data-loss guard (FIX-F5-001), ZERO VP pin. BC-3.4.017 Gate B overlap exit-64; BC-3.4.021 dry-run plannedChanges.components array-of-{action,name} shape — both unaccounted. Fix: mint VP for BC-3.4.020 (+ maybe BC-3.4.021), document/cite for BC-3.4.017; make §3 claim true. (pending intent verification: whether pre-existing bc-3 base VP covers the --component extension.)

LOW-1 [bc-8 BC-8.3.002 Precond2 ~1491-1510, BC-8.3.004 ~1577-1595]: numeric-OLD exit-64 rejection under --all-projects vs --dry-run per-project discovery order unstated; almost certainly pre-flight-first (VP-026 pins zero HTTP) but no explicit EC. Fix: EC pinning exit-64-before-discovery.

INFO-1: reserved-syntax escape-hatches (none/not:/all:/comma/100) all documented w/ --jql workaround — consistent. INFO-2: ADR-0018 §1/§2 now CURRENT (all 4 confirming-GET numeric paths + cache derivation) — no staleness. INFO-3: DEC-188 exit-code class consistently applied. INFO-4: Component.id typing chain sound (embedded Option<String>, full-resource String, bulk u64 parse, cache String). INFO-5: VP 001-026 all anchored, 014/021 split holds, VP-012 sole live-Jira. Novelty MEDIUM (MEDIUM-1/2 trace to P6 edits; MEDIUM-3 standing gap).

---

## Fix-burst resolutions applied (product-owner, pass-7 remediation)

- MEDIUM-1: BC-8.1.008 Behavior rewritten — branch (1) scoped to numeric `NAME|ID` with a KNOWN project ONLY (its own confirming-GET 404 case); a new branch (0) added for NAME `NAME|ID` resolution failures, which now defers entirely to BC-8.4.002 (zero-match, "Available: <list>") / BC-8.4.003 (ambiguous, "Matches: <candidates>") instead of restating a distinct, contradicting message. Trace/EC updated to cite BC-8.4.002/003 explicitly; the shared-resolver invariant (identical message for `jr component edit`/`delete` and `issue list --component` on the same input/candidate set) is now stated explicitly.
- MEDIUM-2: prd-delta-components.md L53 corrected to "list/edit/delete" (rename removed, BC-8.3.005 cited as rename's owner). L164 split into three per-subcommand rows: list/edit/delete → 64/BC-8.1.004 (narrowed wording), create → clap exit 2/BC-8.1.005 (new row), and the pre-existing L174 rename row (64/BC-8.3.005) is now the sole rename entry (no change needed there — it was already correct, just previously shadowed by L164's overly-broad wording).
- MEDIUM-3: VP-COMPONENT-027 minted, pinning BC-3.4.020 Precondition 3's `--label`+`--component` mutual-exclusion exit-64 guard (zero PUT/bulk-POST `.expect(0)`); cited in BC-3.4.020's new Verification Properties subsection. VP-COMPONENT-028 minted, pinning BC-3.4.021 EC-3.4.021-20's dry-run `plannedChanges.components` flat-array shape (mirrors VP-DRY-RUN-003 for labels); cited in BC-3.4.021's existing Verification Properties list. BC-3.4.017 is NOT given a new VP-COMPONENT-0xx — its Gate B `components` extension is already pinned by the pre-existing base VP-396-005 (updated in-place at issue #605 F2 to enumerate `components` as the fifth field); verification-delta §3's has-VP enumeration and prd-delta now cite VP-396-005 for BC-3.4.017 explicitly instead of leaving it unlisted. §3's completeness claim is now true: all three BCs appear in the has-VP list with explicit VP citations (BC-3.4.017→VP-396-005 [base], BC-3.4.020→VP-COMPONENT-027, BC-3.4.021→VP-DRY-RUN-001/003/COMPONENT-028).
- LOW-1: BC-8.3.002 gained EC-8.3.002-4 and BC-8.3.004 gained EC-8.3.004-2, both pinning that the numeric-`OLD` exit-64 pre-flight rejection (Precondition 2) fires BEFORE any per-project discovery loop, INCLUDING under `--dry-run` — `rename 10042 New --all-projects --dry-run` exits 64 with zero HTTP calls (no `list_projects`, no per-project component-list GETs), not merely a dry-run preview of a rejected operation.

**Count impact:** zero new BCs (699 unchanged). Two new VPs minted (VP-COMPONENT-027, VP-COMPONENT-028) — VPs are not subject to the BC count gate.
