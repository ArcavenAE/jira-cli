Adversarial Spec-Delta Review — Component Management bundle (F2, pass 3). VERDICT: NOT CLEAN. Counts: 1 HIGH, 2 MEDIUM, 6 LOW, 1 INFO.
H1 [HIGH, bc-8 BC-8.2.007 Postcondition 3&5 ~L713-736; cross-ref CLAUDE.md search_issue_keys]: snapshot reuses search_issue_keys keys-only loop whose JRACLOUD-95368 anti-loop guard sets has_more=true on abort and returns a PARTIAL deduped key set (NOT an error), so the fail-closed-on-error path never triggers → silent undercount of affectedIssueCount/affectedIssues on irreversible delete. Also snapshot JQL is bare `component = <resolvedId>` with NO ORDER BY (Postcondition 4); JRACLOUD-95368 stability needs ORDER BY key ASC. Failure: component on 3000 issues, drift trips anti-loop at page 2, snapshot ~2000 keys has_more=true, --orphan prompt "remove from 2000", user confirms, delete orphans 3000, reconstruction missing ~1000 keys. Fix: (a) treat non-completed pagination as fail-closed like fetch error; (b) append ORDER BY key ASC; (c) VP-017 add drift/anti-loop fixture .expect(0) on DELETE.
M1 [MEDIUM, bc-8 BC-8.2.002 ~L458-479 / BC-8.2.003 ~L509-536 / BC-8.4.001 ~L1091-1101]: numeric SOURCE id never project-validated (only --move-to TARGET is). BC-8.2.002 "already-known project key" undefined for numeric source (BC-8.1.008 numeric bypass fires no GET). Failure: `delete 20007 --project A --move-to Frontend` where 20007 is in project B → --move-to scoped to wrong project A → cross-project DELETE?moveIssuesTo reaches API as raw 400, the outcome BC-8.2.003 promises to prevent; --project A silently ignored. Fix: fire same one-GET project confirmation for numeric SOURCE, or document numeric source derives own project via GET.
M2 [MEDIUM, bc-3 BC-3.4.023 Invariant 2 ~L2490-2500; contradicts bc-2 BC-2.3.040 ~L772-811]: Invariant 2 cites resolver String id as "the Component.id: Option<String> shape established by BC-2.3.040" but BC-2.3.040 says Option<String> is ONLY the embedded struct and resolve_component reads the FULL resource type (component.rs Component.id: String, required). Mis-anchoring → implementer may think resolver returns Option<String> or unify the two types. Fix: cite full resource type required String, not embedded Option<String>.
L1 [bc-8 BC-8.2.001 Invariant 1 ~L429-432, EC-8.2.001-3]: not-found-before-disposition-guard guarantee fails for nonexistent numeric source id (bypass checks no existence, no DELETE follows) → emits disposition-guard msg not "not found". Fix: document guarantee applies to name resolution only; numeric existence confirmed by DELETE.
L2 [bc-3 BC-3.4.012/013 ~L944-954/L1055 vs BC-3.4.021 item 6b ~L2251]: bare --component NAME echo unspecified (add:NAME normalized vs NAME raw); dry-run says "prefix preserved". Divergence risk. Fix: pin + align.
L3 [bc-8 BC-8.2.006 ~L621-623 vs BC-8.2.007 P1 ~L696-710]: BC-8.2.006 non-interactive msg needs <N> (requires snapshot fired) but BC-8.2.007 P1 firing boundary ambiguous for --orphan-without-yes; VP-006 only asserts .expect(0) DELETE. Fix: state snapshot fires before --yes check for --orphan; add <N> assertion to VP-006.
L4 [bc-2 BC-2.3.040 ~L766-811]: embedded Option<String> id has no in-cycle consumer (all use full resource type); rationale misleading. Fix: name consumer or state forward-looking prerequisite.
L5 [bc-3 EC-3.4.023-4 ~L2524-2531]: garbled wording + undefined partial-chunk-success reporting for >1000-issue chunked bulk (chunk1 commits, chunk2 fails, non-transactional, no report). Fix: repair wording + specify partial-commit reporting.
L6 [bc-8 BC-8.3.006 ~L1029-1041 / BC-8.3.002 ~L843-860 / VP-019]: no-short-circuit case-only rename rule framed around partial_match resolver, doesn't cover --all-projects exact-equality path; VP-019 pins only single-project. Fix: extend to --all-projects; broaden VP-019.
I1 [INFO, verification-delta VP-012 ~L196 vs bc-3 P6 ~L2533-2537]: VP-012 restatement "ceil(N/1000)" should be per-action / 2*ceil(N/1000) for mixed, matching BC-3.4.023 Postcondition 6.
CLEAN areas: exit-codes/DEC-188 (all correct), filter #606, rename #608 core, BC↔VP coverage 001-026, ADR↔BC snapshot consistency, output-channel profiles, Component.id serde split. No #607/#609 drift. Novelty decreasing (20→10→9).
